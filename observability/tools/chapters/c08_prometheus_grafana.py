from kit import *

SCRAPE_WIDGET = '''<div class="interactive scrape" data-scrape><div class="widget-head"><b>Сбор по запросу · кто кого вызывает</b><button data-scrape-step>Следующий шаг →</button></div><div class="scrape-route"><div><b>Приложение</b><code>/metrics</code></div><div class="scrape-arrow">← HTTP GET</div><div><b>Prometheus</b><code>опрос → хранение</code></div><div class="scrape-arrow">← запрос</div><div><b>Grafana</b><code>панель</code></div></div><p class="widget-detail" aria-live="polite">1. Приложение обновляет свои измерения при обработке запросов.</p></div>'''

CHAPTER = chapter(
    slug='08-prometheus-grafana',
    title='Prometheus и Grafana: как метрики попадают на график',
    lead='Проходим путь метрики учебного сервиса от строки в /metrics до панели Grafana. Разбираем по снимкам стенда, как Prometheus опрашивает приложение, как читать таблицу и график запроса и из чего состоит дашборд.',
    epigraph='Приложение отдаёт текущее значение; историю создаёт тот, кто его регулярно читает.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Связанные главы', '1.4 — метрики, 1.3 — OpenTelemetry'),
        ('Зачем это знать', 'Понимать, откуда берётся каждое число на панели и где искать ошибку, если его нет'),
    ],
    sections=[
        ('roles', 'Кто за что отвечает', p(
            'В предыдущих главах мы разбирали, какие данные нужны для расследования. Теперь посмотрим на инструменты, которые эти данные собирают, хранят и показывают. '
            'Путаница обычно начинается с того, что всё вместе называют «Grafana» или «мониторинг». На деле за каждый участок пути отвечает свой компонент.'
        ) + table(['Компонент', 'Что делает', 'Чего не делает'], [
            ['Приложение с инструментацией', 'Считает события и длительности, отдаёт текущие значения по адресу /metrics', 'Не хранит историю: после перезапуска счётчики начинаются с нуля'],
            ['Prometheus', 'Регулярно опрашивает приложения, хранит временные ряды, выполняет запросы PromQL и проверяет правила оповещения', 'Не хранит логи и трассы; не рисует полноценные дашборды'],
            ['Grafana', 'Отправляет запросы к источникам данных и показывает результаты панелями', 'Сама метрики не собирает и не хранит'],
            ['Alertmanager', 'Получает сработавшие оповещения от Prometheus, группирует их и отправляет в почту или мессенджер', 'Не вычисляет условия оповещений'],
            ['OpenTelemetry', 'Создаёт телеметрию в коде и передаёт её через Collector в хранилища', 'Не хранит данные и не строит графики'],
        ]) + p(
            'На учебном стенде работают первые три компонента. API запущен как обычный Python-процесс, а Prometheus и Grafana — в контейнерах Docker. '
            'Ниже мы пройдём по пути одного числа: от строки кода, которая увеличивает счётчик, до цифры на панели Grafana.'
        ) + source('https://prometheus.io/docs/introduction/overview/', 'Prometheus · Overview')),

        ('exposition', 'Приложение отдаёт текущие значения', p(
            'Разбор ответа <code>/metrics</code> по строкам есть в главе 1.4. Здесь посмотрим, как этот ответ формируется в коде учебного сервиса.'
        ) + code('''@app.get("/metrics", include_in_schema=False)
async def metrics():
    lines = ["# HELP lab_http_requests_total Completed products requests.",
             "# TYPE lab_http_requests_total counter"]
    for status, value in totals.items():
        lines.append(f'lab_http_requests_total{{route="/products",status="{status}"}} {value}')
    ...
    return Response("\\n".join(lines) + "\\n", media_type="text/plain; version=0.0.4")''', 'Python · фрагмент app.py', 'app.py') + p(
            'Обработчик собирает список строк и возвращает его обычным текстом. Словарь <code>totals</code> хранит два счётчика: <code>{"200": ..., "500": ...}</code>. '
            'Обработчик <code>/products</code> увеличивает нужный из них в конце каждого запроса. '
            'Двойные фигурные скобки в f-строке выводят одинарные: так в тексте появляется <code>{route="/products",status="200"}</code>. '
            'Параметр <code>include_in_schema=False</code> прячет адрес из Swagger, потому что он предназначен для Prometheus, а не для людей.'
        ) + p(
            'Главное свойство этого адреса: он возвращает только текущее состояние счётчиков. '
            'Два запроса к <code>/metrics</code> подряд покажут два значения, но где-то хранить их по времени приложение не будет. Историю создаёт тот, кто регулярно читает этот адрес.'
        ) + p(
            'В учебном сервисе формат собран вручную, чтобы механизм был виден целиком. В рабочих приложениях используют библиотеку <code>prometheus_client</code>: '
            'она ведёт счётчики и гистограммы, правильно экранирует значения меток и корректно работает, когда приложение запущено в нескольких процессах.'
        )),

        ('pull', 'Prometheus сам опрашивает приложение', term('Опрос (scrape)', 'HTTP-запрос, которым Prometheus читает метрики с адреса приложения. '
             'Опросы повторяются с заданным интервалом, и каждое прочитанное значение сохраняется с отметкой времени.') + p(
            'Такую схему называют сбором по запросу (pull): инициатор — система мониторинга, а приложение только отвечает. '
            'Пройдите по шагам схемы и обратите внимание, в какую сторону идёт HTTP-запрос и в какую — данные.'
        ) + SCRAPE_WIDGET + p(
            'Стрелки на схемах мониторинга часто рисуют от приложения к Prometheus, по направлению движения данных. '
            'Сетевой запрос при этом идёт в обратную сторону. Это различие важно при настройке сети: '
            'Prometheus должен иметь доступ к адресу приложения, а приложению не нужно знать адрес Prometheus.'
        ) + p(
            'Какие адреса опрашивать, Prometheus читает из файла конфигурации. На учебном стенде он короткий.'
        ) + code('''global:
  scrape_interval: 5s
scrape_configs:
  - job_name: telemetry-lab
    static_configs:
      - targets: ['host.docker.internal:8015']''', 'YAML · prometheus.yml', 'prometheus.yml') + ul([
            '<code>scrape_interval: 5s</code> — опрашивать каждые 5 секунд. По умолчанию Prometheus опрашивает раз в минуту; короткий интервал выбран, чтобы на стенде изменения появлялись быстро.',
            '<code>job_name: telemetry-lab</code> — имя группы целей. Оно попадёт в каждый ряд меткой <code>job</code>.',
            '<code>targets</code> — список адресов. Путь <code>/metrics</code> не указан, потому что он используется по умолчанию.',
            '<code>host.docker.internal</code> — имя, по которому контейнер обращается к компьютеру, на котором он запущен. API работает не в контейнере, поэтому Prometheus ищет его на хост-машине.',
        ]) + p(
            'У сбора по запросу есть альтернатива: приложение само отправляет метрики. Её используют для коротких задач, которые завершаются раньше, чем их успеют опросить, '
            'например для ночного скрипта резервного копирования. Для таких случаев у Prometheus есть отдельный компонент Pushgateway. Долгоживущий веб-сервис удобнее опрашивать напрямую.'
        ) + source('https://prometheus.io/docs/prometheus/latest/configuration/configuration/', 'Prometheus · Configuration')),

        ('targets', 'Проверка цели на странице Target health', p(
            'Первое, что стоит открыть после запуска Prometheus, — страницу состояния целей. Она отвечает на вопрос, получается ли вообще опрашивать приложение. '
            'Если здесь ошибка, все остальные запросы и панели будут пустыми.'
        ) + shot('prometheus-targets', 'страница Status → Target health в Prometheus 3.5', p(
            'Одна группа целей telemetry-lab, в ней одна цель. Зелёная полоса слева и счётчик «1 / 1 up» показывают, что все цели группы доступны.'
        )) + p(
            'Разберём строку цели по колонкам.'
        ) + table(['Колонка', 'Значение на снимке', 'Что означает'], [
            ['Endpoint', 'http://host.docker.internal:8015/metrics', 'Полный адрес опроса: адрес из targets и путь по умолчанию'],
            ['Labels', 'instance="host.docker.internal:8015", job="telemetry-lab"', 'Метки, которые Prometheus добавит к каждому ряду этой цели'],
            ['Last scrape', '3.194s ago · 2ms', 'Последний опрос был 3,2 секунды назад и занял 2 мс'],
            ['State', 'UP', 'Последний опрос завершился успешно'],
        ]) + p(
            'Метки <code>instance</code> и <code>job</code> приложение не отдавало: их добавляет сам Prometheus. '
            'Так ряды разных экземпляров одного сервиса не смешиваются, даже если метрики называются одинаково.'
        ) + p(
            'Колонка Last scrape помогает проверить интервал. При <code>scrape_interval: 5s</code> значение «сколько секунд назад» не должно превышать пяти. '
            'Если цель недоступна, в колонке State появится DOWN, а рядом текст ошибки, например <code>connection refused</code> — по указанному адресу никто не слушает.'
        ) + note('Метрика up', 'Для каждой цели Prometheus сам записывает ряд <code>up</code>: 1, если опрос успешен, и 0, если нет. '
                 'Правило оповещения на <code>up == 0</code> сообщит о пропаже цели. Без него сломанный сбор выглядит так же, как сервис без запросов.')),

        ('table', 'Текущие значения в таблице', p(
            'На вкладке Query в поле выражения вводят запрос на языке PromQL и нажимают Execute. '
            'Самый простой запрос — имя метрики. Он возвращает последние значения всех её рядов.'
        ) + shot('prometheus-query', 'запрос lab_http_requests_total на вкладке Table', p(
            'Выражение состоит только из имени метрики. Prometheus нашёл два ряда и показал последнее значение каждого.'
        )) + p(
            'Каждая строка таблицы — один временной ряд. Слева имя и полный набор меток, справа значение. '
            'В фигурных скобках четыре метки. <code>route</code> и <code>status</code> пришли из ответа приложения, <code>instance</code> и <code>job</code> добавил Prometheus при опросе.'
        ) + p(
            'Значения 363 и 38 — счётчики с момента запуска API: 363 успешных запроса и 38 ошибочных. '
            'Справа над таблицей <code>Result series: 2</code> — число найденных рядов, <code>Load time: 24ms</code> — время выполнения запроса. '
            'Поле Evaluation time позволяет посмотреть значения на любой момент в прошлом, а не только сейчас.'
        ) + p(
            'Для ответа на вопрос «как работает сервис» эти числа мало что дают. 38 ошибок могли накопиться за сутки или за минуту. '
            'Нужна скорость, и её показывает следующий запрос.'
        )),

        ('graph', 'Скорость на графике', p(
            'Переключимся на вкладку Graph и введём выражение, которое переводит счётчик в скорость и разделяет её по кодам ответа.'
        ) + code('sum by (status) (rate(lab_http_requests_total[1m]))', 'PromQL · запросов в секунду по коду ответа') + p(
            'Читать выражение удобно изнутри наружу.'
        ) + ol([
            '<code>lab_http_requests_total[1m]</code> — все значения каждого ряда за последнюю минуту. При опросе раз в 5 секунд это около двенадцати точек.',
            '<code>rate(...)</code> — средний прирост в секунду за эту минуту. Функция учитывает сбросы счётчика при перезапуске, о которых шла речь в главе 1.4.',
            '<code>sum by (status) (...)</code> — сложить ряды, у которых совпадает метка <code>status</code>, и убрать остальные метки. Если бы экземпляров было три, их скорости сложились бы в одну линию на каждый код.',
        ]) + shot('prometheus-graph', 'график скорости запросов по кодам ответа за 5 минут', p(
            'Две линии: верхняя — ответы 200, нижняя, которая появляется в середине, — ответы 500. По вертикали — запросы в секунду.'
        )) + p(
            'Снимок сделан сразу после специального прогона. Две минуты скрипт <code>tools/traffic.py</code> отправлял только быстрые запросы, '
            'затем полторы минуты — смесь, где каждый четвёртый запрос завершается ошибкой, а каждый двадцать пятый выполняется медленно. Прочитаем график по участкам.'
        ) + table(['Участок графика', 'Что видно', 'Почему так'], [
            ['Слева, до начала нагрузки', 'Нулевая линия', 'Запросов к API не было'],
            ['Подъём ступеньками', 'Скорость 200 растёт около минуты', 'Окно [1m] заполняется постепенно; ступенька — каждый новый опрос раз в 5 секунд'],
            ['Ровный участок около 2,0', 'Два успешных запроса в секунду', 'Скрипт ждёт 0,4 с между запросами, а запрос длится около 85 мс'],
            ['Появление нижней линии', 'Скорость 500 растёт до 0,4', 'Каждый четвёртый запрос стал ошибкой'],
            ['Верхняя линия снижается до 1,3', 'Успешных ответов стало меньше', 'Четверть запросов ушла в ошибки, а медленные запросы по 3 с уменьшили общий темп'],
            ['Спад справа', 'Обе линии падают', 'Нагрузка закончилась, и окно [1m] постепенно освобождается'],
        ]) + p(
            'Посмотрим внимательнее на плавность линий. Нагрузка поменялась мгновенно, а линии меняются около минуты. '
            'Это прямое следствие окна <code>[1m]</code>: скорость усредняется по последней минуте. '
            'Короткое окно быстрее реагирует, но сильнее скачет. Длинное окно сглаживает шум, но позже показывает изменения.'
        ) + example('Почему окно rate не берут короче четырёх интервалов опроса', p(
            'Функция <code>rate</code> считает прирост по точкам, попавшим в окно. Точек в окне столько, сколько опросов туда поместилось. '
            'Возьмём рабочую настройку <code>scrape_interval: 15s</code> и посмотрим, что даёт разное окно.'
        ) + table(['Окно', 'Точек в окне', 'Что получится на графике'], [
            ['<code>[20s]</code>', '1 или 2', 'Прирост посчитать не из чего: при одной точке значения нет, линия рвётся'],
            ['<code>[30s]</code>', '2 или 3', 'Линия есть, но один пропущенный опрос снова разрывает её'],
            ['<code>[1m]</code>', '4 или 5', 'Линия непрерывна и переживает один пропущенный опрос'],
            ['<code>[5m]</code>', '20 или 21', 'Линия гладкая, но изменение нагрузки видно с задержкой в несколько минут'],
        ]) + p(
            'Отсюда правило: окно берут не меньше четырёх интервалов опроса. При <code>scrape_interval: 15s</code> это <code>[1m]</code>, '
            'при интервале 5 секунд учебного стенда хватило бы и <code>[20s]</code> — мы всё равно берём <code>[1m]</code>, чтобы линия была спокойнее.'
        ) + p(
            'Отдельно отметим, что окно и интервал опроса — разные настройки. Интервал задаётся в <code>prometheus.yml</code> и определяет, '
            'как часто появляются точки. Окно пишется в самом запросе и определяет, по скольким точкам считается скорость. '
            'Менять интервал ради красивой линии не нужно: достаточно подобрать окно.'
        )) + p(
            'Доля ошибок на втором участке: 0,43 / (1,3 + 0,43) ≈ 25%, то есть как раз каждый четвёртый запрос. '
            'Такой расчёт по графику совпал с тем, как была задана нагрузка. Это хорошая проверка, что метрика и запрос устроены правильно.'
        ) + source('https://prometheus.io/docs/prometheus/latest/querying/functions/#rate', 'Prometheus · rate()')),

        ('grafana', 'Дашборд Grafana', p(
            'Интерфейс Prometheus удобен для проверки отдельных запросов. Для постоянного наблюдения используют Grafana. '
            'Она хранит набор панелей, каждая из которых выполняет свой запрос к источнику данных и показывает результат.'
        ) + term('Дашборд', 'страница Grafana с набором панелей. У каждой панели есть источник данных, запрос, временной диапазон и способ отображения.') + p(
            'Источник данных Grafana на стенде настраивается файлом, поэтому после запуска контейнера ничего не нужно указывать вручную.'
        ) + code('''apiVersion: 1
datasources:
  - name: Prometheus · учебный стенд
    type: prometheus
    uid: prometheus-lab
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false''', 'YAML · grafana/provisioning/datasources/prometheus.yaml', 'grafana/provisioning/datasources/prometheus.yaml') + p(
            'Адрес <code>http://prometheus:9090</code> работает внутри Docker Compose: контейнеры одного проекта находят друг друга по имени сервиса. '
            '<code>access: proxy</code> означает, что запросы к Prometheus отправляет сервер Grafana, а не браузер пользователя. '
            '<code>editable: false</code> запрещает менять источник через интерфейс, чтобы стенд у всех был одинаковым.'
        ) + shot('grafana-dashboard', 'дашборд «Telemetry Lab · от запроса к метрике» за последние 5 минут', p(
            'Четыре панели: два счётчика с момента запуска сверху, скорость и средняя длительность снизу. Справа вверху выбран диапазон Last 5 minutes и автообновление каждые 5 секунд.'
        )) + table(['Панель', 'Запрос PromQL', 'Что показывает'], [
            ['Завершённые запросы · с запуска', '<code>sum(lab_http_requests_total)</code>', 'Сумма обоих рядов счётчика'],
            ['Ошибки HTTP 500 · с запуска', '<code>sum(lab_http_requests_total{status="500"})</code>', 'Только ряд с кодом 500'],
            ['Поток запросов · запросов/с', '<code>sum(rate(lab_http_requests_total[1m]))</code>', 'Скорость всех запросов'],
            ['Средняя длительность · обработчик', '<code>rate(..._sum[1m]) / rate(..._count[1m])</code>', 'Прирост суммы длительностей, делённый на прирост числа запросов'],
        ]) + p(
            'Сверим панели с Prometheus. Верхняя левая показывает 401, и это ровно 363 + 38 из таблицы выше. Верхняя правая показывает 38. '
            'Числа совпадают, потому что Grafana выполнила запросы к тому же Prometheus.'
        ) + p(
            'На мини-графиках под крупными цифрами есть деталь, которая хорошо иллюстрирует сбросы счётчика. '
            'Слева линия держится на ровном уровне, затем резко падает к нулю и снова растёт. '
            'Ровный уровень — счётчик предыдущего запуска API, падение — перезапуск процесса перед прогоном, рост — новый счёт с нуля.'
        ) + p(
            'Нижняя правая панель показывает среднюю длительность. На первом участке она держится около 83 мс: все запросы быстрые. '
            'На втором поднимается до 185 мс. Причина — медленные запросы: каждый двадцать пятый длится 3 секунды и добавляет к среднему около 3000 / 25 = 120 мс. '
            'Быстрые ошибки немного тянут среднее вниз. Это та же картина, что в главе 1.4: пара долгих запросов сильно сдвигает среднее.'
        ) + p(
            'В правом верхнем углу Grafana стоит кнопка Sign in. На стенде включён анонимный просмотр с ролью Viewer: смотреть дашборд можно без входа, менять нельзя. '
            'Для локального учебного стенда это удобно, для рабочей Grafana — недопустимо.'
        ) + source('https://grafana.com/docs/grafana/latest/fundamentals/', 'Grafana · Fundamentals')),

        ('compose', 'Как запущен стенд', p(
            'Prometheus и Grafana на стенде запускаются одной командой через Docker Compose. Файл описывает оба контейнера и их настройки.'
        ) + code('''name: makarov-telemetry-intro
services:
  prometheus:
    image: prom/prometheus:v3.5.0
    ports: ['127.0.0.1:9095:9090']
    volumes: ['./prometheus.yml:/etc/prometheus/prometheus.yml:ro']
    extra_hosts: ['host.docker.internal:host-gateway']
  grafana:
    image: grafana/grafana:12.1.1
    ports: ['127.0.0.1:3015:3000']
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: 'true'
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
      GF_AUTH_DISABLE_LOGIN_FORM: 'true'
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro''', 'YAML · compose.yaml (фрагмент)', 'compose.yaml') + ul([
            '<code>image</code> указывает образ с точной версией. Без версии Docker возьмёт последнюю, и через полгода стенд может выглядеть иначе, чем на снимках.',
            '<code>ports: 127.0.0.1:9095:9090</code> публикует порт 9090 контейнера на порт 9095 компьютера, причём только для локального адреса. С других машин стенд недоступен.',
            '<code>volumes</code> подключает файлы конфигурации внутрь контейнера. Суффикс <code>:ro</code> — только для чтения.',
            '<code>extra_hosts: host-gateway</code> добавляет имя <code>host.docker.internal</code> на Linux. В Docker Desktop для macOS и Windows это имя есть и без настройки.',
            'Переменные <code>GF_AUTH_*</code> включают анонимный просмотр и скрывают форму входа.',
        ]) + code('''# в папке telemetry-lab, API запущен с --host 0.0.0.0
docker compose up -d
docker compose ps
docker compose down''', 'Терминал · запуск и остановка стенда') + p(
            '<code>docker compose up -d</code> скачивает образы при первом запуске и запускает контейнеры в фоне. '
            '<code>docker compose ps</code> показывает их состояние. <code>docker compose down</code> останавливает и удаляет контейнеры этого проекта, не затрагивая другие.'
        ) + p(
            'API для стенда запускают с <code>--host 0.0.0.0</code>, иначе контейнер Prometheus не достучится до него. '
            'Такой адрес открывает API на всех сетевых интерфейсах компьютера, поэтому стенд запускают только в доверенной сети. '
            'Для практики без Prometheus достаточно адреса <code>127.0.0.1</code>.'
        )),
    ],
    mistakes=[
        ['Ждут, что приложение само сохранит историю метрик', 'После перезапуска значения пропадают, графиков нет', 'Настроить опрос Prometheus и проверить цель на странице Target health'],
        ['Строят панели, не проверив состояние цели', 'Пустые графики принимают за отсутствие запросов', 'Сначала убедиться, что цель в состоянии UP'],
        ['Выводят сырой счётчик с подписью «запросов в минуту»', 'Число только растёт и не отражает нагрузку', 'Использовать rate или increase за окно'],
        ['Считают, что Grafana хранит данные', 'Ищут пропавшие данные не в той системе', 'Проверять источник данных и сам запрос панели'],
        ['Выбирают окно rate короче двух интервалов опроса', 'В окне меньше двух точек, линия прерывается', 'Брать окно не меньше четырёх интервалов опроса'],
        ['Открывают стенд с анонимным доступом наружу', 'Любой в сети видит данные', 'Публиковать порты только на 127.0.0.1'],
    ],
    selfcheck=[
        ('Кто инициирует HTTP-запрос при сборе метрик по схеме pull?',
         p('Prometheus. Он отправляет GET к адресу /metrics приложения, а приложение возвращает текущие значения в ответе.')),
        ('На странице Target health цель в состоянии DOWN с ошибкой connection refused. Что проверить?',
         p('Запущен ли API, совпадает ли порт, слушает ли API адрес, доступный из контейнера (0.0.0.0, а не только 127.0.0.1), и разрешается ли имя host.docker.internal.')),
        ('Откуда в рядах метрики появились метки instance и job, если приложение их не отдаёт?',
         p('Их добавляет Prometheus при опросе: job — из job_name конфигурации, instance — из адреса цели.')),
        ('Что вычисляет выражение sum by (status) (rate(lab_http_requests_total[1m]))?',
         p('Среднюю скорость запросов в секунду за последнюю минуту, сложенную по всем рядам с одинаковым кодом ответа. Получается по одной линии на каждый код.')),
        ('Нагрузка изменилась мгновенно, а линия rate(...[1m]) меняется около минуты. Почему?',
         p('Скорость усредняется по окну в одну минуту. Новые значения вытесняют старые из окна постепенно, поэтому линия меняется плавно.')),
    ],
    cheatsheet=[
        'Приложение отдаёт текущие значения, Prometheus хранит историю, Grafana показывает.',
        'Сбор по запросу (pull): Prometheus опрашивает /metrics с интервалом scrape_interval.',
        'Target health: UP — опрос успешен; метрика up = 1 или 0.',
        'Метки instance и job добавляет Prometheus.',
        'Имя метрики в запросе — последние значения всех её рядов.',
        'rate(x[1m]) — средний прирост в секунду за минуту с учётом сбросов.',
        'sum by (метка) — сложить ряды, оставив только указанную метку.',
        'Панель Grafana = источник + запрос + диапазон + отображение.',
        'Средняя длительность из гистограммы: rate(_sum) / rate(_count).',
    ],
    links=[
        ('https://prometheus.io/docs/introduction/overview/', 'Prometheus · Overview'),
        ('https://prometheus.io/docs/prometheus/latest/configuration/configuration/', 'Prometheus · Configuration'),
        ('https://prometheus.io/docs/prometheus/latest/querying/basics/', 'Prometheus · Querying basics'),
        ('https://grafana.com/docs/grafana/latest/fundamentals/', 'Grafana · Fundamentals'),
        ('https://grafana.com/docs/grafana/latest/administration/provisioning/', 'Grafana · Provisioning'),
    ],
)
