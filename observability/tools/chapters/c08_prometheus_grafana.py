from kit import *

SCRAPE_HTML = '''<div class="interactive scrape" data-scrape><div class="widget-head"><b>Сбор по запросу · кто кого вызывает</b><button data-scrape-step>Следующий шаг →</button></div><div class="scrape-route"><div><b>Приложение</b><code>/metrics</code></div><div class="scrape-arrow">← HTTP GET</div><div><b>Prometheus</b><code>опрос → хранение</code></div><div class="scrape-arrow">← запрос</div><div><b>Grafana</b><code>панель</code></div></div><p class="widget-detail" aria-live="polite">1. Приложение обновляет свои измерения при обработке запросов.</p></div>'''

SCRAPE_WIDGET = widget(SCRAPE_HTML, 'кто кого вызывает при сборе метрик',
                       'Кнопка «Следующий шаг» проходит четыре шага по кругу. Рамкой выделен компонент, который работает на этом шаге; '
                       'стрелка между приложением и Prometheus показывает, куда идёт HTTP-запрос и куда — данные.')

TARGETS_PINS = [
    (12.0, 30.6, 'Группа целей', '<code>telemetry-lab</code> — значение <code>job_name</code> из prometheus.yml.'),
    (87.8, 30.6, '1 / 1 up', 'сколько целей группы доступны.'),
    (23.6, 46.7, 'Endpoint', 'полный адрес опроса.'),
    (58.3, 46.7, 'Labels', 'метки, которые Prometheus добавит к каждому ряду этой цели.'),
    (83.6, 39.8, 'Last scrape', 'когда был последний опрос и сколько он длился.'),
    (96.6, 46.9, 'State', 'UP — последний опрос прошёл успешно.'),
]

QUERY_PINS = [
    (19.1, 21.5, 'Выражение', 'только имя метрики, без функций.'),
    (18.6, 46.0, 'Evaluation time', 'момент, на который считаются значения; пусто — «сейчас».'),
    (82.8, 46.0, 'Load time и Result series', 'время выполнения запроса и число найденных рядов.'),
    (50.3, 57.4, 'Ряд', 'имя метрики и полный набор меток.'),
    (95.1, 57.4, 'Значение', 'последнее значение счётчика: 363 успешных запроса.'),
]

GRAPH_PINS = [
    (35.8, 13.2, 'Выражение', 'скорость запросов по кодам ответа.'),
    (4.3, 32.2, 'Диапазон', 'график за последние 5 минут.'),
    (33.3, 57.9, 'Ступеньки', 'окно [1m] заполняется постепенно, каждая ступенька — новый опрос.'),
    (55.6, 42.1, 'Ответы 200', 'ровный участок около 2 запросов в секунду.'),
    (81.9, 57.9, 'Спад линии 200', 'часть запросов стала ошибками, медленные замедлили темп.'),
    (83.3, 86.2, 'Ответы 500', 'нижняя линия поднимается до 0,4 запроса в секунду.'),
]

GRAFANA_PINS = [
    (68.0, 11.1, 'Диапазон и обновление', 'последние 5 минут, автообновление каждые 5 секунд.'),
    (42.6, 16.1, 'Завершённые запросы', 'сумма обоих рядов счётчика с запуска API: 401.'),
    (79.0, 16.1, 'Ошибки HTTP 500', 'только ряд с кодом 500: 38.'),
    (27.8, 44.0, 'Сброс счётчика', 'падение к нулю — перезапуск API перед прогоном.'),
    (39.8, 50.3, 'Поток запросов', 'скорость всех запросов в секунду.'),
    (82.4, 50.3, 'Средняя длительность', 'прирост суммы длительностей, делённый на прирост числа запросов.'),
]

CHAPTER = chapter(
    slug='08-prometheus-grafana',
    title='Prometheus и Grafana: как метрики попадают на график',
    lead='Проходим путь метрики сервиса от строки в /metrics до панели Grafana. По снимкам стенда разбираем, как Prometheus опрашивает приложение, как читать таблицу и график запроса и из чего состоит дашборд.',
    epigraph='Приложение отдаёт только текущие значения. Историю из них собирает Prometheus, который регулярно их читает.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Связанные главы', '1.4 — метрики, 1.3 — OpenTelemetry'),
        ('Зачем это знать', 'Понимать, откуда берётся каждое число на панели и где искать ошибку, если его нет'),
    ],
    sections=[
        ('roles', 'Кто за что отвечает', p(
            'В предыдущих главах мы разбирали, какие данные нужны для расследования. Теперь посмотрим на инструменты, которые эти данные собирают, хранят и показывают. '
            'Всё вместе часто называют «Grafana» или «мониторинг», но за каждый участок пути отвечает свой компонент.'
        ) + flow([
            ('Приложение', 'считает и отдаёт /metrics'),
            ('Prometheus', 'опрашивает, хранит, считает PromQL'),
            ('Grafana', 'запрашивает и рисует панели'),
        ], 'На стенде работают эти три компонента: API — обычный Python-процесс, Prometheus и Grafana — контейнеры Docker.') + table(['Компонент', 'Что делает', 'Чего не делает'], [
            ['Приложение с инструментацией', 'Считает события и длительности, отдаёт текущие значения по адресу /metrics', 'Не хранит историю: после перезапуска счётчики начинаются с нуля'],
            ['Prometheus', 'Регулярно опрашивает приложения, хранит временные ряды, выполняет запросы PromQL и проверяет правила оповещения', 'Не хранит логи и трассы; полноценные дашборды не рисует'],
            ['Grafana', 'Отправляет запросы к источникам данных и показывает результаты панелями', 'Сама метрики не собирает и не хранит'],
            ['Alertmanager', 'Получает сработавшие оповещения от Prometheus, группирует их и отправляет в почту или мессенджер', 'Условия оповещений не вычисляет'],
            ['OpenTelemetry', 'Создаёт телеметрию в коде и передаёт её через Collector в хранилища', 'Данные не хранит и графики не строит'],
        ]) + p(
            'Дальше пройдём по пути одного числа: от строки кода, которая увеличивает счётчик, до цифры на панели Grafana.'
        ) + source('https://prometheus.io/docs/introduction/overview/', 'Prometheus · Overview')),

        ('exposition', 'Приложение отдаёт текущие значения', p(
            'Ответ <code>/metrics</code> по строкам разобран в главе 1.4. Здесь посмотрим, как этот ответ формируется в коде.'
        ) + code('''@app.get("/metrics", include_in_schema=False)
async def metrics():
    lines = ["# HELP lab_http_requests_total Completed products requests.",
             "# TYPE lab_http_requests_total counter"]
    for status, value in totals.items():
        lines.append(f'lab_http_requests_total{{route="/products",status="{status}"}} {value}')
    ...
    return Response("\\n".join(lines) + "\\n", media_type="text/plain; version=0.0.4")''', 'Python · фрагмент app.py', 'app.py') + table(['Фрагмент', 'Что делает'], [
            ['<code>totals</code>', 'Словарь с двумя счётчиками: <code>{"200": ..., "500": ...}</code>. Обработчик <code>/products</code> увеличивает нужный в конце каждого запроса'],
            ['<code>{{route=...}}</code>', 'Двойные фигурные скобки в f-строке выводят одинарные: так в тексте появляется <code>{route="/products",status="200"}</code>'],
            ['<code>include_in_schema=False</code>', 'Прячет адрес из Swagger: он нужен Prometheus, а не людям'],
            ['<code>Response(...)</code>', 'Возвращает строки обычным текстом в формате, который понимает Prometheus'],
        ]) + p(
            'Этот адрес возвращает только текущее состояние счётчиков. '
            'Два запроса к <code>/metrics</code> подряд покажут два значения, но хранить их по времени приложение не будет. Историю создаёт тот, кто регулярно читает адрес.'
        ) + p(
            'На стенде формат собран вручную, чтобы механизм был виден целиком. В рабочих приложениях используют библиотеку <code>prometheus_client</code>: '
            'она ведёт счётчики и гистограммы, правильно экранирует значения меток и работает, когда приложение запущено в нескольких процессах.'
        )),

        ('pull', 'Prometheus сам опрашивает приложение', term('Опрос (scrape)', 'HTTP-запрос, которым Prometheus читает метрики с адреса приложения. '
             'Опросы повторяются с заданным интервалом, и каждое прочитанное значение сохраняется с отметкой времени.') + p(
            'Такую схему называют сбором по запросу (pull): запрос отправляет система мониторинга, а приложение только отвечает. '
            'Пройдите по шагам схемы и посмотрите, в какую сторону идёт HTTP-запрос и в какую — данные.'
        ) + SCRAPE_WIDGET + p(
            'Стрелки на схемах мониторинга часто рисуют от приложения к Prometheus, по направлению движения данных, '
            'а сетевой запрос при этом идёт в обратную сторону. Это нужно помнить при настройке сети: '
            'Prometheus должен иметь доступ к адресу приложения, а приложению адрес Prometheus знать не нужно.'
        ) + p(
            'Какие адреса опрашивать, Prometheus берёт из файла конфигурации. На стенде он короткий.'
        ) + code('''global:
  scrape_interval: 5s
scrape_configs:
  - job_name: telemetry-lab
    static_configs:
      - targets: ['host.docker.internal:8015']''', 'YAML · prometheus.yml', 'prometheus.yml') + table(['Строка', 'Что означает'], [
            ['<code>scrape_interval: 5s</code>', 'Опрашивать каждые 5 секунд. По умолчанию Prometheus опрашивает раз в минуту; на стенде интервал короткий, чтобы изменения появлялись быстро'],
            ['<code>job_name: telemetry-lab</code>', 'Имя группы целей. Оно попадёт в каждый ряд меткой <code>job</code>'],
            ['<code>targets</code>', 'Список адресов. Путь <code>/metrics</code> не указан: он используется по умолчанию'],
            ['<code>host.docker.internal</code>', 'Имя, по которому контейнер обращается к компьютеру, на котором он запущен. API работает не в контейнере, поэтому Prometheus ищет его на хост-машине'],
        ]) + p(
            'Бывает и обратная схема: приложение само отправляет метрики. Её используют для коротких задач, которые завершаются раньше, чем их успеют опросить, '
            'например для ночного скрипта резервного копирования. Для них у Prometheus есть отдельный компонент Pushgateway. Долго работающий веб-сервис удобнее опрашивать напрямую.'
        ) + source('https://prometheus.io/docs/prometheus/latest/configuration/configuration/', 'Prometheus · Configuration')),

        ('targets', 'Проверка цели на странице Target health', p(
            'После запуска Prometheus первым делом открывают страницу состояния целей. Она показывает, получается ли вообще опрашивать приложение. '
            'Если здесь ошибка, все остальные запросы и панели будут пустыми.'
        ) + shot('prometheus-targets', 'страница Status → Target health в Prometheus 3.5', p(
            'Одна группа целей telemetry-lab, в ней одна цель. Зелёная полоса слева означает, что все цели группы доступны.'
        ), pins=TARGETS_PINS) + table(['Колонка', 'Значение на снимке', 'Что означает'], [
            ['Endpoint', 'http://host.docker.internal:8015/metrics', 'Адрес из targets и путь по умолчанию'],
            ['Labels', 'instance="host.docker.internal:8015", job="telemetry-lab"', 'Метки, которые Prometheus добавит к каждому ряду этой цели'],
            ['Last scrape', '3.194s ago · 2ms', 'Последний опрос был 3,2 секунды назад и занял 2 мс'],
            ['State', 'UP', 'Последний опрос завершился успешно'],
        ]) + p(
            'Метки <code>instance</code> и <code>job</code> приложение не отдавало, их добавляет сам Prometheus. '
            'Благодаря им ряды разных экземпляров одного сервиса не смешиваются, даже если метрики называются одинаково.'
        ) + p(
            'По колонке Last scrape проверяют интервал: при <code>scrape_interval: 5s</code> значение «сколько секунд назад» не должно превышать пяти. '
            'Если цель недоступна, в колонке State появится DOWN и текст ошибки, например <code>connection refused</code> — по указанному адресу никто не слушает.'
        ) + note('Метрика up', 'Для каждой цели Prometheus сам записывает ряд <code>up</code>: 1, если опрос успешен, и 0, если нет. '
                 'Правило оповещения на <code>up == 0</code> сообщит о пропаже цели. Без него сломанный сбор выглядит так же, как сервис без запросов.')),

        ('table', 'Текущие значения в таблице', p(
            'На вкладке Query в поле выражения вводят запрос на языке PromQL и нажимают Execute. '
            'Самый простой запрос — имя метрики. Он возвращает последние значения всех её рядов.'
        ) + shot('prometheus-query', 'запрос lab_http_requests_total на вкладке Table', p(
            'Prometheus нашёл два ряда и показал последнее значение каждого.'
        ), pins=QUERY_PINS) + p(
            'Каждая строка таблицы — один временной ряд: слева имя и полный набор меток, справа значение. '
            'Из четырёх меток <code>route</code> и <code>status</code> пришли из ответа приложения, а <code>instance</code> и <code>job</code> добавил Prometheus при опросе.'
        ) + p(
            'Значения 363 и 38 — счётчики с момента запуска API: 363 успешных запроса и 38 ошибочных. '
            'Поле Evaluation time позволяет посмотреть значения на любой момент в прошлом.'
        ) + p(
            'Для вопроса «как работает сервис» эти числа мало что дают: 38 ошибок могли накопиться за сутки или за минуту. '
            'Нужна скорость, и её даёт следующий запрос.'
        )),

        ('graph', 'Скорость на графике', p(
            'Переключимся на вкладку Graph и введём выражение, которое переводит счётчик в скорость и делит её по кодам ответа.'
        ) + code('sum by (status) (rate(lab_http_requests_total[1m]))', 'PromQL · запросов в секунду по коду ответа') + p(
            'Выражение читают изнутри наружу.'
        ) + steps([
            ('lab_http_requests_total[1m]', 'Все значения каждого ряда за последнюю минуту. При опросе раз в 5 секунд это около двенадцати точек.'),
            ('rate(...)', 'Средний прирост в секунду за эту минуту. Функция учитывает сбросы счётчика при перезапуске, о которых шла речь в главе 1.4.'),
            ('sum by (status) (...)', 'Сложить ряды с одинаковой меткой <code>status</code> и убрать остальные метки. Будь экземпляров три, их скорости сложились бы в одну линию на каждый код.'),
        ]) + shot('prometheus-graph', 'график скорости запросов по кодам ответа за 5 минут', p(
            'Две линии: верхняя — ответы 200, нижняя, которая появляется в середине, — ответы 500. По вертикали — запросы в секунду.'
        ), pins=GRAPH_PINS) + p(
            'Снимок сделан сразу после специального прогона. Две минуты скрипт <code>tools/traffic.py</code> отправлял только быстрые запросы, '
            'затем полторы минуты — смесь, где каждый четвёртый запрос завершается ошибкой, а каждый двадцать пятый выполняется медленно.'
        ) + table(['Участок графика', 'Что видно', 'Почему так'], [
            ['Слева, до начала нагрузки', 'Нулевая линия', 'Запросов к API не было'],
            ['Подъём ступеньками', 'Скорость 200 растёт около минуты', 'Окно [1m] заполняется постепенно; ступенька — каждый новый опрос раз в 5 секунд'],
            ['Ровный участок около 2,0', 'Два успешных запроса в секунду', 'Скрипт ждёт 0,4 с между запросами, а запрос длится около 85 мс'],
            ['Появление нижней линии', 'Скорость 500 растёт до 0,4', 'Каждый четвёртый запрос стал ошибкой'],
            ['Верхняя линия снижается до 1,3', 'Успешных ответов стало меньше', 'Четверть запросов ушла в ошибки, а медленные запросы по 3 с уменьшили общий темп'],
            ['Спад справа', 'Обе линии падают', 'Нагрузка закончилась, и окно [1m] постепенно освобождается'],
        ]) + p(
            'Нагрузка поменялась мгновенно, а линии меняются около минуты, потому что скорость усредняется по последней минуте окна <code>[1m]</code>. '
            'Короткое окно быстрее реагирует, но сильнее скачет. Длинное сглаживает шум, но позже показывает изменения.'
        ) + example('Почему окно rate не берут короче четырёх интервалов опроса', p(
            'Функция <code>rate</code> считает прирост по точкам, попавшим в окно, а точек в окне столько, сколько опросов туда поместилось. '
            'Возьмём обычную рабочую настройку <code>scrape_interval: 15s</code>.'
        ) + table(['Окно', 'Точек в окне', 'Что получится на графике'], [
            ['<code>[20s]</code>', '1 или 2', 'Прирост посчитать не из чего: при одной точке значения нет, линия рвётся'],
            ['<code>[30s]</code>', '2 или 3', 'Линия есть, но один пропущенный опрос снова разрывает её'],
            ['<code>[1m]</code>', '4 или 5', 'Линия непрерывна и переживает один пропущенный опрос'],
            ['<code>[5m]</code>', '20 или 21', 'Линия гладкая, но изменение нагрузки видно с задержкой в несколько минут'],
        ]) + p(
            'Поэтому окно берут не меньше четырёх интервалов опроса. При <code>scrape_interval: 15s</code> это <code>[1m]</code>, '
            'при 5 секундах на стенде хватило бы и <code>[20s]</code>, но мы берём <code>[1m]</code>, чтобы линия была спокойнее.'
        ) + compare(
            ('prometheus.yml', 'Интервал опроса', p('Определяет, как часто появляются точки.'), 'plain'),
            ('Запрос PromQL', 'Окно', p('Определяет, по скольким точкам считается скорость. Ради ровной линии достаточно подобрать окно, интервал менять не нужно.'), 'plain'),
        )) + formula('доля ошибок ≈ <em>0,43</em> / (1,3 + 0,43) ≈ 25%', [
            ('0,43', 'скорость ответов 500 на втором участке'),
            ('1,3', 'скорость ответов 200 на том же участке'),
        ], 'Каждый четвёртый запрос — ровно так была задана нагрузка. Совпадение подтверждает, что метрика и запрос устроены правильно.') + source('https://prometheus.io/docs/prometheus/latest/querying/functions/#rate', 'Prometheus · rate()')),

        ('grafana', 'Дашборд Grafana', p(
            'В интерфейсе Prometheus удобно проверять отдельные запросы, а для постоянного наблюдения используют Grafana. '
            'Она хранит набор панелей, каждая выполняет свой запрос к источнику данных и показывает результат.'
        ) + term('Дашборд', 'страница Grafana с набором панелей. У каждой панели есть источник данных, запрос, диапазон времени и способ отображения.') + p(
            'Источник данных на стенде настраивается файлом, поэтому после запуска контейнера ничего не нужно указывать вручную.'
        ) + code('''apiVersion: 1
datasources:
  - name: Prometheus · стенд
    type: prometheus
    uid: prometheus-lab
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false''', 'YAML · grafana/provisioning/datasources/prometheus.yaml', 'grafana/provisioning/datasources/prometheus.yaml') + table(['Строка', 'Что означает'], [
            ['<code>url: http://prometheus:9090</code>', 'Адрес работает внутри Docker Compose: контейнеры одного проекта находят друг друга по имени сервиса'],
            ['<code>access: proxy</code>', 'Запросы к Prometheus отправляет сервер Grafana, а не браузер пользователя'],
            ['<code>editable: false</code>', 'Источник нельзя изменить через интерфейс, поэтому стенд у всех одинаковый'],
        ]) + shot('grafana-dashboard', 'дашборд «Telemetry Lab · от запроса к метрике» за последние 5 минут', p(
            'Четыре панели: два счётчика с момента запуска сверху, скорость и средняя длительность снизу.'
        ), pins=GRAFANA_PINS) + table(['Панель', 'Запрос PromQL', 'Что показывает'], [
            ['Завершённые запросы · с запуска', '<code>sum(lab_http_requests_total)</code>', 'Сумма обоих рядов счётчика'],
            ['Ошибки HTTP 500 · с запуска', '<code>sum(lab_http_requests_total{status="500"})</code>', 'Только ряд с кодом 500'],
            ['Поток запросов · запросов/с', '<code>sum(rate(lab_http_requests_total[1m]))</code>', 'Скорость всех запросов'],
            ['Средняя длительность · обработчик', '<code>rate(..._sum[1m]) / rate(..._count[1m])</code>', 'Прирост суммы длительностей, делённый на прирост числа запросов'],
        ]) + p(
            'Сверим панели с Prometheus. Верхняя левая показывает 401 — ровно 363 + 38 из таблицы выше. Верхняя правая показывает 38. '
            'Числа совпадают, потому что Grafana выполнила запросы к тому же Prometheus.'
        ) + p(
            'На мини-графиках под крупными цифрами видно, как сбрасывается счётчик. '
            'Слева линия держится на ровном уровне, затем резко падает к нулю и снова растёт: '
            'ровный уровень — счётчик предыдущего запуска API, падение — перезапуск процесса перед прогоном, рост — новый счёт с нуля.'
        ) + p(
            'Нижняя правая панель показывает среднюю длительность. На первом участке она держится около 83 мс: все запросы быстрые. '
            'На втором поднимается до 185 мс из-за медленных запросов: каждый двадцать пятый длится 3 секунды и добавляет к среднему около 3000 / 25 = 120 мс. '
            'Быстрые ошибки немного тянут среднее вниз. Та же картина была в главе 1.4: пара долгих запросов сильно сдвигает среднее.'
        ) + warn('Анонимный просмотр', 'В правом верхнем углу Grafana стоит кнопка Sign in. На стенде включён анонимный просмотр с ролью Viewer: дашборд можно смотреть без входа, но нельзя менять. '
                 'Для стенда на своём компьютере это удобно, для рабочей Grafana недопустимо.') + source('https://grafana.com/docs/grafana/latest/fundamentals/', 'Grafana · Fundamentals')),

        ('compose', 'Как запущен стенд', p(
            'Prometheus и Grafana запускаются одной командой через Docker Compose. Файл описывает оба контейнера и их настройки.'
        ) + code('''name: makarov-telemetry-intro
services:
  prometheus:
    image: prom/prometheus:v3.5.0
    ports: ['127.0.0.1:9095:9090']
    volumes: ['./prometheus.yml:/etc/prometheus/prometheus.yml:ro']
    extra_hosts: ['host.docker.internal:host-gateway']
  grafana:
    image: grafana/grafana:12.1.1
    ports: ['127.0.0.1:3015:3000']
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: 'true'
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
      GF_AUTH_DISABLE_LOGIN_FORM: 'true'
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro''', 'YAML · compose.yaml (фрагмент)', 'compose.yaml') + table(['Строка', 'Что означает'], [
            ['<code>image</code>', 'Образ с точной версией. Без версии Docker возьмёт последнюю, и через полгода стенд может выглядеть иначе, чем на снимках'],
            ['<code>ports: 127.0.0.1:9095:9090</code>', 'Порт 9090 контейнера доступен на порту 9095 компьютера, и только с локального адреса. С других машин стенд недоступен'],
            ['<code>volumes</code>', 'Подключает файлы конфигурации внутрь контейнера. Суффикс <code>:ro</code> — только для чтения'],
            ['<code>extra_hosts: host-gateway</code>', 'Добавляет имя <code>host.docker.internal</code> на Linux. В Docker Desktop для macOS и Windows это имя есть и без настройки'],
            ['<code>GF_AUTH_*</code>', 'Включают анонимный просмотр и скрывают форму входа'],
        ]) + steps([
            ('docker compose up -d', 'Скачивает образы при первом запуске и запускает контейнеры в фоне. API к этому моменту уже запущен с <code>--host 0.0.0.0</code>.'),
            ('docker compose ps', 'Показывает состояние контейнеров.'),
            ('docker compose down', 'Останавливает и удаляет контейнеры этого проекта, не затрагивая другие.'),
        ]) + warn('Адрес 0.0.0.0', 'API для стенда запускают с <code>--host 0.0.0.0</code>, иначе контейнер Prometheus до него не достучится. '
                  'Такой адрес открывает API на всех сетевых интерфейсах компьютера, поэтому стенд запускают только в доверенной сети. '
                  'Для практики без Prometheus достаточно адреса <code>127.0.0.1</code>.')),
    ],
    mistakes=[
        ['Ждут, что приложение само сохранит историю метрик', 'После перезапуска значения пропадают, графиков нет', 'Настроить опрос Prometheus и проверить цель на странице Target health'],
        ['Строят панели, не проверив состояние цели', 'Пустые графики принимают за отсутствие запросов', 'Сначала убедиться, что цель в состоянии UP'],
        ['Выводят сырой счётчик с подписью «запросов в минуту»', 'Число только растёт и не отражает нагрузку', 'Использовать rate или increase за окно'],
        ['Считают, что Grafana хранит данные', 'Ищут пропавшие данные не в той системе', 'Проверять источник данных и сам запрос панели'],
        ['Выбирают окно rate короче двух интервалов опроса', 'В окне меньше двух точек, линия прерывается', 'Брать окно не меньше четырёх интервалов опроса'],
        ['Открывают стенд с анонимным доступом наружу', 'Любой в сети видит данные', 'Публиковать порты только на 127.0.0.1'],
    ],
    selfcheck=[
        ('Кто отправляет HTTP-запрос при сборе метрик по схеме pull?',
         p('Prometheus. Он отправляет GET к адресу /metrics приложения, а приложение возвращает текущие значения в ответе.')),
        ('На странице Target health цель в состоянии DOWN с ошибкой connection refused. Что проверить?',
         p('Запущен ли API, совпадает ли порт, слушает ли API адрес, доступный из контейнера (0.0.0.0, а не только 127.0.0.1), и разрешается ли имя host.docker.internal.')),
        ('Откуда в рядах метрики появились метки instance и job, если приложение их не отдаёт?',
         p('Их добавляет Prometheus при опросе: job — из job_name конфигурации, instance — из адреса цели.')),
        ('Что вычисляет выражение sum by (status) (rate(lab_http_requests_total[1m]))?',
         p('Среднюю скорость запросов в секунду за последнюю минуту, сложенную по всем рядам с одинаковым кодом ответа. Получается по одной линии на каждый код.')),
        ('Нагрузка изменилась мгновенно, а линия rate(...[1m]) меняется около минуты. Почему?',
         p('Скорость усредняется по окну в одну минуту. Новые значения вытесняют старые из окна постепенно, поэтому линия меняется плавно.')),
    ],
    cheatsheet=[
        'Приложение отдаёт текущие значения, Prometheus хранит историю, Grafana показывает.',
        'Сбор по запросу (pull): Prometheus опрашивает /metrics с интервалом scrape_interval.',
        'Target health: UP — опрос успешен; метрика up = 1 или 0.',
        'Метки instance и job добавляет Prometheus.',
        'Имя метрики в запросе — последние значения всех её рядов.',
        'rate(x[1m]) — средний прирост в секунду за минуту с учётом сбросов.',
        'sum by (метка) — сложить ряды, оставив только указанную метку.',
        'Панель Grafana = источник + запрос + диапазон + отображение.',
        'Средняя длительность из гистограммы: rate(_sum) / rate(_count).',
    ],
    links=[
        ('https://prometheus.io/docs/introduction/overview/', 'Prometheus · Overview'),
        ('https://prometheus.io/docs/prometheus/latest/configuration/configuration/', 'Prometheus · Configuration'),
        ('https://prometheus.io/docs/prometheus/latest/querying/basics/', 'Prometheus · Querying basics'),
        ('https://grafana.com/docs/grafana/latest/fundamentals/', 'Grafana · Fundamentals'),
        ('https://grafana.com/docs/grafana/latest/administration/provisioning/', 'Grafana · Provisioning'),
    ],
)
