from kit import *

SIGNAL_CARDS = '''<div class="signal-cards"><div><span class="signal-icon">∑</span><h3>Метрики</h3><p>Числа и распределения за интервал: сколько запросов, какая доля ошибок, как распределились длительности.</p><code>errors / requests</code></div><div><span class="signal-icon">{ }</span><h3>Логи</h3><p>Записи об отдельных событиях: когда, где и что произошло, с каким результатом.</p><code>event=request_completed</code></div><div><span class="signal-icon">↳</span><h3>Трассы</h3><p>Путь одной операции: из каких этапов она состояла и сколько длился каждый.</p><code>request → wait → serialize</code></div></div>'''

CHAPTER = chapter(
    slug='03-observability',
    title='Наблюдаемость и телеметрия',
    lead='Разбираем, чем наблюдаемость отличается от мониторинга, какие данные система сообщает о себе, как их связать между собой и почему даже полная телеметрия может ошибаться.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Связанные главы', '1.2 — мониторинг, 1.5 — логи, 1.6 — трассы'),
        ('Зачем это знать', 'Понимать, какие данные нужно создать заранее, чтобы потом найти причину сбоя'),
    ],
    sections=[
        ('question', 'Вопрос, для которого нет готовой панели', p(
            'Мониторинг из главы 1.2 отвечает на вопросы, которые мы задали заранее. Мы решили, что важны доля ошибок и длительность запросов, '
            'настроили измерения, построили панели и правила. Пока проблема укладывается в эти вопросы, панели её показывают.'
        ) + p(
            'Теперь представим сервис построения отчётов. Панели в порядке: CPU, память и доля ошибок не изменились. '
            'Пользователи при этом пишут, что отчёты с большим числом строк иногда формируются по минуте, а обычные отчёты — за секунду. '
            'Ни одна панель не делит отчёты по размеру, потому что заранее никто не думал, что размер важен.'
        ) + p(
            'Чтобы разобраться, нужно задать системе новый вопрос: как длительность формирования зависит от числа строк. '
            'Ответ возможен только в одном случае — если размер отчёта был где-то записан вместе с длительностью. '
            'Если такой записи нет, остаётся добавить её в код, выпустить новую версию и ждать, пока проблема повторится.'
        ) + p(
            'Этот пример показывает главную мысль главы. Способность разобраться в новой проблеме определяется тем, какие данные система создавала и сохраняла до её появления. '
            'Эту способность и называют наблюдаемостью.'
        )),

        ('definition', 'Наблюдаемость', term('Наблюдаемость (observability)', 'свойство системы, которое показывает, насколько по её внешним данным можно понять, что происходит внутри. '
             'Для инженера это возможность задать новый вопрос о поведении сервиса и получить ответ из уже собранных данных, не меняя код.') + p(
            'Термин пришёл из теории управления. В 1960 году инженер Рудольф Калман назвал систему наблюдаемой, если её внутреннее состояние можно восстановить по измерениям выходов. '
            'В программных системах смысл тот же: у нас нет доступа к памяти работающего процесса, есть только сигналы, которые он отправил наружу.'
        ) + p(
            'Важно, что наблюдаемость — свойство самой системы, а не программа, которую можно установить. '
            'Сервис может отправлять данные в самые мощные хранилища и оставаться плохо наблюдаемым, если в данных нет нужных подробностей. '
            'И наоборот, небольшой сервис с продуманными записями о событиях бывает наблюдаем хорошо, хотя использует простые инструменты.'
        ) + p(
            'Уровень наблюдаемости удобно оценивать по конкретному вопросу. Для сервиса отчётов вопрос звучит так: '
            'можно ли по сохранённым данным найти медленные отчёты, узнать их размер и увидеть, на каком этапе ушло время. '
            'Если все три ответа есть, для этой проблемы сервис наблюдаем. Если нет размера, наблюдаемость недостаточна, и это можно исправить.'
        ) + source('https://opentelemetry.io/docs/concepts/observability-primer/', 'OpenTelemetry · Observability primer')),

        ('relation', 'Как связаны мониторинг и наблюдаемость', p(
            'Часто встречается короткое противопоставление: мониторинг отвечает на вопрос «что сломалось», наблюдаемость — «почему». '
            'Как первое приближение оно полезно, но на практике границы размыты. '
            'Хорошо подобранные показатели мониторинга часто сразу подсказывают причину. А несколько записей о событиях без нужного контекста причину не объясняют.'
        ) + p(
            'Точнее описывать эти понятия через работу инженера. Мониторинг заранее следит за известными признаками проблем и сообщает, когда они появились. '
            'Наблюдаемость позволяет разобрать проблему, признаки которой заранее никто не описал. '
            'Ниже типичная последовательность действий при сбое, где обе стороны работают вместе.'
        ) + table(['Шаг', 'Что делает инженер', 'На что опирается'], [
            ['1', 'Получает уведомление: доля ошибок /products выросла', 'Мониторинг: заранее настроенное правило'],
            ['2', 'Уточняет область: какая версия, маршрут, экземпляр', 'Метрики с разбивкой по признакам'],
            ['3', 'Находит отдельные неуспешные запросы', 'Логи с идентификатором запроса'],
            ['4', 'Смотрит, на каком этапе запрос потратил время', 'Трасса запроса'],
            ['5', 'Добавляет недостающее измерение, если его не хватило', 'Изменение инструментации'],
        ]) + p(
            'Последний шаг особенно важен. Каждое расследование показывает, каких данных не хватило. '
            'Если после сбоя в код добавить недостающее измерение, следующая похожая проблема разбирается быстрее. '
            'Так наблюдаемость сервиса растёт постепенно, от инцидента к инциденту.'
        )),

        ('telemetry', 'Телеметрия: данные, которые система сообщает о себе', term('Телеметрия', 'данные о работе системы, которые она создаёт во время работы и передаёт для хранения и анализа. '
             'Слово составлено из греческих «теле» — далеко и «метрон» — мера: измерение на расстоянии.') + p(
            'Название хорошо описывает суть. Раньше телеметрией называли, например, показания датчиков космического аппарата, которые передаются на Землю по радио. '
            'Инженеры не могут подойти к аппарату, но по переданным данным понимают, что с ним происходит. '
            'С работающим сервисом положение похожее: мы не можем остановить процесс и заглянуть в его память, но можем получать данные, которые он отправляет.'
        ) + p(
            'В этом справочнике мы используем три основных вида телеметрии. Их часто называют сигналами.'
        ) + SIGNAL_CARDS + p(
            'Сигналы различаются тем, сколько подробностей сохраняют и сколько это стоит. '
            '<strong>Метрика</strong> сжимает тысячи запросов в несколько чисел. Она дёшево хранится годами и хорошо показывает масштаб, но теряет подробности отдельных запросов. '
            '<strong>Лог</strong> хранит запись о каждом событии со всеми полями. Он показывает, что именно случилось, но при большом потоке занимает много места. '
            '<strong>Трасса</strong> хранит этапы одной операции на общей временной шкале. Она показывает, где операция потратила время, но описывает только ту операцию, которую записали.'
        ) + table(['Вопрос', 'Лучший сигнал', 'Почему'], [
            ['Какая доля запросов завершилась ошибкой за час?', 'Метрика', 'Хранит количества, из которых считается доля'],
            ['Какое исключение возникло у запроса 2797ad2d…?', 'Лог', 'Хранит текст ошибки конкретного события'],
            ['На каком этапе медленный запрос потратил три секунды?', 'Трасса', 'Хранит начало и длительность каждого этапа'],
        ]) + p(
            'Сигналы дополняют друг друга, а не заменяют. Типичный разбор идёт от метрики к логу и трассе: '
            'метрика показывает, что и когда ухудшилось, лог — что случилось с отдельными запросами, трасса — где именно ушло время.'
        ) + p(
            'Кроме трёх основных сигналов, существуют и другие. Профили показывают, какие функции программы тратят процессорное время и память. '
            'В этой теме они не нужны: для первых расследований достаточно метрик, логов и трасс.'
        ) + source('https://opentelemetry.io/docs/concepts/signals/', 'OpenTelemetry · Signals')),

        ('instrumentation', 'Инструментация: откуда берутся данные', p(
            'Телеметрия не появляется сама. Если в коде обработчика никто не измерил длительность, никакая система мониторинга её не покажет. '
            'Программа должна сама создать измерение, запись о событии или этап трассы.'
        ) + term('Инструментация', 'код, который добавляют в программу, чтобы она создавала телеметрию: измеряла длительности, считала события, записывала контекст. '
                 'Название происходит от измерительных инструментов, которыми оснащают приборы.') + p(
            'Посмотрим на инструментацию учебного сервиса. Обработчик <code>/products</code> состоит из трёх этапов, и каждый этап выполняется через вспомогательную функцию <code>stage</code>. '
            'Она выполняет работу этапа и записывает, когда этап начался и сколько длился.'
        ) + code('''started = time.perf_counter()
spans = []

async def stage(name, seconds):
    offset = time.perf_counter() - started
    begin = time.perf_counter()
    await asyncio.sleep(seconds)
    spans.append({"name": name, "offset_ms": round(offset * 1000, 3),
                  "duration_ms": round((time.perf_counter() - begin) * 1000, 3)})''', 'Python · фрагмент app.py', 'app.py') + p(
            'Разберём по строкам. <code>time.perf_counter()</code> возвращает показания точного таймера в секундах. '
            'Само число не связано с часами на стене, но разность двух показаний даёт длительность промежутка. '
            'Переменная <code>started</code> хранит момент начала всего запроса.'
        ) + ul([
            '<code>offset</code> — сколько секунд прошло от начала запроса до начала этапа. По этому значению этап ставится на общую временную шкалу.',
            '<code>begin</code> — момент начала самого этапа.',
            '<code>await asyncio.sleep(seconds)</code> — работа этапа. В учебном сервисе это ожидание заданной длительности, в настоящем — например, запрос к базе данных.',
            '<code>spans.append(...)</code> — запись результата: имя этапа, смещение и длительность в миллисекундах, округлённые до трёх знаков.',
        ]) + p(
            'Без этих строк обработчик работал бы так же, но после медленного запроса мы знали бы только общую длительность. '
            'С ними видно, что две секунды из трёх ушли на ожидание зависимости. Такая разница и отличает наблюдаемый код от ненаблюдаемого.'
        ) + p(
            'Инструментацию пишут вручную или подключают автоматически. Автоматическая инструментация — это библиотеки, '
            'которые встраиваются в веб-фреймворк или драйвер базы данных и сами измеряют типовые операции: HTTP-запросы, SQL-запросы, вызовы внешних API. '
            'Она быстро даёт общую картину, но ничего не знает о смысле программы. '
            'Размер отчёта или номер тарифа пользователя может записать только разработчик, который понимает, что эти подробности важны.'
        ) + source('https://opentelemetry.io/docs/concepts/instrumentation/', 'OpenTelemetry · Instrumentation')),

        ('correlation', 'Время и идентификатор связывают события', p(
            'Сервис одновременно обрабатывает много запросов. Записи о событиях разных запросов перемешиваются в журнале, и две строки с одинаковой секундой могут относиться к разным пользователям. '
            'Чтобы собрать все события одного запроса, в каждую запись добавляют общий идентификатор.'
        ) + code('''{
  "timestamp": "2026-09-15T13:06:29.890773+00:00",
  "level": "ERROR",
  "service": "products-api",
  "event": "request_completed",
  "status": 500,
  "request_id": "2797ad2d8174461eb96c36e59f8126da"
}''', 'JSON · поля события учебного сервиса') + p(
            'Поля выполняют разные задачи. <code>timestamp</code> ставит событие на общую шкалу времени. '
            'Окончание <code>+00:00</code> означает время по UTC — всемирному координированному времени без смещения часового пояса. '
            'Если все компоненты пишут время в UTC, записи с серверов в разных часовых поясах можно сравнивать без пересчёта.'
        ) + p(
            '<code>request_id</code> связывает события одного запроса. Учебный сервис создаёт его функцией <code>uuid.uuid4().hex</code>: '
            'это 32 шестнадцатеричных символа, случайно сгенерированных для каждого запроса. Вероятность совпадения двух таких значений настолько мала, что на практике её не учитывают. '
            'Тот же идентификатор сервис возвращает клиенту в заголовке <code>X-Request-ID</code>. '
            'Пользователь может переслать его в поддержку, и разработчик найдёт по нему все записи.'
        ) + p(
            'Когда запрос проходит через несколько сервисов, идентификатор нужно передать дальше, иначе цепочка оборвётся на первом же вызове. '
            'Передачу контекста между сервисами называют распространением контекста (context propagation). '
            'В распределённой трассировке для этого используют два идентификатора: '
            '<code>trace_id</code> — общий для всей операции, и <code>span_id</code> — свой у каждого этапа. '
            'Учебный сервис состоит из одного процесса, поэтому ему достаточно одного <code>request_id</code>.'
        ) + source('https://opentelemetry.io/docs/concepts/context-propagation/', 'OpenTelemetry · Context propagation')),

        ('cardinality', 'Почему идентификатор запроса не добавляют в метрику', p(
            'Раз <code>request_id</code> так полезен, возникает желание добавить его и в метрики. Это одна из самых дорогих ошибок в мониторинге, и причина связана с устройством хранилища.'
        ) + p(
            'Метрику хранят как набор временных рядов. Признаки, по которым делят метрику, называют метками (labels). '
            'Каждое уникальное сочетание значений меток создаёт отдельный временной ряд. '
            'Метрика запросов с метками <code>route</code> и <code>status</code> на нашем стенде хранит два ряда: для статуса 200 и для статуса 500.'
        ) + term('Кардинальность', 'число уникальных значений метки или число временных рядов, которые получаются из всех сочетаний меток. '
                 'Чем выше кардинальность, тем больше памяти и места нужно хранилищу.') + table(['Метки', 'Уникальных значений', 'Временных рядов'], [
            ['route, status', '5 маршрутов × 4 статуса', 'до 20'],
            ['route, status, instance', '5 × 4 × 3 экземпляра', 'до 60'],
            ['route, status, user_id', '5 × 4 × 100 000 пользователей', 'до 2 000 000'],
            ['route, status, request_id', 'новое значение у каждого запроса', 'растёт без ограничения'],
        ]) + p(
            'С <code>request_id</code> каждый запрос создаёт новый ряд, в котором будет ровно одна точка. '
            'Хранилище, рассчитанное на тысячи рядов, получает миллионы, расходует память и замедляет запросы к данным. '
            'При этом пользы нет: ряд из одной точки не показывает ни тенденции, ни масштаба.'
        ) + p(
            'Отсюда практическое правило. В метки метрик кладут признаки с небольшим заранее известным набором значений: маршрут, код ответа, экземпляр, версию. '
            'Идентификаторы запросов, пользователей и заказов записывают в логи и трассы. Там каждое событие и так хранится отдельно, и лишний ряд не создаётся.'
        ) + note('Шаблон маршрута вместо адреса', 'Маршрут в метке записывают шаблоном: <code>/products/{id}</code>, а не <code>/products/1842</code>. '
                 'Иначе каждый товар создаст свой ряд, и метка маршрута превратится в метку идентификатора.')),

        ('opentelemetry', 'OpenTelemetry: общий способ создавать и передавать телеметрию', p(
            'Раньше каждая система мониторинга предлагала свои библиотеки для инструментации. '
            'Если команда меняла хранилище трасс, приходилось переписывать код во всех сервисах. '
            'Чтобы решить эту проблему, в 2019 году проекты OpenTracing и OpenCensus объединились в OpenTelemetry.'
        ) + term('OpenTelemetry', 'открытый стандарт и набор инструментов для создания, сбора и передачи телеметрии: метрик, логов и трасс. '
                 'Проект развивается в фонде CNCF и не привязан к конкретному хранилищу.') + p(
            'В OpenTelemetry входят несколько частей. Каждая отвечает за свой участок пути данных.'
        ) + table(['Часть', 'Что делает'], [
            ['API', 'Набор функций, которыми код создаёт измерения, записи и этапы трасс'],
            ['SDK', 'Реализация API для конкретного языка: обрабатывает данные и отправляет их дальше'],
            ['Библиотеки инструментации', 'Автоматически измеряют типовые операции фреймворков и драйверов'],
            ['Collector', 'Отдельная программа, которая принимает телеметрию, обрабатывает её и пересылает в хранилища'],
            ['OTLP', 'Протокол передачи телеметрии между частями системы'],
        ]) + p(
            'Важно понимать, чего в этом списке нет. OpenTelemetry не хранит данные и не строит графики. '
            'Метрики сохраняет, например, Prometheus, трассы — Jaeger или Grafana Tempo, логи — Loki или Elasticsearch. '
            'OpenTelemetry отвечает за то, чтобы данные были созданы одинаково и доставлены туда, куда нужно.'
        ) + p(
            'Учебный сервис этой темы OpenTelemetry не использует: этапы запроса измеряет короткая функция <code>stage</code>, которую мы разобрали выше. '
            'Так механизм виден целиком, без библиотек. Термины при этом совпадают с OpenTelemetry, поэтому переход на стандартную инструментацию не потребует новых понятий.'
        ) + source('https://opentelemetry.io/docs/what-is-opentelemetry/', 'What is OpenTelemetry?')),

        ('limits', 'Телеметрия тоже может ошибаться', p(
            'Телеметрия описывает только то, что было измерено и доставлено. Между событием в программе и записью в хранилище много участков, и на каждом данные могут исказиться или потеряться.'
        ) + table(['Источник искажения', 'Что происходит', 'Как это проявляется'], [
            ['Потеря при аварии', 'Процесс завершился раньше, чем отправил накопленные записи', 'Последних событий перед падением нет в журнале'],
            ['Выборка трасс', 'При большом потоке сохраняют только часть трасс', 'Нужной трассы нет, хотя запрос был'],
            ['Интервал сбора', 'Метрики читаются раз в несколько секунд', 'Короткий всплеск не виден на графике'],
            ['Расхождение часов', 'Часы разных серверов отличаются на секунды', 'Ответ записан раньше, чем запрос'],
            ['Недоступность цели', 'Система сбора не смогла прочитать метрики', 'На графике разрыв линии'],
        ]) + p(
            'Выборку трасс называют сэмплированием (sampling). Сервис, который обрабатывает десять тысяч запросов в секунду, не может хранить трассу каждого из них. '
            'Поэтому сохраняют, например, один процент трасс или все трассы с ошибками. '
            'Это разумное решение, но его нужно учитывать: сто сохранённых медленных трасс не значат, что все запросы медленные, если сохраняли в первую очередь медленные.'
        ) + p(
            'Из таблицы следует важное правило для расследований. Отсутствие события в данных не доказывает, что события не было. '
            'Прежде чем написать «ошибок в 14:05 не было», стоит проверить, собирались ли в 14:05 данные вообще.'
        ) + example('Совпадение по времени', p(
            'После выпуска новой версии выросла длительность запросов. В тот же момент выросла загрузка CPU. '
            'Инженер записывает в отчёт: «причина — новый алгоритм сортировки».'
        ) + p(
            'Два графика изменились одновременно, но это основание для гипотезы, а не доказательство. '
            'В тот же момент мог вырасти поток пользователей или измениться состав данных. '
            'Для проверки сравнивают одинаковые операции до выпуска и после, смотрят этапы медленных запросов в трассах, '
            'а при возможности откатывают версию и проверяют, вернулось ли поведение. Если после отката длительность снизилась при том же потоке, гипотеза подтверждена.'
        ))),
    ],
    mistakes=[
        ['Считают, что наблюдаемость можно установить', 'Данные есть, но в них нет подробностей для нужного вопроса', 'Определить вопросы расследования и добавить в код измерения, которые на них отвечают'],
        ['Надеются только на автоматическую инструментацию', 'Видны HTTP и SQL, но не видны размер отчёта, тариф, тип операции', 'Добавлять вручную признаки, важные для смысла программы'],
        ['Добавляют request_id или user_id в метки метрики', 'Число рядов растёт без ограничения, хранилище перегружено', 'Идентификаторы писать в логи и трассы, в метки — признаки с небольшим набором значений'],
        ['Пишут время в местном часовом поясе каждого сервера', 'События разных серверов нельзя сравнить без пересчёта', 'Записывать время в UTC'],
        ['Не передают идентификатор при вызове другого сервиса', 'Цепочка запроса обрывается на границе сервиса', 'Передавать контекст в заголовках каждого исходящего вызова'],
        ['Считают отсутствие записи доказательством', 'Потерянные события принимают за «ничего не случилось»', 'Сначала проверить, собирались ли данные за этот период'],
    ],
    selfcheck=[
        ('Сервис отправляет данные в Prometheus и Grafana. Можно ли сказать, что он наблюдаем?',
         p('Нельзя сказать заранее. Наблюдаемость оценивают по вопросу: можно ли по сохранённым данным разобраться в конкретной проблеме. '
           'Если в данных нет нужных подробностей, наличие инструментов не поможет.')),
        ('Чем метрика, лог и трасса отличаются по количеству сохраняемых подробностей?',
         p('Метрика сжимает много событий в числа и теряет подробности отдельных запросов. Лог хранит запись о каждом событии с полями. '
           'Трасса хранит этапы одной операции с их началом и длительностью.')),
        ('Почему метка user_id в метрике запросов опасна для хранилища?',
         p('Каждое уникальное сочетание меток создаёт отдельный временной ряд. У сервиса с 100 000 пользователей метрика получит до 100 000 рядов на каждое сочетание остальных меток.')),
        ('Что делает строка offset = time.perf_counter() - started в функции stage?',
         p('Вычисляет, сколько секунд прошло от начала запроса до начала этапа. По этому смещению этап ставится на общую временную шкалу запроса.')),
        ('Хранит ли OpenTelemetry метрики и трассы?',
         p('Нет. OpenTelemetry создаёт и передаёт телеметрию. Хранят данные отдельные системы, например Prometheus для метрик и Tempo или Jaeger для трасс.')),
    ],
    cheatsheet=[
        'Наблюдаемость — насколько по внешним данным можно понять, что происходит внутри системы.',
        'Наблюдаемость оценивают по конкретному вопросу расследования.',
        'Телеметрия — данные, которые система создаёт о себе во время работы.',
        'Метрики показывают масштаб, логи — отдельные события, трассы — этапы операции.',
        'Инструментация — код, который создаёт телеметрию; смысловые признаки добавляет разработчик.',
        'Время в UTC ставит события на общую шкалу, идентификатор связывает события одной операции.',
        'Метки метрик — признаки с небольшим набором значений; идентификаторы — в логи и трассы.',
        'OpenTelemetry создаёт и передаёт телеметрию, но не хранит её.',
        'Отсутствие записи не доказывает отсутствие события.',
    ],
    links=[
        ('https://opentelemetry.io/docs/concepts/observability-primer/', 'OpenTelemetry · Observability primer'),
        ('https://opentelemetry.io/docs/concepts/signals/', 'OpenTelemetry · Signals'),
        ('https://opentelemetry.io/docs/concepts/instrumentation/', 'OpenTelemetry · Instrumentation'),
        ('https://opentelemetry.io/docs/concepts/context-propagation/', 'OpenTelemetry · Context propagation'),
        ('https://opentelemetry.io/docs/concepts/sampling/', 'OpenTelemetry · Sampling'),
    ],
)
