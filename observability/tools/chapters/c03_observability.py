from kit import *

SIGNAL_MAP = diagram(
    'подробность сигнала и охват операций',
    'Чем больше сигнал сохраняет об одной операции, тем меньше операций он способен охватить. Метрика описывает все запросы несколькими числами, '
    'лог хранит запись о каждом событии, трасса — этапы отдельной операции; трассы чаще всего сохраняют выборочно. '
    'Расположение блоков показывает порядок величин, а не измеренные значения.',
    '<svg viewBox="0 0 680 330" role="img" aria-label="Две оси: охват событий и подробность одной операции. Метрики охватывают все запросы при малой подробности, логи — каждое событие при средней, трассы — часть операций при высокой.">\n<path class="dg-frame" d="M70 30v238h580"/>\n<path class="dg-grid" d="M70 90H650M70 150H650M70 210H650M260 30v238M450 30v238"/>\n<text class="dg-axis" x="62" y="46" text-anchor="end">все</text>\n<text class="dg-axis" x="62" y="154" text-anchor="end">часть</text>\n<text class="dg-axis" x="62" y="264" text-anchor="end">одна</text>\n<text class="dg-small" x="14" y="150" transform="rotate(-90 14 150)" text-anchor="middle">Сколько операций охватывает</text>\n<text class="dg-axis" x="165" y="290" text-anchor="middle">мало</text>\n<text class="dg-axis" x="355" y="290" text-anchor="middle">средне</text>\n<text class="dg-axis" x="550" y="290" text-anchor="middle">много</text>\n<text class="dg-small" x="360" y="316" text-anchor="middle">Сколько подробностей об одной операции</text>\n<g><rect class="dg-box-teal" x="96" y="44" width="138" height="56" rx="2"/><text class="dg-name" x="165" y="68" text-anchor="middle">Метрики</text><text class="dg-small" x="165" y="87" text-anchor="middle">числа за интервал</text></g>\n<g><rect class="dg-box-accent" x="286" y="112" width="138" height="56" rx="2"/><text class="dg-name" x="355" y="136" text-anchor="middle">Логи</text><text class="dg-small" x="355" y="155" text-anchor="middle">событие с полями</text></g>\n<g><rect class="dg-box" x="481" y="196" width="138" height="56" rx="2"/><text class="dg-name" x="550" y="220" text-anchor="middle">Трассы</text><text class="dg-small" x="550" y="239" text-anchor="middle">этапы операции</text></g>\n<path class="dg-arrow" d="M234 72h44l8 44M424 140h44l8 60" stroke-dasharray="5 4"/>\n<text class="dg-small" x="672" y="46" text-anchor="end">дёшево хранить долго</text>\n<text class="dg-small" x="672" y="186" text-anchor="end">дорого: часть трасс отбрасывают</text>\n</svg>')

CHAPTER = chapter(
    slug='03-observability',
    title='Наблюдаемость и телеметрия',
    lead='Разбираем, чем наблюдаемость отличается от мониторинга, какие данные система сообщает о себе, как связать их между собой и где телеметрия искажается или теряется.',
    epigraph='Наблюдаемость оценивают по конкретному вопросу: можно ли ответить на него по данным, которые сервис уже сохранил.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Связанные главы', '1.2 — мониторинг, 1.5 — логи, 1.6 — трассы'),
        ('Зачем это знать', 'Понимать, какие данные создать заранее, чтобы потом найти причину сбоя'),
    ],
    sections=[
        ('question', 'Вопрос, для которого нет готовой панели', p(
            'Мониторинг из главы 1.2 отвечает на вопросы, заданные заранее. Мы решили, что важны доля ошибок и длительность запросов, '
            'настроили измерения, построили панели и правила. Пока проблема укладывается в эти вопросы, панели её показывают.'
        ) + example('Медленные большие отчёты', p(
            'Сервис строит отчёты. CPU, память и доля ошибок на панелях не изменились. '
            'Пользователи при этом пишут, что отчёты с большим числом строк иногда формируются минуту, а обычные — секунду. '
            'Ни одна панель не делит отчёты по размеру: заранее никто не знал, что размер важен.'
        ) + p(
            'Чтобы разобраться, системе нужно задать новый вопрос: как длительность формирования зависит от числа строк. '
            'Ответить на него можно, только если размер отчёта где-то записан вместе с длительностью. '
            'Если такой записи нет, остаётся добавить её в код, выпустить новую версию и ждать, пока проблема повторится.'
        )) + p(
            'Получается, что разобраться в новой проблеме можно только по данным, которые система создавала и сохраняла до её появления. '
            'Эту способность системы и называют наблюдаемостью.'
        )),

        ('definition', 'Наблюдаемость', term('Наблюдаемость (observability)', 'свойство системы: насколько по её внешним данным можно понять, что происходит внутри. '
             'Для инженера это возможность задать новый вопрос о поведении сервиса и получить ответ из уже собранных данных, не меняя код.') + p(
            'Термин пришёл из теории управления. В 1960 году инженер Рудольф Калман назвал систему наблюдаемой, если её внутреннее состояние можно восстановить по измерениям выходов. '
            'В программах смысл тот же: доступа к памяти работающего процесса у нас нет, есть только сигналы, которые он отправил наружу.'
        ) + p(
            'Наблюдаемость — свойство самой системы, а не программа, которую можно установить. '
            'Сервис может отправлять данные в любые хранилища и оставаться плохо наблюдаемым, если в данных нет нужных подробностей. '
            'И наоборот, небольшой сервис с продуманными записями о событиях бывает хорошо наблюдаем на простых инструментах.'
        ) + p(
            'Уровень наблюдаемости оценивают по конкретному вопросу. Для сервиса отчётов вопрос такой: '
            'можно ли по сохранённым данным найти медленные отчёты, узнать их размер и увидеть, на каком этапе ушло время. '
            'Если все три ответа есть, для этой проблемы сервис наблюдаем. Если размера нет, наблюдаемости не хватает, и это исправляется добавлением поля.'
        ) + source('https://opentelemetry.io/docs/concepts/observability-primer/', 'OpenTelemetry · Observability primer')),

        ('relation', 'Как связаны мониторинг и наблюдаемость', p(
            'Часто их противопоставляют так: мониторинг отвечает на вопрос «что сломалось», наблюдаемость — «почему». '
            'Для первого знакомства это годится, но на практике граница размыта. '
            'Хорошо подобранные показатели мониторинга часто сразу подсказывают причину, а записи о событиях без нужного контекста причину не объясняют.'
        ) + p(
            'Точнее описать эти понятия через работу инженера. Мониторинг следит за известными признаками проблем и сообщает, когда они появились. '
            'Наблюдаемость позволяет разобрать проблему, признаки которой заранее никто не описал. '
            'При сбое обе стороны работают вместе.'
        ) + steps([
            ('Получить уведомление', 'Доля ошибок /products выросла. Опора — мониторинг, заранее настроенное правило.'),
            ('Уточнить область', 'Какая версия, маршрут, экземпляр. Опора — метрики с разбивкой по признакам.'),
            ('Найти отдельные неуспешные запросы', 'Опора — логи с идентификатором запроса.'),
            ('Посмотреть, на каком этапе ушло время', 'Опора — трасса запроса.'),
            ('Добавить недостающее измерение', 'Если данных не хватило, в код добавляют новое измерение.'),
        ]) + p(
            'После каждого разбора ясно, каких данных не хватило. Если добавить их в код, следующая похожая проблема разбирается быстрее. '
            'Так наблюдаемость сервиса растёт от инцидента к инциденту.'
        )),

        ('telemetry', 'Телеметрия: данные, которые система сообщает о себе', term('Телеметрия', 'данные о работе системы, которые она создаёт во время работы и передаёт для хранения и анализа. '
             'Слово составлено из греческих «теле» — далеко и «метрон» — мера: измерение на расстоянии.') + p(
            'Раньше телеметрией называли, например, показания датчиков космического аппарата, которые передаются на Землю по радио. '
            'Подойти к аппарату нельзя, но по переданным данным понятно, что с ним происходит. '
            'С работающим сервисом похоже: остановить процесс и заглянуть в его память нельзя, но можно получать данные, которые он отправляет.'
        ) + p(
            'В справочнике используются три основных вида телеметрии. Их называют сигналами.'
        ) + cards([
            ('∑ Метрики', 'Числа за интервал', 'Сколько запросов, какая доля ошибок, как распределились длительности. Сжимают тысячи запросов в несколько чисел, дёшево хранятся годами, но теряют подробности отдельных запросов.'),
            ('{ } Логи', 'Записи о событиях', 'Когда, где и что произошло, с каким результатом. Показывают, что случилось с конкретным запросом, но при большом потоке занимают много места.'),
            ('↳ Трассы', 'Этапы одной операции', 'Из каких шагов состояла операция и сколько длился каждый. Показывают, где ушло время, но только у той операции, которую записали.'),
        ], columns=3) + SIGNAL_MAP + table(['Вопрос', 'Сигнал', 'Почему'], [
            ['Какая доля запросов завершилась ошибкой за час?', 'Метрика', 'Хранит количества, из которых считается доля'],
            ['Какое исключение возникло у запроса 2797ad2d…?', 'Лог', 'Хранит текст ошибки конкретного события'],
            ['На каком этапе медленный запрос потратил три секунды?', 'Трасса', 'Хранит начало и длительность каждого этапа'],
        ]) + p(
            'Обычно разбор идёт от метрики к логу и трассе: '
            'метрика показывает, что и когда ухудшилось, лог — что случилось с отдельными запросами, трасса — где именно ушло время. '
            'Ни один сигнал не заменяет двух других.'
        ) + flow([
            ('Метрика', 'что и когда ухудшилось'),
            ('Лог', 'что случилось с запросом'),
            ('Трасса', 'где ушло время'),
        ]) + p(
            'Кроме трёх основных сигналов, есть и другие. Например, профили показывают, какие функции программы тратят процессорное время и память. '
            'В этой теме они не понадобятся.'
        ) + source('https://opentelemetry.io/docs/concepts/signals/', 'OpenTelemetry · Signals')),

        ('instrumentation', 'Инструментация: откуда берутся данные', p(
            'Телеметрия сама не появляется. Если в коде обработчика никто не измерил длительность, никакая система мониторинга её не покажет: '
            'измерение, запись о событии или этап трассы должна создать сама программа.'
        ) + term('Инструментация', 'код, который добавляют в программу, чтобы она создавала телеметрию: измеряла длительности, считала события, записывала контекст. '
                 'Название — от измерительных инструментов, которыми оснащают приборы.') + p(
            'Посмотрим на инструментацию нашего сервиса. Обработчик <code>/products</code> состоит из трёх этапов, и каждый выполняется через вспомогательную функцию <code>stage</code>. '
            'Она выполняет работу этапа и записывает, когда этап начался и сколько длился.'
        ) + code('''started = time.perf_counter()
spans = []

async def stage(name, seconds):
    offset = time.perf_counter() - started
    begin = time.perf_counter()
    await asyncio.sleep(seconds)
    spans.append({"name": name, "offset_ms": round(offset * 1000, 3),
                  "duration_ms": round((time.perf_counter() - begin) * 1000, 3)})''', 'Python · фрагмент app.py', 'app.py') + p(
            '<code>time.perf_counter()</code> возвращает показания точного таймера в секундах. '
            'С часами на стене это число не связано, но разность двух показаний даёт длительность промежутка. '
            'Переменная <code>started</code> хранит момент начала всего запроса.'
        ) + table(['Строка', 'Что делает'], [
            ['<code>offset = …</code>', 'Сколько секунд прошло от начала запроса до начала этапа. По этому значению этап ставится на общую шкалу времени'],
            ['<code>begin = …</code>', 'Момент начала самого этапа'],
            ['<code>await asyncio.sleep(seconds)</code>', 'Работа этапа. На стенде это ожидание заданной длительности, в настоящем сервисе — например, запрос к базе данных'],
            ['<code>spans.append(...)</code>', 'Запись результата: имя этапа, смещение и длительность в миллисекундах с тремя знаками после запятой'],
        ]) + p(
            'Без этих строк обработчик работал бы так же, но о медленном запросе мы знали бы только общую длительность. '
            'С ними видно, что почти все три секунды ушли на ожидание зависимости.'
        ) + compare(
            ('Автоматическая', 'Библиотеки для фреймворков и драйверов', p('Сами измеряют типовые операции: HTTP-запросы, SQL-запросы, вызовы внешних API. Быстро дают общую картину, но о смысле программы ничего не знают.'), 'plain'),
            ('Ручная', 'Код, который пишет разработчик', p('Добавляет признаки, важные для задачи: размер отчёта, тариф пользователя, тип операции. Их может записать только тот, кто понимает, что они важны.'), 'plain'),
        ) + source('https://opentelemetry.io/docs/concepts/instrumentation/', 'OpenTelemetry · Instrumentation')),

        ('correlation', 'Время и идентификатор связывают события', p(
            'Сервис одновременно обрабатывает много запросов. Записи разных запросов перемешиваются в журнале, и две строки с одинаковой секундой могут относиться к разным пользователям. '
            'Чтобы собрать все события одного запроса, в каждую запись добавляют общий идентификатор.'
        ) + code('''{
  "timestamp": "2026-09-15T13:06:29.890773+00:00",
  "level": "ERROR",
  "service": "products-api",
  "event": "request_completed",
  "status": 500,
  "request_id": "2797ad2d8174461eb96c36e59f8126da"
}''', 'JSON · поля события products-api') + p(
            '<code>timestamp</code> ставит событие на общую шкалу времени. '
            'Окончание <code>+00:00</code> означает время по UTC — всемирному координированному времени без смещения часового пояса. '
            'Если все компоненты пишут время в UTC, записи с серверов в разных часовых поясах сравнивают без пересчёта.'
        ) + p(
            '<code>request_id</code> связывает события одного запроса. Сервис создаёт его функцией <code>uuid.uuid4().hex</code>: '
            'это 32 шестнадцатеричных символа, случайных для каждого запроса. Вероятность совпадения двух таких значений настолько мала, что на практике её не учитывают. '
            'Тот же идентификатор сервис возвращает клиенту в заголовке <code>X-Request-ID</code>. '
            'Пользователь пересылает его в поддержку, и разработчик находит по нему все записи.'
        ) + p(
            'Когда запрос проходит через несколько сервисов, идентификатор передают дальше, иначе цепочка оборвётся на первом же вызове. '
            'Передачу контекста между сервисами называют распространением контекста (context propagation). '
            'В распределённой трассировке для этого два идентификатора: '
            '<code>trace_id</code> — общий для всей операции и <code>span_id</code> — свой у каждого этапа. '
            'Наш сервис состоит из одного процесса, поэтому ему хватает одного <code>request_id</code>.'
        ) + source('https://opentelemetry.io/docs/concepts/context-propagation/', 'OpenTelemetry · Context propagation')),

        ('cardinality', 'Почему идентификатор запроса не добавляют в метрику', p(
            'Раз <code>request_id</code> так помогает, его хочется добавить и в метрики. Делать этого нельзя, и причина в устройстве хранилища.'
        ) + p(
            'Метрику хранят как набор временных рядов. Признаки, по которым её делят, называют метками (labels). '
            'Каждое уникальное сочетание значений меток создаёт отдельный временной ряд. '
            'Метрика запросов с метками <code>route</code> и <code>status</code> на стенде хранит два ряда: для статуса 200 и для статуса 500.'
        ) + term('Кардинальность', 'число уникальных значений метки или число временных рядов, которые получаются из всех сочетаний меток. '
                 'Чем выше кардинальность, тем больше памяти и места нужно хранилищу.') + stats([
            ('до 20', 'route, status', '5 маршрутов × 4 статуса'),
            ('до 60', '+ instance', '× 3 экземпляра'),
            ('до 2 000 000', '+ user_id', '× 100 000 пользователей'),
            ('без предела', '+ request_id', 'новое значение у каждого запроса'),
        ]) + p(
            'С <code>request_id</code> каждый запрос создаёт новый ряд, в котором ровно одна точка. '
            'Хранилище, рассчитанное на тысячи рядов, получает миллионы, расходует память и медленнее отвечает на запросы. '
            'Пользы от этого нет: ряд из одной точки не показывает ни тенденции, ни масштаба.'
        ) + compare(
            ('В метки метрик', 'Небольшой известный набор значений', p('Маршрут, код ответа, экземпляр, версия.'), 'good'),
            ('В логи и трассы', 'Идентификаторы', p('Запроса, пользователя, заказа. Там каждое событие и так хранится отдельно, лишний ряд не создаётся.'), 'plain'),
        ) + warn('Шаблон маршрута вместо адреса', 'Маршрут в метке записывают шаблоном: <code>/products/{id}</code>, а не <code>/products/1842</code>. '
                 'Иначе каждый товар создаст свой ряд, и метка маршрута превратится в метку идентификатора.')),

        ('opentelemetry', 'OpenTelemetry: общий способ создавать и передавать телеметрию', p(
            'Раньше каждая система мониторинга предлагала свои библиотеки для инструментации, '
            'и при смене хранилища трасс приходилось переписывать код во всех сервисах. '
            'В 2019 году проекты OpenTracing и OpenCensus объединились в OpenTelemetry, чтобы у всех был один способ.'
        ) + term('OpenTelemetry', 'открытый стандарт и набор инструментов для создания, сбора и передачи телеметрии: метрик, логов и трасс. '
                 'Проект развивается в фонде CNCF и не привязан к конкретному хранилищу.') + p(
            'OpenTelemetry состоит из нескольких частей, каждая отвечает за свой участок пути данных.'
        ) + flow([
            ('API', 'функции, которыми код создаёт измерения, записи и этапы'),
            ('SDK', 'обрабатывает данные в процессе и отправляет дальше'),
            ('Collector', 'принимает, обрабатывает и пересылает телеметрию'),
            ('Хранилища', 'Prometheus, Tempo, Jaeger, Loki'),
        ], 'Между частями данные идут по протоколу OTLP. Библиотеки инструментации подключаются к SDK и измеряют типовые операции фреймворков и драйверов.') + p(
            'Хранилища в этой цепочке — отдельные системы. OpenTelemetry данные не хранит и графики не строит. '
            'Метрики сохраняет, например, Prometheus, трассы — Jaeger или Grafana Tempo, логи — Loki или Elasticsearch. '
            'OpenTelemetry отвечает за то, чтобы данные были созданы одинаково и доставлены по адресу.'
        ) + p(
            'Сервис этой темы OpenTelemetry не использует: этапы запроса измеряет короткая функция <code>stage</code>, разобранная выше. '
            'Так механизм виден целиком, без библиотек. Термины при этом те же, что в OpenTelemetry, и при переходе на стандартную инструментацию новые понятия не понадобятся.'
        ) + source('https://opentelemetry.io/docs/what-is-opentelemetry/', 'What is OpenTelemetry?')),

        ('limits', 'Где телеметрия искажается и теряется', p(
            'Телеметрия описывает только то, что было измерено и доставлено. Между событием в программе и записью в хранилище много участков, и на каждом данные могут исказиться или потеряться.'
        ) + table(['Источник искажения', 'Что происходит', 'Как это проявляется'], [
            ['Потеря при аварии', 'Процесс завершился раньше, чем отправил накопленные записи', 'Последних событий перед падением нет в журнале'],
            ['Выборка трасс', 'При большом потоке сохраняют только часть трасс', 'Нужной трассы нет, хотя запрос был'],
            ['Интервал сбора', 'Метрики читаются раз в несколько секунд', 'Короткий всплеск не виден на графике'],
            ['Расхождение часов', 'Часы разных серверов отличаются на секунды', 'Ответ записан раньше, чем запрос'],
            ['Недоступность цели', 'Система сбора не смогла прочитать метрики', 'На графике разрыв линии'],
        ]) + p(
            'Выборку трасс называют сэмплированием (sampling). Сервис с десятью тысячами запросов в секунду не может хранить трассу каждого, '
            'поэтому сохраняет, например, один процент трасс или все трассы с ошибками. '
            'Это нужно помнить при выводах: сто сохранённых медленных трасс не значат, что все запросы медленные, если медленные сохраняли в первую очередь.'
        ) + warn('Сначала проверьте, собирались ли данные', 'Прежде чем написать «ошибок в 14:05 не было», проверьте, собирались ли в 14:05 данные вообще.') + example('Совпадение по времени', p(
            'После выпуска новой версии выросла длительность запросов. В тот же момент выросла загрузка CPU. '
            'Инженер записывает в отчёт: «причина — новый алгоритм сортировки».'
        ) + p(
            'Два графика изменились одновременно, но это основание для гипотезы, а не доказательство: '
            'в тот же момент мог вырасти поток пользователей или измениться состав данных. '
            'Для проверки сравнивают одинаковые операции до выпуска и после, смотрят этапы медленных запросов в трассах, '
            'а если можно — откатывают версию. Если после отката длительность снизилась при том же потоке, гипотеза подтверждена.'
        ))),
    ],
    mistakes=[
        ['Считают, что наблюдаемость можно установить', 'Данные есть, но в них нет подробностей для нужного вопроса', 'Определить вопросы расследования и добавить в код измерения, которые на них отвечают'],
        ['Полагаются только на автоматическую инструментацию', 'Видны HTTP и SQL, но не видны размер отчёта, тариф, тип операции', 'Добавлять вручную признаки, важные для задачи программы'],
        ['Добавляют request_id или user_id в метки метрики', 'Число рядов растёт без ограничения, хранилище перегружено', 'Идентификаторы писать в логи и трассы, в метки — признаки с небольшим набором значений'],
        ['Пишут время в местном часовом поясе каждого сервера', 'События разных серверов нельзя сравнить без пересчёта', 'Записывать время в UTC'],
        ['Не передают идентификатор при вызове другого сервиса', 'Цепочка запроса обрывается на границе сервиса', 'Передавать контекст в заголовках каждого исходящего вызова'],
        ['Считают отсутствие записи доказательством', 'Потерянные события принимают за «ничего не случилось»', 'Сначала проверить, собирались ли данные за этот период'],
    ],
    selfcheck=[
        ('Сервис отправляет данные в Prometheus и Grafana. Можно ли сказать, что он наблюдаем?',
         p('Заранее нельзя. Наблюдаемость оценивают по вопросу: можно ли по сохранённым данным разобраться в конкретной проблеме. '
           'Если в данных нет нужных подробностей, инструменты не помогут.')),
        ('Чем метрика, лог и трасса отличаются по количеству сохраняемых подробностей?',
         p('Метрика сжимает много событий в числа и теряет подробности отдельных запросов. Лог хранит запись о каждом событии с полями. '
           'Трасса хранит этапы одной операции с их началом и длительностью.')),
        ('Почему метка user_id в метрике запросов опасна для хранилища?',
         p('Каждое уникальное сочетание меток создаёт отдельный временной ряд. У сервиса со 100 000 пользователей метрика получит до 100 000 рядов на каждое сочетание остальных меток.')),
        ('Что делает строка offset = time.perf_counter() - started в функции stage?',
         p('Вычисляет, сколько секунд прошло от начала запроса до начала этапа. По этому смещению этап ставится на общую шкалу времени запроса.')),
        ('Хранит ли OpenTelemetry метрики и трассы?',
         p('Нет. OpenTelemetry создаёт и передаёт телеметрию. Хранят данные отдельные системы, например Prometheus для метрик и Tempo или Jaeger для трасс.')),
    ],
    cheatsheet=[
        'Наблюдаемость — насколько по внешним данным можно понять, что происходит внутри системы.',
        'Наблюдаемость оценивают по конкретному вопросу расследования.',
        'Телеметрия — данные, которые система создаёт о себе во время работы.',
        'Метрики показывают масштаб, логи — отдельные события, трассы — этапы операции.',
        'Инструментация — код, который создаёт телеметрию; признаки, важные для задачи, добавляет разработчик.',
        'Время в UTC ставит события на общую шкалу, идентификатор связывает события одной операции.',
        'Метки метрик — признаки с небольшим набором значений; идентификаторы — в логи и трассы.',
        'OpenTelemetry создаёт и передаёт телеметрию, но не хранит её.',
        'Отсутствие записи не доказывает отсутствие события.',
    ],
    links=[
        ('https://opentelemetry.io/docs/concepts/observability-primer/', 'OpenTelemetry · Observability primer'),
        ('https://opentelemetry.io/docs/concepts/signals/', 'OpenTelemetry · Signals'),
        ('https://opentelemetry.io/docs/concepts/instrumentation/', 'OpenTelemetry · Instrumentation'),
        ('https://opentelemetry.io/docs/concepts/context-propagation/', 'OpenTelemetry · Context propagation'),
        ('https://opentelemetry.io/docs/concepts/sampling/', 'OpenTelemetry · Sampling'),
    ],
)
