from kit import *

# Полосы рисуются при сборке: водопад виден и без JavaScript, скрипт лишь добавляет выбор этапа.
def _rows():
    event = lab_slow_event()
    total = event['duration_ms']
    spans = [{'name': 'GET /products', 'offset_ms': 0.0, 'duration_ms': total}, *event['spans']]
    out = []
    for i, span in enumerate(spans):
        left = span['offset_ms'] / total * 100
        width = min(100 - left, span['duration_ms'] / total * 100)
        label = f"{span['name']}: начало {span['offset_ms']:.1f} мс; длительность {span['duration_ms']:.1f} мс"
        out.append(f'<div class="waterfall-row{" active" if i == 0 else ""}">'
                   f'<button type="button" aria-pressed="{"true" if i == 0 else "false"}">{span["name"]}</button>'
                   f'<div class="span-track" aria-label="{label}">'
                   f'<div class="span-bar" style="left:{left:.2f}%;width:{width:.2f}%"></div></div></div>')
    return ''.join(out)


ROWS = _rows()

WATERFALL_WIDGET = '''<div class="interactive waterfall" data-waterfall><div class="widget-head"><b>Этапы медленного запроса учебного прогона</b><div class="controls"><button data-trace-step="next">Следующий этап →</button><button data-trace-step="reset">Сначала</button></div></div><div class="waterfall-axis"><span>0 мс</span><span class="trace-end">≈ 3020 мс</span></div><div class="waterfall-rows">''' + ROWS + '''</div><p class="widget-detail" aria-live="polite">Выберите этап, чтобы увидеть его начало и длительность.</p><small>Данные из events.json учебного прогона. Этапы измерены функцией stage в обработчике, без OpenTelemetry.</small></div>'''

QUEUE_TIMELINE = '''<div class="queue-timeline"><div><b>HTTP</b><span>принять запрос → поставить задание в очередь → ответ 202</span><em>0–40 мс</em></div><div><b>Очередь</b><span>задание ждёт свободный обработчик</span><em>40–1200 мс</em></div><div><b>Обработчик</b><span>прочитать данные → построить PDF → сохранить файл</span><em>1200–6200 мс</em></div></div>'''

CHAPTER = chapter(
    slug='06-traces',
    title='Трассы: где операция потратила время',
    lead='Разбираем, из чего состоит трасса, как читать этапы на временной шкале, почему длительности вложенных и параллельных этапов нельзя просто складывать и как связать HTTP-запрос с фоновой задачей.',
    epigraph='Длительность операции говорит сколько; где — говорят только этапы.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Связанные главы', '1.3 — инструментация, 1.5 — логи'),
        ('Зачем это знать', 'Находить этап, на котором медленная операция теряет время'),
    ],
    sections=[
        ('path', 'Операция состоит из этапов', p(
            'Метрики из главы 1.4 показали, что два запроса из двадцати длились около трёх секунд. Логи из главы 1.5 показывают, что с этими запросами произошло: код 200, исключения нет. '
            'Но ни метрика, ни запись о событии не отвечают на вопрос, куда ушли три секунды.'
        ) + p(
            'Обработчик <code>/products</code> выполняет три шага: проверяет параметры, ждёт ответа зависимости и готовит JSON. '
            'Если для каждого шага сохранить момент начала и длительность, станет видно, какой шаг занял время. '
            'Такую запись о шагах одной операции называют трассой.'
        ) + term('Трасса (trace)', 'запись о выполнении одной операции в виде связанных этапов на общей временной шкале. '
                 'Трасса показывает, из каких шагов состояла операция, в каком порядке они шли и сколько длился каждый.') + term(
            'Этап трассы (span)', 'один интервал работы внутри операции: вызов функции, запрос к базе данных, обращение к другому сервису. '
            'У этапа есть имя, время начала, длительность и ссылка на родительский этап.'
        ) + p(
            'Английское слово span означает промежуток, пролёт. В русскоязычной документации его чаще оставляют без перевода, поэтому в этом справочнике мы пишем «этап», '
            'а в коде и интерфейсах инструментов встретится span.'
        ) + p(
            'Трасса полезна уже в одном процессе, как на учебном стенде. Распределённой её называют, когда этапы одной операции выполняются в разных сервисах: '
            'API вызывает сервис цен, тот обращается к кэшу, а общая трасса связывает все три.'
        ) + source('https://opentelemetry.io/docs/concepts/signals/traces/', 'OpenTelemetry · Traces')),

        ('span-fields', 'Что хранит этап трассы', p(
            'В учебном сервисе этап — это словарь из трёх полей. Посмотрим на запись медленного запроса из файла <code>events.json</code>.'
        ) + code('''"spans": [
  {"name": "validate",                    "offset_ms": 0.004,    "duration_ms": 11.168},
  {"name": "dependency.wait (simulated)", "offset_ms": 11.177,   "duration_ms": 3000.058},
  {"name": "serialize",                   "offset_ms": 3011.24,  "duration_ms": 10.419}
]''', 'JSON · этапы медленного запроса') + p(
            '<code>name</code> — имя этапа. <code>offset_ms</code> — смещение: сколько миллисекунд прошло от начала запроса до начала этапа. '
            '<code>duration_ms</code> — длительность этапа. Смещение и длительность вместе задают положение и длину полосы на шкале.'
        ) + p(
            'Проверим согласованность. <code>validate</code> начался почти сразу и длился 11,168 мс. Значит, закончился около 11,17 мс, '
            'и следующий этап начался на 11,177 мс. Ожидание зависимости длилось 3000 мс и закончилось на 3011,2 мс. Там же начался <code>serialize</code>. '
            'Этапы идут друг за другом без промежутков.'
        ) + p(
            'В стандартных системах трассировки у этапа больше полей. Они нужны, чтобы собирать этапы из разных процессов в одну трассу.'
        ) + table(['Поле', 'Назначение'], [
            ['trace_id', 'Идентификатор всей операции; одинаковый у всех её этапов'],
            ['span_id', 'Идентификатор этого этапа'],
            ['parent_span_id', 'Идентификатор родительского этапа; по нему строится дерево'],
            ['start_time, end_time', 'Абсолютное время начала и конца'],
            ['attributes', 'Пары ключ–значение с подробностями: SQL-запрос, адрес, размер ответа'],
            ['events', 'События внутри этапа с отметкой времени, например исключение'],
            ['status', 'Результат этапа: успешно или с ошибкой'],
        ])),

        ('waterfall', 'Как читать временную шкалу', p(
            'Этапы трассы принято рисовать горизонтальными полосами друг под другом на общей оси времени. '
            'Из-за ступенчатого вида такую диаграмму называют водопадом (waterfall). Ниже этапы медленного запроса учебного прогона.'
        ) + WATERFALL_WIDGET + p(
            'Верхняя полоса <code>GET /products</code> — весь запрос целиком. Это корневой этап: все остальные входят в него. '
            'Ниже три дочерних этапа. Нажимайте «Следующий этап» и сравнивайте положение полосы с числами в подписи.'
        ) + p(
            'Полоса <code>dependency.wait</code> занимает почти всю ширину шкалы. Больше ничего выяснять не нужно: из трёх секунд запроса три секунды приходятся на ожидание зависимости. '
            'Проверка параметров и подготовка ответа занимают по 11 мс и на такой шкале видны как тонкие черты.'
        ) + shot('api-slow', 'этапы медленного запроса на странице стенда', p(
            'Блок TRACE внизу страницы стенда рисует те же этапы, что и схема выше, но для другого медленного запроса.'
        )) + p(
            'На снимке обработчик работал 3022,7 мс, а сумма этапов равна 11,1 + 3001,1 + 10,6 = 3022,8 мс. '
            'Суммы совпадают с точностью до округления, значит, между этапами код почти ничего не делает.'
        ) + p(
            'Имя этапа в учебном сервисе прямо говорит, что ожидание смоделировано. В настоящем приложении этап с именем <code>database</code> и длительностью 3 секунды оставил бы несколько вариантов: '
            'запрос ждал свободное соединение, ждал сеть или сам SQL выполнялся долго. '
            'Чтобы трасса отвечала точно, крупный этап разбивают на части: получение соединения, отправка запроса, выполнение, чтение результата.'
        )),

        ('nesting', 'Вложенные этапы не складывают', p(
            'Этапы образуют дерево. Функция обработчика вызывает функцию загрузки товаров, та отправляет SQL-запрос. '
            'Каждый вызов становится этапом, вложенным в этап вызывающей функции.'
        ) + table(['Этап', 'Родитель', 'Интервал', 'Длительность'], [
            ['GET /products', '—', '0–500 мс', '500 мс'],
            ['load_products', 'GET /products', '50–450 мс', '400 мс'],
            ['SELECT products', 'load_products', '100–400 мс', '300 мс'],
        ]) + p(
            'Сложим длительности: 500 + 400 + 300 = 1200 мс. Запрос при этом длился 500 мс. '
            'Ошибка в том, что 300 мс SQL-запроса уже входят в 400 мс функции загрузки, а те — в 500 мс всего запроса. Складывать вложенные интервалы нельзя.'
        ) + p(
            'Полезнее посчитать собственное время каждого этапа — время, не занятое дочерними этапами. '
            'У <code>GET /products</code> оно равно 500 − 400 = 100 мс, у <code>load_products</code> 400 − 300 = 100 мс, у SQL-запроса все 300 мс. '
            'Сумма собственных времён даёт ровно 500 мс. Такой расчёт показывает, что основное время тратит сам запрос к базе.'
        )),

        ('parallel', 'Параллельные этапы перекрываются', p(
            'Обработчик может ждать несколько зависимостей одновременно. В асинхронном Python для этого используют <code>asyncio.gather</code>. '
            'Сравним последовательный и параллельный вызовы двух зависимостей: кэша на 40 мс и внешнего API на 900 мс.'
        ) + code('''import asyncio
import time


async def call(name, seconds):
    await asyncio.sleep(seconds)
    return name


async def main():
    start = time.perf_counter()
    await call("redis", 0.04)
    await call("external_api", 0.9)
    print(f"последовательно: {(time.perf_counter() - start) * 1000:.0f} мс")

    start = time.perf_counter()
    await asyncio.gather(call("redis", 0.04), call("external_api", 0.9))
    print(f"параллельно: {(time.perf_counter() - start) * 1000:.0f} мс")


asyncio.run(main())''', 'Python · parallel.py') + output('''последовательно: 942 мс
параллельно: 902 мс''') + p(
            'В первой части второй <code>await</code> ждёт завершения первого, и длительности складываются: 40 + 900 мс плюс пара миллисекунд накладных расходов. '
            '<code>asyncio.gather</code> запускает обе корутины сразу и ждёт, пока завершатся все. Общее время равно самой долгой из них: около 900 мс.'
        ) + table(['Порядок', 'Интервалы этапов', 'Длительность операции'], [
            ['Последовательно', 'redis: 0–40 мс; external_api: 40–940 мс', '≈ 940 мс'],
            ['Параллельно', 'redis: 0–40 мс; external_api: 0–900 мс', '≈ 900 мс'],
        ]) + p(
            'На водопаде параллельные этапы начинаются в одной точке и перекрываются. Их длительности тоже нельзя складывать: 40 + 900 мс не равно 900 мс операции.'
        ) + p(
            'Из этого примера следует практический вывод. Цепочку этапов, от которой зависит длительность всей операции, называют критическим путём. '
            'Если ускорить кэш с 40 до 5 мс, параллельная операция всё равно будет длиться 900 мс: на критическом пути стоит внешний API. '
            'Трасса сразу показывает, оптимизация какого этапа даст результат.'
        )),

        ('gaps', 'Промежутки между этапами', p(
            'Иногда между полосами на водопаде остаётся пустое место. Родительский этап длится 800 мс, дочерние в сумме 300 мс, и 500 мс ничем не покрыты.'
        ) + p(
            'Пустой промежуток означает одно: в это время не шёл ни один измеренный этап. Причин может быть несколько. '
            'Код выполнял работу, для которой этап не создали. Процесс ждал блокировку. '
            'Асинхронная задача ждала своей очереди в цикле событий, потому что цикл был занят другой синхронной работой.'
        ) + example('Как закрыть промежуток измерениями', p(
            'В трассе отчёта родительский этап <code>build_report</code> длится 800 мс. Внутри него один дочерний этап <code>SELECT rows</code> на 300 мс. '
            'Остальные 500 мс на водопаде пустые. Записывать в отчёт «база тормозит» нельзя: измеренный SQL как раз быстрый, а неизвестен именно непокрытый участок.'
        ) + p(
            'Разработчик добавляет два этапа вокруг подозрительных участков кода: <code>render_rows</code> вокруг сборки строк отчёта '
            'и <code>write_pdf</code> вокруг записи файла. Одна строка инструментации на участок, новая версия выкатывается, и следующая трасса показывает картину целиком.'
        ) + table(['Этап', 'Длительность', 'Что изменилось'], [
            ['<code>build_report</code>', '800 мс', 'Как и раньше'],
            ['<code>SELECT rows</code>', '300 мс', 'Как и раньше'],
            ['<code>render_rows</code>', '480 мс', 'Был частью пустого промежутка'],
            ['<code>write_pdf</code>', '15 мс', 'Был частью пустого промежутка'],
        ]) + p(
            'Промежуток сократился с 500 мс до 5 мс, и место задержки названо: сборка строк отчёта, а не база данных. '
            'Это обычный порядок работы с трассами: пустой участок — не ответ, а указание, куда добавить следующий этап.'
        )) + p(
            'По одному промежутку причину не определить. Нужно добавить этапы в подозрительные участки кода или посмотреть записи логов, которые попадают в этот интервал по времени и request_id. '
            'Точно утверждать можно только то, что текущая инструментация этот участок не покрывает.'
        )),

        ('queue', 'HTTP-ответ и фоновая задача', p(
            'Не вся работа выполняется внутри HTTP-запроса. Построение отчёта занимает секунды, и держать соединение всё это время неудобно. '
            'Поэтому маршрут <code>POST /reports</code> кладёт задание в очередь и сразу отвечает кодом 202 Accepted — «принято к обработке». '
            'Отчёт строит отдельный процесс-обработчик, когда до задания дойдёт очередь.'
        ) + QUEUE_TIMELINE + p(
            'Схема показывает учебные числа, но расклад типичен. HTTP-запрос длился 40 мс. Задание ещё 1,2 секунды ждало свободный обработчик, а потом строилось 5 секунд. '
            'Пользователь получил отчёт через 6,2 секунды после нажатия кнопки, а метрика длительности API показывает 40 мс.'
        ) + p(
            'Отсюда два вывода для наблюдаемости. Первый: для фоновых операций измеряют три разных времени — ответ API, ожидание в очереди и выполнение задания. '
            'Каждое отвечает на свой вопрос, и ни одно не заменяет другое.'
        ) + p(
            'Второй: фоновую задачу нельзя рисовать внутри этапа HTTP-запроса, будто запрос ждал её завершения. '
            'Чтобы связать их в трассе, идентификатор трассы кладут в сообщение очереди вместе с заданием. '
            'Обработчик читает его и создаёт свои этапы с тем же <code>trace_id</code>. '
            'Когда одна задача обрабатывает сразу несколько сообщений, в OpenTelemetry используют связи между этапами (span links) вместо отношения родитель–потомок.'
        )),

        ('sampling', 'Одна трасса и весь сервис', p(
            'Трасса описывает одну операцию. Длинный этап в ней показывает, где время потратил именно этот запрос, но не говорит, как ведут себя остальные.'
        ) + p(
            'Поэтому трассы используют вместе с метриками. Метрика сообщает, что p95 вырос и медленными стали 5% запросов. '
            'Затем берут несколько трасс медленных запросов и несколько трасс быстрых и сравнивают этапы. Если у всех медленных длинный один и тот же этап, а у быстрых он короткий, место проблемы найдено.'
        ) + term('Сэмплирование трасс (sampling)', 'сохранение только части трасс. Сервис с десятками тысяч запросов в секунду не может хранить трассу каждого, поэтому сохраняет выборку.') + table(['Способ', 'Когда принимается решение', 'Особенность'], [
            ['По началу (head-based)', 'В начале операции, до того как известен результат', 'Дёшево; медленные и ошибочные трассы попадают в выборку с той же вероятностью, что и обычные'],
            ['По результату (tail-based)', 'После завершения операции', 'Можно сохранить все ошибки и все медленные трассы; требует буферизации в Collector'],
        ]) + p(
            'Сэмплирование нужно учитывать при выводах. Если сохраняли все медленные трассы и 1% обычных, то среди сохранённых медленных будет большинство. '
            'Отсюда нельзя заключить, что медленных запросов большинство. Долю медленных запросов считают по метрикам, а трассы используют для поиска места.'
        ) + source('https://opentelemetry.io/docs/concepts/sampling/', 'OpenTelemetry · Sampling')),
    ],
    mistakes=[
        ['Складывают длительности всех этапов трассы', 'Сумма больше длительности операции', 'Учитывать вложенность и считать собственное время этапов'],
        ['Складывают длительности параллельных этапов', 'Завышенная оценка и неверный выбор, что оптимизировать', 'Смотреть перекрытие и критический путь на временной шкале'],
        ['Оставляют один крупный этап «database»', 'Непонятно, ждали соединение, сеть или SQL', 'Разбивать крупные этапы на получение соединения, выполнение и чтение'],
        ['Считают пустой промежуток на шкале ожиданием', 'Причину приписывают без измерений', 'Добавить этапы на непокрытый участок или сверить с логами'],
        ['Измеряют фоновую задачу только по ответу API 202', 'Задержка очереди и выполнения не видна', 'Измерять ответ API, ожидание в очереди и выполнение отдельно'],
        ['Считают долю медленных запросов по сохранённым трассам', 'Сэмплирование искажает пропорции', 'Долю считать по метрикам, трассы использовать для поиска места'],
    ],
    selfcheck=[
        ('Запрос длился 600 мс, дочерний этап загрузки данных — 450 мс, вложенный в него SQL — 400 мс. Каково собственное время этапа загрузки данных?',
         p('450 − 400 = 50 мс. Остальные 400 мс приходятся на вложенный SQL-запрос.')),
        ('Обработчик одновременно вызвал два сервиса: на 300 мс и на 700 мс. Сколько займёт ожидание и ускорение какого вызова его сократит?',
         p('Около 700 мс. Сократить ожидание может только ускорение вызова на 700 мс: он лежит на критическом пути.')),
        ('На снимке медленного запроса сумма этапов совпала с временем обработчика. Что это говорит о коде между этапами?',
         p('Код между этапами почти не тратит времени. Все значимые затраты покрыты измеренными этапами.')),
        ('API ответил 202 за 30 мс, а пользователь получил файл через 2 минуты. Какие измерения помогут найти задержку?',
         p('Время ожидания задания в очереди и время выполнения задания обработчиком, связанные с исходным запросом общим идентификатором трассы.')),
        ('Из 100 сохранённых трасс 80 медленные. Можно ли сказать, что 80% запросов медленные?',
         p('Нельзя, если неизвестна стратегия сэмплирования. Медленные трассы могли сохраняться целиком, а обычные — малой долей. Долю медленных считают по метрикам.')),
    ],
    cheatsheet=[
        'Трасса — этапы одной операции на общей временной шкале.',
        'Этап (span): имя, начало, длительность, родитель, атрибуты, статус.',
        'trace_id общий для операции, span_id свой у каждого этапа.',
        'Вложенные этапы входят в родительский; их длительности не складывают.',
        'Параллельные этапы перекрываются; длительность задаёт критический путь.',
        'Пустой промежуток — участок без измерений, а не доказанное ожидание.',
        'Фоновую задачу связывают с запросом через trace_id в сообщении очереди.',
        'Сэмплирование сохраняет часть трасс; доли считают по метрикам.',
    ],
    links=[
        ('https://opentelemetry.io/docs/concepts/signals/traces/', 'OpenTelemetry · Traces'),
        ('https://opentelemetry.io/docs/concepts/sampling/', 'OpenTelemetry · Sampling'),
        ('https://docs.python.org/3/library/asyncio-task.html#asyncio.gather', 'Python · asyncio.gather'),
    ],
)
