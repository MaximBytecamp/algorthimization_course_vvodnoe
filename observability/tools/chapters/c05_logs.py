from kit import *

LOG_WIDGET = '''<div class="interactive log-anatomy" data-log><div class="widget-head"><b>Запись о событии по полям</b></div><div class="log-fields"><button data-field="timestamp">timestamp</button><button data-field="level">level</button><button data-field="service">service</button><button data-field="event">event</button><button data-field="status">status</button><button data-field="request_id">request_id</button><button data-field="duration_ms">duration_ms</button></div><pre><code>{"timestamp": "2026-09-15T13:07:57.857556+00:00", "level": "ERROR",
 "service": "products-api", "event": "request_completed", "status": 500,
 "request_id": "b5408f2265bb49168b4bdfbec2bc0dc7", "duration_ms": 72.936}</code></pre><p class="widget-detail" aria-live="polite">Выберите поле, чтобы увидеть, на какой вопрос оно отвечает и чего не гарантирует.</p></div>'''

CHAPTER = chapter(
    slug='05-logs',
    title='Логи: запись о событии и её контекст',
    lead='Разбираем, из каких полей состоит полезная запись о событии, как работает модуль logging в Python, зачем писать логи в JSON и как читать traceback настоящей ошибки учебного сервиса.',
    epigraph='Запись без контекста нельзя связать ни с запросом, ни с результатом.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Связанные главы', '1.3 — телеметрия, 1.4 — метрики'),
        ('Зачем это знать', 'Писать записи, по которым можно найти конкретный запрос и понять, что с ним случилось'),
    ],
    sections=[
        ('event', 'Лог: запись о событии', p(
            'Из главы 1.4 мы знаем, что метрика сообщает масштаб: за пять минут 5% запросов вернули ошибку. '
            'Но какая это ошибка, у каких запросов и в какой строке кода она возникла, метрика не сохраняет. Для этого нужна запись о каждом событии.'
        ) + term('Лог (журнал событий)', 'последовательность записей о событиях, произошедших в программе. '
                 'Каждая запись фиксирует, когда что-то произошло, где и с каким результатом.') + p(
            'Событием может быть что угодно: завершение HTTP-запроса, вход пользователя, неудачная попытка подключения к базе, запуск фоновой задачи. '
            'Разработчик сам решает, какие события записывать, и от этого решения зависит, что удастся выяснить при расследовании.'
        ) + p(
            'Сравним две записи об одном и том же запросе.'
        ) + code('''Request processed''', 'Лог · запись без контекста') + code('''2026-09-15T13:07:57.857Z ERROR products-api request_completed route=/products status=500 duration_ms=72.936 request_id=b5408f22…''', 'Лог · запись с контекстом') + p(
            'Первая строка сообщает только, что выполнение дошло до вызова логирования. Неизвестно, когда это было, какой запрос обработан и успешно ли. '
            'Если за минуту таких строк тысяча, связать хотя бы одну с жалобой пользователя невозможно.'
        ) + p(
            'Во второй строке есть ответы на вопросы, которые возникают при расследовании: время, уровень важности, сервис, тип события, маршрут, результат, длительность и идентификатор запроса. '
            'По идентификатору эту запись можно найти, получив его от пользователя.'
        )),

        ('fields', 'Какие поля нужны записи', p(
            'Состав полей определяется вопросами, на которые должна отвечать запись. Для веб-сервиса набор почти всегда один и тот же. '
            'Ниже запись учебного сервиса о запросе, завершившемся ошибкой. Выберите поле, чтобы прочитать, что оно даёт.'
        ) + LOG_WIDGET + table(['Поле', 'На какой вопрос отвечает', 'Пример'], [
            ['timestamp', 'Когда произошло событие', '2026-09-15T13:07:57.857556+00:00'],
            ['level', 'Насколько событие важно', 'ERROR'],
            ['service', 'Какой компонент создал запись', 'products-api'],
            ['event', 'Что произошло', 'request_completed'],
            ['route, status', 'Какая операция и с каким результатом', '/products, 500'],
            ['duration_ms', 'Сколько длилась операция и в каких единицах', '72.936'],
            ['request_id', 'К какому запросу относится запись', 'b5408f2265bb49168b4bdfbec2bc0dc7'],
        ]) + p(
            'Отдельно скажем о единицах. Поле <code>duration</code> со значением 72 допускает несколько трактовок: секунды, миллисекунды, микросекунды. '
            'Если единица записана в имени поля, как в <code>duration_ms</code>, ошибиться невозможно. '
            'Время события записывается в UTC с явным смещением <code>+00:00</code>, чтобы записи с разных серверов сравнивались без пересчёта.'
        ) + p(
            'В рабочих сервисах к этому набору добавляют версию приложения и имя экземпляра. '
            'Без версии нельзя понять, появилась ли ошибка после выпуска. Без экземпляра нельзя заметить, что сбоит только один сервер из трёх.'
        )),

        ('levels', 'Уровни важности', p(
            'У каждой записи есть уровень важности. Он позволяет отфильтровать поток: в обычной работе смотреть только предупреждения и ошибки, '
            'а при отладке включать подробные сообщения. В модуле <code>logging</code> Python пять стандартных уровней, у каждого есть числовое значение.'
        ) + table(['Уровень', 'Число', 'Когда использовать', 'Пример'], [
            ['DEBUG', '10', 'Подробности для отладки, которые в рабочей среде обычно выключены', 'Значения параметров запроса к базе'],
            ['INFO', '20', 'Обычные события, подтверждающие работу', 'Запрос обработан, задача запущена'],
            ['WARNING', '30', 'Что-то пошло не так, но операция выполнена', 'Запрос длился дольше порога, выполнен повтор'],
            ['ERROR', '40', 'Операция не выполнена', 'Исключение при обращении к зависимости'],
            ['CRITICAL', '50', 'Приложение не может продолжать работу', 'Не удалось прочитать обязательную настройку при запуске'],
        ]) + p(
            'Логгер пропускает записи, уровень которых не ниже заданного. Если установить уровень INFO, записи DEBUG отбрасываются, а INFO, WARNING, ERROR и CRITICAL проходят. '
            'Сравнение идёт по числу, поэтому уровни можно задавать и своими значениями, хотя обычно хватает стандартных.'
        ) + note('Уровень не показывает масштаб', 'Одна запись ERROR сообщает о сбое одной операции. Одинаковые записи ERROR могут появляться раз в месяц или в каждом запросе. '
                 'Чтобы понять, насколько проблема серьёзна, число ошибочных событий сравнивают с числом всех операций за тот же интервал.')),

        ('logging', 'Модуль logging в Python', p(
            'Для записи логов в Python есть стандартный модуль <code>logging</code>. Им пользуется и учебный сервис, и сам Uvicorn. '
            'Разберём минимальную настройку на отдельном примере.'
        ) + code('''import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(levelname)s %(name)s %(message)s",
)
logger = logging.getLogger("telemetry_lab")

logger.debug("подробности для отладки")
logger.info("request_completed status=%s duration_ms=%s", 200, 82.627)
logger.warning("slow_request duration_ms=%s", 3022.741)''', 'Python · log_basic.py') + output('''2026-09-15 16:17:58,865 INFO telemetry_lab request_completed status=200 duration_ms=82.627
2026-09-15 16:17:58,865 WARNING telemetry_lab slow_request duration_ms=3022.741''') + p(
            'Разберём программу по частям.'
        ) + ul([
            '<code>logging.basicConfig</code> настраивает корневой логгер: куда писать и в каком виде. Без аргументов записи идут в стандартный поток ошибок.',
            '<code>level=logging.INFO</code> задаёт порог. Поэтому строка <code>debug</code> в вывод не попала: её уровень 10 ниже порога 20.',
            '<code>format</code> описывает шаблон строки. <code>%(asctime)s</code> — время, <code>%(levelname)s</code> — уровень, <code>%(name)s</code> — имя логгера, <code>%(message)s</code> — текст.',
            '<code>logging.getLogger("telemetry_lab")</code> возвращает именованный логгер. Имя попадает в запись и показывает, какая часть программы её создала.',
            'В <code>logger.info</code> значения переданы отдельными аргументами, а не через f-строку. Модуль подставит их в <code>%s</code>, только если запись пройдёт порог уровня.',
        ]) + p(
            'Последний пункт важен для производительности. В записи <code>logger.debug(f"данные: {big_object}")</code> Python сначала построит строку, '
            'а потом логгер её отбросит, если DEBUG выключен. С отдельными аргументами строка не строится вовсе.'
        ) + p(
            'Время в выводе записано по местному времени машины и без часового пояса: <code>2026-09-15 16:17:58,865</code>. '
            'Для одного компьютера это удобно, для нескольких серверов — нет. Учебный сервис поэтому формирует время события сам через <code>datetime.now(timezone.utc)</code>.'
        ) + source('https://docs.python.org/3/library/logging.html', 'Python · logging')),

        ('structure', 'Структурированные записи в JSON', p(
            'Строка <code>status=200 duration_ms=82.627</code> читается человеком, но программе её придётся разбирать. '
            'Если в тексте сообщения встретится пробел или знак равенства, разбор сломается. '
            'Поэтому в рабочих сервисах записи часто формируют в JSON: каждое поле становится ключом объекта.'
        ) + code('''import json
import logging

logging.basicConfig(level=logging.INFO, format="%(message)s")
logger = logging.getLogger("telemetry_lab")

event = {
    "event": "request_completed",
    "route": "/products",
    "status": 200,
    "duration_ms": 82.627,
    "request_id": "e14dff1e8db440709a2640f599322917",
}
logger.info(json.dumps(event, ensure_ascii=False))''', 'Python · log_json.py') + output('''{"event": "request_completed", "route": "/products", "status": 200, "duration_ms": 82.627, "request_id": "e14dff1e8db440709a2640f599322917"}''') + p(
            'Здесь запись собрана в словарь, а <code>json.dumps</code> превращает его в строку JSON. '
            'Параметр <code>ensure_ascii=False</code> оставляет русские буквы как есть, без замены на коды вида <code>\\u0442</code>. '
            'В формате логгера оставлено только <code>%(message)s</code>, чтобы каждая строка вывода была корректным JSON.'
        ) + p(
            'Выгода проявляется при поиске. Системы хранения логов разбирают JSON на поля, и запрос «все записи, где <code>status</code> равен 500 и <code>route</code> равен <code>/products</code>» выполняется точно. '
            'С текстовыми строками пришлось бы писать регулярное выражение и надеяться, что формат сообщения нигде не отличается.'
        ) + p(
            'Формат JSON сам по себе запись не улучшает. Если в одном месте кода поле называется <code>status</code>, а в другом <code>http_status</code>, '
            'поиск по одному имени найдёт только половину записей. Имена и типы полей договариваются одинаковыми для всего сервиса.'
        ) + p(
            'Куда писать логи, в современных развёртываниях решают просто: в стандартный вывод процесса. '
            'Docker и Kubernetes собирают вывод каждого контейнера, а отдельный агент пересылает записи в хранилище. '
            'Приложению не нужно самому управлять файлами логов и их ротацией.'
        )),

        ('exception', 'Исключение и traceback', p(
            'Самые ценные записи при расследовании — записи об ошибках. Для них в логгере есть метод <code>logger.exception</code>, '
            'который добавляет к сообщению полный traceback текущего исключения.'
        ) + term('Traceback', 'отчёт Python о цепочке вызовов, которая привела к исключению. '
                 'Он показывает, в каком файле, строке и функции возникла ошибка и через какие вызовы выполнение туда пришло.') + code('''import logging

logging.basicConfig(format="%(levelname)s %(message)s")
logger = logging.getLogger("telemetry_lab")


def load_products():
    raise TimeoutError("dependency timeout")


try:
    load_products()
except TimeoutError:
    logger.exception("dependency_timeout request_id=%s", "b5408f2265bb49168b4bdfbec2bc0dc7")''', 'Python · log_exception.py') + output('''ERROR dependency_timeout request_id=b5408f2265bb49168b4bdfbec2bc0dc7
Traceback (most recent call last):
  File ".../log_exception.py", line 12, in <module>
    load_products()
  File ".../log_exception.py", line 8, in load_products
    raise TimeoutError("dependency timeout")
TimeoutError: dependency timeout''', 'Вывод в терминале · путь к файлу сокращён') + p(
            'Traceback удобнее читать снизу вверх. Последняя строка называет тип исключения и сообщение: <code>TimeoutError: dependency timeout</code>. '
            'Над ней — место, где исключение возникло: файл, строка 8, функция <code>load_products</code> и сам код строки. '
            'Ещё выше — место вызова: строка 12, код верхнего уровня <code>&lt;module&gt;</code>. Так восстанавливается путь выполнения до ошибки.'
        ) + p(
            'Первая строка вывода — сообщение, которое мы передали в <code>logger.exception</code>. Метод всегда пишет запись уровня ERROR. '
            'Вызывают его только внутри блока <code>except</code>: вне обработки исключения traceback взять неоткуда.'
        ) + p(
            'В Python 3.12 traceback содержит абсолютный путь к файлу, например <code>/home/deploy/app/log_exception.py</code>. '
            'В журнал рабочего сервиса он попадёт вместе с устройством файловой системы сервера. '
            'Учебный сервис перед сохранением убирает путь к папке проекта и оставляет только имя файла.'
        )),

        ('real-error', 'Ошибка 500 на учебном стенде', p(
            'Теперь посмотрим на настоящую ошибку. Запустим на странице стенда сценарий <code>error</code>. '
            'Обработчик ждёт зависимость, затем поднимает <code>TimeoutError</code>, перехватывает его, записывает событие и возвращает клиенту ответ 500.'
        ) + shot('api-error', 'сценарий error на странице стенда', p(
            'Тот же обработчик, что и в сценариях fast и slow. Отличаются код ответа, тело ответа, уровень записи и поле exception.'
        )) + p(
            'Разберём снимок. Строка итога — <code>HTTP 500 · клиент: 74.5 ms · обработчик: 72.9 ms</code>. '
            'Ошибка вернулась быстрее, чем успешный быстрый ответ: этап <code>serialize</code> не выполнялся.'
        ) + p(
            '<strong>Блок HTTP</strong> показывает, что получил клиент: объект <code>detail</code> с кратким описанием <code>simulated dependency timeout</code> и <code>request_id</code>. '
            'Traceback клиенту не отправляется. Внутреннее устройство сервиса пользователю знать не нужно, а сведения о коде могут помочь злоумышленнику.'
        ) + p(
            '<strong>Блок LOG</strong> содержит полную запись. Уровень <code>ERROR</code>, код 500 и сценарий <code>error</code>. '
            'В поле <code>exception</code> — тот же traceback, что мы разобрали выше, только записанный в одну строку JSON: переносы строк заменены на <code>\\n</code>. '
            'Прочитаем его снизу вверх: <code>TimeoutError: Simulated dependency timeout; no database is connected</code> возникла в файле <code>app.py</code>, строка 55, функция <code>products</code>.'
        ) + p(
            '<strong>Блок TRACE</strong> показывает только два этапа: <code>validate</code> — 11,1 мс и <code>dependency.wait (simulated)</code> — 61,1 мс. '
            'Третьего этапа нет, потому что исключение прервало выполнение до него.'
        ) + p(
            'Вся связка держится на <code>request_id</code>. Клиент получил <code>b5408f2265bb49168b4bdfbec2bc0dc7</code> в ответе и в заголовке <code>X-Request-ID</code>. '
            'По этому значению разработчик найдёт запись с полным traceback, хотя у самого пользователя его нет.'
        ) + code('''try:
    await stage("dependency.wait (simulated)", 3.0 if scenario == "slow" else 0.06)
    if scenario == "error":
        raise TimeoutError("Simulated dependency timeout; no database is connected")
    await stage("serialize", 0.01)
except TimeoutError:
    status = 500
    exception_text = traceback.format_exc().replace(str(Path(__file__).parent) + os.sep, "")
    logger.exception("dependency_timeout request_id=%s", request_id)''', 'Python · обработка ошибки в app.py', 'app.py') + p(
            'Фрагмент кода показывает весь путь. <code>traceback.format_exc()</code> возвращает текст traceback текущего исключения в виде строки. '
            'Метод <code>replace</code> убирает из него путь к папке проекта. Строка попадает в поле <code>exception</code> записи, '
            'а <code>logger.exception</code> дополнительно выводит сообщение с traceback в терминал Uvicorn.'
        ) + source('https://docs.python.org/3/library/traceback.html', 'Python · traceback')),

        ('count', 'Можно ли считать метрики по логам', p(
            'Раз в логах есть запись о каждом запросе, из них можно получить и количества. '
            'Возьмём файл <code>events.json</code> учебного прогона: в нём записи обо всех двадцати запросах.'
        ) + code('''import json

with open("results/events.json") as file:
    events = json.load(file)

errors = sum(1 for event in events if event["status"] >= 500)
print("событий:", len(events))
print("ошибок:", errors)
print(f"доля: {errors / len(events):.1%}")''', 'Python · доля ошибок по записям') + output('''событий: 20
ошибок: 1
доля: 5.0%''') + p(
            'Результат совпал с расчётом по CSV из главы 1.4. Так бывает, когда выполнены два условия: каждый запрос записан ровно один раз, и в записи есть нужное поле. '
            'В учебном сервисе оба условия выполняются: событие <code>request_completed</code> создаётся один раз в конце обработчика.'
        ) + example('Почему нельзя просто посчитать строки с ERROR', p(
            'Первый запрос трижды пытался обратиться к зависимости: две попытки завершились ошибкой, третья успешно. Каждая неудачная попытка записала строку ERROR. '
            'Второй запрос упал сразу, без повторов, и тоже записал ERROR.'
        ) + table(['Что считаем', 'Результат'], [
            ['Строк с уровнем ERROR', '3'],
            ['Неуспешных попыток обращения к зависимости', '3'],
            ['Неуспешных запросов', '1'],
        ]) + p(
            'Три числа отвечают на три разных вопроса. Чтобы по логам считать неуспешные запросы, нужно событие, которое пишется один раз на запрос и содержит итоговый статус.'
        )) + p(
            'Даже при правильных событиях считать метрики по логам дорого. Сервис с 1000 запросов в секунду и записью по 500 байт создаёт около 43 ГБ логов в сутки. '
            'Метрика с тем же смыслом занимает несколько рядов. Поэтому доли и длительности обычно считают метриками, а логи держат для подробностей.'
        )),

        ('privacy', 'Чего не должно быть в логах', p(
            'Логи читают многие: разработчики, дежурные инженеры, иногда подрядчики. Их пересылают в отдельные системы и хранят месяцами. '
            'Поэтому в записи нельзя попадать данным, доступ к которым должен быть ограничен.'
        ) + table(['Нельзя записывать', 'Почему', 'Что записать вместо'], [
            ['Пароли, токены, заголовок Authorization', 'Любой читатель лога получит доступ к чужой учётной записи', 'Факт успешной или неуспешной проверки'],
            ['Полные номера банковских карт', 'Требования платёжных систем запрещают их хранение в открытом виде', 'Последние четыре цифры'],
            ['Персональные данные: паспорт, адрес, телефон', 'Их обработку регулирует закон, в России — 152-ФЗ', 'Внутренний идентификатор пользователя'],
            ['Тело запроса целиком', 'В нём могут оказаться любые из перечисленных данных', 'Размер тела и нужные для разбора поля'],
        ]) + p(
            'Лучше решать это при проектировании записи, а не после утечки. Удобный приём — перечислить поля, которые можно писать, вместо того чтобы запрещать отдельные. '
            'Тогда новое поле в запросе не попадёт в лог случайно.'
        )),
    ],
    mistakes=[
        ['Пишут сообщения без контекста: «Request processed»', 'Запись нельзя связать ни с запросом, ни с результатом', 'Добавлять время, маршрут, статус, длительность и идентификатор'],
        ['Не указывают единицы измерения', 'Число 72 читают как секунды вместо миллисекунд', 'Писать единицу в имени поля: duration_ms'],
        ['Используют f-строки в logger.debug', 'Строка строится, даже если уровень DEBUG выключен', 'Передавать значения аргументами: logger.debug("x=%s", x)'],
        ['Вызывают logger.error вместо logger.exception в except', 'В запись не попадает traceback', 'В блоке except использовать logger.exception'],
        ['Отправляют traceback клиенту в ответе', 'Пользователь видит устройство сервиса', 'Возвращать краткое описание и request_id, traceback — только в лог'],
        ['Считают ошибочные запросы по числу строк ERROR', 'Повторы и промежуточные ошибки завышают результат', 'Считать по событию, которое пишется один раз на запрос'],
        ['Записывают токены и персональные данные', 'Утечка доступа и нарушение закона', 'Перечислять разрешённые поля записи'],
    ],
    selfcheck=[
        ('Уровень логгера — WARNING. Какие из вызовов debug, info, warning, error попадут в вывод?',
         p('warning и error. Логгер пропускает записи с уровнем не ниже заданного: WARNING — 30, ERROR — 40, а DEBUG — 10 и INFO — 20 ниже порога.')),
        ('В каком порядке удобно читать traceback и что находится в последней строке?',
         p('Снизу вверх. В последней строке тип исключения и сообщение, над ней — место, где оно возникло, выше — цепочка вызовов, которая туда привела.')),
        ('Пользователь прислал снимок экрана с ошибкой 500 и значением request_id. Как найти причину?',
         p('Найти в хранилище логов запись с этим request_id. В ней уровень ERROR, маршрут и поле exception с traceback, которого у пользователя нет.')),
        ('Почему на снимке сценария error в блоке TRACE только два этапа?',
         p('Исключение TimeoutError возникло после этапа dependency.wait и прервало выполнение блока try, поэтому этап serialize не выполнялся.')),
        ('Один запрос выполнил два неудачных повтора и в итоге завершился успешно. Сколько неуспешных запросов он добавляет?',
         p('Ноль. Неуспешных попыток две, но итоговый результат запроса успешный.')),
    ],
    cheatsheet=[
        'Лог — последовательность записей о событиях: когда, где, что и с каким результатом.',
        'Обязательные поля: время в UTC, уровень, сервис, событие, результат, длительность, идентификатор.',
        'Уровни logging: DEBUG 10, INFO 20, WARNING 30, ERROR 40, CRITICAL 50.',
        'Значения передают аргументами логгера, а не f-строкой.',
        'JSON делает поля доступными для точного поиска; имена полей одинаковы во всём сервисе.',
        'logger.exception внутри except добавляет traceback.',
        'Traceback читают снизу вверх: исключение → место → цепочка вызовов.',
        'Клиенту — краткое описание и request_id, в лог — полный traceback.',
        'Число строк ERROR не равно числу неуспешных запросов.',
        'Пароли, токены и персональные данные в лог не пишут.',
    ],
    links=[
        ('https://docs.python.org/3/library/logging.html', 'Python · logging'),
        ('https://docs.python.org/3/howto/logging.html', 'Python · Logging HOWTO'),
        ('https://docs.python.org/3/library/traceback.html', 'Python · traceback'),
        ('https://opentelemetry.io/docs/concepts/signals/logs/', 'OpenTelemetry · Logs'),
    ],
)
