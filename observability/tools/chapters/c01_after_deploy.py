from kit import *

ARCHITECTURE_WIDGET = '''<div class="interactive architecture" data-architecture><div class="widget-head"><b>Где проходит запрос</b><div class="controls"><button data-mode="local" aria-pressed="true">Локально</button><button data-mode="production" aria-pressed="false">После развёртывания</button></div></div><div class="system-route"><span>Разработчик</span><i>→</i><span>FastAPI</span><i>→</i><span>Хранилище</span></div><p class="widget-detail" aria-live="polite">Один запрос. Терминал и состояние процесса доступны разработчику.</p></div>'''

CHAPTER = chapter(
    slug='01-posle-deploy',
    title='После развёртывания: что происходит с приложением',
    lead='Знакомимся с учебным сервисом и разбираем, какие сведения о программе теряет разработчик после её запуска на сервере. На двух настоящих ответах HTTP 200 видно, почему успешный код ответа ещё ничего не говорит о качестве работы.',
    epigraph='Разобрать сбой можно только по тем данным, которые сохранили до него.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Что понадобится', 'Понимание HTTP-запроса и ответа, основы FastAPI'),
        ('Зачем это знать', 'Понимать, какие данные о сервисе нужно собирать до того, как придёт первая жалоба'),
    ],
    sections=[
        ('lab', 'Учебный сервис products-api', p(
            'Все главы первой темы опираются на один и тот же небольшой сервис. Сначала разберём, что это за программа и почему для обучения выбрана именно она.'
        ) + p(
            '<strong>products-api</strong> — веб-приложение на FastAPI, которое отвечает на запрос <code>GET /products</code> списком товаров. '
            'Настоящего каталога за ним нет: сервис возвращает число товаров и служебные поля. '
            'Нам важен не список товаров, а то, как сервис ведёт себя при обработке запроса и какие данные о себе оставляет.'
        ) + p(
            'Реальные сервисы тормозят и падают непредсказуемо, и повторить сбой по заказу почти невозможно. '
            'Поэтому в учебном сервисе поведение задаётся параметром <code>scenario</code>. У него три значения.'
        ) + table(['Сценарий', 'Что происходит', 'Результат'], [
            ['<code>fast</code>', 'Обработчик ждёт зависимость около 60 мс', 'HTTP 200 примерно за 85 мс'],
            ['<code>slow</code>', 'Обработчик ждёт зависимость 3 секунды', 'HTTP 200 примерно за 3 секунды'],
            ['<code>error</code>', 'Ожидание зависимости заканчивается исключением <code>TimeoutError</code>', 'HTTP 500'],
        ]) + p(
            'Зависимостью называют любую внешнюю систему, от которой зависит ответ: базу данных, кэш, другой сервис. '
            'В учебном сервисе базы данных нет, её заменяет ожидание через <code>asyncio.sleep</code>. '
            'Это честная модель: код не делает вид, что работает с PostgreSQL, а на странице стенда об этом прямо написано. '
            'Зато каждый сценарий можно повторить сколько угодно раз и получить одинаковое поведение.'
        ) + p(
            'Кроме <code>/products</code>, у сервиса есть служебные адреса. Они понадобятся в этой главе и дальше.'
        ) + table(['Адрес', 'Что возвращает'], [
            ['<code>/lab</code>', 'Страница стенда: кнопки сценариев, ответ, запись о событии и этапы обработки'],
            ['<code>/health</code>', 'Короткий ответ о том, что процесс работает'],
            ['<code>/events</code>', 'Последние 2000 записей о запросах, которые процесс хранит в памяти'],
            ['<code>/metrics</code>', 'Счётчики и гистограмма в текстовом формате Prometheus'],
            ['<code>/docs</code>', 'Документация Swagger, которую FastAPI строит автоматически'],
        ]) + p(
            'Код сервиса, скрипт эксперимента и инструкция по запуску лежат в архиве: '
            '<a href="../assets/telemetry-lab.zip" download>telemetry-lab.zip ↓</a>. '
            'Для чтения глав запускать его не обязательно: все снимки и измерения сделаны на этом сервисе и приведены в тексте.'
        )),

        ('local', 'Как мы находим ошибку на своём компьютере', p(
            'Вспомним, как проходит отладка, пока программа запущена локально. Мы написали обработчик, отправили запрос из браузера или через Swagger и смотрим результат. '
            'Всё нужное находится под рукой.'
        ) + ul([
            '<strong>Терминал.</strong> Uvicorn выводит каждый запрос и полный traceback при исключении.',
            '<strong>Отладчик.</strong> Можно поставить точку останова, остановить выполнение и посмотреть значения переменных.',
            '<strong>Повторение.</strong> Если результат странный, запрос отправляют ещё раз и сравнивают.',
            '<strong>Изменение кода.</strong> Можно добавить <code>print</code>, перезапустить сервер и сразу увидеть вывод.',
        ]) + p(
            'Все четыре способа держатся на одном условии: разработчик находится рядом с процессом в тот момент, когда происходит ошибка. '
            'Он видит терминал, может остановить программу и повторить действие. '
            'После развёртывания это условие перестаёт выполняться.'
        )),

        ('deploy', 'Что меняется после развёртывания', term('Развёртывание', 'установка и запуск версии программы в среде, где с ней работают пользователи. '
             'Эту среду обычно называют рабочей или продакшеном (production).') + p(
            'Переключите схему ниже и сравните, через что проходит запрос в двух случаях.'
        ) + ARCHITECTURE_WIDGET + p(
            'На сервере картина другая. Приложение запущено в нескольких экземплярах. '
            '<strong>Экземпляр</strong> — отдельный запущенный процесс той же программы. Экземпляров делают несколько, чтобы выдерживать нагрузку и не останавливать сервис при падении одного процесса. '
            'Запросы пользователей между экземплярами распределяет балансировщик нагрузки. Часть работы вообще идёт вне HTTP-запроса: '
            'обработчики очереди формируют отчёты и отправляют письма уже после того, как пользователь получил ответ.'
        ) + p(
            'Теперь вернёмся к четырём способам отладки и посмотрим, что от них осталось.'
        ) + table(['Способ', 'Локально', 'После развёртывания'], [
            ['Терминал', 'Виден сразу', 'Вывод десятков процессов на разных серверах; после перезапуска контейнера может исчезнуть'],
            ['Отладчик', 'Можно остановить программу', 'Остановка процесса остановит обслуживание пользователей'],
            ['Повторение', 'Отправили запрос ещё раз', 'Неизвестно, какой запрос, от кого и на какой экземпляр пришёл'],
            ['Изменение кода', 'Добавили print и перезапустили', 'Нужен выпуск новой версии, а сбой к тому времени может пройти'],
        ]) + p(
            'Посмотрим на типичную ситуацию. Ночью в 3:10 на одном экземпляре кончилась память, процесс упал, система оркестрации запустила его заново. '
            'Утром разработчик получает жалобу. Процесса, в памяти которого были данные о сбое, уже нет. '
            'Нового процесса в 3:10 ещё не существовало. Вопрос «что происходит с приложением» превращается в вопрос «какие данные о 3:10 кто-то сохранил».'
        ) + p(
            'Отсюда первый вывод всей темы. Данные для разбора сбоя нужно собирать и сохранять заранее, пока сервис работает. '
            'После сбоя собрать их уже не получится.'
        )),

        ('promises', 'Что сервис обещает пользователю', p(
            'Чтобы решить, какие данные собирать, сначала определим, что значит «сервис работает хорошо». '
            'Для пользователя это складывается из четырёх свойств. Они часто пересекаются, но проверяются по-разному.'
        ) + table(['Свойство', 'Что проверяем', 'Пример нарушения'], [
            ['Доступность', 'Можно ли получить ответ', 'Сервис не отвечает или возвращает 503 Service Unavailable'],
            ['Производительность', 'Сколько времени и ресурсов занимает работа', 'Каталог открывается за 3 секунды вместо 100 мс'],
            ['Надёжность', 'Сохраняется ли поведение во времени и при сбоях', 'Раз в несколько часов теряются фоновые задания'],
            ['Корректность', 'Соответствует ли результат смыслу операции', 'Пришёл HTTP 200, но заказ не записан в базу'],
        ]) + p(
            'Разберём, как свойства пересекаются. Браузер или мобильное приложение не ждут ответа бесконечно. '
            'Если сервис отвечает дольше заданного у клиента предела, например 30 секунд, клиент прерывает запрос с ошибкой тайм-аута. '
            'Для пользователя медленный сервис превращается в недоступный, хотя сервер продолжает честно обрабатывать запрос.'
        ) + p(
            'Корректность проверить сложнее всего. Код HTTP описывает результат на уровне протокола: запрос принят и обработан без исключения. '
            'Он ничего не знает о смысле операции. Обработчик оформления заказа может вернуть 200, забыв записать заказ, '
            'и ни один показатель HTTP это не заметит. Поэтому для важных операций успех определяют отдельно: заказ появился в базе, письмо ушло, платёж подтверждён.'
        )),

        ('health', 'Что проверяет адрес /health', p(
            'Почти у каждого веб-сервиса есть адрес проверки состояния. Системы оркестрации и балансировщики регулярно обращаются к нему, '
            'чтобы решить, отправлять ли на экземпляр запросы и не пора ли его перезапустить. У учебного сервиса это <code>/health</code>.'
        ) + shot('api-health', 'ответ /health в браузере', p(
            'Браузер Chrome получил JSON и показал его одной строкой. Флажок Pretty-print в левом верхнем углу добавляет сам браузер: он форматирует JSON с отступами.'
        )) + p(
            'В ответе два поля. <code>"status": "ok"</code> означает, что обработчик выполнился. '
            '<code>"scope": "process only; dependencies not checked"</code> прямо сообщает границу проверки: проверен только процесс, зависимости не проверялись.'
        ) + code('''@app.get("/health", tags=["Проверка процесса"])
async def health():
    return {"status": "ok", "scope": "process only; dependencies not checked"}''', 'Python · обработчик /health из app.py', 'app.py') + p(
            'Код подтверждает подпись. Обработчик не обращается к зависимостям и не выполняет сценарий <code>/products</code>. '
            'Он отвечает 200, если процесс способен принять HTTP-запрос и выполнить простую функцию. '
            'Если <code>/products</code> в этот момент завершается ошибкой в каждом запросе, <code>/health</code> всё равно ответит <code>ok</code>.'
        ) + p(
            'Это нормальное поведение, если понимать назначение проверки. Проверка процесса отвечает на вопрос «жив ли экземпляр», и для перезапуска зависшего процесса этого достаточно. '
            'На вопрос «получают ли пользователи товары» она не отвечает. Для него нужны данные о настоящих запросах к <code>/products</code>.'
        )),

        ('two-requests', 'Два ответа HTTP 200', p(
            'Откроем страницу стенда <code>/lab</code> и выполним два сценария подряд: сначала <code>fast</code>, затем <code>slow</code>. '
            'Страница отправляет настоящий запрос к <code>/products</code> и показывает всё, что сервис сохранил о нём.'
        ) + shot('api-fast', 'сценарий fast на странице стенда', p(
            'Страница состоит из четырёх частей: строка итога под кнопками, блок HTTP с телом ответа, блок LOG с записью о событии и блок TRACE с этапами обработки.'
        )) + p(
            'Разберём снимок сверху вниз. <strong>Строка итога</strong> — <code>HTTP 200 · клиент: 88.0 ms · обработчик: 82.6 ms</code>. '
            'Время клиента измерил JavaScript в браузере: от отправки запроса до получения ответа. '
            'Время обработчика измерил сам сервис: от начала до конца функции <code>products</code>. '
            'Разница 5,4 мс ушла на путь запроса и ответа, а также на работу Uvicorn до и после обработчика.'
        ) + p(
            '<strong>Блок HTTP</strong> показывает тело ответа, которое получил клиент. '
            '<code>status: "ok"</code> и <code>items: 10</code> — полезная нагрузка. '
            '<code>request_id</code> — уникальный идентификатор этого запроса. '
            '<code>duration_ms: 82.627</code> — время обработчика с точностью до тысячных долей миллисекунды.'
        ) + p(
            '<strong>Блок LOG</strong> — запись о событии, которую сервис сохранил у себя. В ней уже больше полей: время по UTC в <code>timestamp</code>, '
            'уровень важности <code>level</code>, имя сервиса, тип события, маршрут, код ответа, сценарий и поле <code>exception</code>. '
            'Значение <code>null</code> в последнем поле означает, что исключения не было. '
            '<code>request_id</code> совпадает с блоком HTTP: по нему запись связывается с ответом.'
        ) + p(
            '<strong>Блок TRACE</strong> показывает три этапа обработчика на общей шкале: <code>validate</code> — 11,1 мс, '
            '<code>dependency.wait (simulated)</code> — 60,5 мс, <code>serialize</code> — 11,1 мс. '
            'Полосы идут друг за другом и вместе занимают всю шкалу от 0 до 82,6 мс. Пометка <code>simulated</code> в имени этапа напоминает, что ожидание смоделировано.'
        ) + shot('api-slow', 'сценарий slow на странице стенда', p(
            'Тот же обработчик, тот же код ответа HTTP 200, то же тело ответа. Отличаются длительности.'
        )) + p(
            'Сравним снимки. В строке итога снова <code>HTTP 200</code>, но клиент ждал 3026,0 мс, а обработчик работал 3022,7 мс. '
            'В блоке HTTP те же <code>status: "ok"</code> и <code>items: 10</code>. В блоке LOG уровень <code>INFO</code>, как и у быстрого запроса: '
            'с точки зрения записи ничего плохого не случилось.'
        ) + p(
            'Разница видна только в числах. В блоке TRACE этап <code>dependency.wait</code> занял 3001,1 мс и растянулся почти на всю шкалу. '
            'Этапы <code>validate</code> и <code>serialize</code> остались по 11 мс и превратились в тонкие полоски по краям.'
        ) + p(
            'Отсюда второй вывод. Если сервис сохраняет только код ответа, эти два запроса неразличимы: оба успешны. '
            'Пользователь при этом ждал в 34 раза дольше. Чтобы заметить проблему производительности, нужно сохранять длительность, '
            'а чтобы найти её место — длительность отдельных этапов.'
        )),

        ('one-check', 'Почему одна проверка ничего не доказывает', p(
            'Представим, что пользователь пожаловался на медленный каталог. Разработчик открыл страницу, она загрузилась за 90 мс, и жалобу закрыли как неподтверждённую. '
            'Разберёмся, чего стоит такая проверка.'
        ) + example('Вероятность попасть на медленный запрос', p(
            'Пусть медленным оказывается каждый двадцатый запрос, то есть 5%. Тогда быстрым будет 95% запросов. '
            'Вероятность, что одна ручная проверка окажется быстрой, равна 0,95 — разработчик почти наверняка ничего не увидит.'
        ) + table(['Число проверок подряд', 'Вероятность, что все быстрые'], [
            ['1', '0,95 = 95%'],
            ['3', '0,95³ ≈ 86%'],
            ['10', '0,95¹⁰ ≈ 60%'],
        ]) + p(
            'Даже десять проверок подряд с вероятностью 60% не покажут ни одного медленного запроса. '
            'Проблема, которая касается каждого двадцатого пользователя, ручными проверками почти не обнаруживается.'
        )) + p(
            'Для ответа на жалобу нужны данные за тот интервал, о котором говорит пользователь: сколько было запросов, какая часть из них выполнялась медленно, как это менялось во времени. '
            'Одна быстрая проверка доказывает только одно: конкретный запрос в конкретный момент выполнился быстро.'
        )),

        ('evidence', 'Симптом, наблюдение, гипотеза и вывод', p(
            'При разборе проблем полезно разделять четыре вида утверждений. Они звучат похоже, но имеют разный вес, и путаница между ними приводит к ложным выводам.'
        ) + table(['Вид утверждения', 'Что это', 'Пример'], [
            ['Симптом', 'То, что заметил пользователь или внешняя проверка', '«Каталог иногда долго открывается»'],
            ['Наблюдение', 'Факт, подтверждённый измерением', 'С 14:00 два из двадцати запросов /products длятся больше 3 секунд, код ответа 200'],
            ['Гипотеза', 'Предположение о механизме, которое ещё не проверено', 'Запросы долго ждут ответа зависимости'],
            ['Вывод', 'Гипотеза, подтверждённая проверкой', 'Задержка приходится на этап dependency.wait; остальные этапы не изменились'],
        ]) + p(
            'Ошибка, которую совершают чаще всего, — записать гипотезу как вывод. Возьмём график загрузки процессора: CPU 95%. '
            'Хочется сразу написать «сервис тормозит из-за процессора». Но высокая загрузка может быть и причиной задержек, и следствием наплыва запросов, '
            'и обычным состоянием вычислительной задачи, которая всегда так работает.'
        ) + p(
            'Чтобы отличить одно от другого, нужен контекст: была ли загрузка такой же вчера, когда жалоб не было, совпадает ли её рост с ростом длительности, '
            'выросло ли число запросов. Пока эти данные не проверены, CPU 95% остаётся наблюдением, а «причина в процессоре» — гипотезой.'
        )),

        ('need', 'Какие данные нужно сохранять заранее', p(
            'Соберём выводы главы в список. Чтобы после развёртывания можно было разобрать жалобу, сервис должен во время работы сохранять несколько видов сведений.'
        ) + table(['Что сохранять', 'На какой вопрос отвечает', 'Пример из стенда'], [
            ['Количество операций и их результаты за интервал', 'Каков масштаб проблемы', 'Сколько запросов /products завершилось кодом 200 и 500'],
            ['Распределение длительностей', 'Сколько запросов медленные, а не только каково среднее', 'Сколько запросов уложилось в 100 мс, сколько длилось больше секунды'],
            ['Записи о событиях с контекстом', 'Что случилось с конкретным запросом', 'Запись с request_id, кодом ответа и текстом исключения'],
            ['Этапы операции', 'Где операция потратила время', 'validate, dependency.wait, serialize'],
        ]) + p(
            'У всех записей из таблицы есть общие требования. Каждое значение хранится с отметкой времени, иначе его нельзя поставить в историю. '
            'Каждое значение привязано к компоненту: к сервису, экземпляру, версии. И всё это хранится вне процесса, который может упасть или перезапуститься.'
        ) + p(
            'Регулярный сбор таких сведений называют мониторингом, а сами сведения — телеметрией. '
            'Четыре строки таблицы соответствуют трём видам телеметрии: первые две — метрикам, третья — логам, четвёртая — трассам.'
        ) + source('https://sre.google/sre-book/monitoring-distributed-systems/', 'Google SRE Book · Monitoring Distributed Systems')),
    ],
    mistakes=[
        ['Считают HTTP 200 признаком того, что всё хорошо', 'Медленные и некорректные успешные ответы остаются незамеченными', 'Сохранять длительность и отдельно проверять смысловой результат операции'],
        ['Проверяют работу сервиса только через /health', 'Процесс жив, а рабочий маршрут завершается ошибкой', 'Смотреть данные о настоящих запросах к рабочим маршрутам'],
        ['Закрывают жалобу после одной ручной проверки', 'Редкие проблемы не воспроизводятся и остаются в сервисе', 'Смотреть данные за весь интервал жалобы'],
        ['Надеются разобрать сбой по выводу терминала', 'После перезапуска процесса вывод недоступен', 'Сохранять телеметрию вне процесса во время работы'],
        ['Записывают гипотезу как вывод', 'Исправляют не то, проблема остаётся', 'Отмечать, какие утверждения измерены, а какие только предположены'],
        ['Путают время клиента и время обработчика', 'Задержку сети или очереди Uvicorn приписывают коду', 'Указывать, в какой точке измерена длительность'],
    ],
    selfcheck=[
        ('Почему после развёртывания нельзя разобрать ночной сбой так же, как локальную ошибку?',
         p('Процесс, в котором произошёл сбой, мог упасть и перезапуститься. Разработчика не было рядом в момент ошибки, терминал и память процесса недоступны. '
           'Остаются только данные, сохранённые во время работы.')),
        ('На стенде /health отвечает {"status": "ok"}, а /products возвращает 500. Как это возможно?',
         p('Обработчик /health проверяет только, что процесс принимает запросы. Он не обращается к зависимости и не выполняет сценарий /products, поэтому сбой рабочего маршрута на нём не отражается.')),
        ('На снимке fast клиент ждал 88,0 мс, а обработчик работал 82,6 мс. Откуда разница?',
         p('Время клиента включает путь запроса и ответа, а также работу Uvicorn до и после вызова обработчика. Время обработчика измерено только внутри функции products.')),
        ('Медленным оказывается каждый десятый запрос. С какой вероятностью три ручные проверки подряд все будут быстрыми?',
         p('0,9³ = 0,729, то есть около 73%. В большинстве случаев три проверки не покажут проблему.')),
        ('«CPU 90%, поэтому API медленный». Какое это утверждение: наблюдение, гипотеза или вывод?',
         p('«CPU 90%» — наблюдение. Связь с медленной работой API — гипотеза, пока не проверено, совпадает ли рост загрузки с ростом длительности и нет ли другой причины.')),
    ],
    cheatsheet=[
        'products-api — учебный FastAPI-сервис: /products со сценариями fast, slow и error.',
        'Развёртывание — запуск версии программы в среде, где с ней работают пользователи.',
        'После развёртывания нет терминала, отладчика и возможности повторить запрос.',
        'Данные для разбора сбоя сохраняют заранее, во время работы сервиса.',
        'Качество сервиса: доступность, производительность, надёжность, корректность.',
        '/health проверяет процесс, но не рабочие маршруты и зависимости.',
        'HTTP 200 не различает быстрый и медленный ответ; нужна длительность.',
        'Одна быстрая проверка не опровергает жалобу на редкие задержки.',
        'Симптом → наблюдение → гипотеза → проверка → вывод.',
    ],
    links=[
        ('https://sre.google/sre-book/monitoring-distributed-systems/', 'Google SRE Book · Monitoring Distributed Systems'),
        ('https://fastapi.tiangolo.com/tutorial/first-steps/', 'FastAPI · First steps'),
    ],
)
