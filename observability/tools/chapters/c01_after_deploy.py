from kit import *

ARCHITECTURE_WIDGET = widget(
    '''<div class="interactive architecture" data-architecture><div class="widget-head"><b>Где проходит запрос</b><div class="controls"><button data-mode="local" aria-pressed="true">Локально</button><button data-mode="production" aria-pressed="false">После развёртывания</button></div></div><div class="system-route"><span>Разработчик</span><i>→</i><span>FastAPI</span><i>→</i><span>Хранилище</span></div><p class="widget-detail" aria-live="polite">Один запрос. Терминал и состояние процесса доступны разработчику.</p></div>''',
    'путь запроса на своём компьютере и на сервере',
    'Кнопки переключают схему. «Локально» — один процесс, и разработчик сидит рядом с ним. '
    '«После развёртывания» — пользователи, три экземпляра API, база и кэш, очередь и пять обработчиков фоновых задач.')

# Метки на снимках страницы /lab: координаты в процентах от ширины и высоты кадра.
FAST_PINS = [
    (45.1, 17.4, 'Кнопки сценариев', 'отправляют настоящий запрос <code>GET /products?scenario=…</code>.'),
    (41.7, 23.2, 'Строка итога', 'код ответа и два времени: сколько ждал браузер и сколько работал обработчик.'),
    (29.9, 29.8, 'HTTP', 'тело ответа, которое получил клиент.'),
    (31.9, 48.6, 'LOG', 'запись о событии, которую сервис сохранил у себя.'),
    (38.7, 77.3, 'TRACE', 'этапы обработчика на общей шкале времени.'),
    (77.1, 95.9, 'Пометка стенда', 'зависимость моделируется ожиданием, базы данных нет.'),
]

SLOW_PINS = [
    (44.4, 23.1, 'Строка итога', 'снова HTTP 200, но клиент ждал 3026,6 мс.'),
    (29.2, 40.4, 'duration_ms', 'тело ответа то же, отличается только длительность.'),
    (25.3, 56.0, 'level', 'уровень записи INFO, как у быстрого запроса.'),
    (35.4, 80.8, 'validate', '11,1 мс — на этой шкале тонкая черта у левого края.'),
    (59.7, 84.3, 'dependency.wait', '3000,6 мс — полоса на всю ширину.'),
    (84.4, 88.6, 'serialize', '10,6 мс — тонкая черта у правого края.'),
]

CHAPTER = chapter(
    slug='01-posle-deploy',
    title='После развёртывания: что происходит с приложением',
    lead='Знакомимся с сервисом, на котором построена вся тема, и разбираем, какие сведения о программе разработчик теряет, когда она начинает работать на сервере. На двух ответах HTTP 200 видно, что успешный код ответа ничего не говорит о скорости.',
    epigraph='После сбоя разобрать его можно только по тем данным, которые сервис сохранил заранее.',
    passport=[
        ('Раздел', 'Основы наблюдаемости'),
        ('Уровень', 'Начальный · Python знаком'),
        ('Что понадобится', 'Понимание HTTP-запроса и ответа, основы FastAPI'),
        ('Зачем это знать', 'Решать, какие данные о сервисе собирать до первой жалобы пользователя'),
    ],
    sections=[
        ('lab', 'Сервис products-api', p(
            'Все главы первой темы опираются на один небольшой сервис.'
        ) + p(
            '<strong>products-api</strong> — веб-приложение на FastAPI. На запрос <code>GET /products</code> оно отвечает списком товаров. '
            'Настоящего каталога за ним нет: сервис возвращает число товаров и служебные поля. '
            'Для темы важен не список товаров, а то, как сервис обрабатывает запрос и какие данные о себе оставляет.'
        ) + p(
            'Настоящие сервисы тормозят и падают в случайные моменты, и повторить сбой по заказу почти невозможно. '
            'Поэтому поведение нашего сервиса задаёт параметр <code>scenario</code>. У него три значения.'
        ) + cards([
            ('fast', 'Быстрый ответ', 'Обработчик ждёт зависимость около 60 мс и возвращает HTTP 200 примерно за 85 мс.'),
            ('slow', 'Медленный ответ', 'Обработчик ждёт зависимость 3 секунды. Ответ тот же — HTTP 200, но через 3 секунды.'),
            ('error', 'Ошибка', 'Ожидание зависимости заканчивается исключением <code>TimeoutError</code>, клиент получает HTTP 500.'),
        ], columns=3) + p(
            'Зависимостью называют внешнюю систему, от которой зависит ответ: базу данных, кэш, другой сервис. '
            'Базы данных у стенда нет, вместо обращения к ней обработчик ждёт через <code>asyncio.sleep</code>. '
            'Об этом прямо сказано и в коде, и на странице стенда. Зато каждый сценарий можно повторить сколько угодно раз и получить одинаковое поведение.'
        ) + p(
            'Кроме <code>/products</code>, у сервиса есть служебные адреса. Они понадобятся в этой главе и дальше.'
        ) + table(['Адрес', 'Что возвращает'], [
            ['<code>/lab</code>', 'Страница стенда: кнопки сценариев, ответ, запись о событии и этапы обработки'],
            ['<code>/health</code>', 'Короткий ответ о том, что процесс работает'],
            ['<code>/events</code>', 'Последние 2000 записей о запросах, которые процесс хранит в памяти'],
            ['<code>/metrics</code>', 'Счётчики и гистограмма в текстовом формате Prometheus'],
            ['<code>/docs</code>', 'Документация Swagger, которую FastAPI строит по коду'],
        ]) + p(
            'Код сервиса, скрипт эксперимента и инструкция по запуску лежат в архиве '
            '<a href="../assets/telemetry-lab.zip" download>telemetry-lab.zip ↓</a>. '
            'Для чтения глав запускать его не обязательно: все снимки и измерения в тексте сделаны на этом сервисе.'
        )),

        ('local', 'Как мы находим ошибку на своём компьютере', p(
            'Пока программа запущена локально, отладка выглядит так: мы написали обработчик, отправили запрос из браузера или через Swagger и смотрим результат. '
            'Всё нужное под рукой.'
        ) + cards([
            ('1', 'Терминал', 'Uvicorn выводит каждый запрос и полный traceback при исключении.'),
            ('2', 'Отладчик', 'Можно поставить точку останова, остановить выполнение и посмотреть значения переменных.'),
            ('3', 'Повтор запроса', 'Если результат странный, запрос отправляют ещё раз и сравнивают.'),
            ('4', 'Правка кода', 'Добавили <code>print</code>, перезапустили сервер и сразу видим вывод.'),
        ]) + p(
            'Все четыре способа работают при одном условии: разработчик находится рядом с процессом в момент ошибки. '
            'Он видит терминал, может остановить программу и повторить действие. После развёртывания это условие уже не выполняется.'
        )),

        ('deploy', 'Что меняется после развёртывания', term('Развёртывание', 'установка и запуск версии программы в среде, где с ней работают пользователи. '
             'Эту среду называют рабочей или продакшеном (production).') + p(
            'Переключите схему ниже и сравните, через что проходит запрос в двух случаях.'
        ) + ARCHITECTURE_WIDGET + p(
            'На сервере приложение запущено в нескольких экземплярах. '
            '<strong>Экземпляр</strong> — отдельный запущенный процесс той же программы. Их делают несколько, чтобы выдерживать нагрузку и не останавливать сервис, если упадёт один процесс. '
            'Запросы между экземплярами распределяет балансировщик нагрузки. Часть работы вообще идёт вне HTTP-запроса: '
            'обработчики очереди формируют отчёты и отправляют письма уже после того, как пользователь получил ответ.'
        ) + p(
            'Теперь посмотрим, что осталось от четырёх способов отладки.'
        ) + compare(
            ('Локально', 'Всё под рукой', ul([
                'Терминал виден сразу.',
                'Программу можно остановить отладчиком.',
                'Запрос отправили ещё раз.',
                'Добавили <code>print</code> и перезапустили.',
            ]), 'good'),
            ('На сервере', 'Что с ними происходит', ul([
                'Вывод десятков процессов на разных серверах; после перезапуска контейнера он может исчезнуть.',
                'Остановка процесса остановит обслуживание пользователей.',
                'Неизвестно, какой запрос, от кого и на какой экземпляр пришёл.',
                'Нужен выпуск новой версии, а сбой к тому времени может пройти.',
            ]), 'bad'),
        ) + example('Сбой ночью в 3:10', p(
            'Ночью на одном экземпляре кончилась память, процесс упал, система оркестрации запустила его заново. '
            'Утром разработчик получает жалобу. Процесса, в памяти которого были сведения о сбое, уже нет, а новый процесс в 3:10 ещё не существовал.'
        ) + p(
            'Получается, что вопрос «что случилось с приложением в 3:10» сводится к вопросу «какие данные о 3:10 сервис куда-то сохранил». '
            'Если не сохранил ничего, разобрать сбой нельзя.'
        )) + p(
            'Поэтому данные для разбора сбоя собирают и сохраняют заранее, пока сервис работает. После сбоя собрать их уже не получится.'
        )),

        ('promises', 'Что сервис обещает пользователю', p(
            'Чтобы решить, какие данные собирать, сначала определим, что значит «сервис работает хорошо». '
            'Для пользователя это складывается из четырёх свойств. Они пересекаются, но проверяются по-разному.'
        ) + cards([
            ('Доступность', 'Можно ли получить ответ', 'Нарушение: сервис не отвечает или возвращает 503 Service Unavailable.'),
            ('Производительность', 'Сколько времени занимает работа', 'Нарушение: каталог открывается за 3 секунды вместо 100 мс.'),
            ('Надёжность', 'Сохраняется ли поведение во времени', 'Нарушение: раз в несколько часов теряются фоновые задания.'),
            ('Корректность', 'Правилен ли результат', 'Нарушение: пришёл HTTP 200, но заказ не записан в базу.'),
        ]) + p(
            'Доступность и производительность связаны. Браузер и мобильное приложение не ждут ответа бесконечно: '
            'если сервис отвечает дольше предела, заданного у клиента, например 30 секунд, клиент прерывает запрос с ошибкой тайм-аута. '
            'Для пользователя медленный сервис становится недоступным, хотя сервер продолжает обрабатывать запрос.'
        ) + p(
            'Корректность проверить сложнее всего. Код HTTP сообщает результат на уровне протокола: запрос принят и обработан без исключения. '
            'Смысл операции в нём не отражён. Обработчик оформления заказа может вернуть 200 и не записать заказ, '
            'и ни один показатель HTTP этого не заметит. Поэтому для важных операций успех проверяют отдельно: заказ появился в базе, письмо ушло, платёж подтверждён.'
        )),

        ('health', 'Что проверяет адрес /health', p(
            'Почти у каждого веб-сервиса есть адрес проверки состояния. Балансировщик и система оркестрации регулярно обращаются к нему '
            'и по ответу решают, отправлять ли на экземпляр запросы и не пора ли его перезапустить. У нашего сервиса это <code>/health</code>.'
        ) + shot('api-health', 'ответ /health в браузере', p(
            'Chrome получил JSON и показал его одной строкой.'
        ), pins=[
            (11.8, 6.3, 'Pretty-print', 'флажок добавляет сам браузер: он форматирует JSON с отступами. К ответу сервиса флажок не относится.'),
            (5.5, 37.0, 'status', 'значение <code>ok</code> означает только то, что обработчик выполнился.'),
            (27.3, 37.0, 'scope', 'сервис сам сообщает границу проверки: проверен процесс, зависимости не проверялись.'),
        ]) + code('''@app.get("/health", tags=["Проверка процесса"])
async def health():
    return {"status": "ok", "scope": "process only; dependencies not checked"}''', 'Python · обработчик /health из app.py', 'app.py') + p(
            'Код подтверждает поле <code>scope</code>. Обработчик не обращается к зависимостям и не выполняет сценарий <code>/products</code>. '
            'Он отвечает 200, если процесс принимает HTTP-запросы и может выполнить простую функцию. '
            'Даже если <code>/products</code> в этот момент падает в каждом запросе, <code>/health</code> ответит <code>ok</code>.'
        ) + p(
            'Для своей задачи такая проверка подходит. Она отвечает на вопрос «жив ли экземпляр», и для перезапуска зависшего процесса этого достаточно. '
            'На вопрос «получают ли пользователи товары» она не отвечает: для него нужны данные о настоящих запросах к <code>/products</code>.'
        )),

        ('two-requests', 'Два ответа HTTP 200', p(
            'Откроем страницу стенда <code>/lab</code> и выполним два сценария подряд: сначала <code>fast</code>, затем <code>slow</code>. '
            'Страница отправляет настоящий запрос к <code>/products</code> и показывает всё, что сервис сохранил о нём.'
        ) + shot('api-fast', 'сценарий fast на странице стенда', p(
            'Страница стенда после нажатия «Быстрый 200». Метки отмечают шесть частей, о которых идёт речь дальше.'
        ), pins=FAST_PINS) + p(
            'Разберём снимок сверху вниз. <strong>Строка итога</strong>: <code>HTTP 200 · клиент: 84.0 ms · обработчик: 81.8 ms</code>. '
            'Время клиента измерил JavaScript в браузере — от отправки запроса до получения ответа. '
            'Время обработчика измерил сам сервис — от начала до конца функции <code>products</code>. '
            'Разница 2,2 мс ушла на путь запроса и ответа и на работу Uvicorn до и после обработчика.'
        ) + p(
            '<strong>Блок HTTP</strong> — тело ответа, которое получил клиент. '
            '<code>status: "ok"</code> и <code>items: 10</code> — полезные данные. '
            '<code>request_id</code> — уникальный идентификатор этого запроса. '
            '<code>duration_ms: 81.837</code> — время обработчика с точностью до тысячных долей миллисекунды.'
        ) + p(
            '<strong>Блок LOG</strong> — запись о событии, которую сервис сохранил у себя. Полей в ней больше: время по UTC в <code>timestamp</code>, '
            'уровень важности <code>level</code>, имя сервиса, тип события, маршрут, код ответа, сценарий и поле <code>exception</code>. '
            'Значение <code>null</code> в последнем поле означает, что исключения не было. '
            '<code>request_id</code> совпадает с блоком HTTP, и по нему запись связывается с ответом.'
        ) + p(
            '<strong>Блок TRACE</strong> показывает три этапа обработчика на общей шкале: <code>validate</code> — 11,1 мс, '
            '<code>dependency.wait (simulated)</code> — 60,3 мс, <code>serialize</code> — 10,5 мс. '
            'Полосы идут друг за другом и вместе занимают всю шкалу от 0 до 81,8 мс. Слово <code>simulated</code> в имени этапа напоминает, что ожидание смоделировано.'
        ) + shot('api-slow', 'сценарий slow на странице стенда', p(
            'Тот же обработчик, тот же код ответа HTTP 200, то же тело ответа. Отличаются только длительности.'
        ), pins=SLOW_PINS) + p(
            'Сравним снимки. В строке итога снова <code>HTTP 200</code>, но клиент ждал 3026,6 мс, а обработчик работал 3022,3 мс. '
            'В блоке HTTP те же <code>status: "ok"</code> и <code>items: 10</code>. В блоке LOG уровень <code>INFO</code>, как у быстрого запроса: '
            'по записи не видно, что с запросом что-то не так.'
        ) + p(
            'Разница есть только в числах. В блоке TRACE этап <code>dependency.wait</code> занял 3000,6 мс и растянулся почти на всю шкалу, '
            'а <code>validate</code> и <code>serialize</code> остались по 11 мс и стали тонкими чертами по краям.'
        ) + stats([
            ('84,0 мс', 'fast · клиент', 'HTTP 200, уровень INFO'),
            ('3026,6 мс', 'slow · клиент', 'HTTP 200, уровень INFO'),
            ('в 36 раз', 'разница', 'по коду ответа не видна'),
        ]) + p(
            'Если сервис сохраняет только код ответа, эти два запроса выглядят одинаково: оба успешны. '
            'А пользователь во втором случае ждал в 36 раз дольше. Чтобы заметить медленную работу, нужно сохранять длительность, '
            'а чтобы найти её место — длительность отдельных этапов.'
        )),

        ('one-check', 'Почему одна проверка ничего не доказывает', p(
            'Пользователь пожаловался на медленный каталог. Разработчик открыл страницу, она загрузилась за 90 мс, и жалобу закрыли как неподтверждённую. '
            'Посчитаем, чего стоит такая проверка.'
        ) + example('Вероятность попасть на медленный запрос', p(
            'Пусть медленным оказывается каждый двадцатый запрос, то есть 5%. Тогда быстрых 95%, '
            'и вероятность, что одна ручная проверка окажется быстрой, равна 0,95.'
        ) + formula('P(все n проверок быстрые) = <em>0,95</em><sup>n</sup>', [
            ('0,95', 'доля быстрых запросов'),
            ('n', 'сколько раз подряд разработчик проверил страницу'),
        ]) + meter([
            ('1 проверка', 95, '95%'),
            ('3 проверки', 86, '≈ 86%'),
            ('10 проверок', 60, '≈ 60%'),
        ], 'Длина полосы — вероятность, что ни одна проверка не покажет медленный запрос.') + p(
            'Даже десять проверок подряд с вероятностью 60% не покажут ни одного медленного запроса. '
            'Проблему, которая касается каждого двадцатого пользователя, ручными проверками почти не найти.'
        )) + p(
            'Для ответа на жалобу нужны данные за тот интервал, о котором говорит пользователь: сколько было запросов, какая их часть выполнялась медленно, как это менялось во времени. '
            'Одна быстрая проверка доказывает только то, что конкретный запрос в конкретный момент выполнился быстро.'
        )),

        ('evidence', 'Симптом, наблюдение, гипотеза и вывод', p(
            'При разборе проблем полезно различать четыре вида утверждений. Звучат они похоже, но вес у них разный, и если их путать, получаются ложные выводы.'
        ) + cards([
            ('Симптом', 'Что заметил пользователь', '«Каталог иногда долго открывается».'),
            ('Наблюдение', 'Факт, подтверждённый измерением', 'С 14:00 два из двадцати запросов /products длятся больше 3 секунд, код ответа 200.'),
            ('Гипотеза', 'Предположение, которое ещё не проверено', 'Запросы долго ждут ответа зависимости.'),
            ('Вывод', 'Гипотеза, подтверждённая проверкой', 'Задержка приходится на этап dependency.wait, остальные этапы не изменились.'),
        ]) + p(
            'Чаще всего гипотезу записывают как вывод. Например, на графике загрузка процессора 95%, и хочется сразу написать «сервис тормозит из-за процессора». '
            'Но высокая загрузка бывает и причиной задержек, и следствием наплыва запросов, и обычным состоянием вычислительной задачи.'
        ) + p(
            'Чтобы отличить одно от другого, нужен контекст: такой ли была загрузка вчера, когда жалоб не было, совпадает ли её рост с ростом длительности, '
            'выросло ли число запросов. Пока это не проверено, «CPU 95%» остаётся наблюдением, а «причина в процессоре» — гипотезой.'
        )),

        ('need', 'Какие данные нужно сохранять заранее', p(
            'Соберём выводы главы. Чтобы после развёртывания можно было разобрать жалобу, сервис во время работы сохраняет четыре вида сведений.'
        ) + table(['Что сохранять', 'На какой вопрос отвечает', 'Пример со стенда'], [
            ['Количество операций и их результаты за интервал', 'Каков масштаб проблемы', 'Сколько запросов /products завершилось кодом 200 и сколько кодом 500'],
            ['Распределение длительностей', 'Сколько запросов медленные, а не только каково среднее', 'Сколько запросов уложилось в 100 мс, сколько длилось больше секунды'],
            ['Записи о событиях с контекстом', 'Что случилось с конкретным запросом', 'Запись с request_id, кодом ответа и текстом исключения'],
            ['Этапы операции', 'Где операция потратила время', 'validate, dependency.wait, serialize'],
        ]) + p(
            'Ко всем этим записям одни и те же требования. Каждое значение хранится с отметкой времени, иначе его не поставить в историю. '
            'Каждое значение привязано к компоненту: сервису, экземпляру, версии. И всё это хранится вне процесса, который может упасть или перезапуститься.'
        ) + p(
            'Регулярный сбор таких сведений называют мониторингом, а сами сведения — телеметрией. '
            'Строки таблицы соответствуют трём видам телеметрии: первые две — метрикам, третья — логам, четвёртая — трассам.'
        ) + flow([
            ('Метрики', 'количества и длительности'),
            ('Логи', 'записи о событиях'),
            ('Трассы', 'этапы операции'),
        ], 'Три вида телеметрии разобраны в главах 1.4, 1.5 и 1.6.') + source('https://sre.google/sre-book/monitoring-distributed-systems/', 'Google SRE Book · Monitoring Distributed Systems')),
    ],
    mistakes=[
        ['Считают HTTP 200 признаком того, что всё хорошо', 'Медленные и некорректные успешные ответы остаются незамеченными', 'Сохранять длительность и отдельно проверять результат операции'],
        ['Проверяют работу сервиса только через /health', 'Процесс жив, а рабочий маршрут завершается ошибкой', 'Смотреть данные о настоящих запросах к рабочим маршрутам'],
        ['Закрывают жалобу после одной ручной проверки', 'Редкие проблемы не воспроизводятся и остаются в сервисе', 'Смотреть данные за весь интервал жалобы'],
        ['Рассчитывают разобрать сбой по выводу терминала', 'После перезапуска процесса вывод недоступен', 'Сохранять телеметрию вне процесса во время работы'],
        ['Записывают гипотезу как вывод', 'Исправляют не то, проблема остаётся', 'Отмечать, какие утверждения измерены, а какие только предположены'],
        ['Путают время клиента и время обработчика', 'Задержку сети или Uvicorn приписывают коду', 'Указывать, в какой точке измерена длительность'],
    ],
    selfcheck=[
        ('Почему после развёртывания нельзя разобрать ночной сбой так же, как локальную ошибку?',
         p('Процесс, в котором произошёл сбой, мог упасть и перезапуститься. Разработчика не было рядом в момент ошибки, терминал и память процесса недоступны. '
           'Остаются только данные, сохранённые во время работы.')),
        ('/health отвечает {"status": "ok"}, а /products возвращает 500. Как это возможно?',
         p('Обработчик /health проверяет только, что процесс принимает запросы. Он не обращается к зависимости и не выполняет сценарий /products, поэтому сбой рабочего маршрута на нём не отражается.')),
        ('На снимке fast клиент ждал 84,0 мс, а обработчик работал 81,8 мс. Откуда разница?',
         p('Время клиента включает путь запроса и ответа и работу Uvicorn до и после вызова обработчика. Время обработчика измерено только внутри функции products.')),
        ('Медленным оказывается каждый десятый запрос. С какой вероятностью три ручные проверки подряд будут быстрыми?',
         p('0,9³ = 0,729, то есть около 73%. В большинстве случаев три проверки проблему не покажут.')),
        ('«CPU 90%, поэтому API медленный». Какое это утверждение: наблюдение, гипотеза или вывод?',
         p('«CPU 90%» — наблюдение. Связь с медленной работой API — гипотеза, пока не проверено, совпадает ли рост загрузки с ростом длительности и нет ли другой причины.')),
    ],
    cheatsheet=[
        'products-api — сервис на FastAPI: /products со сценариями fast, slow и error.',
        'Развёртывание — запуск версии программы в среде, где с ней работают пользователи.',
        'После развёртывания нет терминала, отладчика и возможности повторить запрос.',
        'Данные для разбора сбоя сохраняют заранее, во время работы сервиса.',
        'Качество сервиса: доступность, производительность, надёжность, корректность.',
        '/health проверяет процесс, но не рабочие маршруты и зависимости.',
        'HTTP 200 не различает быстрый и медленный ответ; нужна длительность.',
        'Одна быстрая проверка не опровергает жалобу на редкие задержки.',
        'Симптом → наблюдение → гипотеза → проверка → вывод.',
    ],
    links=[
        ('https://sre.google/sre-book/monitoring-distributed-systems/', 'Google SRE Book · Monitoring Distributed Systems'),
        ('https://fastapi.tiangolo.com/tutorial/first-steps/', 'FastAPI · First steps'),
    ],
)
