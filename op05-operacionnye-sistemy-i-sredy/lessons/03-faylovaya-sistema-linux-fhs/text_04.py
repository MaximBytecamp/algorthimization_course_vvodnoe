from blocks import *

CHAPTER = dict(
slug='04-srok-zhizni', layer='Изменяемые данные', color='#B84E2E',
title='Изменяемые и временные данные',
lead='В этой части рассмотрим каталог /var, где программы хранят данные, изменяющиеся во время работы, и каталоги временных файлов /tmp, /var/tmp и /run.',
sections=[
dict(title='Каталог /var', blocks=[
  P('Программы не только читают свои файлы, но и создают новые данные: записывают журналы, ставят задания в очередь, ведут базы. Такие данные нельзя хранить рядом с программой в <code>/usr</code>, поэтому для них предназначен отдельный каталог <code>/var</code> (от английского variable — изменяемый).'),
  P('Разницу между <code>/usr</code> и <code>/var</code> можно представить так. Программа — это учебник: его текст не меняется, сколько бы его ни читали. Журнал — это тетрадь, в которую каждый день дописывают новые записи. Учебники и тетради хранят отдельно: учебник можно заменить новым изданием, а тетрадь с записями терять нельзя.'),
  P('Внутри <code>/var</code> данные разделены по тому, насколько они важны и можно ли их восстановить. Постоянные данные программ, например базы, хранятся в <code>/var/lib</code>: их потеря приводит к утрате информации. Журналы находятся в <code>/var/log</code>. Кэш, то есть данные, которые программа может вычислить заново, хранится в <code>/var/cache</code>. Очереди заданий, ожидающих обработки, — в <code>/var/spool</code>.'),
  TABLE(['Каталог', 'Данные', 'Последствия удаления'], [
    ['<code>/var/lib</code>', 'постоянные данные программ, базы', 'данные теряются'],
    ['<code>/var/log</code>', 'журналы событий', 'теряется история, программа работает'],
    ['<code>/var/cache</code>', 'кэш', 'программа создаёт его заново'],
    ['<code>/var/spool</code>', 'очереди заданий', 'теряются необработанные задания'],
  ]),
  P('<b>Примеры.</b> Сервер баз данных MySQL хранит базы в <code>/var/lib/mysql</code>. Веб-сервер Apache пишет журнал посещений сайта в <code>/var/log/apache2</code>. Менеджер пакетов apt держит скачанные пакеты в кэше <code>/var/cache/apt</code>: если кэш очистить, пакеты просто скачаются заново. Задания печати ждут своей очереди в <code>/var/spool/cups</code>.'),
  TERM('журнал', 'Файл, в который программа последовательно записывает сведения о событиях: запуск, ошибки, обращения. В Ubuntu системные события также собирает служба журналирования, их выводит команда <code>journalctl</code>.'),
  P('Запись журнала тоже состоит из частей: дата и время события, имя компьютера, название программы с номером процесса в квадратных скобках и само сообщение. Например, строка <code>Sep 14 18:53:08 ubuntu systemd-resolved[1223]: Using degraded feature set…</code> означает: 14 сентября в 18:53 на компьютере ubuntu служба разрешения имён systemd-resolved (процесс 1223) сообщила, что сменила режим работы с DNS-сервером. По таким строкам восстанавливают, что и когда происходило в системе.'),
  PROBE('Содержимое /var', [
    ('ls /var', 'подкаталоги /var'),
    ('ls -lah /var/log | head -12', 'первые файлы журналов'),
    ('ls /var/lib | head -8', 'постоянные данные программ'),
    ('journalctl -n 3 --no-pager', 'три последние записи системного журнала'),
  ], 'в /var есть cache, lib, log и spool.'),
  CORE('10-var', 'Подкаталоги /var.',
    LENS(0, 1, [(1, 9, 14, 1), (1, 23, 26, 2), (1, 41, 44, 3), (1, 77, 82, 4)],
      ['кэш', 'постоянные данные', 'журналы', 'очереди'], cols=90)),
]),
dict(title='Временные файлы', blocks=[
  P('Для временных данных в системе предусмотрено три каталога с разным сроком хранения. В <code>/tmp</code> программы создают промежуточные файлы; система может удалять их в любой момент, как правило при перезагрузке. <code>/var/tmp</code> также предназначен для временных файлов, но они сохраняются после перезагрузки. В <code>/run</code> хранятся сведения о работающих службах: номера процессов и файлы для обмена данными между программами. Этот каталог очищается при каждой загрузке системы.'),
  P('<b>Пример.</b> Архиватор распаковывает файлы во временный каталог внутри <code>/tmp</code> и после работы удаляет их. Работающая служба записывает в <code>/run</code> номер своего процесса, чтобы систему можно было попросить её остановить. После перезагрузки номер процесса будет другим, поэтому старое значение хранить незачем.'),
  TERM('tmpfs', 'Файловая система, которая хранит файлы в оперативной памяти. Её содержимое исчезает при перезагрузке. В Ubuntu на tmpfs расположен каталог <code>/run</code>.'),
  PROBE('На какой файловой системе находятся /run и /tmp', [
    ('ls -ld /tmp /var/tmp /run', 'сведения о самих каталогах'),
    ('findmnt -T /run -o TARGET,SOURCE,FSTYPE', ''),
    ('findmnt -T /tmp -o TARGET,SOURCE,FSTYPE', ''),
    ('ls /run | head -8', 'содержимое /run'),
  ], 'для /run указан тип tmpfs.'),
  CORE('11-temporary', 'В Live-сессии и /run, и /tmp находятся в оперативной памяти.',
    LENS(4, 13, [(8, 0, 16, 1), (13, 0, 16, 2)],
      ['/run — tmpfs', '/tmp — tmpfs в этой сессии'], cols=60)),
  NOTE('В установленной Ubuntu <code>/tmp</code> обычно находится на диске, а в Live-сессии в памяти работает вся система, поэтому после перезагрузки пропадает и <code>~/linux-fhs-lab</code>.'),
]),
dict(title='Изменяемые данные службы печати', blocks=[
  P('На примере CUPS видно, как данные одной службы распределяются по сроку хранения. Настройки находятся в <code>/etc/cups</code>, журналы — в <code>/var/log/cups</code>, очередь печати — в <code>/var/spool/cups</code>, кэш — в <code>/var/cache/cups</code>. Файл <code>cups.sock</code>, через который программы обращаются к работающей службе, находится в <code>/run/cups</code> и после перезагрузки создаётся заново.'),
  PROBE('Каталоги CUPS', [
    ('ls /etc/cups', 'настройки'),
    ('ls -l /var/log/cups', 'журналы'),
    ('ls -ld /var/spool/cups /var/cache/cups', 'очередь печати и кэш'),
    ('ls -l /run/cups', 'файлы работающей службы'),
    ('findmnt -n -o TARGET,FSTYPE -T /run/cups | cat', ''),
    ('findmnt -n -o TARGET,FSTYPE -T /var/log/cups | cat', ''),
  ], '/run/cups находится на tmpfs, журналы — на основной файловой системе.'),
  CORE('cups-lifetime', 'Файлы службы печати с разным сроком хранения.',
    LENS(0, 17, [(1, 19, 29, 1), (5, 39, 49, 2), (12, 43, 52, 3), (15, 7, 12, 4), (17, 7, 14, 5)],
      ['основной файл настроек', 'журнал обращений к службе', 'файл для связи со службой', '/run/cups — в оперативной памяти', 'журналы — на основной файловой системе'], cols=80)),
]),
],
summary=[
  'Данные, изменяющиеся во время работы программ, хранятся в <code>/var</code>.',
  '<code>/var/lib</code> — постоянные данные, <code>/var/log</code> — журналы, <code>/var/cache</code> — кэш, <code>/var/spool</code> — очереди.',
  '<code>/tmp</code> и <code>/run</code> очищаются при перезагрузке, <code>/var/tmp</code> — нет.',
  'tmpfs хранит файлы в оперативной памяти.',
],
report='Снимок с ls /var и findmnt для /run.',
)
