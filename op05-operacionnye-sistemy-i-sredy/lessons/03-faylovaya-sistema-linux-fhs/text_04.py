from blocks import *

CHAPTER = dict(
slug='04-srok-zhizni', layer='Срок хранения', color='#B84E2E',
title='Сколько хранятся данные',
question='Почему данные одной программы лежат в разных каталогах: /var, /tmp и /run?',
strata='Журнал должен сохраниться после перезагрузки, поэтому он в /var/log. Каждый день старая часть журнала откладывается, хранится 7 копий.',
intro='<p>Данные, которые меняются во время работы, лежат в <code>/var</code>. Но у CUPS таких данных несколько видов: журнал, очередь заданий на печать, кэш и файл для связи со службой.</p>'
      '<p><mark>Эти данные различаются тем, сколько их нужно хранить.</mark> Одни должны сохраниться после перезагрузки, другие программа может создать заново, третьи нужны только пока служба работает.</p>',
sections=[
dict(title='Что лежит внутри /var', blocks=[
  TABLE(['Каталог', 'Что хранит', 'Что будет, если удалить'], [
    ['<code>/var/lib/имя</code>', 'данные программы: базы, списки, учёт', 'данные пропадут, программа их не восстановит'],
    ['<code>/var/log/имя</code>', 'журналы — записи о том, что происходило', 'пропадёт история, программа продолжит работать'],
    ['<code>/var/spool/имя</code>', 'очереди: задания, которые ещё не выполнены', 'пропадут, например, задания печати, которые ещё не распечатаны'],
    ['<code>/var/cache/имя</code>', 'кэш — то, что программа может создать заново', 'программа создаст его снова, но это займёт время'],
    ['<code>/var/tmp</code>', 'временные файлы, которые переживают перезагрузку', 'обычно ничего важного'],
  ]),
  D('журнал', 'log',
    'Файл с записями о событиях: что произошло, когда и какие были ошибки.',
    'Программа дописывает новые записи в конец журнала. В Ubuntu многие события собирает системный журнал, его показывает команда <code>journalctl</code>. Чтобы журналы не росли бесконечно, служба logrotate регулярно откладывает старые части.',
    'По журналу выясняют, почему служба не запустилась или кто входил в систему.',
    'Если удалить журнал, программа продолжит работать. Если удалить данные из <code>/var/lib</code>, программа может перестать работать.'),
  PROBE('Смотрим содержимое /var', [
    ('ls /var', 'каталоги внутри /var'),
    ('ls -lah /var/log | head -12', 'первые журналы'),
    ('ls /var/lib | head -8', 'данные служб'),
    ('journalctl -n 3 --no-pager', 'три последние записи системного журнала'),
  ], 'в /var есть cache, lib, log, spool и tmp.'),
  CORE('10-var', 'Каталоги внутри /var. Зелёным фоном терминал выделяет каталоги с особыми правами доступа.',
    LENS(0, 1, [(1, 9, 14, 1), (1, 23, 26, 2), (1, 41, 44, 3), (1, 77, 82, 4), (1, 84, 87, 5), (1, 66, 69, 6)],
      ['кэш', 'данные программ', 'журналы', 'очереди', 'временные файлы, которые переживают перезагрузку', 'ссылка на /run'], cols=90)),
  ASK('На диске кончилось место. Можно ли очистить /var/lib?',
      'Нет. Там могут лежать данные, которых больше нигде нет, например база программы. Место освобождают с кэша и старых журналов, и только когда понятно, чьи это файлы.'),
]),
dict(title='Временные файлы: /tmp, /var/tmp и /run', blocks=[
  D('tmpfs', 'файловая система в памяти',
    'Файловая система, которая хранит файлы в оперативной памяти.',
    'Файлы в tmpfs существуют, пока она подключена. После перезагрузки она пустая.',
    'Подходит для данных, которые не нужны после перезагрузки: они пропадут сами, и убирать их не нужно.',
    'Слово tmp в названии каталога ещё не значит, что это tmpfs. Проверяют командой <code>findmnt -T путь</code>.'),
  TABLE(['Каталог', 'Сколько хранятся файлы', 'Пример'], [
    ['<code>/run</code>', 'до перезагрузки', 'файл /run/cups/cups.sock для связи со службой печати'],
    ['<code>/tmp</code>', 'могут удалиться в любой момент, система чистит его сама', 'промежуточный файл программы'],
    ['<code>/var/tmp</code>', 'переживают перезагрузку, но старые файлы могут удаляться', 'распакованный архив, нужный на время'],
  ]),
  PROBE('Проверяем, где хранятся временные файлы', [
    ('ls -ld /tmp /var/tmp /run', ''),
    ('findmnt -T /run -o TARGET,SOURCE,FSTYPE', 'на какой файловой системе /run'),
    ('findmnt -T /tmp -o TARGET,SOURCE,FSTYPE', 'на какой файловой системе /tmp'),
    ('ls /run | head -8', 'что лежит в /run'),
  ], 'у /run тип tmpfs.'),
  CORE('11-temporary', 'В этой Live-сессии и /run, и /tmp хранятся в памяти. В установленной Ubuntu /tmp часто лежит на диске.',
    LENS(0, 13, [(1, 42, 46, 1), (3, 42, 50, 2), (8, 0, 16, 3), (13, 0, 16, 4)],
      ['каталог /run', '/var/tmp — отдельный каталог, не то же самое, что /tmp', '/run хранится в памяти (tmpfs)', '/tmp в этой сессии тоже в памяти'], cols=60)),
  NOTE('В Live-сессии вся система работает в памяти, поэтому после перезагрузки пропадает и каталог <code>linux-fhs-lab</code>. В установленной Ubuntu домашний каталог сохраняется.'),
]),
dict(title='Что сохранится после перезагрузки', blocks=[
  P('Проверим правило на файлах CUPS. Выбирайте событие в модели: перезагрузка, обновление пакета, очистка кэша, удаление пакета. Модель показывает, что станет с каждым файлом.'),
  P('Про удаление пакета важно знать: команда <code>apt remove</code> удаляет программу, но оставляет её настройки в <code>/etc</code>. Чтобы удалить и настройки, используют <code>apt purge</code>.'),
  W('lifetime'),
  PROBE('Файлы CUPS в системе', [
    ('ls /etc/cups', 'настройки'),
    ('ls -l /var/log/cups', 'журналы'),
    ('ls -ld /var/spool/cups /var/cache/cups', 'очередь и кэш'),
    ('ls -l /run/cups', 'файлы работающей службы'),
    ('findmnt -n -o TARGET,FSTYPE -T /run/cups | cat', 'где хранится /run/cups'),
    ('findmnt -n -o TARGET,FSTYPE -T /var/log/cups | cat', 'где хранятся журналы'),
  ], '/run/cups хранится в памяти (tmpfs), журналы — нет.'),
  CORE('cups-lifetime', 'Файлы CUPS: файл для связи со службой хранится в памяти, журнал — в /var/log.',
    LENS(0, 17, [(1, 18, 28, 1), (5, 39, 49, 2), (12, 0, 1, 3), (12, 43, 52, 3), (15, 7, 12, 4), (17, 7, 14, 5)],
      ['главный файл настроек cupsd.conf', 'журнал access_log — путь, который мы разбираем весь урок', 'буква <code>s</code> — сокет: через этот файл программы обращаются к службе печати', '/run/cups хранится в памяти и после перезагрузки исчезнет', 'журналы хранятся на корневой файловой системе и сохранятся'], cols=80)),
  ASK('Куда положить базу заказов своего сервиса: в /tmp, /run или /var/lib?',
      'В <code>/var/lib/имя-сервиса</code>. Каталог /run очищается при каждой загрузке, файлы в /tmp могут удалиться, а база должна храниться постоянно.'),
]),
],
outro='Данные, которые меняются, раскладывают по сроку хранения: <b>/var/lib</b> — постоянно, <b>/var/log</b> и <b>/var/cache</b> — можно потерять без поломки программы, <b>/run</b> — только до перезагрузки.',
report='Таблица «вид данных → каталог → что будет после перезагрузки» и кадр с findmnt для /run.',
)
