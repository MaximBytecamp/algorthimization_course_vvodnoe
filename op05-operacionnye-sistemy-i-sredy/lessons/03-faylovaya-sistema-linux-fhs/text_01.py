from blocks import *

CHAPTER = dict(
slug='01-adres', layer='Адрес', color='#E8B45A',
title='Одно дерево каталогов',
question='Откуда начинается путь к файлу и как его прочитать?',
strata='Путь читается слева направо: корень /, каталог var, в нём log, в нём cups, в нём файл access_log.',
intro='<p>На обложке мы увидели, что файлы службы печати CUPS лежат в разных каталогах. Чтобы разобраться почему, сначала научимся читать путь к файлу.</p>'
      '<p>В Windows у каждого диска своя буква: <code>C:</code>, <code>D:</code>. В Linux букв нет. <mark>Все файлы собраны в одно дерево каталогов</mark>, и у этого дерева одна вершина — корневой каталог.</p>',
sections=[
dict(title='Корневой каталог', blocks=[
  D('корневой каталог', '/',
    'Самый верхний каталог системы. Все остальные каталоги и файлы лежат внутри него. Обозначается одной косой чертой <code>/</code>.',
    'Путь <code>/etc/hosts</code> читается слева направо: начинаем с корня, заходим в каталог <code>etc</code>, берём в нём файл <code>hosts</code>. Если подключить флешку, она не получит букву, а появится как ещё один каталог внутри этого дерева.',
    'Программе достаточно знать путь к файлу. На каком диске он лежит, ей знать не нужно.',
    '<code>/</code> — корневой каталог. <code>/root</code> — домашний каталог пользователя root. Это два разных каталога.'),
  P('Откройте терминал: <kbd>Ctrl</kbd> + <kbd>Alt</kbd> + <kbd>T</kbd>. Слева от курсора стоит строка вида <code>ubuntu@ubuntu:~$</code>. Это <b>приглашение</b>: так оболочка Bash показывает, что готова принять команду. Приглашение вводить не нужно, команды набираются после него.'),
  CORE('02-paths', 'Приглашение меняется после каждого перехода: в нём виден каталог, в котором вы находитесь.',
    LENS(3, 3, [(3, 0, 6, 1), (3, 7, 13, 2), (3, 14, 24, 3), (3, 24, 25, 4)],
      ['имя пользователя', 'имя компьютера', 'текущий каталог — сейчас /usr/share', 'знак <code>$</code> означает обычного пользователя, у root здесь <code>#</code>'], cols=40)),
  PROBE('Переходим в корень', [
    ('cd /', 'перейти в корневой каталог'),
    ('pwd', 'показать, в каком каталоге мы сейчас'),
    ('ls -lah /', 'список корня: -l подробно, -a со скрытыми файлами, -h размеры в K и M'),
  ], 'pwd выводит одну косую черту /. В списке есть etc, home, usr и var.'),
  CORE('01-root', 'Корневой каталог Ubuntu. Каталоги из этого списка мы будем разбирать по одному на протяжении всего урока.',
    LENS(1, 13, [(2, 0, 1, 1), (7, 0, 1, 2), (7, 43, 57, 3), (12, 43, 46, 4), (13, 43, 47, 4)],
      ['pwd вывел <code>/</code> — мы в корне', 'буква <code>l</code> в начале строки означает ссылку', '<code>bin</code> — ссылка на <code>usr/bin</code>, разберём в слое 3', 'обычные каталоги: etc — настройки, home — каталоги пользователей'], cols=62)),
  P('Тот же корень можно открыть в файловом менеджере. Список каталогов будет тем же, что показала команда <code>ls</code>.'),
  CORE('files-root', 'Файловый менеджер Ubuntu открыт в корне. Ссылки bin, lib и sbin отмечены стрелкой на значке.'),
  ASK('Можно ли считать /home и /usr такими же отдельными дисками, как C: и D:?',
      'Нет. Это каталоги одного дерева. Они могут находиться на одном диске или на разных, но по имени каталога этого не узнать. Проверять будем командой <code>findmnt</code> в слое 7.'),
]),
dict(title='Абсолютный и относительный путь', blocks=[
  D('абсолютный путь', 'начинается с /',
    'Путь от корневого каталога. Например, <code>/usr/bin</code>.',
    'Отсчёт всегда идёт от <code>/</code>, поэтому неважно, в каком каталоге вы находитесь.',
    'Абсолютный путь приводит в одно и то же место из любого каталога.',
    'Путь без <code>/</code> в начале — относительный.'),
  D('относительный путь', 'без / в начале',
    'Путь, который отсчитывается от текущего каталога — того, что показывает <code>pwd</code>.',
    'Оболочка добавляет относительный путь к текущему каталогу. Две точки <code>..</code> означают каталог на уровень выше, одна точка <code>.</code> — сам текущий каталог. Путь <code>../bin</code> значит: подняться на уровень выше и зайти в <code>bin</code>.',
    'Короткие пути удобны, когда вы работаете внутри одного каталога.',
    'Одна и та же команда с относительным путём из разных каталогов приводит в разные места.'),
  P('Пример. Если вы находитесь в <code>/usr</code>, команда <code>cd bin</code> приведёт в <code>/usr/bin</code>. Если вы в домашнем каталоге, та же команда будет искать <code>/home/ubuntu/bin</code>, а такого каталога нет. Нажимайте команды в модели и смотрите, как меняется текущий каталог.'),
  W('walk'),
  PROBE('Приходим в /usr/bin двумя способами', [
    ('cd /usr/share', 'абсолютный путь'),
    ('pwd', ''),
    ('cd ..', 'на уровень выше: из /usr/share в /usr'),
    ('pwd', ''),
    ('cd /', ''),
    ('cd usr', 'относительный путь: из / в /usr'),
    ('cd bin', 'относительный путь: из /usr в /usr/bin'),
    ('pwd', ''),
  ], 'pwd по очереди выводит /usr/share, /usr и /usr/bin.'),
  CORE('02-paths', 'Два относительных перехода привели в тот же каталог /usr/bin.',
    LENS(3, 10, [(3, 26, 31, 1), (5, 0, 4, 1), (7, 17, 23, 2), (8, 19, 25, 2), (10, 0, 8, 3)],
      ['<code>cd ..</code> поднял нас из /usr/share в /usr', 'два перехода по относительному пути: usr, затем bin', 'результат — /usr/bin'], cols=40)),
  TABLE(['', 'Windows', 'Ubuntu'], [
    ['Пример пути', '<code>C:\\Users\\Student\\notes.txt</code>', '<code>/home/student/notes.txt</code>'],
    ['Разделитель', '<code>\\</code>', '<code>/</code>'],
    ['Показать текущий каталог', '<code>Get-Location</code>', '<code>pwd</code>'],
    ['Data и data', 'одно и то же имя', 'два разных имени'],
    ['Второй диск', 'отдельная буква D:', 'каталог внутри общего дерева'],
  ]),
  ASK('Вы в /usr/share. Куда приведёт команда cd ../bin?',
      'В <code>/usr/bin</code>. Сначала <code>..</code> поднимает в <code>/usr</code>, затем мы заходим в <code>bin</code>. Команда <code>cd bin</code> без двух точек искала бы <code>/usr/share/bin</code>.'),
]),
dict(title='Домашний каталог и скрытые файлы', blocks=[
  D('домашний каталог', '~ или $HOME',
    'Личный каталог пользователя для его файлов и настроек. У пользователя ubuntu это <code>/home/ubuntu</code>.',
    'Знак <code>~</code> оболочка заменяет на путь к домашнему каталогу. Переменная <code>$HOME</code> хранит тот же путь.',
    'В своём домашнем каталоге пользователь может создавать файлы без прав администратора. Поэтому все опыты урока мы делаем в нём.',
    '<code>pwd</code> показывает, где вы сейчас. <code>$HOME</code> показывает ваш домашний каталог. Они совпадают, только если вы в него перешли.'),
  PROBE('Домашний каталог', [
    ('cd ~', 'перейти в домашний каталог'),
    ('echo "$HOME"', 'вывести путь из переменной HOME'),
    ('pwd', ''),
    ('ls', 'список файлов'),
    ('ls -a', 'список вместе со скрытыми файлами'),
    ('getent passwd root', 'сведения о пользователе root'),
  ], 'ls -a показывает больше имён, чем ls. В строке про root есть путь /root.'),
  CORE('03-home', 'Скрытые файлы — это обычные файлы, имя которых начинается с точки.',
    LENS(1, 11, [(2, 0, 12, 1), (8, 4, 16, 2), (9, 18, 25, 2), (11, 16, 21, 3)],
      ['домашний каталог пользователя ubuntu', 'имена с точкой, например <code>.bash_logout</code> и <code>.config</code>, видны только с ключом -a', 'домашний каталог root — <code>/root</code>'], cols=70)),
  P('Точка в начале имени только прячет файл из обычного списка. Защитой она не является: прочитать такой файл может любой, у кого есть права. Программы хранят так свои настройки, чтобы они не мешали в списке документов.'),
  ASK('Станет ли файл .config недоступным для других пользователей из-за точки в имени?',
      'Нет. Точка влияет только на то, покажет ли файл команда <code>ls</code> без ключа <code>-a</code>. Кто может читать файл, определяют права доступа.'),
]),
dict(title='Рабочий каталог для опытов', blocks=[
  P('Все опыты урока мы выполняем в каталоге <code>~/linux-fhs-lab</code>. Системные каталоги только читаем и ничего в них не меняем. Исключение одно: в слое 7 мы ненадолго подключим файловую систему и потом отключим её.'),
  PROBE('Создаём рабочий каталог', [
    ('mkdir -p ~/linux-fhs-lab/{config,data,logs,tmp,screenshots}', 'создать пять каталогов; -p не выдаёт ошибку, если они уже есть'),
    ('cd ~/linux-fhs-lab', ''),
    ('printf "Linux filesystem\\n" > data/info.txt', 'знак > записывает текст в файл'),
    ('printf "mode=lab\\n" > config/app.conf', ''),
    ('find . -maxdepth 2 -type f', 'найти файлы не глубже двух уровней'),
  ], 'find выводит ./data/info.txt и ./config/app.conf.'),
  CORE('04-lab', 'Рабочий каталог создан, в нём два файла.',
    LENS(0, 7, [(0, 42, 76, 1), (2, 60, 61, 2), (6, 0, 15, 3), (7, 0, 17, 3)],
      ['из фигурных скобок оболочка делает пять путей', '<code>&gt;</code> записывает вывод команды в файл', 'find нашёл оба файла'], cols=80)),
  ASK('Настройки системы хранятся в /etc. Почему наш config лежит в домашнем каталоге?',
      'Это учебная работа одного пользователя, а не программа, установленная в систему. Правила размещения, которые мы разберём дальше, относятся к установленным программам.'),
]),
],
outro='Путь к любому файлу начинается с корня <code>/</code>. Значит, вопрос «где лежат файлы CUPS» — это вопрос о том, <b>в какие каталоги дерева</b> они попали. Почему именно в эти, разберём в следующем слое.',
report='Кадр с pwd и ls -lah / и объяснение, чем отличаются /, /root и ~.',
)
