from blocks import *

CHAPTER = dict(
slug='08-sborka', layer='Своё приложение', color='#2B1A2D',
title='Размещаем своё приложение',
question='Куда положить файлы нового сервиса и как проверить, что выбор правильный?',
strata='Мы разобрали путь полностью: где он в дереве, что в файле, кто его пишет, сколько он хранится, как устроено имя и какая файловая система за ним стоит.',
intro='<p>Мы разобрали путь <code>/var/log/cups/access_log</code> со всех сторон. Теперь сделаем обратное: у нас есть новый сервис <code>shop-api</code>, и <mark>мы сами выбираем каталог для каждого его файла</mark>.</p>'
      '<p>Системные каталоги <code>/etc</code> и <code>/var</code> мы не трогаем. Всю структуру соберём внутри своего рабочего каталога.</p>',
sections=[
dict(title='Путь access_log целиком', blocks=[
  P('В таблице собрано всё, что мы узнали о пути <code>/var/log/cups/access_log</code>, по одной строке на слой. В правом столбце — команда, которой это можно проверить на своём компьютере.'),
  W('fullcut'),
  CORE('cups-unit', 'Проверка на настоящей системе: кто запускает службу, как хранится журнал и какой у него inode.',
    LENS(0, 12, [(1, 2, 39, 1), (2, 0, 25, 2), (4, 0, 18, 3), (5, 8, 13, 3), (7, 8, 16, 3), (12, 0, 42, 4)],
      ['файл службы лежит в /usr/lib: путь /lib ведёт туда же', 'systemd запускает cupsd по полному пути', 'журналы CUPS откладываются каждый день, хранится 7 копий', 'у access_log свой номер inode, в файле 336 байт'], cols=76)),
  P('Те же вопросы можно задать о любом файле. Сначала: <b>где он в дереве</b>, <b>что в нём хранится</b>, <b>кто его создаёт</b> и <b>как долго он нужен</b>. Затем: <b>лежит ли он на диске</b>, <b>какой у него inode</b> и <b>какая файловая система за ним стоит</b>.'),
]),
dict(title='Файлы сервиса shop-api', blocks=[
  P('У сервиса шесть файлов: программа, настройки, база данных, журнал, файл с номером запущенного процесса и промежуточный файл, который можно потерять. Выберите каталог для каждого и нажмите «Проверить».'),
  W('placement'),
  P('Теперь соберём такую же структуру в каталоге <code>model-root</code>. Путь <code>model-root/etc/shop-api/app.conf</code> изображает системный <code>/etc/shop-api/app.conf</code>, но это обычный файл в вашем рабочем каталоге. Команда <code>cp</code> делает отдельную копию файла, это не жёсткая ссылка.'),
  PROBE('Собираем структуру', [
    ('cd ~/linux-fhs-lab', ''),
    ('mkdir -p model-root/{usr/local/bin,etc/shop-api,var/lib/shop-api,var/log/shop-api,run/shop-api}', 'создать пять каталогов'),
    ('cp config/app.conf model-root/etc/shop-api/', 'настройки'),
    ('cp data/info.txt model-root/var/lib/shop-api/', 'данные'),
    ('printf "service started\\n" > model-root/var/log/shop-api/app.log', 'журнал'),
    ('find model-root -type f', 'список файлов'),
  ], 'find показывает три файла: в etc, var/lib и var/log.'),
  CORE('22-placement', 'Структура сервиса внутри рабочего каталога. Системные /etc и /var не изменились.',
    LENS(6, 9, [(7, 11, 18, 1), (8, 11, 18, 2), (9, 11, 14, 3)],
      ['журнал — в var/log', 'данные — в var/lib', 'настройки — в etc'], cols=44)),
  ASK('Где хранить файлы сайта, которые сервис показывает посетителям?',
      'По FHS для этого есть каталог <code>/srv</code>, например <code>/srv/www</code>. Многие веб-серверы по умолчанию используют <code>/var/www</code>. Точный путь смотрят в документации и настройках сервера.'),
]),
dict(title='Отчёт', blocks=[
  P('Скачайте шаблон отчёта в конце страницы и сохраните его как <code>README.md</code> в каталоге <code>linux-fhs-lab</code>. Снимки экрана делайте клавишей <kbd>Print Screen</kbd> и сохраняйте в каталог <code>screenshots</code> под именами из таблицы.'),
  P('Под каждым снимком напишите одно-три предложения: что вы проверяли, что увидели и какой из этого вывод.'),
  PROBE('Начинаем отчёт из терминала', [
    ('cd ~/linux-fhs-lab', ''),
    ('printf "# Linux FHS lab\\n\\n" > README.md', '> создаёт файл заново: не запускайте, если отчёт уже заполнен'),
    ('printf "## System\\n" >> README.md', '>> дописывает в конец файла'),
    ('cat /etc/os-release | head -4 >> README.md', ''),
    ('printf "\\n## Root filesystem\\n" >> README.md', ''),
    ('findmnt / -o TARGET,SOURCE,FSTYPE >> README.md', ''),
    ('cat README.md', ''),
  ], 'в README.md есть сведения о системе и тип корневой файловой системы.'),
  CORE('23-report', 'Начало отчёта: вывод команд записан в файл.',
    LENS(0, 17, [(1, 60, 61, 1), (2, 52, 54, 2), (10, 0, 32, 3), (17, 0, 21, 4)],
      ['<code>&gt;</code> создаёт файл заново', '<code>&gt;&gt;</code> дописывает в конец', 'сведения о системе в файле', 'тип корневой файловой системы'], cols=84)),
  NOTE('В Live-сессии все файлы пропадут после выключения. Перед выключением скопируйте linux-fhs-lab на флешку или в облако.'),
  ASK('Можно ли вставить в отчёт снимок из урока, если на нём правильный вывод?',
      'Нет. Снимки урока — пример. Отчёт показывает, что вы выполнили работу на своём компьютере: номера процессов, inode и типы файловых систем у вас свои.'),
]),
],
outro='Главная мысль урока собрана. Каталог для файла выбирают по тому, что в нём хранится и как долго он нужен. За путём может стоять диск, память или ядро, а имя файла указывает на inode.',
report='README с 13 снимками, структура shop-api и разбор шести путей из итоговой задачи.',
)
