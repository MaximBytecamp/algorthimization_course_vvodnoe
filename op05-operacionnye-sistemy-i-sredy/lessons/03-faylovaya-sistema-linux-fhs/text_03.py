from blocks import *

CHAPTER = dict(
slug='03-programmy', layer='Программы', color='#D06C33',
title='Где хранятся программы и как оболочка их находит',
lead='В этой части рассмотрим каталог /usr, переменную PATH, по которой оболочка ищет исполняемые файлы, и ссылки /bin, /sbin, /lib в современных дистрибутивах.',
sections=[
dict(title='Каталог /usr', blocks=[
  P('Программы, установленные менеджером пакетов, размещаются в <code>/usr</code>. Внутри него есть несколько подкаталогов с разным назначением. Исполняемые файлы для всех пользователей находятся в <code>/usr/bin</code>, программы администрирования и системные службы — в <code>/usr/sbin</code>, библиотеки — в <code>/usr/lib</code>, документация и другие общие данные — в <code>/usr/share</code>.'),
  P('Программы, которые администратор устанавливает самостоятельно, без менеджера пакетов, принято размещать в <code>/usr/local</code>. Так они не смешиваются с файлами дистрибутива и не будут затронуты при его обновлении.'),
  P('<b>Пример.</b> Если администратор скачал исходный код программы и собрал её сам, исполняемый файл кладут в <code>/usr/local/bin</code>. При обновлении Ubuntu менеджер пакетов этот каталог не трогает.'),
  TABLE(['Каталог', 'Содержимое'], [
    ['<code>/usr/bin</code>', 'команды для всех пользователей'],
    ['<code>/usr/sbin</code>', 'программы администрирования и службы'],
    ['<code>/usr/lib</code>', 'библиотеки и служебные файлы программ'],
    ['<code>/usr/share</code>', 'документация, переводы, общие данные'],
    ['<code>/usr/local</code>', 'программы, установленные администратором вручную'],
  ]),
]),
dict(title='Поиск команды: переменная PATH', blocks=[
  P('Чтобы запустить программу, достаточно ввести её имя, например <code>python3</code>, а не полный путь <code>/usr/bin/python3</code>. Оболочка находит исполняемый файл сама, используя переменную окружения <b>PATH</b>.'),
  TERM('PATH', 'Переменная окружения со списком каталогов, в которых оболочка ищет исполняемые файлы. Каталоги разделены двоеточием и просматриваются по порядку; запускается первый найденный файл.',
       'Текущий каталог в PATH не входит. Программу из текущего каталога запускают с указанием пути: <code>./app</code>.'),
  P('PATH можно сравнить со списком полок, которые вы по очереди осматриваете в поисках книги: нашли книгу на второй полке — остальные уже не проверяете. <b>Пример.</b> Команда <code>python3</code> работает из любого каталога, потому что файл лежит в <code>/usr/bin</code>, а этот каталог есть в PATH. Скрипт <code>hello.sh</code>, созданный в домашнем каталоге, по короткому имени не запустится: домашнего каталога в PATH нет. Его запускают так: <code>./hello.sh</code>.'),
  P('По шагам выполнение команды выглядит так:'),
  TABLE(['Шаг', 'Что делает оболочка'], [
    ['1', 'читает введённую строку и делит её на имя команды, ключи и аргументы'],
    ['2', 'проверяет, не псевдоним ли это; если да, подставляет вместо имени полную команду'],
    ['3', 'проверяет, не встроенная ли это команда; встроенную, например <code>cd</code>, выполняет сама'],
    ['4', 'ищет исполняемый файл по каталогам из PATH, слева направо'],
    ['5', 'запускает найденную программу и передаёт ей ключи и аргументы'],
    ['6', 'ждёт, пока программа завершится, и снова выводит приглашение'],
  ]),
  P('Если в имени есть косая черта, например <code>./app</code> или <code>/usr/bin/python3</code>, поиск по PATH не нужен: оболочка сразу запускает файл по указанному пути.'),
  PROBE('Как оболочка разрешает имена команд', [
    ('command -v python3', 'путь к найденному файлу'),
    ('type cd', 'тип команды cd'),
    ('type ls', 'тип команды ls'),
    ('printf "%s\\n" "$PATH" | tr : "\\n"', 'каталоги PATH по одному в строке'),
    ('/usr/bin/python3 --version', 'запуск по полному пути'),
  ], 'python3 найден в /usr/bin, cd — встроенная команда, ls — псевдоним.'),
  CORE('08-programs', 'Разрешение имён команд и содержимое PATH.',
    LENS(0, 16, [(1, 0, 16, 1), (3, 0, 21, 2), (5, 0, 34, 3), (10, 0, 8, 4)],
      ['файл программы python3', 'встроенная команда оболочки', 'псевдоним: ls выполняется с ключом --color=auto', 'каталог, в котором найден python3'], cols=72)),
]),
dict(title='Ссылки /bin, /sbin и /lib', blocks=[
  P('Исторически часть программ располагалась в <code>/bin</code> и <code>/sbin</code>, а часть — в <code>/usr/bin</code> и <code>/usr/sbin</code>. В современных дистрибутивах, в том числе в Ubuntu, все программы перенесены в <code>/usr</code>. Прежние каталоги <code>/bin</code>, <code>/sbin</code> и <code>/lib</code> сохранены в виде символических ссылок на соответствующие каталоги внутри <code>/usr</code>. Поэтому <code>/bin/bash</code> и <code>/usr/bin/bash</code> — один и тот же файл, а скрипты со старыми путями продолжают работать.'),
  P('<b>Пример.</b> Многие скрипты начинаются со строки <code>#!/bin/bash</code>, которая указывает, какой программой выполнять скрипт. Благодаря ссылке <code>/bin</code> → <code>/usr/bin</code> такие скрипты работают, хотя сама программа bash лежит в <code>/usr/bin</code>.'),
  PROBE('Куда ведут /bin, /sbin и /lib', [
    ('ls -ld /bin /sbin /lib', 'сведения о самих ссылках'),
    ('readlink /bin', 'путь, записанный в ссылке'),
    ('readlink -f /bin', 'полный путь, на который указывает ссылка'),
    ('ls /usr', ''),
    ('ls /usr/local', 'программы, установленные вручную'),
  ], 'строки начинаются с буквы l, ссылка /bin указывает на /usr/bin.'),
  CORE('09-usrmerge', '/bin, /lib и /sbin в Ubuntu являются ссылками на каталоги внутри /usr.',
    LENS(0, 7, [(1, 0, 1, 1), (1, 38, 53, 2), (7, 0, 8, 3)],
      ['<code>l</code> — символическая ссылка', 'ссылка указывает на usr/bin', 'полный путь цели'], cols=58)),
]),
],
summary=[
  'Программы из пакетов хранятся в <code>/usr</code>, установленные вручную — в <code>/usr/local</code>.',
  'Оболочка ищет исполняемый файл по каталогам из переменной <code>PATH</code> и запускает первый найденный.',
  'Встроенные команды, такие как <code>cd</code>, выполняет сама оболочка.',
  'В Ubuntu <code>/bin</code>, <code>/sbin</code> и <code>/lib</code> — ссылки на каталоги внутри <code>/usr</code>.',
],
report='Снимок с command -v, type и выводом PATH.',
)
