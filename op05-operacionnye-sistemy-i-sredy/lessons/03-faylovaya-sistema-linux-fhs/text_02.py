from blocks import *

CHAPTER = dict(
slug='02-naznachenie', layer='Назначение каталогов', color='#E0913F',
title='Назначение каталогов и стандарт FHS',
lead='В этой части рассмотрим, по какому принципу файлы распределяются по каталогам, какие каталоги описаны стандартом FHS и что хранится в каталоге настроек /etc.',
sections=[
dict(title='Стандарт FHS', blocks=[
  P('Расположение файлов в Linux не случайно. Оно описано стандартом <b>FHS</b> (Filesystem Hierarchy Standard), который поддерживает Linux Foundation. Стандарт определяет, для каких данных предназначен каждый основной каталог. Благодаря ему администратор знает, где искать настройки, журналы или исполняемые файлы любой установленной программы.'),
  P('Основной принцип FHS: файлы распределяются не по программам, а по типу данных. У одной программы исполняемый файл, настройки и журналы окажутся в разных каталогах. Такое разделение позволяет обновлять программы, не затрагивая настройки, и делать резервные копии только тех данных, которые меняются.'),
  TABLE(['Каталог', 'Назначение'], [
    ['<code>/etc</code>', 'настройки системы и программ'],
    ['<code>/usr</code>', 'программы, библиотеки, документация'],
    ['<code>/var</code>', 'данные, которые изменяются во время работы: журналы, очереди, базы'],
    ['<code>/home</code>', 'домашние каталоги пользователей'],
    ['<code>/tmp</code>', 'временные файлы'],
    ['<code>/boot</code>', 'файлы загрузки системы'],
    ['<code>/dev</code>', 'файлы устройств'],
    ['<code>/proc</code>, <code>/sys</code>', 'сведения о процессах и устройствах, которые предоставляет ядро'],
    ['<code>/mnt</code>, <code>/media</code>', 'точки подключения дисков и съёмных носителей'],
    ['<code>/opt</code>', 'сторонние программы, устанавливаемые целиком в один каталог'],
    ['<code>/srv</code>', 'данные, которые отдают сетевые службы'],
  ]),
  PROBE('Основные каталоги в системе', [
    ('ls -d /boot /etc /home /opt /srv /usr /var /mnt /media', 'вывести сами каталоги'),
    ('ls -ld /root', 'каталог /root'),
    ('ls -la /opt /srv', 'содержимое /opt и /srv'),
  ], 'все каталоги присутствуют; /opt и /srv пусты.'),
  CORE('05-fhs', 'Каталоги из стандарта присутствуют в Ubuntu. Пустые /opt и /srv означают, что в них ещё ничего не устанавливали.',
    LENS(0, 13, [(1, 0, 56, 1), (7, 40, 41, 2), (8, 40, 42, 2), (12, 40, 41, 2), (13, 40, 42, 2)],
      ['все каталоги на месте', 'в /opt и /srv нет файлов'], cols=62)),
  P('Краткое описание иерархии входит в справочную систему Ubuntu и открывается командой <code>man hier</code>.'),
  CORE('man-hier', 'Страница справки hier(7).'),
]),
dict(title='Каталог настроек /etc', blocks=[
  P('В <code>/etc</code> находятся <b>конфигурационные файлы</b> — текстовые файлы, в которых задаются параметры системы и служб. Программы читают их при запуске. Исполняемых файлов в <code>/etc</code> нет. Когда пакет обновляется, менеджер пакетов заменяет файлы программы, но не перезаписывает изменённые администратором настройки.'),
  P('Для примера рассмотрим три файла. <code>/etc/os-release</code> содержит название и версию дистрибутива, <code>/etc/hostname</code> — имя компьютера, <code>/etc/hosts</code> — таблицу соответствия имён и IP-адресов, которую система использует до обращения к DNS.'),
  PROBE('Чтение конфигурационных файлов', [
    ('cat /etc/os-release', ''),
    ('cat /etc/hostname', ''),
    ('cat /etc/hosts', ''),
  ], 'выводятся версия Ubuntu, имя компьютера и строка 127.0.0.1 localhost.'),
  CORE('06-config', 'Три конфигурационных файла из /etc.',
    LENS(0, 18, [(1, 0, 32, 1), (6, 0, 9, 2), (15, 0, 6, 3), (17, 0, 19, 4)],
      ['название и версия дистрибутива', 'идентификатор для скриптов', 'имя компьютера', 'адрес 127.0.0.1 обозначает сам компьютер'], cols=66)),
]),
dict(title='Учётные записи пользователей', blocks=[
  P('Сведения о пользователях также относятся к настройкам и хранятся в файле <code>/etc/passwd</code>. Каждая строка описывает одну учётную запись и состоит из семи полей, разделённых двоеточием: имя, отметка о пароле, числовой идентификатор пользователя (UID), идентификатор группы (GID), описание, домашний каталог и командная оболочка. Сами пароли хранятся отдельно, в защищённом файле <code>/etc/shadow</code>.'),
  PROBE('Запись текущего пользователя', [
    ('getent passwd "$(id -un)"', 'вывести строку своей учётной записи'),
    ('find /etc -maxdepth 1 -type f | head -8', 'первые файлы в /etc'),
    ('find /etc -mindepth 1 -maxdepth 1 -type d | head -8', 'первые подкаталоги /etc'),
  ], 'выводится строка с UID, домашним каталогом и оболочкой.'),
  CORE('07-accounts', 'Строка учётной записи из семи полей.',
    LENS(1, 1, [(1, 0, 6, 1), (1, 7, 8, 2), (1, 9, 13, 3), (1, 14, 18, 4), (1, 40, 52, 5), (1, 53, 62, 6)],
      ['имя', 'пароль хранится в /etc/shadow', 'UID', 'GID', 'домашний каталог', 'оболочка'], cols=64)),
]),
dict(title='Размещение файлов программы на примере CUPS', blocks=[
  P('Принцип FHS хорошо виден на службе печати CUPS. Она устанавливается одним пакетом <code>cups-daemon</code>, но его файлы распределены по разным каталогам. Список файлов пакета выводит команда <code>dpkg -L</code>.'),
  PROBE('Файлы пакета cups-daemon', [
    ('cd ~', ''),
    ('dpkg -S /usr/sbin/cupsd', 'пакет, которому принадлежит программа cupsd'),
    ("dpkg -L cups-daemon | grep --color=never -xE '/(etc/cups|usr/sbin/cupsd|var/(log|spool|cache)/cups|lib/systemd/system/cups.service)'", 'основные файлы пакета'),
    ('ls -ld /run/cups', 'каталог работающей службы'),
    ('systemctl is-active cups', 'состояние службы'),
  ], 'файлы находятся в /etc, /usr (через /lib) и /var.'),
  CORE('cups-spread', 'Файлы одного пакета распределены по назначению.',
    LENS(4, 10, [(5, 0, 9, 1), (6, 0, 32, 2), (7, 0, 15, 2), (8, 0, 15, 3), (9, 0, 13, 3), (10, 0, 15, 3)],
      ['настройки печати', 'программа и описание службы', 'кэш, журналы и очередь заданий печати'], cols=72)),
],
),
],
summary=[
  'Стандарт FHS определяет назначение основных каталогов Linux.',
  'Файлы распределяются по типу данных: настройки — <code>/etc</code>, программы — <code>/usr</code>, изменяемые данные — <code>/var</code>.',
  'Конфигурационные файлы — текстовые, программы читают их при запуске.',
  'Учётные записи описаны в <code>/etc/passwd</code>, пароли хранятся в <code>/etc/shadow</code>.',
],
report='Снимок с cat /etc/os-release и строкой своей учётной записи.',
)
