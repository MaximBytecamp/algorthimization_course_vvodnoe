from blocks import *

CHAPTER = dict(
slug='02-naznachenie', layer='Назначение', color='#E0913F',
title='Каталог выбирают по назначению',
question='По какому правилу файл попадает в тот или иной каталог?',
strata='access_log — журнал. Журнал меняется во время работы и относится к этому компьютеру, поэтому он лежит в /var.',
intro='<p>Мы умеем читать путь. Теперь вернёмся к вопросу с обложки: <mark>почему файлы одной программы лежат в разных каталогах?</mark></p>'
      '<p>В Linux файлы раскладывают не по программам, а по тому, что в них хранится. Программа идёт в один каталог, её настройки — в другой, журналы — в третий. Это правило записано в стандарте FHS.</p>',
sections=[
dict(title='Файловая система и стандарт FHS', blocks=[
  P('Два понятия этого слоя легко перепутать, поэтому разберём их по очереди.'),
  D('файловая система', 'ext4, btrfs, tmpfs, proc',
    'Способ хранения файлов: как записываются имена, сведения о файлах и их содержимое.',
    'ext4 хранит файлы на диске, tmpfs — в оперативной памяти, а proc вообще ничего не хранит: содержимое её файлов выдаёт ядро при чтении. Для программ все они выглядят одинаково — как каталоги и файлы.',
    'К одному дереву каталогов можно подключить несколько файловых систем сразу.',
    'Файловая система отвечает на вопрос «<b>как</b> хранить файлы». В какой каталог класть настройки, она не решает.'),
  D('FHS', 'Filesystem Hierarchy Standard',
    'Стандарт, который описывает, для чего нужен каждый основной каталог Linux.',
    'По FHS в <code>/etc</code> лежат настройки, в <code>/usr</code> — программы, в <code>/var</code> — данные, которые меняются во время работы. Текущая версия стандарта — FHS 3.0. Дистрибутивы следуют ему с небольшими отличиями.',
    'Зная стандарт, администратор найдёт журнал или настройки даже незнакомой программы.',
    'FHS отвечает на вопрос «<b>где</b> хранить файлы». Как именно они записаны на диск, стандарт не описывает.'),
  PROBE('Проверяем каталоги из стандарта', [
    ('ls -d /boot /etc /home /opt /srv /usr /var /mnt /media', '-d показывает сами каталоги, а не их содержимое'),
    ('ls -ld /root', ''),
    ('ls -la /opt /srv', 'что лежит внутри /opt и /srv'),
  ], 'все каталоги существуют. /opt и /srv пустые — это нормально.'),
  CORE('05-fhs', 'Все каталоги из стандарта есть. /opt и /srv пустые: в них пока ничего не устанавливали.',
    LENS(0, 13, [(1, 0, 56, 1), (7, 40, 41, 2), (8, 40, 42, 2), (12, 40, 41, 2), (13, 40, 42, 2)],
      ['все девять каталогов на месте', 'внутри /opt и /srv только . и .. — каталоги пустые'], cols=62)),
  P('Краткое описание каталогов есть в самой Ubuntu. Команда <code>man hier</code> открывает справку, выход — клавиша <kbd>q</kbd>.'),
  CORE('man-hier', 'Справка hier(7): описание каталогов системы прямо в терминале.'),
  ASK('/home — это каталог. Может ли в нём быть подключён отдельный диск?',
      'Да. К любому каталогу можно подключить файловую систему с другого диска. Путь останется <code>/home</code>, а файлы будут лежать на подключённом диске. Как это проверить, разберём в слое 7.'),
]),
dict(title='Два вопроса, по которым выбирают каталог', blocks=[
  P('Чтобы понять, куда положить файл, FHS задаёт два вопроса.'),
  P('Первый: <mark>меняется ли файл, пока программа работает?</mark> Сама программа после установки не меняется. Журнал, наоборот, пополняется каждую минуту.'),
  P('Второй: <mark>можно ли использовать этот файл на другом компьютере?</mark> Программа одной версии одинакова на всех компьютерах. А настройки сети подходят только к этому.'),
  D('статические и изменяемые данные', 'static и variable',
    'Статические данные не меняются сами: программы, библиотеки, справка. Изменяемые меняются во время работы: журналы, очереди, базы данных.',
    'Статические данные можно хранить в каталоге, куда запрещена запись. Изменяемым нужен каталог, куда программа может писать.',
    'Когда они разделены, программу можно обновить и не задеть данные, а резервную копию делать только для того, что меняется.',
    'Настройки в <code>/etc</code> считаются статическими, хотя администратор их правит: сама программа во время работы их не меняет.'),
  W('axes'),
  P('Эта таблица взята из стандарта FHS 3.0. Журналов и кэша в ней нет, но правило к ним применяется так же: они меняются во время работы и относятся к этому компьютеру. Поэтому они лежат в <code>/var</code>.'),
  ASK('В какую клетку таблицы попадёт файл /etc/hostname с именем компьютера?',
      'В клетку «не меняются, только для этого компьютера». Программы этот файл не меняют, а имя относится только к этому компьютеру.'),
]),
dict(title='Настройки: каталог /etc', blocks=[
  D('конфигурационный файл', 'хранится в /etc',
    'Текстовый файл, в котором записаны настройки системы или программы.',
    'Программа читает этот файл при запуске. В <code>/etc</code> лежат только настройки, самих программ там нет.',
    'При обновлении программы её файлы в <code>/usr</code> заменяются. Изменённый файл настроек менеджер пакетов не перезапишет, а спросит, что с ним делать.',
    '<code>/etc/os-release</code> описывает дистрибутив. Версию ядра показывает другая команда — <code>uname -r</code>.'),
  PROBE('Читаем три файла настроек', [
    ('cat /etc/os-release', 'какой дистрибутив и какая версия'),
    ('cat /etc/hostname', 'имя компьютера'),
    ('cat /etc/hosts', 'адреса, которые компьютер знает без интернета'),
  ], 'в os-release есть NAME, VERSION и ID, в hosts есть строка localhost.'),
  CORE('06-config', 'Три файла из /etc: сведения о системе, имя компьютера и список известных адресов.',
    LENS(0, 18, [(1, 0, 32, 1), (6, 0, 9, 2), (15, 0, 6, 3), (17, 0, 19, 4)],
      ['название системы для человека', 'ID — короткое название для скриптов', 'имя компьютера', 'localhost — адрес 127.0.0.1, то есть сам этот компьютер'], cols=66)),
  ASK('Можно ли по файлу /etc/os-release узнать, сколько свободного места на диске?',
      'Нет. В этом файле только сведения о системе. Свободное место показывает команда <code>df</code>, её разберём в слое 7.'),
]),
dict(title='Учётная запись пользователя', blocks=[
  P('Сведения о пользователях — тоже настройки, поэтому они хранятся в <code>/etc/passwd</code>. Команда <code>getent passwd</code> выводит эти сведения. Запись <code>$(id -un)</code> оболочка заменяет на ваше имя пользователя.'),
  PROBE('Своя учётная запись и содержимое /etc', [
    ('getent passwd "$(id -un)"', 'строка с вашей учётной записью'),
    ('find /etc -maxdepth 1 -type f | head -8', 'первые восемь файлов в /etc'),
    ('find /etc -mindepth 1 -maxdepth 1 -type d | head -8', 'первые восемь каталогов в /etc'),
  ], 'в строке учётной записи видны номер пользователя, домашний каталог и оболочка.'),
  CORE('07-accounts', 'Строка учётной записи состоит из семи полей, разделённых двоеточием.',
    LENS(1, 1, [(1, 0, 6, 1), (1, 7, 8, 2), (1, 9, 13, 3), (1, 14, 18, 4), (1, 40, 52, 5), (1, 53, 62, 6)],
      ['имя пользователя', '<code>x</code> значит, что пароль хранится в другом файле — /etc/shadow', 'UID — номер пользователя', 'GID — номер его основной группы', 'домашний каталог', 'оболочка, которая запускается при входе'], cols=64)),
  P('Поле <code>Live session user,,,</code> — описание для людей. Система его не использует.'),
  ASK('Попадёт ли символическая ссылка в результат команды find /etc -type f?',
      'Нет. Ключ <code>-type f</code> выбирает только обычные файлы, а у ссылки свой тип — <code>l</code>.'),
]),
dict(title='Раскладываем файлы CUPS', blocks=[
  P('Теперь можно ответить на вопрос с обложки. Менеджер пакетов <code>dpkg</code> помнит, какие файлы установил каждый пакет. <code>dpkg -S</code> показывает, какому пакету принадлежит файл, <code>dpkg -L</code> — список файлов пакета. Из длинного списка мы отобрали шесть главных строк.'),
  PROBE('Где лежит служба печати', [
    ('dpkg -S /usr/sbin/cupsd', 'из какого пакета программа cupsd'),
    ("dpkg -L cups-daemon | grep --color=never -xE '/(etc/cups|usr/sbin/cupsd|var/(log|spool|cache)/cups|lib/systemd/system/cups.service)'", 'шесть главных строк из списка файлов пакета'),
    ('ls -ld /run/cups', 'этот каталог создаёт сама служба при запуске'),
    ('systemctl is-active cups', 'проверить, что служба работает'),
  ], 'файлы одного пакета cups-daemon лежат в /etc, /usr, /var и /run.'),
  CORE('cups-spread', 'Один пакет — несколько каталогов. Каждый файл лежит там, где хранят данные этого вида.',
    LENS(1, 14, [(2, 0, 11, 1), (5, 0, 9, 2), (6, 0, 32, 3), (7, 0, 15, 3), (8, 0, 15, 4), (9, 0, 13, 4), (10, 0, 15, 4), (12, 38, 47, 5), (14, 0, 6, 6)],
      ['программа cupsd из пакета cups-daemon', 'настройки печати — в /etc', 'программа и описание службы — в /usr (путь /lib ведёт туда же)', 'кэш, журналы и очередь печати — в /var', 'каталог в /run служба создаёт при запуске', 'служба работает'], cols=72)),
  W('spread'),
  ASK('Почему файл cups.service лежит в /lib/systemd/system, а не в /etc/systemd/system?',
      'Этот файл пришёл вместе с пакетом, то есть он часть программы. Такие файлы лежат в <code>/lib</code>, а в Ubuntu это на самом деле <code>/usr/lib</code>. Каталог <code>/etc/systemd/system</code> оставлен администратору для его собственных настроек.'),
]),
],
outro='Каждый файл CUPS лежит там, где хранят данные его вида: <b>программа</b> — в /usr, <b>настройки</b> — в /etc, <b>то, что меняется во время работы</b>, — в /var. Дальше разберём, как система находит программу по имени.',
report='Разбор строки учётной записи и таблица: в каком каталоге лежат программа, настройки и журнал.',
)
