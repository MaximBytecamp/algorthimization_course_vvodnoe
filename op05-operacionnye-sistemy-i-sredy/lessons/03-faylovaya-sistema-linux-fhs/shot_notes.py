# Выводы после снимков: что показывает снимок и где это пригодится.
# Ключ — «номер части:имя снимка», для повторного снимка в той же части — «:2».

NOTES = {
'1:02-paths': ('Приглашение оболочки состоит из четырёх частей: имя пользователя, имя компьютера, текущий каталог и знак <code>$</code>.',
               'По приглашению всегда видно, под каким пользователем и в каком каталоге вы работаете. Прежде чем удалять или перемещать файлы, стоит убедиться, что вы находитесь в нужном каталоге и работаете не от имени root.'),
'1:01-root': ('Команда <code>pwd</code> подтверждает, что текущий каталог — корень, а <code>ls -lah</code> выводит каталоги верхнего уровня, в том числе ссылки bin, lib и sbin.',
              'С этого списка начинается знакомство с любым незнакомым Linux-компьютером или сервером: по нему видно, какие основные каталоги есть в системе.'),
'1:files-root': ('Графический файловый менеджер показывает то же самое дерево каталогов, что и команда <code>ls</code>.',
                 'Терминал и файловый менеджер работают с одними и теми же файлами: всё, что сделано в терминале, видно в менеджере, и наоборот. На серверах графического интерфейса обычно нет, поэтому с файлами там работают только в терминале.'),
'1:02-paths:2': ('Цепочка переходов <code>cd ..</code>, <code>cd usr</code>, <code>cd bin</code> привела в тот же каталог <code>/usr/bin</code>, что и абсолютный путь.',
                 'Относительные пути сокращают ввод, когда вы работаете внутри одного проекта. Абсолютные пути нужны в скриптах и настройках, которые могут запускаться из любого каталога.'),
'1:03-home': ('Домашний каталог пользователя — <code>/home/ubuntu</code>; скрытые файлы с точкой видны только с ключом <code>-a</code>; домашний каталог root — <code>/root</code>.',
              'Личные настройки программ, например <code>.bashrc</code> или каталог <code>.config</code>, хранятся в скрытых файлах домашнего каталога. Если программа ведёт себя не так, как ожидается, её настройки ищут именно там.'),
'1:04-lab': ('Команда <code>mkdir</code> создала каталоги, <code>printf</code> — два файла, а <code>find</code> подтвердила, что файлы на месте.',
             'Так быстро создают структуру проекта. Команда <code>find</code> пригодится, когда нужно найти файл по имени или типу в большом дереве каталогов.'),

'2:05-fhs': ('Все каталоги из стандарта FHS есть в системе; <code>/opt</code> и <code>/srv</code> пока пусты.',
             'Стандарт один для большинства дистрибутивов Linux. Зная его, в Ubuntu, Debian или Fedora вы будете искать настройки, программы и журналы в одних и тех же каталогах.'),
'2:man-hier': ('Встроенная справка описывает назначение каталогов прямо в системе, без доступа к интернету.',
               'Команда <code>man</code> работает и для команд: <code>man ls</code>, <code>man find</code>. Это первый источник, когда нужно вспомнить ключ команды или назначение файла.'),
'2:06-config': ('Файлы в <code>/etc</code> — обычные текстовые файлы: сведения о версии системы, имя компьютера и таблица адресов.',
                'Версию системы проверяют перед установкой программ и указывают в сообщениях об ошибках. Файл <code>/etc/hosts</code> правят, чтобы сервер в локальной сети открывался по имени.'),
'2:07-accounts': ('Строка из базы учётных записей: семь полей пользователя ubuntu, разделённых двоеточием.',
                  'По этой строке администратор узнаёт домашний каталог и оболочку пользователя. Номер UID важен при настройке прав доступа: владельца файла система определяет именно по номеру.'),
'2:07-accounts:2': ('В <code>/etc</code> лежат и отдельные файлы настроек, и подкаталоги крупных программ.',
                    'Когда нужно изменить настройки программы, сначала ищут файл или подкаталог с её названием в <code>/etc</code>.'),
'2:cups-spread': ('Файлы одного пакета cups-daemon разложены по каталогам <code>/etc</code>, <code>/usr</code> и <code>/var</code>, а сама служба работает.',
                  'Команды <code>dpkg -S</code> и <code>dpkg -L</code> помогают найти, где лежат настройки или журналы установленной программы, и выяснить, из какого пакета появился незнакомый файл.'),

'3:08-programs': ('Оболочка показывает, чем является каждое имя: <code>python3</code> — файл в <code>/usr/bin</code>, <code>cd</code> — встроенная команда, <code>ls</code> — псевдоним. Ниже выведен список каталогов PATH.',
                  'Если при вводе команды появляется сообщение «command not found», проверяют, установлена ли программа и есть ли её каталог в PATH. <code>command -v</code> показывает, какая именно программа запустится, если установлено несколько версий.'),
'3:09-usrmerge': ('<code>/bin</code>, <code>/lib</code> и <code>/sbin</code> — символические ссылки на каталоги внутри <code>/usr</code>.',
                  'Поэтому в инструкциях встречаются и <code>/bin/bash</code>, и <code>/usr/bin/bash</code>: оба пути ведут к одному файлу, и оба будут работать.'),

'4:10-var': ('Подкаталоги <code>/var</code>: кэш, постоянные данные, журналы и очереди; ниже — последние записи системного журнала.',
             'Когда служба работает с ошибками, первым делом смотрят её журнал в <code>/var/log</code> или через <code>journalctl</code>. Когда на диске заканчивается место, проверяют <code>/var/log</code> и <code>/var/cache</code>.'),
'4:11-temporary': ('Команда <code>findmnt</code> показывает, что <code>/run</code> хранится в оперативной памяти — на файловой системе tmpfs.',
                   'Важные файлы нельзя оставлять в <code>/tmp</code> и <code>/run</code>: после перезагрузки их может не оказаться.'),
'4:cups-lifetime': ('У одной службы настройки лежат в <code>/etc</code>, журнал — в <code>/var/log</code>, а файл для связи со службой — в <code>/run</code>, в оперативной памяти.',
                    'Зная срок хранения данных, администратор решает, что включать в резервную копию (<code>/etc</code>, <code>/var/lib</code>), а что сохранять не нужно (<code>/run</code>, кэш).'),

'5:12-dev': ('<code>/dev/null</code> — символьное устройство; запись в него прошла успешно с кодом 0; <code>lsblk</code> показывает диски и разделы.',
             'Код завершения проверяют в скриптах, чтобы понять, выполнилась ли команда. <code>lsblk</code> используют, чтобы найти подключённый диск или флешку перед монтированием.'),
'5:13-proc': ('Ядро выдаёт сведения о памяти и о процессе оболочки: его номер, текущий каталог и исполняемый файл.',
              'Через <code>/proc</code> узнают, сколько памяти свободно и что делает конкретный процесс. На этих файлах основаны системные мониторы и диспетчеры задач.'),
'5:proc-uptime': ('Размер <code>/proc/uptime</code> равен нулю, но содержимое меняется при каждом чтении; копия на диске не меняется.',
                  'Сведения из <code>/proc</code> нельзя сохранить копированием на будущее. Чтобы следить за изменениями, файл читают заново, например в скрипте мониторинга.'),
'5:14-sys': ('В <code>/sys/class/net</code> перечислены сетевые интерфейсы, а файл <code>operstate</code> содержит состояние интерфейса.',
             'Через <code>/sys</code> проверяют, какие сетевые карты видит система и включены ли они. Это помогает при настройке и диагностике сети.'),

'6:15-types': ('Команда <code>file</code> определяет содержимое файлов, а первая буква в выводе <code>ls -ld</code> — тип объекта: l, c, d и -.',
               'Если файл не открывается, <code>file</code> подскажет, что в нём на самом деле: текст, архив или программа, — независимо от расширения.'),
'6:17-inode': ('<code>stat</code> показывает сведения из inode, а после <code>ln</code> у двух имён одинаковый номер inode и счётчик ссылок равен 2.',
               'Жёсткие ссылки позволяют сделать один файл доступным под двумя именами без копирования. Этим пользуются программы резервного копирования: неизменённые файлы в новой копии не дублируются, а связываются жёсткими ссылками.'),
'6:16-links': ('Ссылка <code>info-link.txt</code> хранит путь <code>data/info.txt</code>, а <code>cat</code> читает содержимое целевого файла.',
               'Символическими ссылками переключают версии программ и настроек: ссылка указывает на текущую версию, а при обновлении её перенаправляют на новую.'),
'6:link-broken': ('После удаления имени данные остались доступны через жёсткую ссылку, а символическая ссылка перестала работать.',
                  'Так появляются «битые» ссылки после удаления или переноса файлов. Команда <code>ls</code> выделяет такие ссылки красным цветом.'),

'7:18-mounts': ('Домашний каталог находится на корневой файловой системе; диск sda с разделом ext4 в Live-сессии не подключён.',
                '<code>findmnt</code> и <code>lsblk</code> используют, чтобы узнать, на каком диске лежат данные, и перед подключением нового диска.'),
'7:19-space': ('Файл содержит 17 байт, но на диске под него выделен блок 4 КБ; свободное место и занятые inode считаются для всей файловой системы.',
               'При нехватке места <code>df</code> показывает, какая файловая система заполнена, а <code>du</code> — какой каталог занимает больше всего.'),
'7:20-mount-before': ('После <code>mount</code> каталог стал точкой монтирования tmpfs и выглядит пустым.',
                      'Так подключают флешки, дополнительные диски и сетевые хранилища. Файлы, которые лежали в каталоге до монтирования, становятся недоступны, пока файловая система подключена.'),
'7:21-mount-after': ('После <code>umount</code> снова виден исходный файл, а файл, записанный в tmpfs, исчез.',
                     'Перед извлечением флешки её отключают, чтобы все данные успели записаться. Данные в tmpfs после отключения не сохраняются.'),

'8:22-placement': ('Структура каталогов сервиса повторяет системные каталоги: настройки, данные и журнал разложены по назначению.',
                   'Так размещают файлы при установке собственной службы на сервер: администратор и другие программы найдут их в стандартных местах.'),
'8:23-report': ('Вывод команд записан в файл отчёта: <code>&gt;</code> создал файл, <code>&gt;&gt;</code> дописал строки в конец.',
                'Перенаправление вывода в файл используют для отчётов, журналов работы скриптов и сохранения результатов команд.'),
}
