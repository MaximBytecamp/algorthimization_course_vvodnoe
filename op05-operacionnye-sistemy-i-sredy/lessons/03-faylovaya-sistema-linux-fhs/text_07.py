from blocks import *

CHAPTER = dict(
slug='07-montirovanie', layer='Монтирование', color='#4A2447',
title='Файловые системы и монтирование',
lead='В этой части рассмотрим, как файловые системы разных устройств объединяются в одно дерево каталогов, как определить файловую систему для конкретного пути и как измерить занятое и свободное место.',
sections=[
dict(title='Файловая система и монтирование', blocks=[
  P('<b>Файловая система</b> определяет способ хранения файлов: как записываются имена, сведения о файлах и данные. На дисках в Linux обычно используется ext4, в оперативной памяти — tmpfs, а proc и sysfs, как мы видели в части 5, данные вообще не хранят.'),
  P('Чтобы файловая система стала доступна, её <b>монтируют</b> — подключают к каталогу общего дерева. Такой каталог называется <b>точкой монтирования</b>. Корневая файловая система подключена к <code>/</code>, proc — к <code>/proc</code>, флешка может быть подключена к <code>/media/ubuntu/имя</code>. Для программ путь выглядит одинаково, на каком бы устройстве ни находился файл.'),
  TERM('точка монтирования', 'Каталог, к которому подключена файловая система. После подключения в этом каталоге видно содержимое подключённой файловой системы.'),
  P('<b>Пример.</b> Когда вы вставляете флешку, Ubuntu монтирует её, например, в каталог <code>/media/ubuntu/FLASH</code>. Файлы флешки появляются в этом каталоге, и с ними можно работать обычными командами. После безопасного извлечения флешка отключается, и каталог исчезает.'),
  P('Сведения о подключённых файловых системах выводит команда <code>findmnt</code>. Столбец <code>TARGET</code> — точка монтирования, <code>SOURCE</code> — источник (раздел диска, память, ядро), <code>FSTYPE</code> — тип файловой системы. С ключом <code>-T</code> команда показывает, какая файловая система содержит указанный путь. Список дисков и разделов выводит <code>lsblk</code>.'),
  PROBE('Подключённые файловые системы', [
    ('findmnt -o TARGET,SOURCE,FSTYPE | head -10', 'первые строки списка'),
    ('findmnt / -o TARGET,SOURCE,FSTYPE | cat', 'корневая файловая система'),
    ('findmnt -T "$HOME" -o TARGET,SOURCE,FSTYPE | cat', 'файловая система домашнего каталога'),
    ('lsblk -e 7 -o NAME,TYPE,SIZE,FSTYPE,MOUNTPOINTS', 'диски и разделы'),
  ], 'для домашнего каталога TARGET равен /: отдельно /home не подключён.'),
  CORE('18-mounts', 'Домашний каталог находится на корневой файловой системе.',
    LENS(12, 20, [(14, 0, 1, 1), (14, 7, 11, 2), (14, 14, 21, 3), (19, 0, 22, 4)],
      ['точка монтирования — корень', 'источник — память Live-сессии', 'тип файловой системы Live-сессии', 'раздел диска с ext4, в Live-сессии не подключён'], cols=52)),
  NOTE('В установленной Ubuntu для корня будет указан раздел диска, например <code>/dev/sda2</code>, и тип <code>ext4</code>.'),
]),
dict(title='Занятое и свободное место', blocks=[
  P('Для измерения места используют три команды, и каждая отвечает на свой вопрос. <code>ls -lh</code> показывает размер содержимого отдельного файла. <code>du -sh</code> подсчитывает, сколько места на диске занимают файлы в каталоге. <code>df -h</code> показывает общий объём и свободное место на файловой системе. Команда <code>df -i</code> выводит число занятых и свободных inode: если они закончатся, новые файлы создать не получится даже при наличии свободного места.'),
  P('<b>Пример.</b> Если диск заполнен, <code>df -h</code> покажет, что свободного места почти не осталось, а <code>du -sh ~/Downloads</code> поможет выяснить, сколько занимает каталог загрузок. Команда <code>ls -lh</code> здесь не поможет: для каталога она показывает размер самой записи каталога, а не его содержимого.'),
  PROBE('Размер файла, каталога и файловой системы', [
    ('cd ~/linux-fhs-lab', ''),
    ('ls -lh data/info.txt', 'размер файла'),
    ('du -sh data', 'место, занятое каталогом'),
    ('df -h .', 'место на файловой системе'),
    ('df -i .', 'использование inode'),
  ], 'размер файла 17 байт, du показывает 4.0K — минимальный блок на диске.'),
  CORE('19-space', 'Размер файла, занятое каталогом место и свободное место файловой системы.',
    LENS(1, 10, [(2, 27, 29, 1), (4, 0, 4, 2), (7, 28, 32, 3), (10, 38, 40, 4)],
      ['размер содержимого — 17 байт', 'занято на диске — 4 КБ', 'свободное место', 'занято inode'], cols=50)),
]),
dict(title='Подключение файловой системы к каталогу', blocks=[
  P('Подключим к каталогу <code>mount-demo</code> файловую систему tmpfs размером 16 МБ. До подключения в каталоге создадим файл <code>before.txt</code>. После монтирования в каталоге будет видно содержимое tmpfs, а <code>before.txt</code> станет недоступен, хотя и не будет удалён. Монтирование требует прав администратора, поэтому команда выполняется через <code>sudo</code>.'),
  P('То, что произойдёт с <code>before.txt</code>, похоже на лист бумаги, положенный поверх другого листа. Нижний лист никуда не делся, но, пока сверху лежит новый, его не видно. Когда верхний лист убирают командой <code>umount</code>, нижний снова виден.'),
  W('mount'),
  PROBE('Монтирование tmpfs', [
    ('cd ~/linux-fhs-lab', ''),
    ('mkdir -p mount-demo', ''),
    ('printf "on original filesystem\\n" > mount-demo/before.txt', ''),
    ('ls mount-demo', 'содержимое до подключения'),
    ('sudo mount -t tmpfs -o size=16M tmpfs "$HOME/linux-fhs-lab/mount-demo"', 'подключить tmpfs'),
    ('findmnt -T "$HOME/linux-fhs-lab/mount-demo" -o TARGET,SOURCE,FSTYPE', ''),
    ('ls -la mount-demo', 'содержимое после подключения'),
  ], 'после монтирования каталог пуст, findmnt показывает tmpfs.'),
  CORE('20-mount-before', 'До монтирования в каталоге был before.txt, после — пустая tmpfs.',
    LENS(3, 13, [(4, 0, 10, 1), (9, 38, 49, 2), (11, 0, 7, 3)],
      ['файл до подключения', 'подключена tmpfs', 'каталог пуст'], cols=80)),
  P('Запишем в tmpfs файл <code>inside.txt</code> и отключим её командой <code>umount</code>. После отключения в каталоге снова виден <code>before.txt</code>, а <code>inside.txt</code> исчезает вместе с tmpfs. Для записи используется <code>sudo tee</code>, так как у обычного пользователя нет прав на запись в подключённую tmpfs.'),
  PROBE('Отключение tmpfs', [
    ('cd ~/linux-fhs-lab', ''),
    ('printf "inside tmpfs\\n" | sudo tee mount-demo/inside.txt', 'записать файл в tmpfs'),
    ('ls mount-demo', ''),
    ('sudo umount "$HOME/linux-fhs-lab/mount-demo"', 'отключить tmpfs'),
    ('cat mount-demo/before.txt', ''),
    ('findmnt -T "$HOME/linux-fhs-lab/mount-demo" -o TARGET,SOURCE,FSTYPE', ''),
  ], 'before.txt снова доступен, каталог принадлежит корневой файловой системе.'),
  CORE('21-mount-after', 'После отключения tmpfs исходный файл снова виден.',
    LENS(1, 12, [(4, 0, 10, 1), (7, 0, 22, 2), (12, 0, 14, 3)],
      ['файл в tmpfs', 'исходный файл', 'каталог снова на корневой файловой системе'], cols=80)),
  NOTE('Если прав на <code>sudo</code> нет, опишите в отчёте полученную ошибку и разберите опыт по снимкам.'),
]),
],
summary=[
  'Файловая система определяет способ хранения файлов; основные типы — ext4, tmpfs, proc, sysfs.',
  'Монтирование подключает файловую систему к каталогу — точке монтирования.',
  '<code>findmnt</code> показывает подключённые файловые системы, <code>lsblk</code> — диски и разделы.',
  '<code>ls -lh</code> — размер файла, <code>du -sh</code> — место каталога, <code>df -h</code> — место файловой системы.',
],
report='Снимок findmnt для корня и два снимка опыта с tmpfs.',
)
