from blocks import *

CHAPTER = dict(
slug='05-yadro', layer='Ядро', color='#8B3A4A',
title='Файлы, которых нет на диске',
question='Всегда ли за путём стоит файл, записанный на диск?',
strata='access_log — обычный файл, его содержимое записано на диск. У файла /proc/uptime на диске нет ни байта.',
intro='<p>До сих пор за каждым путём стоял файл с содержимым на диске. Но в Linux есть каталоги, где <mark>содержимое файлов выдаёт ядро в момент чтения</mark>.</p>'
      '<p>Зачем так сделано? Чтобы узнать состояние системы или обратиться к устройству, программе хватает обычных команд для работы с файлами: <code>cat</code>, <code>ls</code>, записи в файл.</p>',
sections=[
dict(title='/dev: устройства', blocks=[
  D('файл устройства', 'c — символьное, b — блочное',
    'Специальный файл в <code>/dev</code>, через который программа обращается к устройству.',
    'Когда программа читает или пишет такой файл, ядро передаёт данные драйверу устройства. Символьные устройства принимают и отдают данные потоком, блочные — блоками, как диски.',
    'Для работы с устройством не нужна отдельная программа: достаточно открыть его файл.',
    '<code>/dev/sda</code> — это весь диск целиком. Запись в него уничтожит данные, поэтому в уроке мы пишем только в <code>/dev/null</code>.'),
  P('<code>/dev/null</code> принимает любые данные и сразу их выбрасывает. Поэтому после записи в него на экране ничего не появляется. Узнать, успешно ли выполнилась команда, можно по <b>коду завершения</b>. Его хранит переменная <code>$?</code>: 0 означает успех, другое число — ошибку.'),
  PROBE('Пишем в /dev/null и проверяем результат', [
    ('ls -l /dev/null /dev/zero', 'первая буква c — символьное устройство'),
    ('file /dev/null', 'что это за файл'),
    ('printf "test\\n" > /dev/null', 'отправить текст в /dev/null'),
    ('printf "exit status: %s\\n" "$?"', 'код завершения предыдущей команды'),
    ('lsblk -o NAME,TYPE,SIZE,MOUNTPOINTS', 'диски и разделы'),
  ], 'file выводит character special, код завершения — 0.'),
  CORE('12-dev', 'Файлы устройств /dev/null и /dev/zero и список дисков виртуальной машины.',
    LENS(0, 7, [(1, 0, 1, 1), (1, 23, 27, 2), (4, 11, 34, 3), (7, 13, 14, 4)],
      ['<code>c</code> — символьное устройство', 'у файла устройства вместо размера два номера, по ним ядро находит драйвер', 'file подтверждает: это устройство', 'код 0 — запись прошла успешно'], cols=64)),
  ASK('После записи в /dev/null на экране пусто. Значит, команда не сработала?',
      'Нет. Данные и должны были исчезнуть. Успех проверяют кодом завершения <code>$?</code> сразу после команды.'),
]),
dict(title='/proc: сведения о системе от ядра', blocks=[
  D('псевдофайловая система', 'proc и sysfs',
    'Файловая система, которая ничего не хранит. Её файлы и их содержимое создаёт ядро.',
    'Когда программа читает <code>/proc/meminfo</code>, ядро в этот момент собирает сведения о памяти и выдаёт их как текст. Поэтому команда ls показывает у таких файлов размер 0, а при каждом чтении содержимое новое.',
    'Состояние системы можно узнать обычной командой <code>cat</code>.',
    'Если скопировать файл из /proc, копия будет обычным файлом с данными на момент копирования. Обновляться она не будет.'),
  PROBE('Память и текущая оболочка', [
    ('head -8 /proc/meminfo', 'первые восемь строк сведений о памяти'),
    ('echo $$', 'номер процесса нашей оболочки'),
    ('readlink /proc/$$/cwd', 'в каком каталоге сейчас оболочка'),
    ('readlink /proc/$$/exe', 'какая программа запущена'),
    ('findmnt -T /proc/meminfo -o TARGET,SOURCE,FSTYPE', 'тип файловой системы'),
  ], 'тип файловой системы — proc, каталог оболочки совпадает с тем, что показывает pwd.'),
  CORE('13-proc', 'Ядро выдаёт сведения о памяти и о текущей оболочке в виде текстовых файлов.',
    LENS(0, 19, [(2, 0, 25, 1), (3, 0, 25, 2), (10, 0, 4, 3), (12, 0, 26, 4), (19, 11, 15, 5)],
      ['MemFree — память, которая сейчас ничем не занята', 'MemAvailable — сколько памяти можно отдать новым программам', 'номер процесса нашей оболочки', 'каталог, в котором находится оболочка', 'тип proc: эти файлы выдаёт ядро'], cols=62)),
  P('Лучше всего это видно на файле <code>/proc/uptime</code>. Первое число в нём — сколько секунд прошло с загрузки системы. Прочитаем файл, сделаем копию, подождём пять секунд и сравним.'),
  W('kernelfs'),
  PROBE('Файл из /proc и его копия', [
    ('rm -f saved-uptime.txt', 'удалить копию, если она осталась с прошлого раза'),
    ('ls -l /proc/uptime', 'размер файла'),
    ('cat /proc/uptime', ''),
    ('cp /proc/uptime saved-uptime.txt', 'сделать копию'),
    ('sleep 5', 'подождать пять секунд'),
    ('cat /proc/uptime', 'прочитать ещё раз'),
    ('cat saved-uptime.txt', 'прочитать копию'),
    ('findmnt -n -o TARGET,FSTYPE -T saved-uptime.txt | cat', 'где лежит копия'),
  ], 'число в /proc/uptime выросло, а в копии осталось прежним.'),
  CORE('proc-uptime', 'У /proc/uptime размер 0, но при каждом чтении в нём новое число. Копия не меняется.',
    LENS(2, 17, [(3, 23, 24, 1), (5, 0, 7, 2), (9, 0, 7, 3), (11, 0, 7, 4), (13, 27, 29, 5), (15, 7, 11, 6), (17, 7, 14, 6)],
      ['размер /proc/uptime — 0 байт', 'первое чтение: 1791 секунда с загрузки', 'чтение через пять секунд: уже 1808', 'копию сделали между чтениями, число в ней больше не меняется', 'у копии 16 байт: это обычный файл', 'оригинал на файловой системе proc, копия — на обычной'], cols=60)),
  ASK('Почему копия /proc/meminfo в домашнем каталоге не обновляется?',
      'При копировании сохранился текст на тот момент. Копия — обычный файл на диске, ядро её не меняет.'),
]),
dict(title='/sys: устройства и их параметры', blocks=[
  P('<code>/sys</code> — ещё одна файловая система, которую создаёт ядро. Через <code>/proc</code> мы читали сведения о памяти и процессах. В <code>/sys</code> лежат <mark>сведения об устройствах</mark>: каждый файл содержит одно значение. Например, в <code>/sys/class/net</code> перечислены сетевые подключения компьютера.'),
  PROBE('Сетевые подключения', [
    ('ls /sys', 'разделы /sys'),
    ('ls /sys/class/net', 'сетевые подключения'),
    ('cat /sys/class/net/lo/operstate', 'состояние подключения lo'),
    ('findmnt -T /sys -o TARGET,SOURCE,FSTYPE', ''),
  ], 'есть подключение lo, тип файловой системы — sysfs.'),
  CORE('14-sys', 'Сетевые подключения виртуальной машины в /sys.',
    LENS(0, 10, [(3, 0, 6, 1), (3, 8, 10, 2), (5, 0, 7, 3), (10, 0, 16, 4)],
      ['сетевая карта виртуальной машины, у вас имя может быть другим', 'lo — подключение компьютера к самому себе', 'для lo значение unknown — нормально', 'тип sysfs: эти файлы выдаёт ядро'], cols=44)),
  NOTE('Запись в некоторые файлы /sys меняет настройки ядра. В уроке мы их только читаем.'),
  ASK('Чем /proc отличается от /sys в наших опытах?',
      'Из /proc мы читали сведения о памяти и процессах, из /sys — сведения об устройствах. Оба каталога создаёт ядро.'),
]),
],
outro='За путём не всегда стоит файл на диске. За <b>/home</b> — данные на диске, а за <b>/proc</b>, <b>/sys</b> и <b>/dev</b> — ядро, которое отвечает в момент обращения.',
report='Кадр с /proc/uptime и копией, объяснение, почему размер 0, и код завершения после записи в /dev/null.',
)
