from blocks import *

CHAPTER = dict(
slug='05-yadro', layer='Устройства и ядро', color='#8B3A4A',
title='Устройства и сведения ядра: /dev, /proc, /sys',
lead='В этой части рассмотрим каталоги, содержимое которых не хранится на диске: файлы устройств в /dev и сведения о системе, которые ядро предоставляет через /proc и /sys.',
sections=[
dict(title='Файлы устройств: /dev', blocks=[
  P('В Linux к устройствам обращаются так же, как к файлам. Для каждого устройства в каталоге <code>/dev</code> есть специальный файл. Когда программа читает или записывает такой файл, ядро передаёт данные драйверу соответствующего устройства. Например, <code>/dev/sda</code> — первый диск целиком, <code>/dev/sda1</code> — первый раздел на нём.'),
  P('Устройства делятся на символьные и блочные. Символьные устройства передают данные потоком байтов, блочные — блоками фиксированного размера, как диски. В выводе <code>ls -l</code> первая буква строки обозначает тип: <code>c</code> — символьное, <code>b</code> — блочное.'),
  P('Особое устройство <code>/dev/null</code> принимает любые данные и уничтожает их. В него перенаправляют вывод, который не нужен. Поскольку на экране при этом ничего не появляется, успешность команды проверяют по <b>коду завершения</b>: оболочка хранит его в переменной <code>$?</code>, значение 0 означает успех.'),
  PROBE('Устройство /dev/null и список дисков', [
    ('ls -l /dev/null /dev/zero', 'сведения об устройствах'),
    ('file /dev/null', 'тип объекта'),
    ('printf "test\\n" > /dev/null', 'записать данные в /dev/null'),
    ('printf "exit status: %s\\n" "$?"', 'код завершения предыдущей команды'),
    ('lsblk -o NAME,TYPE,SIZE,MOUNTPOINTS', 'блочные устройства: диски и разделы'),
  ], 'строка /dev/null начинается с буквы c, код завершения — 0.'),
  CORE('12-dev', 'Устройство /dev/null и список блочных устройств виртуальной машины.',
    LENS(0, 7, [(1, 0, 1, 1), (4, 11, 34, 2), (7, 13, 14, 3)],
      ['символьное устройство', 'тип объекта по данным команды file', 'код завершения 0 — запись выполнена'], cols=64)),
  NOTE('Запись в файлы дисков, например <code>/dev/sda</code>, уничтожает данные на диске. В этом уроке запись выполняется только в <code>/dev/null</code>.'),
]),
dict(title='Сведения о процессах и системе: /proc', blocks=[
  P('Каталог <code>/proc</code> не находится на диске. На нём смонтирована <b>псевдофайловая система</b> proc: её файлы создаёт ядро, а содержимое формируется в момент чтения. Через эти файлы можно узнать состояние системы: объём памяти, время работы, сведения о каждом запущенном процессе. Для каждого процесса в <code>/proc</code> есть каталог, названный его номером (PID).'),
  TERM('псевдофайловая система', 'Файловая система, которая не хранит данные на диске. Её файлы и их содержимое формирует ядро при обращении. Примеры: proc и sysfs.'),
  PROBE('Сведения о памяти и текущем процессе', [
    ('head -8 /proc/meminfo', 'сведения об оперативной памяти'),
    ('echo $$', 'номер процесса текущей оболочки'),
    ('readlink /proc/$$/cwd', 'текущий каталог этого процесса'),
    ('readlink /proc/$$/exe', 'исполняемый файл процесса'),
    ('findmnt -T /proc/meminfo -o TARGET,SOURCE,FSTYPE', 'тип файловой системы'),
  ], 'тип файловой системы — proc.'),
  CORE('13-proc', 'Ядро предоставляет сведения о памяти и о процессе оболочки.',
    LENS(0, 19, [(1, 0, 25, 1), (3, 0, 25, 2), (10, 0, 4, 3), (12, 0, 26, 4), (19, 11, 15, 5)],
      ['общий объём памяти', 'память, доступная для новых программ', 'номер процесса оболочки', 'текущий каталог процесса', 'файловая система proc'], cols=62)),
  P('Отличие файлов <code>/proc</code> от обычных видно на <code>/proc/uptime</code>, где первое число — время работы системы в секундах. Размер файла равен нулю, но при каждом чтении содержимое новое. Если скопировать файл, копия окажется обычным файлом на диске, и её содержимое меняться не будет.'),
  PROBE('Файл /proc/uptime и его копия', [
    ('cd ~/linux-fhs-lab', ''),
    ('rm -f saved-uptime.txt', 'удалить копию от прошлого запуска'),
    ('ls -l /proc/uptime', 'размер файла'),
    ('cat /proc/uptime', ''),
    ('cp /proc/uptime saved-uptime.txt', 'сделать копию'),
    ('sleep 5', 'подождать 5 секунд'),
    ('cat /proc/uptime', ''),
    ('cat saved-uptime.txt', 'содержимое копии'),
    ('ls -l saved-uptime.txt', 'размер копии'),
    ('findmnt -n -o TARGET,FSTYPE -T /proc/uptime | cat', 'файловая система оригинала'),
    ('findmnt -n -o TARGET,FSTYPE -T saved-uptime.txt | cat', 'файловая система копии'),
  ], 'значение в /proc/uptime увеличилось, в копии осталось прежним.'),
  CORE('proc-uptime', 'Содержимое /proc/uptime меняется при каждом чтении, содержимое копии — нет.',
    LENS(2, 17, [(3, 23, 24, 1), (5, 0, 7, 2), (9, 0, 7, 2), (11, 0, 7, 3), (15, 7, 11, 4), (17, 7, 14, 4)],
      ['размер 0 байт', 'два чтения с интервалом в пять секунд', 'копия не изменилась', 'оригинал на proc, копия на обычной файловой системе'], cols=60)),
]),
dict(title='Сведения об устройствах: /sys', blocks=[
  P('Каталог <code>/sys</code> — ещё одна псевдофайловая система, sysfs. Если <code>/proc</code> в основном описывает процессы, то <code>/sys</code> описывает устройства и драйверы. Каждый файл в нём содержит одно значение — один параметр устройства. Например, в <code>/sys/class/net</code> перечислены сетевые интерфейсы компьютера.'),
  PROBE('Сетевые интерфейсы', [
    ('ls /sys', 'разделы /sys'),
    ('ls /sys/class/net', 'список сетевых интерфейсов'),
    ('cat /sys/class/net/lo/operstate', 'состояние интерфейса lo'),
    ('findmnt -T /sys -o TARGET,SOURCE,FSTYPE', ''),
  ], 'в списке есть интерфейс lo, тип файловой системы — sysfs.'),
  CORE('14-sys', 'Сетевые интерфейсы виртуальной машины в /sys.',
    LENS(0, 10, [(3, 0, 6, 1), (3, 8, 10, 2), (10, 0, 16, 3)],
      ['сетевая карта', 'lo — внутренний интерфейс компьютера', 'файловая система sysfs'], cols=44)),
]),
],
summary=[
  'Устройства представлены специальными файлами в <code>/dev</code>; <code>c</code> — символьные, <code>b</code> — блочные.',
  'Код завершения последней команды хранится в <code>$?</code>, 0 означает успех.',
  '<code>/proc</code> и <code>/sys</code> — псевдофайловые системы: содержимое их файлов формирует ядро при чтении.',
  '<code>/proc</code> описывает процессы и состояние системы, <code>/sys</code> — устройства.',
],
report='Снимок с /proc/uptime и его копией.',
)
