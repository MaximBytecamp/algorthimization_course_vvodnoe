# Проверка урока «Файловая система Linux» · 15 сентября 2026

- Страницы собраны `python3 build.py`, `node --check lesson.js` без ошибок.
- Chrome через Playwright, ширины 1440, 768 и 390 px: ошибок JavaScript нет, горизонтального переполнения нет, локальные ссылки и изображения доступны.
- На страницах нет блоков вопросов, вступительной истории и нарастающей главной мысли.
- Схемы проверены: переходы cd приводят в /usr/bin, в схеме inode после ln, rm, cat ссылка перестаёт работать, второй этап монтирования показывает tmpfs. Отметка о пройденной части сохраняется после перезагрузки.
- Команды во всех 18 блоках «В терминале», стоящих перед снимками, сверены с командами снимков в `shots/SOURCES.md`.

## Редакция

Урок переписан в форме взрослой лекции по замечаниям автора: убраны история с загадкой CUPS, «разрез пути», вопросы к студенту, нарастающая главная мысль и семь анимированных моделей. В каждой части — последовательное объяснение, определения терминов, команды, снимки и итоги. CUPS оставлен только как пример размещения файлов пакета.

## Снимки

23 снимка первого Live-сеанса и 6 снимков второго сняты с дисплея виртуальной машины через VirtualBox. Команды каждого снимка — `shots/SOURCES.md`.

## Дополнения по замечаниям · 15 сентября 2026

- Страница справки `man hier` объяснена: что такое `man`, зачем открываем `hier`, команда в блоке и перевод шести первых описаний под снимком.
- Раздел 2.3 разобран полностью: `getent`, `id -un`, `$( )`, ключи `find`, `| head`; все семь полей учётной записи; второй фрагмент снимка с файлами и подкаталогами `/etc`.
- Служба CUPS объяснена перед использованием: что это за служба и почему её удобно разбирать как пример.
- Во все части добавлены аналогии и примеры: почтовый адрес, отметка «Вы здесь», папка пользователя Windows, вещи в квартире, полки с книгами, учебник и тетрадь, розетка, табло на вокзале, записная книжка, ярлык Windows, лист поверх листа; реальные примеры `/var/lib/mysql`, `/var/log/apache2`, `/var/cache/apt`, `find … 2>/dev/null`, флешка в `/media`, `./hello.sh`.
