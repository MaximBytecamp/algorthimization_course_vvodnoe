# Проверка урока «Разрез Linux» · 14 сентября 2026

- 9 страниц (обложка и 8 слоёв) собраны `python3 build.py`; `node --check lesson.js` без ошибок.
- Chrome через Playwright, ширины 1440, 768 и 390 px: ошибок JavaScript нет, горизонтального переполнения страницы нет, локальные ссылки ведут на существующие файлы, повторяющихся id нет.
- Длинные команды в блоках «В терминале» на узком экране переносятся внутри блока (раньше текст выходил за край на 9–25 px).
- Модели проверены сценариями: маршрут cd (переход в /usr/bin и ошибка `cd bin` из дома), раскладка CUPS «по программе», пояснение оси FHS, поиск в PATH (`cupsd` → /usr/sbin, `app` → command not found), события срока жизни (перезагрузка убирает только сокет, apt remove оставляет настройки), чтение /proc/uptime двумя значениями из кадра, опыт ln → rm → cat → ln в модели inode, второй этап монтирования, самопроверка размещения, карта корня.
- Отметка «слой пройден» сохраняется после перезагрузки страницы и окрашивает глубиномер.
- Линзы 30 кадров сверены с изображениями: метки стоят на нужных строках и колонках терминала.
- Текст проверен на запрещённые обороты (`вслух`, `давайте`, `представим`, `устная защита`) — не найдено.

## Кадры

23 кадра первого Live-сеанса и 7 кадров второго (после перезагрузки VM) сняты с дисплея Ubuntu-lab-01 через VirtualBox. Во втором сеансе ввод шёл построчно с паузой; кадры с перемешанным набором и выводом переснимались. Команды каждого кадра — `shots/SOURCES.md`.

## Редактура текста · 15 сентября 2026

Текст всех восьми слоёв переписан по замечанию автора: убраны нейросетевые обороты («интуиция подсказывает», «самое наглядное доказательство», «склеивает их»), перегруженные метафоры и конструкции «не X, а Y». Определения и пояснения написаны для студентов, которые видят тему впервые: короткие предложения, термин и сразу объяснение, без ссылок вперёд. Служебные названия блоков заменены на понятные: «Проба» → «В терминале», «Признак успеха» → «Что должно получиться», «Керн» → «Кадр», «Словарь урока» → «Определение». Проверены предложения длиннее 28 слов и запрещённые обороты из `shared/TEXT_REVIEW.md`.
