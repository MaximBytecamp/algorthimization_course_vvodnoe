from blocks import *

CHAPTER = dict(
slug='02-paroli', layer='Пароли', color='#5A3D8C',
title='Пароли и файл /etc/shadow',
lead='В этой части разберём, почему пароли хранятся отдельно от учётных записей, что записано в /etc/shadow и как система хранит пароль, не храня его самого.',
sections=[
dict(title='Почему пароли хранятся отдельно', blocks=[
  P('Файл <code>/etc/passwd</code> должны читать все программы: <code>ls -l</code> по нему превращает UID владельца в имя, а <code>id</code> выводит имена групп. Поэтому права на него открывают чтение всем пользователям. В ранних версиях Unix во втором поле этого файла хранился хеш пароля, и любой пользователь мог скопировать хеши и подбирать к ним пароли на своём компьютере.'),
  P('Чтобы закрыть эту возможность, хеши перенесли в отдельный файл <code>/etc/shadow</code>. Читать его может только root и программы проверки паролей из группы <code>shadow</code>. В <code>/etc/passwd</code> на месте пароля осталась буква <code>x</code> — отметка «пароль в shadow».'),
  P('Права доступа в первом столбце <code>ls -l</code> записаны тремя тройками символов: для владельца, для группы файла и для всех остальных. <code>r</code> — чтение, <code>w</code> — запись, <code>x</code> — выполнение, прочерк — права нет. У <code>/etc/passwd</code> права <code>rw-r--r--</code>: владелец root читает и пишет, остальные только читают. У <code>/etc/shadow</code> права <code>rw-r-----</code>: у остальных пользователей прав нет.'),
  TABLE(['Файл', 'Права', 'Кто читает'], [
    ['<code>/etc/passwd</code>', '<code>rw-r--r--</code>, группа root', 'все пользователи'],
    ['<code>/etc/shadow</code>', '<code>rw-r-----</code>, группа shadow', 'root и участники группы shadow'],
  ]),
  PROBE('Права на passwd и shadow', [
    ('ls -l /etc/passwd /etc/shadow', 'сравнить права'),
    ('cat /etc/shadow', 'от имени обычного пользователя'),
  ], '<code>/etc/passwd</code> открыт для чтения всем; <code>cat /etc/shadow</code> завершается ошибкой <code>Permission denied</code>.'),
  CORE('03-shadow', 'Права на /etc/passwd и /etc/shadow и отказ при чтении shadow.',
    LENS(0, 4, [(1, 0, 10, 1), (2, 0, 10, 2), (2, 18, 24, 3), (4, 0, 35, 4)],
      ['passwd: остальные могут читать (r--)', 'shadow: у остальных прав нет (---)', 'группа shadow — программы проверки паролей', 'обычный пользователь shadow прочитать не может'], cols=60)),
]),
dict(title='Хеш пароля', blocks=[
  P('Система не хранит сам пароль. При смене пароля к нему добавляется соль — случайная строка, — и от результата вычисляется хеш: строка фиксированной длины, по которой нельзя восстановить исходный пароль. При входе система берёт введённый пароль, вычисляет хеш с той же солью и сравнивает результат с записанным. Совпали — пароль верный.'),
  TERM('Хеш пароля', 'Результат одностороннего преобразования пароля вместе с солью. По хешу проверяют, верен ли введённый пароль, но восстановить пароль по хешу нельзя.', 'Шифрование: зашифрованный текст можно расшифровать ключом, а хеш обратно в пароль не превращается.'),
  P('Соль нужна, чтобы одинаковые пароли давали разные хеши. Если две учётные записи получили одинаковый пароль, соль у них разная, и хеши тоже получаются разными. Поэтому по совпадению хешей нельзя узнать, у кого одинаковые пароли.'),
  P('Хеш похож на отпечаток пальца: по отпечатку можно проверить, тот ли это человек, но восстановить по нему самого человека нельзя.'),
  P('Второе поле строки <code>/etc/shadow</code> состоит из частей, разделённых знаком <code>$</code>. Для примера взято поле учебной учётной записи anna, пароль которой задан командой <code>passwd</code>.'),
  TABLE(['Часть поля', 'Пример', 'Значение'], [
    ['алгоритм', '<code>$y$</code>', 'yescrypt — алгоритм по умолчанию в Ubuntu 24.04'],
    ['параметры', '<code>j9T</code>', 'сложность вычисления хеша'],
    ['соль', '<code>g7zEi3hrM12Bp6AjRYdSA/</code>', 'случайная строка, своя для каждого пароля'],
    ['хеш', '<code>hloIoQta77…</code>', 'результат преобразования пароля с солью'],
  ]),
]),
dict(title='Поля /etc/shadow', blocks=[
  P('Строка <code>/etc/shadow</code> состоит из девяти полей. Первое — имя учётной записи, второе — хеш пароля. Остальные поля задают сроки: когда пароль менялся последний раз и как часто его нужно менять. Даты записаны номером дня, отсчитанным от 1 января 1970 года.'),
  TABLE(['Значение во втором поле', 'Что означает'], [
    ['<code>$y$…</code>', 'хеш пароля: вход по паролю возможен'],
    ['пусто', 'пароль не задан; в Live-сеансе у ubuntu поле пустое'],
    ['<code>*</code>', 'пароля нет и войти по паролю нельзя: так у служебных записей'],
    ['<code>!</code> в начале', 'пароль заблокирован командой <code>passwd -l</code>'],
  ]),
  P('Прочитать <code>/etc/shadow</code> можно только с правами администратора, поэтому команды ниже начинаются с <code>sudo</code>. Команда <code>grep</code> отбирает строки, в которых встречается слово. Команда <code>passwd -S</code> выводит состояние пароля кратко: имя, код состояния (<code>P</code> — пароль задан, <code>NP</code> — пароля нет, <code>L</code> — заблокирован), дату смены и сроки.'),
  PROBE('Строки /etc/shadow', [
    ('sudo grep ubuntu /etc/shadow', 'строка пользователя ubuntu'),
    ('sudo grep www-data /etc/shadow', 'строка служебной записи'),
    ('sudo passwd -S ubuntu', 'состояние пароля'),
  ], 'у ubuntu поле пароля пусто, у www-data стоит <code>*</code>; <code>passwd -S</code> выводит <code>NP</code>.'),
  CORE('03-shadow', 'Поля /etc/shadow у пользователя Live-сеанса и у служебной записи.',
    LENS(5, 10, [(6, 0, 6, 1), (6, 6, 8, 2), (6, 8, 13, 3), (6, 14, 15, 4), (6, 16, 21, 5), (6, 22, 23, 6), (8, 9, 10, 7), (10, 7, 9, 8)],
      ['имя учётной записи', 'поле пароля пусто: в Live-сеансе пароль не задан', 'день последней смены: 20717-й день с 1 января 1970 года', 'минимум дней между сменами', 'максимум дней до обязательной смены', 'за сколько дней предупреждать', '* — вход по паролю невозможен', 'NP — пароля нет'], cols=60)),
  NOTE('В установленной Ubuntu у вашего пользователя во втором поле будет хеш <code>$y$…</code>, а <code>passwd -S</code> выведет <code>P</code>. Пустой пароль — особенность Live-сеанса.'),
]),
],
summary=[
  '<code>/etc/passwd</code> читают все, поэтому хеши паролей хранятся в <code>/etc/shadow</code>, закрытом от обычных пользователей.',
  'Права в <code>ls -l</code> — три тройки <code>rwx</code>: владелец, группа, остальные.',
  'Система хранит хеш пароля с солью; восстановить пароль по хешу нельзя.',
  'Второе поле shadow: хеш, пусто, <code>*</code> или <code>!</code>; <code>passwd -S</code> выводит состояние пароля.',
],
report='Снимок ls -l /etc/passwd /etc/shadow с объяснением разницы в правах.',
bridge='Пароль подтверждает, кто вошёл в систему. Что этому пользователю разрешено, во многом определяют его группы.',
)
