from blocks import *

CHAPTER = dict(
slug='01-uchetnye-zapisi', layer='Учётные записи', color='#3D4B8C',
title='Учётные записи',
lead='В этой части разберём, что такое учётная запись, как система различает пользователей по номерам и что записано в файле /etc/passwd.',
sections=[
dict(title='Кто работает в системе', blocks=[
  P('Linux с самого начала создавался как система для многих пользователей. На одном сервере одновременно работают разработчики, база данных, веб-сервер и служба резервного копирования, и у каждого из них свои файлы и свои права. Чтобы отличать их друг от друга, система ведёт учётные записи.'),
  TERM('Учётная запись', 'Запись о пользователе в системе: имя, числовой номер UID, основная группа, домашний каталог и оболочка. Каждый процесс и каждый файл связан с учётной записью через её номер.', 'Человек и учётная запись: у одного человека может быть несколько учётных записей, а у службы — учётная запись без человека.'),
  P('Внутри системы пользователь — это прежде всего номер UID (user identifier). Имя нужно людям, а ядро сравнивает номера: в свойствах файла записан UID владельца, у процесса — UID того, кто его запустил. Номер группы называется GID (group identifier).'),
  P('Сравнить можно с пропуском в офисное здание. На пропуске есть фамилия, но турникет проверяет номер карты. Два пропуска с одной фамилией турникет различит, а один номер на двух картах он будет считать одним человеком.'),
  P('Команда <code>whoami</code> выводит имя текущего пользователя. Команда <code>id</code> выводит все номера сразу: UID, GID основной группы и список всех групп, в которых пользователь состоит. В Live-сеансе Ubuntu пользователь называется <code>ubuntu</code> и имеет UID 1000. У суперпользователя <code>root</code> UID всегда равен 0.'),
  PROBE('Кто я', [
    ('whoami', 'имя текущего пользователя'),
    ('id', 'номера и группы'),
    ('id root', 'номера пользователя root'),
  ], '<code>whoami</code> выводит <code>ubuntu</code>; <code>id</code> — UID 1000 и девять групп; у root UID и GID равны 0.'),
  CORE('01-whoami', 'whoami и id: имя, номер пользователя, основная группа и все группы.',
    LENS(0, 6, [(1, 0, 6, 1), (3, 0, 16, 2), (3, 17, 33, 3), (3, 34, 120, 4), (4, 0, 19, 4), (6, 0, 11, 5)],
      ['имя текущего пользователя', 'UID — номер пользователя', 'GID — номер основной группы', 'groups — все группы пользователя', 'у root UID 0'], cols=120)),
]),
dict(title='Файл /etc/passwd', blocks=[
  P('Учётные записи хранятся в текстовом файле <code>/etc/passwd</code>, по одной строке на запись. На прошлом занятии мы открывали его описание командой <code>man 5 passwd</code>. Строка состоит из семи полей, разделённых двоеточием.'),
  TABLE(['№', 'Поле', 'Пример для ubuntu'], [
    ['1', 'имя входа', '<code>ubuntu</code>'],
    ['2', 'пароль: <code>x</code> означает, что хеш пароля в /etc/shadow', '<code>x</code>'],
    ['3', 'UID', '<code>1000</code>'],
    ['4', 'GID основной группы', '<code>1000</code>'],
    ['5', 'комментарий: полное имя или описание', '<code>Live session user,,,</code>'],
    ['6', 'домашний каталог', '<code>/home/ubuntu</code>'],
    ['7', 'оболочка, которая запускается после входа', '<code>/bin/bash</code>'],
  ]),
  P('Команда <code>getent passwd имя</code> выводит строку из базы учётных записей. Она читает и <code>/etc/passwd</code>, и другие источники, например сетевой каталог пользователей в организации, поэтому в скриптах предпочитают её. Команда <code>cut -d: -f1,3,7</code> вырезает из строк поля 1, 3 и 7: <code>-d:</code> задаёт разделитель, <code>-f</code> — номера полей.'),
  W('record'),
  PROBE('Строки учётных записей', [
    ('getent passwd ubuntu root', 'две строки из базы'),
    ('wc -l /etc/passwd', 'сколько строк в файле'),
    ('cut -d: -f1,3,7 /etc/passwd | head -8', 'имя, UID и оболочка первых восьми записей'),
  ], 'строки ubuntu и root по семь полей; в файле 50 записей; у большинства первых записей оболочка <code>/usr/sbin/nologin</code>.'),
  CORE('01-whoami', 'Строка учётной записи ubuntu по полям; число записей в /etc/passwd.',
    LENS(7, 20, [(8, 0, 6, 1), (8, 7, 8, 2), (8, 9, 13, 3), (8, 14, 18, 4), (8, 19, 39, 5), (8, 40, 52, 6), (8, 53, 62, 7), (11, 0, 2, 8), (14, 0, 26, 9)],
      ['имя входа', 'x — пароль хранится в /etc/shadow', 'UID', 'GID основной группы', 'комментарий', 'домашний каталог', 'оболочка', 'в файле 50 учётных записей', 'системные записи с оболочкой nologin'], cols=70)),
]),
dict(title='Служебные учётные записи', blocks=[
  P('Оболочка <code>/bin/bash</code> в Live-сеансе есть только у двух записей — <code>root</code> и <code>ubuntu</code>, а всего записей в файле пятьдесят. Остальные записи служебные: от их имени работают программы. Веб-сервер работает от имени <code>www-data</code>, журнал ведёт <code>syslog</code>, время синхронизирует <code>systemd-timesync</code>.'),
  P('Служебные записи нужны для разграничения доступа между программами. Если веб-сервер работает от имени <code>www-data</code>, то ошибка в нём даёт доступ только к файлам, которые принадлежат <code>www-data</code>; остальные файлы системы для него закрыты. У служебных записей UID меньше 1000, а вместо оболочки указана программа <code>/usr/sbin/nologin</code>: она печатает отказ и завершает сеанс, поэтому войти в систему под такой записью нельзя.'),
  TABLE(['Диапазон UID', 'Кому выдаётся'], [
    ['0', 'root — суперпользователь'],
    ['1–999', 'системные и служебные учётные записи программ'],
    ['1000 и больше', 'обычные пользователи, первый созданный получает 1000'],
    ['65534', 'nobody — запись без прав, для программ, которым права не нужны'],
  ]),
  P('Команда <code>ps -eo user=</code> выводит владельца каждого процесса системы; <code>sort</code> сортирует список, а <code>uniq -c</code> считает одинаковые строки подряд. В результате видно, от имени каких учётных записей работают процессы.'),
  PROBE('Служебные записи и процессы', [
    ('grep bash /etc/passwd', 'записи с оболочкой bash'),
    ('grep -c nologin /etc/passwd', 'сколько записей без входа'),
    ('getent passwd www-data', 'учётная запись веб-сервера'),
    ('ps -eo user= | sort | uniq -c', 'сколько процессов у каждой записи'),
  ], 'оболочка bash только у root и ubuntu; 39 записей с nologin; процессы работают от имени root, ubuntu и служебных записей.'),
  CORE('02-system-users', 'Оболочка bash у двух записей, nologin у служебных; процессы по учётным записям.',
    LENS(0, 21, [(1, 22, 31, 1), (2, 53, 62, 1), (4, 0, 2, 2), (6, 11, 13, 3), (6, 26, 34, 4), (6, 35, 52, 5), (15, 4, 14, 6), (21, 5, 14, 7)],
      ['у root и ubuntu оболочка /bin/bash', '39 записей с nologin', 'UID 33 — служебная запись www-data', 'домашний каталог веб-сервера', 'nologin: войти под этой записью нельзя', 'процессы root', 'процессы пользователя ubuntu'], cols=64)),
  NOTE('Список служебных записей и число процессов зависят от установленных программ. На сервере без графического интерфейса записей и процессов меньше.'),
]),
],
summary=[
  'Учётная запись — имя, UID, основная группа, домашний каталог и оболочка.',
  'Ядро различает пользователей по номерам: UID и GID; у root UID 0.',
  'В <code>/etc/passwd</code> одна строка на запись, семь полей через двоеточие.',
  'Служебные записи с UID меньше 1000 и оболочкой nologin нужны программам.',
],
report='Снимок id и строка вашей учётной записи из getent passwd с подписанными полями.',
bridge='Во втором поле /etc/passwd стоит x. Дальше — где хранятся пароли и почему их вынесли в отдельный файл.',
)
