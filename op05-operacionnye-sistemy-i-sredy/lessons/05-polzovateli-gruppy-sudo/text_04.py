from blocks import *

CHAPTER = dict(
slug='04-root-i-sudo', layer='root и sudo', color='#8C3D5A',
title='Суперпользователь root и sudo',
lead='В этой части разберём, чем root отличается от остальных пользователей, как sudo решает, можно ли выполнить команду от имени root, где остаются следы каждой такой команды и почему постоянно работать под root нельзя.',
sections=[
dict(title='Суперпользователь root', blocks=[
  P('Проверка прав, которую мы разбирали в части 3, выполняется для всех процессов, кроме одного случая. Если процесс работает с UID 0, ядро права на файлы не проверяет: такому процессу доступны чтение, изменение и удаление любого файла. Учётная запись с UID 0 называется <code>root</code>, или суперпользователь.'),
  TERM('root', 'Учётная запись с UID 0. Для процессов root ядро не проверяет права на файлы, поэтому root может изменить любой файл и любую настройку системы.', 'Администратор и root: администратор — человек, который получает права root на время через sudo.'),
  P('Права root нужны для задач, которые меняют систему целиком: установки программ, создания пользователей, изменения файлов в <code>/etc</code>. Обычная работа — редактирование кода, запуск программ, чтение документации — прав root не требует.'),
  P('Пароль root в Ubuntu по умолчанию не задан, и войти под root напрямую нельзя. Вместо этого пользователь из группы <code>sudo</code> выполняет отдельные команды с правами root через программу <code>sudo</code> (substitute user do — «выполнить от имени другого пользователя»).'),
  PROBE('Права root', [
    ('sudo whoami', 'от чьего имени выполнена команда'),
    ('sudo id', ''),
    ('ls /root', 'домашний каталог root'),
    ('sudo ls -a /root', 'тот же каталог с правами root'),
  ], '<code>sudo whoami</code> выводит <code>root</code>; каталог <code>/root</code> открывается только через sudo.'),
  CORE('05-root', 'Команды через sudo выполняются с UID 0; каталог /root закрыт для обычного пользователя.',
    LENS(0, 7, [(1, 0, 4, 1), (3, 0, 11, 2), (5, 0, 52, 3), (7, 0, 57, 4)],
      ['sudo whoami: команда выполнена от имени root', 'UID 0', 'обычному пользователю каталог /root закрыт', 'с правами root содержимое каталога доступно'], cols=64)),
]),
dict(title='Как sudo решает, можно ли выполнить команду', blocks=[
  P('Программа <code>sudo</code> запускается с правами root: у файла <code>/usr/bin/sudo</code> установлен специальный бит setuid, поэтому программа работает от имени владельца файла — root — независимо от того, кто её вызвал. Получив права root, <code>sudo</code> проверяет, разрешено ли вызвавшему пользователю выполнить эту команду. Правила записаны в файле <code>/etc/sudoers</code> и в файлах каталога <code>/etc/sudoers.d</code>.'),
  TABLE(['Часть правила', 'Пример', 'Значение'], [
    ['кто', '<code>%sudo</code>', 'знак % — группа: правило действует для всех участников sudo'],
    ['на каком компьютере', '<code>ALL=</code>', 'на любом компьютере'],
    ['от чьего имени', '<code>(ALL:ALL)</code>', 'от имени любого пользователя и любой группы'],
    ['какие команды', '<code>ALL</code>', 'любые команды'],
    ['без пароля', '<code>NOPASSWD:</code>', 'пароль не спрашивается'],
  ]),
  P('Если правило найдено, <code>sudo</code> запрашивает пароль того пользователя, который её вызвал; пароль root для этого не нужен. После правильного ввода пароль некоторое время не спрашивается повторно: в Ubuntu 15 минут в одном терминале. Если правила нет, команда не выполняется, а попытка записывается в журнал.'),
  P('Команда <code>sudo -l</code> выводит правила, которые действуют для текущего пользователя. Сам файл <code>/etc/sudoers</code> обычный пользователь прочитать не может. Изменяют его только командой <code>sudo visudo</code>: она проверяет синтаксис перед сохранением. Ошибка в этом файле, сохранённая обычным редактором, может отключить <code>sudo</code> для всех пользователей.'),
  PROBE('Правила sudo', [
    ('sudo -l', 'правила для текущего пользователя'),
    ('ls -l /etc/sudoers', 'права на файл правил'),
    ('sudo grep -v "^#" /etc/sudoers | grep -v "^$"', 'правила без комментариев и пустых строк'),
    ('sudo ls /etc/sudoers.d', 'дополнительные файлы правил'),
  ], 'для ubuntu действуют два правила: группы sudo и правило без пароля; в файле есть правила для root, %admin и %sudo.'),
  CORE('05-root', 'sudo -l перечисляет правила для текущего пользователя.',
    LENS(8, 15, [(13, 0, 53, 1), (14, 4, 19, 2), (15, 4, 23, 3)],
      ['правила, которые действуют для ubuntu', 'правило группы sudo: любые команды от имени любого пользователя', 'правило Live-сеанса: без пароля'], cols=120)),
  CORE('06-sudoers', 'Файл /etc/sudoers без комментариев.',
    LENS(0, 12, [(1, 0, 10, 1), (7, 0, 4, 2), (9, 0, 5, 3), (9, 8, 21, 4), (9, 22, 25, 5), (10, 0, 26, 6), (12, 29, 35, 7)],
      ['файл только для чтения, доступ — у root и группы root', 'правило для root', '% — правило для группы sudo', 'на любом компьютере, от имени любого пользователя и группы', 'любые команды', 'дополнительные правила из каталога sudoers.d', 'файл, созданный Live-сеансом'], cols=72)),
  P('На схеме ниже показан путь команды <code>sudo whoami</code> для трёх пользователей: <code>ubuntu</code> в Live-сеансе, <code>viktor</code>, который не состоит в группе sudo, и пользователя установленной Ubuntu из группы sudo. Первые два сценария совпадают со снимками занятия.'),
  W('sudo'),
  NOTE('В Live-сеансе для пользователя ubuntu действует правило <code>NOPASSWD</code>, поэтому sudo не спрашивает пароль. В установленной Ubuntu пароль будет запрошен при первой команде sudo.'),
]),
dict(title='Журнал sudo', blocks=[
  P('Каждый вызов <code>sudo</code> записывается в системный журнал: кто вызвал, в каком терминале, в каком каталоге, от чьего имени и какую команду. По этим записям администратор восстанавливает, кто и когда менял систему. Журнал читает команда <code>journalctl</code>; условие <code>_COMM=sudo</code> отбирает записи программы sudo, <code>-n 6</code> — последние шесть.'),
  PROBE('Записи sudo в журнале', [
    ('journalctl _COMM=sudo -n 6 --no-pager', 'последние записи sudo'),
  ], 'для каждой команды через sudo в журнале три строки: сама команда, открытие и закрытие сеанса root.'),
  CORE('07-sudo-log', 'Каждая команда через sudo оставляет запись в журнале.',
    LENS(0, 8, [(1, 0, 15, 1), (1, 37, 43, 2), (1, 46, 55, 3), (1, 58, 74, 4), (1, 77, 86, 5), (1, 89, 120, 6), (2, 0, 9, 6), (3, 35, 120, 7)],
      ['время записи', 'кто вызвал sudo', 'терминал', 'каталог, в котором выполнялась команда', 'от чьего имени выполнена команда', 'полная команда', 'сеанс root открыт и закрыт'], cols=120)),
]),
dict(title='Оболочка root: sudo -i', blocks=[
  P('Команда <code>sudo -i</code> открывает командную оболочку root: все следующие команды выполняются с UID 0 без слова <code>sudo</code> перед ними. Приглашение меняется: вместо имени пользователя стоит <code>root</code>, а знак <code>$</code> заменяется на <code>#</code>. Команда <code>exit</code> закрывает оболочку root и возвращает к обычному пользователю.'),
  P('В журнал в этом случае попадает только сама команда <code>sudo -i</code>. Команды внутри оболочки root отдельно не записываются, поэтому по журналу уже нельзя восстановить, какие команды выполнил администратор.'),
  PROBE('Оболочка root', [
    ('sudo -i', 'открыть оболочку root'),
    ('KEY:whoami', 'внутри оболочки root'),
    ('KEY:pwd', ''),
    ('KEY:exit', 'вернуться к обычному пользователю'),
    ('whoami', ''),
  ], 'приглашение меняется на <code>root@ubuntu:~#</code>; после <code>exit</code> снова <code>ubuntu</code>.'),
  CORE('08-sudo-i', 'Оболочка root: знак # в приглашении и домашний каталог /root.',
    LENS(0, 8, [(1, 0, 14, 1), (4, 0, 5, 2), (6, 0, 6, 3), (7, 0, 16, 4)],
      ['приглашение root заканчивается знаком #', 'домашний каталог root — /root', 'exit закрывает оболочку root', 'снова обычный пользователь: знак $'], cols=40)),
]),
dict(title='Принцип минимальных привилегий', blocks=[
  TERM('Принцип минимальных привилегий', 'Каждый пользователь и каждая программа получают только те права, которые нужны для их задачи, и только на то время, пока эти права нужны.'),
  P('Постоянная работа под root нарушает этот принцип. У root нет защиты от ошибок: система не остановит ни опечатку, ни вредную программу. Работа через <code>sudo</code> оставляет права root только для отдельных команд и записывает каждую из них в журнал.'),
  TABLE(['Что случается под root', 'Пример'], [
    ['опечатка удаляет системные файлы', '<code>rm -rf /etc/app</code> вместо <code>rm -rf ./etc/app</code>: из-за пропущенной точки путь ведёт в системный каталог <code>/etc</code>'],
    ['программа получает все права', 'скрипт из интернета или пакет с ошибкой меняет любые файлы системы'],
    ['файлы получают не того владельца', 'файл, созданный root в каталоге проекта, другие участники не могут изменить'],
    ['не видно, кто что сделал', 'команды внутри <code>sudo -i</code> не попадают в журнал по отдельности'],
  ]),
  STEPS('Правила работы с правами root', [
    'Работайте под своей учётной записью; <code>sudo</code> добавляйте только к командам, которые меняют систему.',
    'Перед командой с <code>sudo</code> прочитайте её целиком ещё раз: у неё нет ограничений.',
    'Не открывайте <code>sudo -i</code> без необходимости и закрывайте оболочку root командой <code>exit</code> сразу после работы.',
    'В группу <code>sudo</code> добавляйте только тех, кто администрирует систему.',
  ]),
]),
],
summary=[
  'root — учётная запись с UID 0; для её процессов права на файлы не проверяются.',
  '<code>sudo</code> выполняет команду от имени root, если в <code>/etc/sudoers</code> есть правило для пользователя или его группы.',
  'sudo спрашивает пароль вызвавшего пользователя; <code>sudo -l</code> выводит действующие правила; править правила — только через <code>visudo</code>.',
  'Каждый вызов sudo записывается в журнал; команды внутри <code>sudo -i</code> — нет.',
  'Принцип минимальных привилегий: права root — только для отдельных команд и только когда они нужны.',
],
report='Снимок sudo -l и снимок записей sudo в журнале с подписанными полями.',
bridge='Права root нужны для следующего шага — создания учётных записей для команды разработки.',
)
