from blocks import *

CHAPTER = dict(
slug='03-gruppy', layer='Группы', color='#2F6D8C',
title='Группы',
lead='В этой части разберём основную и дополнительные группы пользователя, файл /etc/group и то, как членство в группе даёт доступ к файлам.',
sections=[
dict(title='Основная и дополнительные группы', blocks=[
  P('Выдавать права каждому пользователю отдельно неудобно: в команде из десяти разработчиков пришлось бы десять раз повторять одни и те же настройки. Поэтому пользователей объединяют в группы и права выдают группе. Новый участник команды получает доступ, как только его добавят в группу.'),
  TERM('Группа', 'Именованный набор учётных записей с номером GID. Права, выданные группе, получают все её участники.', 'Группа пользователя и группа файла: у файла ровно одна группа, а пользователь может состоять во многих группах.'),
  P('У каждого пользователя одна основная группа и сколько угодно дополнительных. Основная группа записана номером в четвёртом поле <code>/etc/passwd</code>. Файлы, которые создаёт пользователь, получают эту группу. В Ubuntu для каждого пользователя создаётся личная основная группа с тем же именем: у <code>ubuntu</code> это группа <code>ubuntu</code>. Дополнительные группы перечислены в файле <code>/etc/group</code>.'),
  P('Строка <code>/etc/group</code> состоит из четырёх полей: имя группы, пароль группы (<code>x</code>, пароли групп практически не используются), GID и список участников через запятую. В этом списке указаны только те, для кого группа дополнительная. Поэтому в строке личной группы <code>ubuntu:x:1000:</code> участников нет: основную группу пользователь получает через поле GID в <code>/etc/passwd</code>.'),
  TABLE(['Группа', 'Зачем нужна'], [
    ['<code>sudo</code>', 'участники выполняют команды от имени root через sudo'],
    ['<code>adm</code>', 'участники читают системные журналы в <code>/var/log</code>'],
    ['<code>cdrom</code>, <code>plugdev</code>', 'доступ к оптическому приводу и съёмным устройствам'],
    ['<code>users</code>', 'общая группа обычных пользователей'],
  ]),
  P('Команда <code>groups</code> выводит имена групп пользователя, <code>id -Gn</code> — то же самое, но её удобнее применять в скриптах вместе с другими ключами <code>id</code>. Команда <code>getent group</code> выводит строки групп из базы.'),
  PROBE('Группы пользователя', [
    ('groups', 'группы текущего пользователя'),
    ('id -Gn', 'то же самое через id'),
    ('getent group sudo adm', 'строки двух групп'),
    ('grep ubuntu /etc/group', 'все группы, где встречается ubuntu'),
  ], 'у ubuntu девять групп, первая — основная <code>ubuntu</code>; в строке <code>sudo</code> среди участников есть ubuntu.'),
  CORE('04-groups', 'Группы пользователя ubuntu и строки файла /etc/group.',
    LENS(0, 16, [(1, 0, 6, 1), (1, 7, 59, 2), (5, 0, 4, 3), (5, 5, 6, 4), (5, 7, 9, 5), (5, 10, 26, 6), (15, 0, 14, 7)],
      ['первая в списке — основная группа ubuntu', 'остальные — дополнительные группы', 'имя группы', 'пароль группы не используется', 'GID группы', 'участники через запятую', 'личная группа: участников в строке нет'], cols=64)),
  NOTE('Учётная запись и группа <code>installer</code> созданы Live-сеансом для программы установки системы.'),
]),
dict(title='Группа даёт доступ к файлам', blocks=[
  P('У каждого файла есть владелец и группа. Права записаны тремя тройками: первая действует для владельца, вторая — для участников группы файла, третья — для всех остальных. Система проверяет тройки по порядку и применяет первую подходящую: если процесс принадлежит владельцу — права владельца; если среди групп процесса есть группа файла — права группы; иначе — права остальных.'),
  P('Системный журнал <code>/var/log/syslog</code> принадлежит служебной записи <code>syslog</code> и группе <code>adm</code>, права <code>rw-r-----</code>. Владелец пишет в журнал, участники группы <code>adm</code> читают его, остальным доступ закрыт. Пользователь <code>ubuntu</code> состоит в <code>adm</code>, поэтому может читать системные журналы без <code>sudo</code>.'),
  P('Домашний каталог <code>/home/ubuntu</code> имеет права <code>rwxr-x---</code>. Для каталога <code>r</code> означает право читать список файлов, <code>x</code> — право входить в каталог, <code>w</code> — право создавать и удалять файлы в нём. Остальные пользователи не могут даже открыть каталог, поэтому личные файлы одного пользователя закрыты от другого.'),
  TABLE(['Право', 'Для файла', 'Для каталога'], [
    ['<code>r</code>', 'читать содержимое', 'выводить список имён'],
    ['<code>w</code>', 'изменять содержимое', 'создавать, удалять и переименовывать файлы внутри'],
    ['<code>x</code>', 'запускать как программу', 'входить в каталог и обращаться к файлам внутри'],
  ]),
  PROBE('Владелец, группа и права', [
    ('ls -l /var/log/syslog', 'журнал: группа adm'),
    ('ls -ld /home/ubuntu', 'домашний каталог'),
  ], 'журнал принадлежит <code>syslog</code> и группе <code>adm</code>; домашний каталог закрыт для остальных.'),
  CORE('04-groups', 'Права на системный журнал и на домашний каталог.',
    LENS(17, 20, [(18, 0, 10, 1), (18, 13, 19, 2), (18, 20, 23, 3), (20, 0, 10, 4)],
      ['владелец rw-, группа r--, остальные ---', 'владелец — служебная запись syslog', 'группа adm: её участники читают журнал', 'домашний каталог: группа читает, остальные — нет'], cols=64)),
]),
],
summary=[
  'Группа — набор учётных записей; права, выданные группе, получают все участники.',
  'Основная группа записана в <code>/etc/passwd</code>, дополнительные — в <code>/etc/group</code>.',
  'Строка <code>/etc/group</code>: имя, пароль группы, GID, участники.',
  'Права проверяются по первой подходящей тройке: владелец, группа, остальные.',
],
report='Снимок groups и getent group sudo с объяснением, где основная группа, а где дополнительные.',
bridge='Одна группа — sudo — даёт особое право: выполнять команды от имени root. Дальше — кто такой root и как работает sudo.',
)
