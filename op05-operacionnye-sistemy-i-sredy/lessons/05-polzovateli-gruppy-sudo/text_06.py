from blocks import *

CHAPTER = dict(
slug='06-obshchiy-katalog', layer='Общий каталог', color='#8C6A3D',
title='Общий каталог команды',
lead='В этой части создадим каталог /srv/devteam, в котором anna и boris работают вместе, проверим доступ от имени каждого участника, войдём под учётной записью viktor и найдём следы этих действий в журнале.',
sections=[
dict(title='Каталог с группой-владельцем', blocks=[
  P('По стандарту FHS, который мы разбирали на занятии 3, в <code>/srv</code> лежат данные, которые система предоставляет другим: сайты, файловые хранилища, общие каталоги. Создадим там каталог <code>/srv/devteam</code>. После <code>mkdir</code> через <code>sudo</code> владельцем и группой каталога будет root, а остальные пользователи смогут только читать его содержимое.'),
  P('Команда <code>chgrp</code> (change group) меняет группу файла или каталога. После <code>sudo chgrp devteam /srv/devteam</code> вторая тройка прав будет действовать для участников devteam. Команда <code>chmod</code> (change mode) меняет сами права. Запись <code>2770</code> читается по цифрам.'),
  TABLE(['Цифра', 'Значение', 'Для /srv/devteam'], [
    ['<code>2</code>', 'бит setgid: новые файлы получают группу каталога', 'файлы anna и boris сразу принадлежат devteam'],
    ['<code>7</code>', 'права владельца: r (4) + w (2) + x (1)', 'root: <code>rwx</code>'],
    ['<code>7</code>', 'права группы', 'devteam: <code>rwx</code>, с битом setgid — <code>rws</code>'],
    ['<code>0</code>', 'права остальных', 'все остальные: <code>---</code>'],
  ]),
  P('Каждая цифра прав — сумма: чтение 4, запись 2, выполнение 1. Семь — все три права, пять — чтение и выполнение, ноль — ничего. Бит setgid нужен в общем каталоге потому, что без него файл получает основную группу создателя: файл anna принадлежал бы группе <code>anna</code>, и boris не смог бы его изменить.'),
  PROBE('Каталог /srv/devteam', [
    ('sudo mkdir /srv/devteam', ''),
    ('ls -ld /srv/devteam', 'права после создания'),
    ('sudo chgrp devteam /srv/devteam', 'группа каталога — devteam'),
    ('sudo chmod 2770 /srv/devteam', 'setgid, rwx владельцу и группе, остальным ничего'),
    ('ls -ld /srv/devteam', ''),
  ], 'права изменились с <code>drwxr-xr-x root root</code> на <code>drwxrws--- root devteam</code>.'),
  CORE('14-shared-setup', 'Каталог /srv/devteam до и после chgrp и chmod.',
    LENS(0, 6, [(2, 0, 10, 1), (2, 18, 22, 2), (6, 1, 4, 3), (6, 4, 7, 4), (6, 7, 10, 5), (6, 18, 25, 6)],
      ['после mkdir: остальные могут читать каталог', 'группа root', 'владелец root: rwx', 'группа: rws — все права и наследование группы', 'остальные: --- доступа нет', 'группа каталога — devteam'], cols=64)),
]),
dict(title='Проверка доступа от имени участников', blocks=[
  P('Проверять доступ удобно, не выходя из своего сеанса. Команда <code>sudo -u имя команда</code> выполняет команду от имени указанного пользователя с его группами. Так администратор видит результат, который получит каждый участник команды.'),
  P('Команда <code>tee -a файл</code> дописывает полученный текст в конец файла. Запись <code>echo … | sudo -u boris tee -a …</code> нужна потому, что перенаправление <code>&gt;&gt;</code> выполняет оболочка текущего пользователя, а <code>tee</code> работает от имени boris и проверяет права boris.'),
  PROBE('Доступ anna, boris, viktor и ubuntu', [
    ('sudo -u anna touch /srv/devteam/plan.txt', 'anna создаёт файл'),
    ('echo "draft by boris" | sudo -u boris tee -a /srv/devteam/plan.txt', 'boris дописывает в файл anna'),
    ('sudo -u boris ls -l /srv/devteam', ''),
    ('sudo -u viktor ls /srv/devteam', 'viktor не в devteam'),
    ('ls /srv/devteam', 'ubuntu тоже не в devteam'),
    ('sudo ls -l /srv/devteam', 'root'),
  ], 'anna и boris работают с одним файлом; viktor и ubuntu получают <code>Permission denied</code>; root видит содержимое.'),
  CORE('15-access-test', 'Участники devteam работают с общим файлом, остальным каталог закрыт.',
    LENS(0, 12, [(0, 17, 29, 1), (2, 0, 14, 2), (5, 13, 25, 3), (5, 26, 28, 4), (7, 0, 59, 5), (9, 0, 59, 6)],
      ['команда выполнена от имени anna', 'boris дописал строку в файл anna', 'владелец anna, группа devteam унаследована от каталога', '15 байт: строка boris записана', 'viktor не состоит в devteam — доступ закрыт', 'ubuntu тоже нет в devteam: группа sudo прав на каталог не даёт'], cols=72)),
  P('Вторая ошибка на снимке получена от имени пользователя <code>ubuntu</code>. Он администратор, он состоит в группе <code>sudo</code>, но без команды <code>sudo</code> работает с правами обычного пользователя, и каталог для него закрыт. Членство в группе sudo даёт право выполнять команды от имени root, а права на файлы проверяются для каждого процесса отдельно.'),
  P('На схеме ниже можно выбрать пользователя и действие и пройти ту же проверку, которую выполняет ядро: сравнение UID с владельцем, поиск группы каталога среди групп процесса и выбор тройки прав.'),
  W('gate'),
]),
dict(title='Вход под другим пользователем: su', blocks=[
  P('Команда <code>su - имя</code> (switch user) открывает оболочку другого пользователя. Она спрашивает пароль того пользователя, в которого вы переключаетесь. Дефис означает полный вход: переменные окружения и текущий каталог становятся такими, как при обычном входе, — открывается домашний каталог пользователя.'),
  P('Сравните две команды. <code>su</code> требует пароль целевого пользователя, поэтому пароль root пришлось бы сообщить всем администраторам. <code>sudo</code> требует пароль того, кто его вызвал, проверяет правила и записывает каждую команду в журнал. Поэтому в Ubuntu для административных задач используют <code>sudo</code>.'),
  PROBE('Сеанс viktor', [
    ('su - viktor', 'войти как viktor'),
    ('KEY:Lab-2026-pass', 'пароль viktor'),
    ('KEY:whoami', 'внутри сеанса viktor'),
    ('KEY:pwd', ''),
    ('KEY:ls /srv/devteam', ''),
    ('KEY:sudo whoami', 'попытка получить права root'),
    ('KEY:Lab-2026-pass', 'пароль viktor для sudo'),
    ('KEY:exit', 'вернуться в сеанс ubuntu'),
    ('whoami', ''),
  ], 'в сеансе viktor каталог команды закрыт; <code>sudo</code> отвечает <code>viktor is not in the sudoers file</code>.'),
  CORE('16-su-viktor', 'Сеанс viktor: нет доступа к каталогу команды и нет права на sudo.',
    LENS(0, 14, [(1, 0, 9, 1), (2, 0, 16, 2), (5, 0, 12, 3), (7, 0, 59, 4), (9, 0, 27, 5), (10, 0, 34, 6)],
      ['su запрашивает пароль viktor', 'приглашение с именем viktor', 'su - открыл домашний каталог viktor', 'доступа к каталогу команды нет', 'sudo спрашивает пароль вызвавшего — viktor', 'viktor нет в правилах sudoers — команда не выполнена'], cols=64)),
]),
dict(title='Следы в журнале', blocks=[
  P('Отказ sudo тоже записывается в журнал — со словами <code>NOT in sudoers</code>, именем пользователя и командой, которую он пытался выполнить. Администратор регулярно просматривает такие записи: они показывают попытки получить права, которых у пользователя нет. Ключ <code>-t sudo</code> отбирает записи по метке программы; та же запись есть в текстовом журнале <code>/var/log/auth.log</code>. Вход через <code>su</code> журнал записывает от имени программы <code>su</code>.'),
  PROBE('Записи об отказе и о входе', [
    ('journalctl -t sudo --no-pager | grep "NOT in sudoers"', 'отказ sudo'),
    ('sudo grep "NOT in sudoers" /var/log/auth.log', 'та же запись в файле'),
    ('journalctl _COMM=su -n 3 --no-pager', 'вход через su'),
  ], 'в журнале видно, что viktor пытался выполнить <code>whoami</code> от имени root, и что ubuntu входил в сеанс viktor.'),
  CORE('17-sudo-denied-log', 'Попытка viktor выполнить sudo и вход через su в журнале.',
    LENS(0, 9, [(1, 37, 43, 1), (1, 46, 65, 2), (1, 99, 108, 3), (2, 0, 14, 4), (4, 0, 32, 5), (7, 33, 60, 6)],
      ['кто пытался выполнить sudo', 'отказ: пользователя нет в правилах', 'от чьего имени он хотел выполнить команду', 'какую команду', 'та же запись в /var/log/auth.log', 'su: ubuntu вошёл как viktor'], cols=120)),
]),
dict(title='Удаление учётной записи', blocks=[
  P('Когда сотрудник уходит из команды, его учётную запись удаляют. Команда <code>userdel -r имя</code> удаляет строки из <code>/etc/passwd</code>, <code>/etc/shadow</code> и <code>/etc/group</code>, а ключ <code>-r</code> — ещё и домашний каталог с почтовым ящиком. Группу, которая больше не нужна, удаляет <code>groupdel</code>.'),
  P('Файлы пользователя за пределами домашнего каталога <code>userdel</code> не трогает. Они остаются с числовым UID удалённой записи, и если позже создать нового пользователя с тем же номером, он станет их владельцем. Поэтому перед удалением записи файлы пользователя находят командой <code>find / -user имя</code> и передают другому владельцу.'),
  PROBE('Удаление viktor', [
    ('sudo userdel -r viktor', 'запись и домашний каталог'),
    ('sudo groupdel testers', 'группа больше не нужна'),
    ('id viktor', 'проверка'),
    ('ls /home', ''),
    ('getent group devteam', 'группа команды не изменилась'),
  ], 'учётной записи viktor нет; каталог <code>/home/viktor</code> удалён; в devteam по-прежнему anna и boris.'),
  CORE('18-cleanup', 'userdel -r удалил учётную запись viktor вместе с домашним каталогом.',
    LENS(0, 8, [(1, 0, 55, 1), (4, 0, 26, 2), (6, 0, 27, 3)],
      ['почтового ящика у viktor не было, каталог /home/viktor удалён', 'учётной записи больше нет', 'в /home нет каталога viktor'], cols=64)),
]),
],
summary=[
  '<code>chgrp</code> меняет группу каталога, <code>chmod 2770</code> открывает его владельцу и группе и закрывает для остальных.',
  'Бит setgid на каталоге передаёт его группу новым файлам.',
  '<code>sudo -u имя команда</code> проверяет доступ от имени другого пользователя.',
  'Членство в группе sudo не даёт прав на файлы: права проверяются для каждого процесса.',
  '<code>su - имя</code> требует пароль целевого пользователя; отказы sudo и входы su записываются в журнал.',
  '<code>userdel -r</code> удаляет запись и домашний каталог; файлы в других каталогах остаются.',
],
report='Снимок ls -ld /srv/devteam и снимок проверки доступа трёх пользователей.',
bridge='Все команды разобраны. Дальше — практическая работа: модель доступа для команды мобильного приложения.',
)
