from blocks import *

CHAPTER = dict(
slug='05-sozdanie', layer='Создание', color='#3D8C6A',
title='Создание пользователей и групп',
lead='В этой части создадим учётные записи для условной команды разработки: двух разработчиков anna и boris, тестировщика viktor и общую группу devteam, а затем проверим, что изменилось в системных файлах.',
sections=[
dict(title='Группа и пользователь: groupadd и useradd', blocks=[
  P('Условная команда разработки состоит из двух разработчиков, Анны и Бориса, и тестировщика Виктора. Разработчикам нужен общий каталог с исходным кодом, тестировщику доступ к нему не нужен. Для этого создадим группу <code>devteam</code> и три учётные записи. Все команды этой части меняют системные файлы, поэтому выполняются через <code>sudo</code>.'),
  P('Команда <code>groupadd</code> создаёт группу: добавляет строку в <code>/etc/group</code> и выдаёт свободный GID. Команда <code>useradd</code> создаёт учётную запись. Без ключей она создаёт только строку в <code>/etc/passwd</code>, поэтому нужные свойства указывают ключами.'),
  TABLE(['Ключ useradd', 'Что делает'], [
    ['<code>-m</code>', 'создать домашний каталог <code>/home/имя</code> и скопировать в него файлы из <code>/etc/skel</code>'],
    ['<code>-s /bin/bash</code>', 'оболочка после входа; без ключа в Ubuntu записывается <code>/bin/sh</code>'],
    ['<code>-c "Anna Developer"</code>', 'комментарий: полное имя'],
    ['<code>-G группы</code>', 'дополнительные группы через запятую'],
  ]),
  P('Каталог <code>/etc/skel</code> (skeleton — «каркас») содержит файлы, которые получает каждый новый пользователь: настройки оболочки <code>.bashrc</code>, <code>.profile</code> и <code>.bash_logout</code>. Если администратору нужно, чтобы у всех новых пользователей была одинаковая настройка, её кладут в <code>/etc/skel</code>.'),
  PROBE('Группа devteam и пользователь anna', [
    ('sudo groupadd devteam', 'новая группа'),
    ('sudo useradd -m -s /bin/bash -c "Anna Developer" anna', 'учётная запись с домашним каталогом'),
    ('getent passwd anna', ''),
    ('getent group devteam', ''),
    ('sudo ls -la /home/anna', 'домашний каталог anna'),
    ('ls -la /etc/skel', 'откуда взялись файлы'),
  ], 'у anna UID 1002 и GID 1003; группа devteam создана без участников; в домашнем каталоге три файла из <code>/etc/skel</code>.'),
  CORE('09-useradd', 'useradd создал учётную запись, личную группу и домашний каталог с файлами из /etc/skel.',
    LENS(0, 19, [(3, 7, 11, 1), (3, 12, 16, 2), (5, 10, 14, 3), (8, 13, 22, 4), (10, 41, 53, 5), (17, 41, 53, 5)],
      ['UID 1002: номер 1001 занят записью installer Live-сеанса', 'личная группа anna получила GID 1003: номер 1002 уже у devteam', 'группа devteam создана без участников', 'домашний каталог принадлежит anna', 'файлы скопированы из /etc/skel'], cols=64)),
  P('На снимке видно, что UID и GID одной учётной записи не обязаны совпадать. Номер группы anna — 1003, потому что 1002 уже заняла группа devteam. Система работает с номерами, и такое расхождение ни на что не влияет.'),
]),
dict(title='Интерактивное создание: adduser', blocks=[
  P('В Ubuntu есть вторая команда — <code>adduser</code>. Это программа-надстройка над <code>useradd</code>: она сама создаёт личную группу и домашний каталог, выбирает оболочку, запрашивает пароль и полное имя. Ключи запоминать не нужно, поэтому для ручного создания пользователей удобнее <code>adduser</code>, а в скриптах пишут <code>useradd</code> с ключами.'),
  P('Пароль вводится дважды, набранные символы на экране не отображаются — даже звёздочками. Остальные вопросы можно пропустить клавишей <kbd>Enter</kbd>. Пустые поля комментария сохраняются запятыми: в строке boris будет <code>Boris Developer,,,</code>.'),
  PROBE('Пользователь boris через adduser', [
    ('sudo adduser boris', 'создание с вопросами'),
    ('KEY:Lab-2026-pass', 'пароль дважды; символы не видны'),
    ('KEY:Boris Developer', 'полное имя; остальные вопросы — Enter'),
    ('KEY:y', 'подтвердить данные'),
  ], '<code>adduser</code> создал группу и пользователя boris с номером 1004, домашний каталог и добавил boris в группу <code>users</code>.'),
  CORE('10-adduser', 'adduser создаёт учётную запись по шагам и задаёт вопросы.',
    LENS(0, 19, [(3, 0, 41, 1), (4, 0, 66, 1), (5, 0, 47, 2), (7, 0, 13, 3), (8, 0, 20, 3), (12, 8, 37, 4), (18, 0, 76, 5)],
      ['adduser создаёт личную группу и пользователя с одинаковым номером', 'домашний каталог и файлы из /etc/skel', 'пароль запрашивается дважды; набранные символы не видны', 'комментарий: полное имя', 'пользователь добавлен в группу users'], cols=80)),
  NOTE('Пароль <code>Lab-2026-pass</code> используется только в учебной виртуальной машине. Для настоящих учётных записей пароль придумывают отдельно для каждой.'),
]),
dict(title='Пароль и дополнительные группы', blocks=[
  P('Учётная запись, созданная <code>useradd</code>, пароля не имеет, и войти под ней нельзя. Пароль задаёт команда <code>passwd имя</code>. Обычный пользователь командой <code>passwd</code> без имени меняет только свой пароль и сначала вводит старый; администратор через <code>sudo</code> задаёт пароль любому пользователю без старого.'),
  P('В группу пользователя добавляет команда <code>usermod -aG группа имя</code>. Ключ <code>-G</code> задаёт дополнительные группы, ключ <code>-a</code> (append — «добавить») означает, что новая группа добавляется к уже существующим. Членство вступает в силу при следующем входе пользователя в систему: процессы, запущенные раньше, продолжают работать со старым списком групп.'),
  PROBE('Пароли и группа devteam', [
    ('sudo passwd anna', 'пароль anna'),
    ('KEY:Lab-2026-pass', 'пароль дважды'),
    ('sudo useradd -m -s /bin/bash -c "Viktor Tester" viktor', 'тестировщик'),
    ('sudo passwd viktor', ''),
    ('KEY:Lab-2026-pass', 'пароль дважды'),
    ('sudo usermod -aG devteam anna', 'anna в devteam'),
    ('sudo usermod -aG devteam boris', 'boris в devteam'),
    ('id anna', ''),
    ('id boris', ''),
    ('getent group devteam', 'участники группы'),
  ], 'в списке групп anna и boris появилась <code>devteam</code>; в строке группы два участника.'),
  CORE('11-passwd-usermod', 'Пароли заданы, anna и boris добавлены в группу devteam.',
    LENS(0, 16, [(3, 0, 37, 1), (9, 30, 33, 2), (12, 48, 61, 3), (14, 62, 75, 3), (16, 15, 25, 4)],
      ['пароль сохранён', '-aG: добавить группу к уже имеющимся', 'devteam появилась в списке групп anna и boris', 'участники группы в /etc/group'], cols=80)),
]),
dict(title='Ключ -a в usermod', blocks=[
  P('Ключ <code>-G</code> без <code>-a</code> работает иначе: он задаёт полный список дополнительных групп. Все группы, которых нет в списке, у пользователя пропадают. Если так ошибиться с администратором, он потеряет группу <code>sudo</code> и вместе с ней возможность исправить ошибку.'),
  P('На снимке viktor сначала получает группу <code>testers</code> правильной командой. Затем <code>usermod -G devteam viktor</code> без <code>-a</code> заменяет весь список: <code>testers</code> пропадает. Последняя команда задаёт список заново — только <code>testers</code>.'),
  WARN('Добавляйте пользователя в группу только командой <code>usermod -aG</code>. Перед изменением групп администратора проверьте команду ещё раз: без <code>-a</code> он потеряет группу <code>sudo</code>.'),
  PROBE('usermod -G без -a', [
    ('sudo groupadd testers', 'группа тестировщиков'),
    ('sudo usermod -aG testers viktor', 'правильно: с -a'),
    ('id viktor', ''),
    ('sudo usermod -G devteam viktor', 'ошибка: без -a'),
    ('id viktor', 'testers пропала'),
    ('sudo usermod -G testers viktor', 'исправить: полный список групп'),
    ('id viktor', ''),
  ], 'после <code>-G devteam</code> у viktor осталась только devteam; после исправления — только testers.'),
  CORE('12-usermod-trap', 'Без ключа -a команда usermod -G заменяет весь список групп.',
    LENS(0, 9, [(3, 54, 67, 1), (4, 30, 32, 2), (6, 54, 67, 3), (9, 54, 67, 4)],
      ['после -aG у viktor есть группа testers', 'usermod -G без -a', 'testers пропала, список заменён на devteam', 'исправление: -G с нужным списком групп'], cols=72)),
]),
dict(title='Что изменилось в системных файлах', blocks=[
  P('Команды <code>useradd</code>, <code>adduser</code>, <code>passwd</code> и <code>usermod</code> меняют те же файлы, которые мы читали в частях 1–3. Новые строки добавляются в конец файлов, поэтому их выводит команда <code>tail</code>: <code>tail -n 3</code> — последние три строки.'),
  PROBE('Новые строки', [
    ('tail -n 3 /etc/passwd', 'три новые учётные записи'),
    ('tail -n 5 /etc/group', 'новые группы'),
    ('sudo tail -n 3 /etc/shadow', 'хеши паролей'),
    ('ls -l /home', 'домашние каталоги'),
  ], 'в каждом файле появились строки anna, boris и viktor; хеши паролей начинаются с <code>$y$</code> и различаются, хотя пароль у всех трёх один.'),
  CORE('13-etc-after', 'Новые строки в /etc/passwd, /etc/group и /etc/shadow и домашние каталоги.',
    LENS(0, 20, [(2, 18, 36, 1), (5, 0, 25, 2), (9, 0, 21, 2), (11, 5, 8, 3), (11, 12, 34, 4), (11, 35, 78, 5), (16, 0, 10, 6)],
      ['пустые поля комментария сохранены запятыми', 'общая группа devteam и группа testers', '$y$ — алгоритм yescrypt', 'соль — своя у каждой записи', 'хеш: одинаковый пароль дал три разных хеша', 'домашние каталоги закрыты для остальных: drwxr-x---'], cols=120)),
]),
],
summary=[
  '<code>groupadd</code> создаёт группу; <code>useradd -m -s /bin/bash -c "…"</code> — учётную запись с домашним каталогом.',
  '<code>adduser</code> создаёт запись по шагам с вопросами; в скриптах пишут <code>useradd</code>.',
  '<code>passwd имя</code> задаёт пароль; <code>usermod -aG группа имя</code> добавляет в группу.',
  '<code>usermod -G</code> без <code>-a</code> заменяет весь список дополнительных групп.',
  'UID и GID одной записи могут не совпадать; одинаковые пароли дают разные хеши.',
],
report='Снимок id anna и id boris с группой devteam и снимок tail /etc/group.',
bridge='Учётные записи и группа готовы. Дальше — общий каталог, в котором anna и boris работают вместе, а viktor доступа не имеет.',
)
