from blocks import *

CHAPTER = dict(
slug='03-udalenie', layer='Удаление', color='#9C4A2F',
title='Удаление файлов и имена с пробелами',
lead='В этой части разберём команды rm и rmdir, порядок безопасного удаления и то, как оболочка делит на части имя с пробелом.',
sections=[
dict(title='Удаление: rm и rmdir', blocks=[
  P('Команда <code>rm</code> (remove — «удалить») удаляет имя файла из каталога. Если это было последнее имя файла, система освобождает место, которое занимали его данные. Корзины у <code>rm</code> нет. Корзина — функция графического файлового менеджера: при удалении он переносит файл в каталог <code>~/.local/share/Trash</code>. Файл, удалённый в терминале, туда не попадает, и восстановить его обычными средствами нельзя.'),
  TERM('rm', 'Команда, которая удаляет имена файлов из каталога. Удалённые файлы не попадают в корзину.', 'Удаление в файловом менеджере: там файл сначала переносится в корзину и его можно вернуть.'),
  P('Без ключей <code>rm</code> удаляет только файлы. Для каталога есть два способа. Команда <code>rmdir</code> удаляет только пустой каталог и отказывается, если внутри что-то есть. Команда <code>rm -r</code> удаляет каталог вместе со всем содержимым: сначала файлы, затем вложенные каталоги и в конце сам каталог. Вывод <code>rm -rv</code> показывает этот порядок по строкам.'),
  P('Ключи <code>-i</code> и <code>-v</code> работают так же, как у <code>cp</code>: первый спрашивает про каждый файл, второй печатает каждое удаление. Ключ <code>-f</code> (force — «принудительно») действует наоборот: не задаёт вопросов и не сообщает об ошибке, если файла нет. Сочетание <code>rm -rf</code> часто встречается в инструкциях. Оно не задаёт вопросов и не выводит предупреждений, поэтому опечатка в пути приводит к удалению другого каталога.'),
  WARN('Не запускайте <code>rm -rf</code> с путём от корня <code>/</code> или с переменной, значение которой вы не проверили. Перед удалением каталога выведите его содержимое командой <code>ls</code> и убедитесь, что это нужный каталог.'),
  PROBE('Удаление файлов и каталога', [
    ('cd ~/files-lab/inbox', ''),
    ('rm -v data-old.csv', 'удалить и сообщить'),
    ('rm -i data.csv.bak', 'спросить перед удалением'),
    ('KEY:y', 'ответ: удалить'),
    ('rm ../backup', 'каталог без -r'),
    ('rmdir ../backup', 'каталог не пуст'),
    ('rm -rv ../backup', 'каталог со всем содержимым'),
    ('ls ..', 'backup больше нет'),
  ], '<code>rm</code> без <code>-r</code> и <code>rmdir</code> отказываются удалять непустой каталог; <code>rm -rv</code> удаляет файлы, затем каталоги.'),
  CORE('06-rm', 'rm удаляет файлы, rmdir — только пустые каталоги, rm -r — каталог с содержимым.',
    LENS(1, 18, [(2, 0, 22, 1), (4, 0, 41, 2), (6, 0, 45, 3), (8, 0, 56, 4), (10, 0, 43, 5), (16, 0, 29, 5), (18, 0, 19, 6)],
      ['-v сообщает об удалённом файле', '-i спрашивает перед удалением', 'без -r rm не удаляет каталог', 'rmdir удаляет только пустой каталог', 'rm -r удаляет сначала файлы, в конце сам каталог', 'в ~/files-lab каталога backup больше нет'], cols=64)),
  STEPS('Безопасное удаление', [
    'Проверьте, в каком каталоге вы находитесь: <code>pwd</code>.',
    'Выведите то, что собираетесь удалить, той же записью: <code>ls путь</code> для файла или каталога.',
    'Удаляйте с ключом <code>-v</code>, а если файлов немного — с ключом <code>-i</code>.',
    'Проверьте результат командой <code>ls</code>: удалённых имён в выводе нет.',
  ]),
]),
dict(title='Имена с пробелами', blocks=[
  P('Оболочка делит введённую строку на слова по пробелам: первое слово — имя команды, остальные — аргументы. Имя <code>my report.txt</code> без кавычек превращается в два аргумента: <code>my</code> и <code>report.txt</code>. Команда <code>ls</code> ищет два отдельных файла и не находит ни одного.'),
  P('С <code>ls</code> такая ошибка безвредна. С <code>rm</code> та же запись опасна: <code>rm my report.txt</code> удалит файл <code>report.txt</code>, если он есть в каталоге, а файл <code>my report.txt</code> останется на месте.'),
  P('Сохранить пробел внутри имени можно двумя способами. Кавычки <code>"my report.txt"</code> объединяют всё, что стоит между ними, в один аргумент. Обратная косая черта перед пробелом <code>my\\ report.txt</code> отменяет особое значение этого пробела. Клавиша <kbd>Tab</kbd> дополняет имя файла и сама ставит нужные символы. В именах собственных файлов пробелы заменяют дефисом или подчёркиванием, тогда кавычки не нужны.'),
  PROBE('Имя с пробелом', [
    ('cd ~/files-lab/inbox', ''),
    ('ls -l my report.txt', 'два аргумента вместо одного'),
    ('ls -l "my report.txt"', 'имя в кавычках'),
    ('ls -l my\\ report.txt', 'пробел после обратной косой черты'),
    ('mv -v "my report.txt" my-report.txt', 'имя без пробела'),
    ('ls -l my*', ''),
  ], 'без кавычек две ошибки <code>cannot access</code>; с кавычками и с <code>\\</code> найден один файл; после <code>mv</code> имя без пробела.'),
  CORE('07-space', 'Без кавычек оболочка делит имя с пробелом на два аргумента.',
    LENS(1, 11, [(2, 0, 49, 1), (3, 0, 57, 1), (5, 42, 57, 2), (7, 42, 57, 2), (9, 0, 42, 3)],
      ['без кавычек ls ищет два файла: my и report.txt', 'кавычки и обратная косая черта сохраняют пробел в имени', 'после переименования кавычки не нужны'], cols=64)),
]),
],
summary=[
  '<code>rm</code> удаляет файлы без корзины; <code>rmdir</code> удаляет только пустой каталог; <code>rm -r</code> — каталог с содержимым.',
  'Перед удалением содержимое выводят командой <code>ls</code> с тем же путём.',
  'Ключ <code>-i</code> спрашивает перед каждым удалением, <code>-f</code> отключает вопросы и сообщения об ошибках.',
  'Имя с пробелом берут в кавычки или ставят перед пробелом <code>\\</code>.',
],
report='Снимок с сообщением rmdir и снимок с именем my report.txt.',
bridge='До сих пор мы указывали каждое имя отдельно. Дальше — маски, которые выбирают группу файлов по шаблону.',
)
