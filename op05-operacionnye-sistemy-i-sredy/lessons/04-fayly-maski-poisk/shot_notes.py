# Выводы после снимков: что показывает снимок и где это пригодится.
# Ключ — «номер части:имя снимка», для повторного снимка в той же части — «:2».

NOTES = {
'1:01-mkdir': ('<code>mkdir</code> без <code>-p</code> не смог создать <code>site/css</code>, потому что каталога <code>site</code> не было; с <code>-p</code> созданы все каталоги, а <code>tree</code> нарисовал результат.',
               'Структуру нового проекта — каталоги для исходного кода, стилей, изображений — создают одной командой <code>mkdir -p</code>. Её же пишут в скриптах установки: повторный запуск не вызывает ошибки.'),
'1:02-touch': ('Первый <code>touch</code> изменил только время файла, размер остался 0; <code>touch -d</code> записал дату 15 января 2026 года.',
               'Время изменения используют программы резервного копирования и системы сборки. <code>touch</code> помогает отметить файл как изменённый или подготовить тестовые данные с нужными датами.'),
'1:03-setup': ('Скрипт создал 28 файлов; скрытый <code>.settings</code> появился только в выводе <code>ls -A</code>; <code>ls -lh</code> показал размеры 5.0M и 40K.',
               'Скрытые файлы с точкой — это обычно настройки программ. Если в каталоге «нет» файла, о котором говорит инструкция, сначала проверяют <code>ls -A</code>.'),

'2:04-cp': ('<code>cp -v</code> печатает каждое копирование; каталог без <code>-r</code> пропущен; <code>cp -i</code> спросил о замене, и после ответа <code>n</code> файл остался прежним.',
            'Перед правкой конфигурационного файла делают его копию: <code>cp -v app.conf app.conf.bak</code>. Ключ <code>-i</code> защищает от случайной замены файла с тем же именем.'),
'2:05-mv': ('<code>mv</code> переименовал файл в том же каталоге и перенёс два журнала в другой каталог; на вопрос о замене <code>notes-2026.md</code> дан ответ <code>n</code>.',
            'Переименование и сортировка файлов по каталогам — повседневная работа с загрузками, журналами и отчётами. <code>mv -i</code> не даст случайно затереть файл с тем же именем.'),
'2:08-ln': ('Ссылка <code>latest-report.txt</code> имеет тип <code>l</code> и хранит путь; копия, сделанная через ссылку, — обычный файл с текстом отчёта.',
            'Ссылки на «текущую» версию файла используют для отчётов, конфигураций и версий программ. Копия нужна, когда файл должен сохраниться неизменным.'),

'3:06-rm': ('<code>rm -v</code> и <code>rm -i</code> удалили файлы; <code>rm</code> без <code>-r</code> и <code>rmdir</code> отказались удалять непустой каталог; <code>rm -rv</code> удалил его, начиная с файлов.',
            'Отказ <code>rmdir</code> — полезная проверка: каталог удаляется, только если он действительно пуст. <code>rm -r</code> применяют к каталогам сборки и временным каталогам после проверки содержимого.'),
'3:07-space': ('Без кавычек <code>ls</code> получил два аргумента и сообщил о двух ошибках; с кавычками и с <code>\\</code> найден один файл.',
               'Файлы с пробелами приходят из Windows и из загрузок браузера. В скриптах имена всегда берут в кавычки: <code>"$file"</code>, иначе команда обработает не тот файл.'),

'4:09-glob': ('Каждая из пяти масок выбрала свою группу файлов: по окончанию, по одному символу, по диапазону, по исключению и по двум вариантам окончания.',
              'Маски позволяют одной командой скопировать все фотографии, перенести журналы за месяц или удалить временные файлы определённого вида.'),
'4:10-glob-echo': ('<code>echo</code> напечатал имена, которые получает команда; маска без совпадений и маска в кавычках остались без изменений; скрытый файл нашла только маска <code>.*</code>.',
                   'Если команда с маской ведёт себя неожиданно, её аргументы проверяют через <code>echo</code>: сразу видно, что подставила оболочка.'),
'4:11-glob-rm': ('<code>ls</code> и <code>rm</code> с одной маской обработали одинаковые два файла; повторный <code>ls</code> подтвердил удаление.',
                 'Этот порядок — сначала <code>ls</code>, потом <code>rm</code> с той же маской — применяют при любой чистке каталога: журналов, временных файлов, старых сборок.'),

'5:12-find-name': ('<code>find</code> нашёл файлы .csv и в <code>inbox</code>, и в <code>archive/2025</code>; <code>-iname</code> нашёл <code>README.TXT</code> по маске строчными буквами; <code>-type d</code> вывел только каталоги.',
                   'Когда известно имя или окончание файла, но неизвестен каталог, <code>find</code> обходит всё дерево проекта или домашнего каталога.'),
'5:12-find-name:2': ('Маска без кавычек раскрылась в каталоге inbox раньше, чем запустился <code>find</code>, и команда завершилась ошибкой; в кавычках маска дошла до <code>find</code>.',
                     'Сообщение <code>paths must precede expression</code> почти всегда означает маску без кавычек. <code>find</code> сам подсказывает это второй строкой.'),
'5:13-find-size-time': ('Условие <code>-size -1M</code> не нашло ни одного файла из-за округления вверх; с верхней границей <code>-500k</code> найдены четыре изображения.',
                        'Поиск больших файлов — первое действие, когда на диске заканчивается место: <code>find ~ -type f -size +100M</code>.'),
'5:13-find-size-time:2': ('<code>-mtime +100</code> нашёл файлы от 10 мая, <code>-mtime +30</code> — ещё и файлы от 1 августа.',
                          'По возрасту выбирают старые журналы и резервные копии для архивации или удаления, например <code>find /var/log -name "*.gz" -mtime +30</code>.'),
'5:14-find-exec': ('<code>-exec ls -l</code> вывел подробные строки трёх журналов; в системных каталогах появились сообщения <code>Permission denied</code>, а <code>2&gt;/dev/null</code> их убрал.',
                   '<code>-exec</code> применяют, чтобы для всех найденных файлов выполнить одну команду: вывести сведения, упаковать в архив, изменить права.'),
'5:14-find-exec:2': ('Сначала <code>find</code> показал файл, затем удалил его действием <code>-delete</code>; повторный поиск ничего не нашёл.',
                     'Так чистят каталоги от временных файлов по условию: сначала проверяют список, затем добавляют <code>-delete</code>.'),
'5:15-locate': ('Новый файл <code>locate</code> нашёл только после <code>updatedb</code>, а после удаления файла продолжал выводить его путь; <code>locate -e</code> вывел только существующие файлы.',
                '<code>locate</code> удобен, чтобы быстро узнать, где в системе лежит файл с известным именем, например конфигурация программы. Для свежих изменений используют <code>find</code>.'),
'5:16-which': ('<code>which</code> вывел пути к программам и ничего не вывел для <code>cd</code>; <code>type</code> назвал псевдоним, встроенную команду и файл.',
               'Если команда ведёт себя не так, как описано в справке, проверяют <code>type</code>: имя могло оказаться псевдонимом или другой программой из PATH.'),

'6:17-man-ls': ('Страница справки ls начинается с разделов NAME, SYNOPSIS и DESCRIPTION; у ключей есть короткая и длинная запись.',
                'Прежде чем искать ответ в интернете, открывают <code>man</code>: там описана установленная версия программы.'),
'6:17-man-ls:2': ('Строка состояния показывает имя страницы и номер строки, а также напоминает клавиши <kbd>h</kbd> и <kbd>q</kbd>.',
                  'Если справка открылась и терминал не реагирует на команды, это страница просмотра: из неё выходят клавишей <kbd>q</kbd>.'),
'6:18-man-search': ('Поиск по образцу <code>sort by file size</code> привёл к ключу <code>-S</code>, найденные слова подсвечены.',
                    'Поиск по описанию действия быстрее, чем чтение страницы целиком: так находят ключи у <code>find</code>, <code>cp</code>, <code>tar</code> и других команд.'),
'6:19-man-f': ('У имени passwd три страницы в разных разделах; <code>man -k</code> нашёл команду <code>cp</code> по описанию; для <code>cd</code> страницы нет.',
               '<code>man -k</code> помогает найти команду, когда известно только действие. Сообщение <code>No manual entry</code> — повод проверить имя командой <code>type</code>.'),
'6:20-man5': ('Раздел 5 описывает формат файла /etc/passwd: одна строка на учётную запись и семь полей через двоеточие.',
              'Перед ручной правкой любого конфигурационного файла читают его страницу в разделе 5: <code>man 5 fstab</code>, <code>man 5 crontab</code>.'),
'6:21-help': ('<code>ls --help</code> вывел краткую справку в терминал, <code>help cd</code> — справку по встроенной команде; <code>type</code> подтвердил, что <code>help</code> встроена в оболочку.',
              'Краткую справку открывают, когда нужно только вспомнить написание ключа. Для встроенных команд <code>cd</code>, <code>history</code>, <code>type</code> справку выводит <code>help</code>.'),

'7:22-history': ('<code>history</code> вывел последние команды с номерами, а перенаправление записало тот же список в файл <code>commands.txt</code>.',
                 'История помогает восстановить, какие команды были выполнены, — для журнала работы, отчёта или разбора ошибки.'),
'7:23-quest-tree': ('В каталоге sorted четыре подкаталога: 2 таблицы, 7 документов, 6 изображений и 4 журнала — всего 19 файлов.',
                    'Сравните дерево своего каталога с эталоном: число файлов в каждом подкаталоге должно совпасть.'),
'7:24-quest-check': ('Скрипт проверил каталог по 12 пунктам и вывел <code>Итог: 12 из 12</code>.',
                     'Проверка скриптом показывает результат сразу. Если какой-то пункт выводит <code>НЕТ</code>, вернитесь к заданию с этим номером проверки.'),
}
