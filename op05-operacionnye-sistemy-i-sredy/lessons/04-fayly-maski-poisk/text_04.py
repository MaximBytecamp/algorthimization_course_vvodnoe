from blocks import *

CHAPTER = dict(
slug='04-maski', layer='Маски', color='#B7791F',
title='Маски имён файлов',
lead='В этой части разберём символы масок, порядок, в котором оболочка раскрывает маску, и способ проверить маску до удаления файлов.',
sections=[
dict(title='Символы масок', blocks=[
  P('В каталоге inbox пять отчётов за разные месяцы, шесть изображений и четыре журнала. Перечислять их по одному долго, а при сотне файлов и вовсе невозможно. Для таких случаев в оболочке есть маски — шаблоны имени со специальными символами.'),
  TERM('Маска (glob)', 'Шаблон имени файла, в котором символы <code>*</code>, <code>?</code> и <code>[ ]</code> заменяют другие символы. Оболочка заменяет маску списком подходящих имён из каталога до запуска команды.', 'Регулярные выражения команды <code>grep</code>: у них другие правила, и <code>*</code> в них означает повторение предыдущего символа.'),
  TABLE(['Запись', 'Что означает', 'Пример из inbox'], [
    ['<code>*</code>', 'любое число любых символов, в том числе ни одного', '<code>*.txt</code> — все имена с окончанием .txt'],
    ['<code>?</code>', 'ровно один любой символ', '<code>report-2026-0?.txt</code> — отчёты за 01, 02 и 03'],
    ['<code>[1-3]</code>', 'один символ из перечисленных или из диапазона', '<code>photo-00[1-3].jpg</code> — три первых фотографии'],
    ['<code>[!0]</code>', 'один любой символ, кроме перечисленных', '<code>report-2026-[!0]?.txt</code> — отчёты за 10 и 11'],
    ['<code>{jpg,png}</code>', 'каждый вариант из списка по очереди', '<code>*.{jpg,png}</code> — изображения обоих форматов'],
  ]),
  P('Фигурные скобки формально к маскам не относятся. Оболочка сначала превращает <code>*.{jpg,png}</code> в два слова <code>*.jpg</code> и <code>*.png</code>, а затем раскрывает каждое как маску. Этот приём мы применяли на прошлом занятии в команде <code>mkdir -p ~/linux-fhs-lab/{config,data,logs,tmp,screenshots}</code>: там скобки дали пять путей.'),
  P('Маска похожа на запрос в библиотечном каталоге: «все книги, название которых начинается на „Отчёт“ и заканчивается на „2026“». Библиотекарь находит подходящие карточки и выдаёт читателю готовую стопку книг.'),
  PROBE('Маски в каталоге inbox', [
    ('cd ~/files-lab', ''),
    ('bash ~/Downloads/files-lab-setup.sh', 'вернуть набор в исходное состояние'),
    ('cd inbox', ''),
    ('ls *.txt', 'все .txt'),
    ('ls report-2026-0?.txt', 'один символ на месте ?'),
    ('ls photo-00[1-3].jpg', 'символ из диапазона'),
    ('ls report-2026-[!0]?.txt', 'первая цифра месяца не 0'),
    ('ls *.{jpg,png}', 'два окончания'),
  ], 'каждая маска выбирает свою группу файлов; <code>*.{jpg,png}</code> выводит пять фотографий jpg и одну png.'),
  CORE('09-glob', 'Пять масок и файлы, которые под них подошли.',
    LENS(4, 14, [(4, 36, 41, 1), (7, 49, 50, 2), (9, 44, 49, 3), (11, 48, 52, 4), (13, 38, 47, 5)],
      ['* — любое число любых символов', '? — ровно один символ', '[1-3] — один символ из диапазона', '[!0] — любой символ, кроме 0', '{jpg,png} — оболочка подставляет оба варианта'], cols=92)),
]),
dict(title='Маску раскрывает оболочка', blocks=[
  P('Команды <code>ls</code>, <code>cp</code> и <code>rm</code> масок не обрабатывают. Маску раскрывает оболочка bash. Получив строку, она находит в ней слова со специальными символами, сравнивает их с именами в каталоге и подставляет вместо маски список совпавших имён. Команда запускается уже с готовым списком аргументов.'),
  P('Результат раскрытия показывает команда <code>echo</code>, которая печатает свои аргументы. <code>echo *.log</code> печатает имена <code>app.log error.log</code>, звёздочки в выводе нет. Эти же аргументы получит любая другая команда с такой маской.'),
  TABLE(['Случай', 'Что делает оболочка'], [
    ['совпадений нет: <code>*.docx</code>', 'оставляет маску как есть; команда ищет файл с именем <code>*.docx</code> и сообщает, что его нет'],
    ['маска в кавычках: <code>"*.log"</code>', 'не раскрывает маску; команда получает символы <code>*.log</code>'],
    ['имя начинается с точки', '<code>*</code> и <code>?</code> не совпадают с точкой в начале имени; скрытые файлы находит маска, которая сама начинается с точки: <code>.*</code>'],
    ['порядок имён', 'имена подставляются по алфавиту; заглавные буквы идут раньше строчных'],
  ]),
  PROBE('Что получает команда', [
    ('cd ~/files-lab/inbox', ''),
    ('echo *.log', 'имена вместо маски'),
    ('echo app.log*', ''),
    ('echo *.docx', 'совпадений нет'),
    ('ls *.docx', ''),
    ('echo "*.log"', 'маска в кавычках'),
    ('echo .*', 'скрытые файлы'),
    ('echo *', 'все имена, кроме скрытых'),
  ], '<code>echo</code> печатает имена файлов; маска без совпадений и маска в кавычках печатаются как есть; в выводе <code>echo *</code> нет <code>.settings</code>.'),
  CORE('10-glob-echo', 'echo показывает, какие аргументы оболочка передаёт команде.',
    LENS(1, 16, [(2, 0, 17, 1), (6, 0, 6, 2), (8, 0, 53, 2), (10, 0, 5, 3), (12, 0, 9, 4), (14, 0, 10, 5)],
      ['оболочка заменила маску именами файлов', 'совпадений нет: маска передана команде как есть', 'в кавычках маска не раскрывается', '.* находит скрытый файл', 'список * начинается с README.TXT, скрытого .settings в нём нет'], cols=120)),
  P('На схеме ниже можно ввести команду с маской и пройти три шага: деление строки на слова, сравнение каждой маски с именами каталога inbox и список аргументов, который получит команда. Имена файлов на схеме совпадают с учебным набором.'),
  W('expand'),
]),
dict(title='Проверка маски перед удалением', blocks=[
  P('Маску раскрывает оболочка, поэтому одна и та же маска у <code>ls</code> и у <code>rm</code> даёт одинаковый список файлов. На этом основан безопасный порядок удаления по маске: сначала выполнить <code>ls</code> с маской и прочитать список, затем заменить в той же строке <code>ls</code> на <code>rm</code>. Набирать строку заново не нужно: клавиша <kbd>↑</kbd> возвращает предыдущую команду, сочетание <kbd>Ctrl</kbd>+<kbd>A</kbd> переводит курсор в начало строки.'),
  WARN('Лишний пробел меняет смысл маски: <code>rm *.bak</code> удаляет резервные копии, а <code>rm * .bak</code> удаляет все файлы каталога и затем пытается удалить файл <code>.bak</code>.'),
  PROBE('Проверка и удаление по маске', [
    ('cd ~/files-lab/inbox', ''),
    ('ls *.bak', 'что попадёт под маску'),
    ('rm -v *.bak', 'та же маска'),
    ('ls *.bak', 'проверка результата'),
  ], '<code>ls</code> и <code>rm</code> обработали одни и те же два файла; повторная проверка сообщает, что файлов <code>.bak</code> нет.'),
  CORE('11-glob-rm', 'Сначала ls с маской, затем rm с той же маской.',
    LENS(1, 7, [(2, 0, 29, 1), (4, 0, 25, 2), (5, 0, 22, 2), (7, 0, 52, 3)],
      ['ls показал файлы, подходящие под маску', 'rm удалил те же два файла', 'повторная проверка: файлов .bak больше нет'], cols=60)),
]),
],
summary=[
  '<code>*</code> — любые символы, <code>?</code> — один символ, <code>[ ]</code> — один символ из набора, <code>{a,b}</code> — варианты.',
  'Маску раскрывает оболочка до запуска команды; <code>echo</code> показывает результат раскрытия.',
  'Маска без совпадений и маска в кавычках передаются команде без изменений; <code>*</code> не находит скрытые файлы.',
  'Перед <code>rm</code> маску проверяют командой <code>ls</code> с той же маской.',
],
report='Снимок пяти масок и снимок echo с маской без совпадений.',
bridge='Маска работает в одном каталоге. Чтобы найти файлы во всём дереве каталогов, нужна команда find.',
)
