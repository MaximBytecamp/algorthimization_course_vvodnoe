from blocks import *

CHAPTER = dict(
slug='06-spravka', layer='Справка', color='#5B4A8B',
title='Справочная система',
lead='В этой части разберём встроенную справку Linux: страницы man и их разделы, поиск по странице, краткую справку --help и справку по встроенным командам оболочки.',
sections=[
dict(title='Страница справки: man', blocks=[
  P('Запоминать все ключи всех команд не нужно. В Linux описание каждой программы устанавливается вместе с ней и открывается командой <code>man</code> (manual — «руководство»). Справка работает без интернета и описывает ту версию программы, которая установлена в системе. На прошлом занятии мы уже открывали страницу <code>man hier</code> с описанием каталогов.'),
  P('Страница открывается на весь терминал в программе просмотра <code>less</code>. Структура у всех страниц одинаковая, поэтому нужный раздел быстро находится на любой из них.'),
  TABLE(['Раздел страницы', 'Содержание'], [
    ['NAME', 'имя команды и её назначение одной строкой'],
    ['SYNOPSIS', 'как записывается команда: в квадратных скобках необязательные части, многоточие означает «можно повторить»'],
    ['DESCRIPTION', 'подробное описание и список ключей'],
    ['EXAMPLES', 'примеры использования; есть не на всех страницах'],
    ['SEE ALSO', 'связанные страницы'],
  ]),
  P('Запись <code>ls [OPTION]... [FILE]...</code> в разделе SYNOPSIS читается так: команда <code>ls</code>, затем любое число ключей, затем любое число файлов, причём и ключи, и файлы можно не указывать. У каждого ключа на странице есть описание; многие ключи записываются в двух видах — коротком (<code>-a</code>) и длинном (<code>--all</code>), оба действуют одинаково.'),
  TABLE(['Клавиша', 'Действие'], [
    ['<kbd>Пробел</kbd> и <kbd>b</kbd>', 'страница вперёд и назад'],
    ['<kbd>↓</kbd> и <kbd>↑</kbd>', 'строка вперёд и назад'],
    ['<kbd>/</kbd> образец <kbd>Enter</kbd>', 'поиск по тексту вперёд'],
    ['<kbd>n</kbd> и <kbd>N</kbd>', 'следующее и предыдущее совпадение'],
    ['<kbd>g</kbd> и <kbd>G</kbd>', 'начало и конец страницы'],
    ['<kbd>h</kbd>', 'список всех клавиш'],
    ['<kbd>q</kbd>', 'выход из справки'],
  ]),
  PROBE('Страница справки ls', [
    ('man ls', 'открыть справку; выход — клавиша q'),
  ], 'на весь терминал открывается страница <code>LS(1)</code> с разделами NAME, SYNOPSIS и DESCRIPTION.'),
  CORE('17-man-ls', 'Начало страницы справки команды ls.',
    LENS(0, 16, [(0, 0, 5, 1), (2, 0, 4, 2), (3, 7, 35, 2), (5, 0, 8, 3), (6, 7, 31, 3), (8, 0, 11, 4), (14, 7, 16, 5)],
      ['имя страницы и номер раздела: 1 — команды пользователя', 'NAME — назначение команды одной строкой', 'SYNOPSIS — запись команды; квадратные скобки — необязательные части', 'DESCRIPTION — описание и список ключей', 'короткий и длинный вариант одного ключа'], cols=120)),
  CORE('17-man-ls', 'Строка состояния внизу страницы.',
    LENS(31, 31, [(31, 0, 56, 1)],
      ['имя страницы и номер строки; h — список клавиш, q — выход'], cols=64)),
]),
dict(title='Поиск внутри страницы', blocks=[
  P('Страница <code>ls</code> занимает больше двухсот строк, и листать её ради одного ключа долго. Вместо этого используют поиск: клавиша <kbd>/</kbd>, затем образец и <kbd>Enter</kbd>. Программа прокручивает страницу к первому совпадению и подсвечивает его, клавиша <kbd>n</kbd> переходит к следующему совпадению.'),
  P('Искать удобнее по действию, которое нужно выполнить. Например, нужен ключ, который сортирует файлы по размеру. Образец <code>sort by file size</code> сразу приводит к строке с ключом <code>-S</code>. Строка состояния внизу экрана показывает номер строки и долю пройденной страницы: <code>line 141/226 73%</code>.'),
  STEPS('Найти ключ по описанию действия', [
    'Откройте справку: <code>man ls</code>.',
    'Нажмите <kbd>/</kbd>: внизу экрана появится строка ввода.',
    'Наберите описание действия по-английски, например <code>sort by file size</code>, и нажмите <kbd>Enter</kbd>.',
    'Если совпадение не то, нажимайте <kbd>n</kbd>, пока не найдёте нужную строку.',
    'Нажмите <kbd>q</kbd> и выполните команду с найденным ключом.',
  ]),
  PROBE('Поиск ключа сортировки', [
    ('man ls', ''),
    ('KEY:/sort by file size', 'внутри справки: / и образец, затем Enter'),
  ], 'страница прокручена к ключу <code>-S</code>, найденные слова подсвечены.'),
  CORE('18-man-search', 'Поиск по странице привёл к ключу -S; внизу строка состояния с номером строки.',
    LENS(0, 4, [(0, 7, 9, 1), (0, 14, 31, 2), (2, 7, 18, 3)],
      ['найденный ключ -S', 'подсвечено совпадение с образцом поиска', 'длинный ключ --sort задаёт тот же порядок словом size'], cols=120)),
]),
dict(title='Разделы справки', blocks=[
  P('Страницы справки разделены на пронумерованные разделы. Одно имя может встречаться в нескольких разделах: <code>passwd</code> — это и команда смены пароля (раздел 1), и формат файла <code>/etc/passwd</code> (раздел 5). Номер раздела пишут в скобках после имени: <code>passwd(5)</code>.'),
  TABLE(['Раздел', 'Содержание', 'Примеры'], [
    ['1', 'команды пользователя', '<code>ls(1)</code>, <code>cp(1)</code>, <code>find(1)</code>'],
    ['5', 'форматы файлов', '<code>passwd(5)</code>, <code>fstab(5)</code>'],
    ['7', 'обзоры и соглашения', '<code>hier(7)</code>, <code>glob(7)</code>'],
    ['8', 'команды администрирования', '<code>useradd(8)</code>, <code>mount(8)</code>'],
  ]),
  P('<code>man имя</code> открывает страницу из первого раздела, где она нашлась. Чтобы открыть конкретный раздел, его номер указывают перед именем: <code>man 5 passwd</code>. Ключ <code>-f</code> показывает, в каких разделах есть страница с таким именем. Ключ <code>-k</code> ищет образец в кратких описаниях всех страниц: он помогает, когда имя команды неизвестно, а действие известно, например «copy files».'),
  P('Для встроенных команд оболочки отдельных страниц нет: на <code>man cd</code> система отвечает <code>No manual entry for cd</code>. Описание встроенных команд выводит встроенная команда <code>help</code>.'),
  PROBE('Поиск страниц', [
    ('man -f passwd', 'в каких разделах есть passwd'),
    ('man -k "copy files"', 'поиск по описаниям'),
    ('man cd', 'встроенная команда'),
  ], 'у <code>passwd</code> есть страницы в разделах 1, 1ssl и 5; <code>-k</code> находит <code>cp</code>; для <code>cd</code> страницы нет.'),
  CORE('19-man-f', 'Одно имя в разных разделах справки и поиск по описаниям.',
    LENS(0, 9, [(1, 7, 10, 1), (3, 7, 10, 1), (4, 27, 46, 2), (5, 0, 6, 2), (9, 0, 22, 3)],
      ['номер раздела: 1 — команда passwd, 5 — формат файла /etc/passwd', 'man -k ищет по описаниям страниц', 'у встроенной команды cd нет страницы man'], cols=64)),
  P('Страница <code>man 5 passwd</code> описывает формат файла учётных записей: одна строка на пользователя и семь полей через двоеточие. Этот файл мы читали на прошлом занятии командой <code>getent passwd</code>.'),
  PROBE('Формат файла /etc/passwd', [
    ('man 5 passwd', 'раздел 5 — форматы файлов'),
  ], 'открывается страница <code>PASSWD(5)</code> с перечнем семи полей.'),
  CORE('20-man5', 'Страница раздела 5: формат файла /etc/passwd.',
    LENS(0, 21, [(0, 0, 9, 1), (6, 7, 76, 2), (9, 11, 21, 3), (11, 11, 38, 3), (13, 11, 28, 3), (15, 11, 29, 3), (17, 11, 37, 3), (19, 11, 30, 3), (21, 11, 44, 3)],
      ['раздел 5 — «File Formats and Configuration»', 'одна строка на учётную запись, семь полей', 'поля по порядку: имя, пароль, UID, GID, комментарий, домашний каталог, оболочка'], cols=120)),
]),
dict(title='Краткая справка: --help и help', blocks=[
  P('Страница man подробная, но иногда нужно только вспомнить написание ключа. Большинство программ выводят краткую справку при запуске с ключом <code>--help</code>: список ключей и описание каждого в одну строку. Вывод длинный, поэтому его ограничивают командой <code>head</code> или листают, передав в <code>less</code>: <code>ls --help | less</code>.'),
  P('Справку по встроенным командам оболочки — <code>cd</code>, <code>type</code>, <code>history</code> — выводит встроенная команда <code>help</code>. Какой справкой пользоваться, подсказывает <code>type</code>: если имя — файл программы, читают <code>--help</code> и <code>man</code>; если имя — встроенная команда, читают <code>help</code>.'),
  STEPS('Где искать ответ про команду', [
    '<code>type имя</code> — узнать, что это: программа или встроенная команда.',
    'Для программы — <code>имя --help</code>, чтобы вспомнить ключ.',
    'Если краткой справки мало — <code>man имя</code> и поиск по странице клавишей <kbd>/</kbd>.',
    'Если неизвестно имя команды — <code>man -k</code> с описанием действия.',
    'Для встроенной команды оболочки — <code>help имя</code>.',
  ]),
  PROBE('Краткая справка', [
    ('ls --help | head -14', 'первые 14 строк справки'),
    ('help cd | head -6', 'справка по встроенной команде'),
    ('type help', ''),
  ], '<code>--help</code> выводит ключи прямо в терминал; <code>help cd</code> описывает встроенную команду <code>cd</code>.'),
  CORE('21-help', 'Краткая справка программы и справка встроенной команды.',
    LENS(0, 23, [(1, 0, 31, 1), (6, 2, 11, 2), (6, 29, 66, 2), (15, 27, 44, 3), (16, 0, 32, 3), (23, 0, 23, 4)],
      ['--help выводит справку прямо в терминал', 'ключ и его описание в одной строке', 'help выводит справку по встроенной команде cd', 'help тоже встроена в оболочку'], cols=90)),
]),
],
summary=[
  '<code>man имя</code> открывает страницу справки; разделы страницы: NAME, SYNOPSIS, DESCRIPTION.',
  'Внутри справки <kbd>/</kbd> ищет по тексту, <kbd>n</kbd> переходит к следующему совпадению, <kbd>q</kbd> закрывает страницу.',
  'Раздел 1 — команды, 5 — форматы файлов, 8 — администрирование; <code>man 5 passwd</code> открывает раздел 5.',
  '<code>man -f</code> показывает разделы страницы, <code>man -k</code> ищет по описаниям.',
  '<code>--help</code> — краткая справка программы, <code>help</code> — справка встроенной команды оболочки.',
],
report='Снимок найденного ключа -S и снимок man -f passwd.',
bridge='Все команды занятия разобраны. Дальше — практическая работа: разбор каталога inbox с журналом команд.',
)
