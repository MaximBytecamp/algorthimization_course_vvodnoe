from blocks import *

# Задания квеста: (задание, подсказка, пункт скрипта проверки). Используются здесь и в build.py.
QUEST = [
    ('Создайте каталог <code>~/files-lab/sorted</code> с подкаталогами <code>docs</code>, <code>images</code>, <code>logs</code> и <code>data</code> одной командой.', '<code>mkdir -p</code> и фигурные скобки', '1'),
    ('Переименуйте <code>my report.txt</code> в <code>my-report.txt</code>.', 'кавычки', '6'),
    ('Перенесите все отчёты <code>report-*.txt</code> и <code>my-report.txt</code> в <code>sorted/docs</code> одной командой.', 'сначала <code>ls</code> с теми же масками', '2'),
    ('Перенесите изображения обоих форматов в <code>sorted/images</code>.', '<code>*.{jpg,png}</code>', '3'),
    ('Перенесите журналы <code>app.log</code>, <code>app.log.1</code>, <code>app.log.2</code> и <code>error.log</code> в <code>sorted/logs</code>.', 'две маски в одной команде', '4'),
    ('Скопируйте таблицы <code>.csv</code> из <code>inbox</code> в <code>sorted/data</code>. Оригиналы остаются в <code>inbox</code>.', '<code>cp</code>', '5'),
    ('Удалите резервные копии <code>*.bak</code>, проверив маску.', '<code>ls</code>, затем <code>rm</code>', '7'),
    ('Найдите в <code>~/files-lab</code> файл больше 1 МБ, узнайте его размер и удалите его.', '<code>find -size</code>, <code>ls -lh</code>', '8'),
    ('Найдите файлы <code>inbox</code>, изменённые больше 30 суток назад, и запишите список в <code>~/files-lab/answers.txt</code>.', '<code>find … &gt; answers.txt</code>', 'проверяет преподаватель'),
    ('Найдите скрытый файл в <code>inbox</code> и допишите его содержимое в <code>answers.txt</code>.', '<code>ls -A</code>, <code>cat … &gt;&gt;</code>', '9'),
    ('Найдите в <code>man ls</code> ключ сортировки по размеру. Допишите в <code>answers.txt</code> команду, которая выводит <code>sorted/images</code> по размеру.', '<kbd>/</kbd> внутри справки', '10'),
    ('Найдите командой <code>locate</code> путь к файлу <code>os-release</code> и допишите первый путь в <code>answers.txt</code>.', '<code>locate</code>, <code>head -1</code>', '11'),
]

CHAPTER = dict(
slug='07-kvest', layer='Практика', color='#7A3E48',
title='Практическая работа № 3 · Разбор каталога inbox',
lead='В этой части выполним квест по разбору каталога inbox, будем вести журнал команд и проверим результат скриптом.',
sections=[
dict(title='Журнал команд', blocks=[
  P('Результат практической работы — не только разобранный каталог, но и журнал: список выполненных команд с объяснением каждой. По журналу преподаватель видит ход решения, а вы сможете повторить работу на другом компьютере.'),
  P('Оболочка bash сохраняет введённые команды в историю. Команда <code>history</code> выводит её с номерами, а вместе с <code>tail -n 4</code> — только последние четыре строки. Вывод истории можно записать в файл знаком <code>&gt;</code> и затем перенести нужные строки в журнал. Клавиша <kbd>↑</kbd> листает историю назад, сочетание <kbd>Ctrl</kbd>+<kbd>R</kbd> ищет в ней команду по фрагменту.'),
  P('Журнал ведётся в файле <code>~/files-lab/journal.md</code> по шаблону из материалов. В каждой строке таблицы — команда, что она сделала, и безопасный вариант: команда, которая сначала показывает результат и ничего не меняет, или вариант с ключом <code>-i</code>. После занятия этот файл становится вашей шпаргалкой команд.'),
  PROBE('История команд', [
    ('cd ~/files-lab', ''),
    ('ls inbox | head -3', ''),
    ('find inbox -name "*.md"', ''),
    ('history | tail -n 4', 'последние четыре команды'),
    ('history | tail -n 4 > commands.txt', 'записать их в файл'),
    ('cat commands.txt', ''),
  ], '<code>history</code> выводит команды с номерами; тот же вывод записан в файл <code>commands.txt</code>.'),
  CORE('22-history', 'История команд с номерами и её запись в файл.',
    LENS(8, 18, [(9, 2, 5, 1), (9, 7, 21, 2), (13, 27, 61, 3), (15, 2, 25, 4)],
      ['номер команды в истории', 'текст команды', 'вывод history записан в файл commands.txt', 'строки истории можно перенести в журнал'], cols=64)),
  NOTE('Номера команд в вашей истории будут другими: они зависят от того, сколько команд вы уже выполнили.'),
]),
dict(title='Задания квеста', blocks=[
  P('Все задания выполняются в каталоге <code>~/files-lab</code>. Каждое следующее задание опирается на результат предыдущего, поэтому порядок менять не нужно. Последняя графа таблицы — номер пункта, по которому результат проверяет скрипт <code>files-lab-check.sh</code>.'),
  STEPS('Перед началом', [
    'Проверьте содержимое каталога <code>~/files-lab/sorted</code>, оставшегося от части 2, и удалите его: <code>rm -r ~/files-lab/sorted</code>.',
    'Верните набор в исходное состояние: <code>bash ~/Downloads/files-lab-setup.sh</code>.',
    'Скопируйте шаблон журнала: <code>cp ~/Downloads/journal-template.md ~/files-lab/journal.md</code>.',
    'Перейдите в каталог набора: <code>cd ~/files-lab/inbox</code>.',
  ]),
  TABLE(['№', 'Задание', 'Подсказка', 'Пункт проверки'], [[str(i), t, h, n] for i, (t, h, n) in enumerate(QUEST, 1)]),
  P('Параллельно заполняйте журнал: не меньше двенадцати строк, по строке на каждую команду, которая изменила файлы или дала ответ. Журнал проверяет пункт 12 скрипта.'),
  WARN('Удаляйте только внутри <code>~/files-lab</code>. Перед каждой командой <code>rm</code> выполните <code>ls</code> с той же маской или путём.'),
]),
dict(title='Проверка результата', blocks=[
  P('Скрипт <code>files-lab-check.sh</code> ничего не изменяет: он только читает каталог <code>~/files-lab</code> и по каждому пункту выводит <code>OK</code> или <code>НЕТ</code>. Запускать его можно сколько угодно раз — после каждого задания или в конце работы. Если пункт не выполнен, вернитесь к заданию с этим номером проверки.'),
  PROBE('Проверка', [
    ('tree ~/files-lab/sorted', 'итоговая структура'),
    ('bash ~/Downloads/files-lab-check.sh', 'проверка по 12 пунктам'),
  ], '<code>tree</code> насчитывает 5 каталогов вместе с самим <code>sorted</code> и 19 файлов; скрипт выводит <code>Итог: 12 из 12</code>.'),
  CORE('23-quest-tree', 'Каталог sorted после выполнения заданий 1–6.'),
  CORE('24-quest-check', 'Результат проверки эталонного решения.'),
]),
],
summary=[
  '<code>history</code> выводит историю команд, <code>&gt;</code> записывает вывод в файл.',
  'Журнал содержит команду, результат и безопасный вариант каждой команды.',
  'Скрипт проверки только читает каталог и показывает, какие пункты выполнены.',
],
report='Снимок tree ~/files-lab/sorted, снимок вывода files-lab-check.sh, файлы answers.txt и journal.md.',
bridge='',
)
