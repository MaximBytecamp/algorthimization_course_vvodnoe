# Проверка урока «Файлы, маски, поиск и справка» · 22 сентября 2026

- Страницы собраны `python3 build.py`: у всех 101 команды есть описание и разбор по частям, у всех 28 снимков — выводы. `node --check lesson.js` без ошибок.
- Chrome через Playwright, ширины 1440, 768 и 390 px: ошибок JavaScript нет, горизонтального переполнения нет, изображения загружаются.
- Все 26 увеличенных фрагментов сняты отдельно и просмотрены: метки стоят на нужных строках и колонках; исправлены три метки (5.1, 5.4, 7.1).
- Схема масок проверена на командах `rm -v *.bak`, `echo *.docx`, `ls -l my report.txt`, `echo .*`, `ls "*.log"`, `cp *.{csv,md} ../`, `ls [a-d]*`: списки аргументов совпадают с поведением bash на снимках 09–11.
- Схема find проверена на семи командах: результаты совпадают со снимками 12–13, для `-size -1M` вывод пуст.
- Скрипты files-lab-setup.sh и files-lab-check.sh запущены в ВМ: набор из 28 файлов, проверка до работы — 0 из 12, эталонное решение — 12 из 12.
- Текст проверен грепами из shared/TEXT_REVIEW.md и по списку оборотов из памяти курса: убраны «именно», «главное», антитезы «не X, а Y», одушевление программ.

## Снимки

24 кадра сняты с дисплея ВМ Ubuntu-lab-01 21 сентября 2026 года. Команды каждого кадра — shots/SOURCES.md.
