from blocks import *

CHAPTER = dict(
slug='05-poisk', layer='Поиск', color='#3B5B92',
title='Поиск файлов',
lead='В этой части найдём файлы командой find по имени, типу, размеру и времени изменения, выполним действие над найденными файлами, сравним find с locate и разберём команды which и type.',
sections=[
dict(title='find: где искать и что искать', blocks=[
  P('Маска выбирает файлы в одном каталоге. Когда неизвестно, где лежит файл, нужна команда <code>find</code>. Она обходит каталог и все вложенные каталоги и проверяет каждый найденный объект по заданным условиям.'),
  P('Команда записывается так: сначала один или несколько каталогов, с которых начинается обход, затем условия. В документации <code>find</code> условия называются тестами. Точка означает текущий каталог. Если условий нет, <code>find</code> выводит всё содержимое дерева.'),
  TERM('find', 'Команда, которая обходит дерево каталогов, начиная с указанного, и выводит объекты, прошедшие все условия.', 'Маски оболочки: маска раскрывается только в одном каталоге, а find проверяет всё дерево.'),
  TABLE(['Условие', 'Что проверяет'], [
    ['<code>-name "маска"</code>', 'имя объекта совпадает с маской; регистр букв учитывается'],
    ['<code>-iname "маска"</code>', 'то же без учёта регистра: README и readme совпадут'],
    ['<code>-type f</code>, <code>-type d</code>', 'объект — обычный файл или каталог'],
    ['<code>-size +N</code>, <code>-size -N</code>', 'размер больше или меньше N'],
    ['<code>-mtime +N</code>, <code>-mtime -N</code>', 'изменён больше или меньше N суток назад'],
    ['<code>-maxdepth N</code>', 'спускаться не глубже N уровней'],
  ]),
  P('Маску в условиях <code>-name</code> и <code>-iname</code> всегда берут в кавычки. Если кавычек нет и в текущем каталоге есть подходящие файлы, оболочка раскроет маску раньше, чем запустится <code>find</code>, и команда получит лишние аргументы. На втором фрагменте снимка это видно: в каталоге inbox строка <code>find . -name *.csv</code> превращается в <code>find . -name data-old.csv data.csv</code>, и <code>find</code> сообщает об ошибке.'),
  P('Скрытые файлы <code>find</code> не пропускает: <code>.settings</code> будет найден так же, как остальные файлы. Этим <code>find</code> отличается от масок оболочки.'),
  PROBE('Поиск по имени и типу', [
    ('cd ~/files-lab', ''),
    ('find . -name "*.csv"', 'имена по маске во всём дереве'),
    ('find . -iname "readme*"', 'без учёта регистра'),
    ('find . -type d', 'только каталоги'),
    ('cd inbox', ''),
    ('find . -name *.csv', 'маска без кавычек'),
    ('find . -name "*.csv"', 'маска в кавычках'),
  ], '<code>find</code> находит три файла .csv, в том числе в <code>archive/2025</code>; маска без кавычек вызывает ошибку <code>paths must precede expression</code>.'),
  CORE('12-find-name', 'find ищет во всех вложенных каталогах.',
    LENS(1, 16, [(4, 0, 31, 1), (6, 0, 18, 2), (8, 0, 1, 3), (9, 0, 7, 3), (12, 0, 8, 3)],
      ['find проверяет и вложенные каталоги: budget.csv лежит в archive/2025', '-iname не различает регистр: readme* совпало с README.TXT', '-type d выводит только каталоги, начиная с самого «.»'], cols=60)),
  CORE('12-find-name', 'Без кавычек маску раскрывает оболочка, и find получает лишние аргументы.',
    LENS(18, 24, [(18, 46, 51, 1), (19, 0, 46, 2), (20, 0, 56, 2), (21, 46, 53, 3), (22, 0, 14, 4), (24, 0, 25, 4)],
      ['маска без кавычек', 'оболочка подставила data-old.csv data.csv, и find сообщил об ошибке', 'маска в кавычках передаётся find без изменений', 'find сравнивает маску с именами во всех каталогах'], cols=64)),
]),
dict(title='Размер и время изменения', blocks=[
  P('Размер в условии <code>-size</code> записывается числом с единицей: <code>c</code> — байты, <code>k</code> — килобайты, <code>M</code> — мегабайты, <code>G</code> — гигабайты. Знак <code>+</code> означает «больше», <code>-</code> — «меньше», число без знака — «ровно». Так находят файлы, которые занимают место на диске, например <code>find ~ -type f -size +100M</code>.'),
  P('У <code>-size</code> есть особенность, которая видна на первом фрагменте снимка. Перед сравнением <code>find</code> переводит размер файла в указанные единицы и округляет вверх. Фотография размером 120 000 байт — это 118 килобайт, но при подсчёте в мегабайтах она занимает одну целую единицу. Условие <code>-size -1M</code> означает «меньше одной единицы», и ему соответствуют только пустые файлы. Поэтому верхнюю границу указывают в более мелких единицах: <code>-size -500k</code>.'),
  P('Условие <code>-mtime</code> считает, сколько полных суток прошло с последнего изменения файла. <code>-mtime +30</code> — изменён больше 30 суток назад, <code>-mtime -1</code> — за последние сутки. Так находят старые журналы, которые пора архивировать, или файлы, изменённые за последний день. Для точности до минут есть условие <code>-mmin</code>.'),
  P('Несколько условий подряд объединяются по правилу «и»: объект попадает в вывод, только если прошёл все проверки по порядку. Это похоже на сито из нескольких сеток: каждая следующая сетка задерживает часть того, что прошло через предыдущую.'),
  PROBE('Размер и время', [
    ('cd ~/files-lab', ''),
    ('find . -type f -size +1M', 'больше 1 МБ'),
    ('find . -type f -size +100k -size -1M', 'верхняя граница в мегабайтах'),
    ('find . -type f -size +100k -size -500k', 'верхняя граница в килобайтах'),
    ('find inbox -type f -mtime +100', 'старше 100 суток'),
    ('find inbox -type f -mtime +30', 'старше 30 суток'),
  ], '<code>dump.bin</code> больше мегабайта; с <code>-1M</code> вывод пуст, с <code>-500k</code> найдены четыре изображения; <code>+30</code> добавляет к списку файлы от 1 августа.'),
  CORE('13-find-size-time', 'Условие -size округляет размер вверх до указанных единиц.',
    LENS(1, 8, [(1, 42, 51, 1), (2, 0, 16, 1), (3, 54, 63, 2), (4, 54, 65, 3), (5, 0, 21, 3), (8, 0, 21, 3)],
      ['+1M — больше одного мегабайта: только dump.bin', 'вывод пуст: -1M означает «меньше одной единицы», то есть 0 байт', '-500k — меньше 500 килобайт: четыре изображения'], cols=72)),
  CORE('13-find-size-time', 'Условие -mtime отбирает файлы по времени последнего изменения.',
    LENS(9, 24, [(9, 46, 57, 1), (10, 0, 18, 1), (16, 46, 56, 2), (19, 0, 15, 3), (20, 0, 24, 3)],
      ['+100 — изменены больше 100 суток назад: файлы от 10 мая', '+30 — больше 30 суток назад', 'добавились файлы от 1 августа'], cols=64)),
  P('На схеме ниже каждое условие команды показано отдельным шагом отбора. После выбора команды видно, на каком условии отсеивается каждый объект каталога inbox и что остаётся в выводе.'),
  W('sieve'),
  NOTE('Даты изменения в учебном наборе заданы скриптом: 10 мая и 1 августа 2026 года. Если вы выполняете команды позже, возраст файлов будет больше, и список для <code>-mtime +100</code> может оказаться длиннее. Порядок строк в выводе <code>find</code> зависит от файловой системы и у вас может отличаться.'),
]),
dict(title='Действия с найденными файлами', blocks=[
  P('По умолчанию <code>find</code> печатает пути найденных объектов. Вместо печати можно выполнить действие. Условие <code>-exec команда {} \\;</code> запускает команду для каждого найденного файла: вместо <code>{}</code> подставляется путь, а <code>\\;</code> отмечает конец команды. Обратная косая черта нужна, чтобы точку с запятой получила <code>find</code>: без неё оболочка приняла бы точку с запятой за конец всей строки.'),
  P('Действие <code>-delete</code> удаляет найденные объекты. Безопасный порядок тот же, что с масками: сначала запустить <code>find</code> без <code>-delete</code>, прочитать список и только потом добавить <code>-delete</code> в конец той же строки.'),
  P('При поиске в системных каталогах обычный пользователь получает сообщения <code>Permission denied</code> о каталогах, в которые у него нет доступа. Сообщения об ошибках программа выводит в отдельный поток — поток ошибок с номером 2. Запись <code>2&gt;/dev/null</code> перенаправляет этот поток в устройство <code>/dev/null</code>, которое отбрасывает всё, что в него записано. На экране остаются только найденные пути.'),
  WARN('Условие <code>-delete</code> ставят в конце команды. <code>find</code> проверяет условия слева направо, и <code>find . -delete -name "*.bak"</code> удалит всё содержимое текущего каталога: удаление выполнится раньше проверки имени.'),
  PROBE('Действие над найденным', [
    ('cd ~/files-lab', ''),
    ('find inbox -name "app.log*" -exec ls -l {} \\;', 'ls -l для каждого найденного'),
    ('find /etc /root -name "passwd"', 'поиск в системных каталогах'),
    ('find /etc /root -name "passwd" 2>/dev/null', 'без сообщений об ошибках'),
    ('find . -name "report-copy.txt"', 'сначала только найти'),
    ('find . -name "report-copy.txt" -delete', 'затем удалить'),
    ('find . -name "report-copy.txt"', 'проверка'),
  ], '<code>-exec</code> выводит подробную строку для трёх журналов; <code>2&gt;/dev/null</code> убирает сообщения <code>Permission denied</code>; после <code>-delete</code> файл не находится.'),
  CORE('14-find-exec', 'find запускает команду для найденных файлов, а 2>/dev/null скрывает сообщения об ошибках.',
    LENS(1, 18, [(1, 55, 73, 1), (6, 0, 41, 2), (15, 0, 32, 2), (9, 0, 17, 3), (10, 0, 11, 3), (16, 58, 69, 4)],
      ['-exec запускает ls -l для каждого найденного файла', 'в эти каталоги у пользователя нет доступа', 'найденные файлы passwd', '2>/dev/null отбрасывает ошибки, остаются только результаты'], cols=80)),
  CORE('14-find-exec', 'Сначала find показывает найденное, затем удаляет его.',
    LENS(19, 22, [(20, 0, 17, 1), (21, 58, 65, 2), (22, 27, 57, 3)],
      ['find без действия только показывает файл', '-delete удаляет найденное', 'повторный поиск ничего не находит'], cols=72)),
]),
dict(title='Быстрый поиск: locate', blocks=[
  P('<code>find</code> при каждом запуске обходит каталоги на диске, и поиск по всему диску занимает заметное время. Команда <code>locate</code> работает иначе: она ищет в заранее составленной базе, где записаны пути всех файлов системы, и отвечает сразу.'),
  P('Базу составляет программа <code>updatedb</code>. В Ubuntu она запускается автоматически раз в сутки. Из этого следует ограничение <code>locate</code>: база отражает состояние диска на момент последнего обновления. Файл, созданный после обновления, <code>locate</code> не найдёт, а удалённый будет находить, пока базу не обновят. Ключ <code>-e</code> проверяет, существует ли файл сейчас, и выводит только существующие.'),
  P('Базу можно сравнить с телефонным справочником, напечатанным в начале года: номер находится быстро, но тех, кто переехал в марте, в справочнике ещё нет, а уехавшие в нём остались.'),
  TABLE(['', '<code>find</code>', '<code>locate</code>'], [
    ['где ищет', 'в каталогах на диске', 'в базе имён'],
    ['скорость', 'зависит от размера дерева', 'ответ сразу'],
    ['новые и удалённые файлы', 'видит сразу', 'только после <code>updatedb</code>'],
    ['условия', 'имя, тип, размер, время и другие', 'только имя'],
  ]),
  NOTE('В Ubuntu Desktop 24.04 команды <code>locate</code> нет. Установите пакет <code>plocate</code>: <code>sudo apt install plocate</code>. Если <code>locate</code> сообщает, что базы нет, выполните <code>sudo updatedb</code>.'),
  PROBE('Поиск по базе', [
    ('cd ~/files-lab', ''),
    ('touch new-note.txt', 'новый файл'),
    ('locate new-note.txt', 'в базе его ещё нет'),
    ('sudo updatedb', 'обновить базу'),
    ('locate new-note.txt', ''),
    ('rm new-note.txt', 'удалить файл'),
    ('locate new-note.txt', 'путь остался в базе'),
    ('locate -e new-note.txt', 'только существующие'),
    ('locate os-release', 'где лежит файл с известным именем'),
  ], 'новый файл находится только после <code>updatedb</code>; после удаления <code>locate</code> по-прежнему выводит путь, <code>locate -e</code> — нет.'),
  CORE('15-locate', 'locate ищет по базе, которая обновляется командой updatedb.',
    LENS(1, 18, [(2, 27, 46, 1), (3, 27, 40, 2), (5, 0, 35, 3), (8, 0, 35, 4), (9, 27, 49, 5), (12, 0, 20, 6), (15, 0, 32, 6)],
      ['файл только что создан, а locate его не находит', 'updatedb обновляет базу имён', 'после обновления базы файл найден', 'файл удалён, а база ещё содержит его путь', '-e выводит только существующие файлы: вывода нет', 'пути в /rofs и /snap относятся к Live-сеансу и пакетам snap'], cols=60)),
]),
dict(title='Где находится программа: which и type', blocks=[
  P('Отдельный вид поиска — узнать, какая программа запустится по имени команды. На прошлом занятии мы разбирали, что оболочка ищет программу в каталогах из переменной <code>PATH</code>. Команда <code>which</code> выполняет тот же поиск и печатает путь к найденному файлу.'),
  P('<code>which</code> ищет только файлы в каталогах <code>PATH</code>. Встроенные команды оболочки, такие как <code>cd</code>, и псевдонимы она не находит и для них ничего не выводит. Полный ответ даёт встроенная команда <code>type</code>: она сообщает, чем является имя — псевдонимом, встроенной командой или файлом. Слово <code>hashed</code> в её выводе означает, что оболочка уже запускала эту программу и запомнила путь к ней. Команда <code>command -v</code> печатает путь к программе и в скриптах проверяет, установлена ли программа: если программы нет, вывода нет.'),
  PROBE('Путь к программе', [
    ('which ls tree locate', 'пути к файлам программ'),
    ('which cd', 'встроенная команда'),
    ('type ls cd tree', 'чем является каждое имя'),
    ('command -v locate', ''),
  ], '<code>which</code> выводит пути в <code>/usr/bin</code> и ничего не выводит для <code>cd</code>; <code>type</code> называет псевдоним, встроенную команду и файл.'),
  CORE('16-which', 'which ищет файлы в PATH, type объясняет, чем является имя.',
    LENS(0, 10, [(1, 0, 11, 1), (4, 27, 35, 2), (6, 0, 34, 3), (7, 0, 21, 4), (8, 0, 30, 5), (10, 0, 15, 6)],
      ['which выводит путь к исполняемому файлу', 'для cd which ничего не выводит', 'ls — псевдоним с ключом --color=auto', 'cd встроена в оболочку', 'hashed — оболочка запомнила путь к tree', 'command -v выводит путь к программе'], cols=60)),
]),
],
summary=[
  '<code>find каталог условия</code> обходит всё дерево; маску в <code>-name</code> берут в кавычки.',
  '<code>-size</code> округляет размер вверх до указанных единиц; <code>-mtime</code> считает полные сутки.',
  '<code>-exec</code> выполняет команду для каждого найденного файла; <code>-delete</code> ставят в конце и только после проверки списка.',
  '<code>2&gt;/dev/null</code> отбрасывает сообщения об ошибках.',
  '<code>locate</code> ищет по базе и видит изменения только после <code>updatedb</code>.',
  '<code>which</code> ищет программу в PATH, <code>type</code> сообщает, чем является имя.',
],
report='Снимок find с -size и -mtime и снимок locate до и после updatedb.',
bridge='В этой части мы опирались на ключи команд. Дальше — где узнать, какие ключи есть у команды и что они делают.',
)
