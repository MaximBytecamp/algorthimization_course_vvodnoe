from blocks import *

CHAPTER = dict(
slug='02-kopirovanie', layer='Копирование', color='#2E7D5B',
title='Копирование, перемещение и ссылки',
lead='В этой части скопируем и перенесём файлы из inbox, разберём, в каких случаях команды заменяют файлы без предупреждения, и чем ссылка отличается от копии.',
sections=[
dict(title='Копирование: cp', blocks=[
  P('Команда <code>cp</code> (copy — «копировать») создаёт новый файл с тем же содержимым. Первый аргумент — что копировать, последний — куда. Если последний аргумент — существующий каталог, копия получит прежнее имя и окажется в этом каталоге. Если указан путь к файлу, копия получит новое имя: так одной командой копируют и переименовывают.'),
  P('Каталог <code>cp</code> без ключа не копирует и сообщает <code>-r not specified</code>. Ключ <code>-r</code> (recursive — «рекурсивно») копирует каталог со всеми вложенными каталогами и файлами. Рекурсивно означает, что команда заходит в каждый вложенный каталог и повторяет в нём то же действие, пока не обойдёт всё дерево.'),
  P('Если в месте назначения уже есть файл с таким же именем, <code>cp</code> заменит его содержимое без вопроса, и отменить замену нельзя. Защищают от этого два ключа: <code>-i</code> (interactive — «с вопросами») спрашивает перед каждой заменой, <code>-n</code> (no-clobber — «не затирать») молча пропускает существующие файлы. Ключ <code>-v</code> (verbose — «подробно») печатает каждую выполненную операцию, по этим строкам удобно проверять результат.'),
  TABLE(['Команда', 'Результат'], [
    ['<code>cp a.txt b.txt</code>', 'копия под именем b.txt в том же каталоге'],
    ['<code>cp a.txt dir/</code>', 'копия a.txt в каталоге dir'],
    ['<code>cp a.txt dir/b.txt</code>', 'копия в каталоге dir под именем b.txt'],
    ['<code>cp -r src dir/</code>', 'каталог src со всем содержимым внутри dir'],
    ['<code>cp -i a.txt dir/</code>', 'вопрос перед заменой, если в dir уже есть a.txt'],
  ]),
  P('Каталог для копий создадим рядом с <code>inbox</code>. Путь <code>../backup</code> читается так: подняться на уровень выше (<code>..</code>), то есть в <code>~/files-lab</code>, и там взять каталог <code>backup</code>. Ключ <code>-R</code> у <code>ls</code> выводит содержимое каталога вместе со всеми вложенными каталогами.'),
  PROBE('Копирование файлов и каталога', [
    ('cd ~/files-lab/inbox', ''),
    ('mkdir ../backup', 'каталог для копий рядом с inbox'),
    ('cp -v data.csv ../backup/', 'копия с прежним именем'),
    ('cp -v report-final.txt ../backup/final-copy.txt', 'копия с новым именем'),
    ('cp archive ../backup/', 'каталог без -r'),
    ('cp -rv archive ../backup/', 'каталог со всем содержимым'),
    ('cp -i data.csv ../backup/', 'data.csv уже есть в backup'),
    ('KEY:n', 'ответ на вопрос overwrite: не заменять'),
    ('ls -R ../backup', 'backup со всеми вложенными каталогами'),
  ], 'копии созданы; без <code>-r</code> каталог пропущен; после ответа <code>n</code> файл в backup не изменился.'),
  CORE('04-cp', 'cp копирует файлы и каталоги, а с ключом -i спрашивает перед заменой.',
    LENS(2, 14, [(3, 0, 34, 1), (5, 0, 48, 2), (7, 0, 50, 3), (9, 0, 32, 4), (12, 0, 64, 4), (14, 0, 38, 5)],
      ['-v печатает, что и куда скопировано', 'копия получила новое имя final-copy.txt', 'без -r каталог не копируется', '-r копирует каталог и всё, что в нём', '-i спрашивает перед заменой; ответ n оставляет прежний файл'], cols=72)),
]),
dict(title='Перемещение и переименование: mv', blocks=[
  P('Команда <code>mv</code> (move — «переместить») переносит файл в другое место. Переименование в Linux тоже выполняется переносом: файл остаётся в том же каталоге, но под новым именем. Отдельной команды для переименования в стандартном наборе нет.'),
  P('Внутри одной файловой системы <code>mv</code> не копирует данные файла. Меняется только запись в каталоге — пара «имя — номер inode», которую мы разбирали на прошлом занятии. Поэтому перенос файла размером в несколько гигабайт выполняется сразу. Если каталог назначения находится на другой файловой системе, например на флешке, <code>mv</code> копирует данные и затем удаляет исходный файл.'),
  P('Как и <code>cp</code>, команда <code>mv</code> без вопроса заменяет существующий файл с тем же именем. При переименовании это особенно заметно: <code>mv todo.md notes-2026.md</code> удалит прежнее содержимое <code>notes-2026.md</code>, и в каталоге останется один файл вместо двух. Ключ <code>-i</code> работает так же, как у <code>cp</code>.'),
  P('Если последний аргумент — каталог, перед ним можно перечислить несколько файлов: все они будут перенесены в этот каталог одной командой.'),
  PROBE('Переименование и перенос', [
    ('cd ~/files-lab/inbox', ''),
    ('mv -v notes.md notes-2026.md', 'новое имя в том же каталоге'),
    ('mkdir -p ../sorted/logs', ''),
    ('mv -v app.log.1 app.log.2 ../sorted/logs/', 'два файла в другой каталог'),
    ('mv -i todo.md notes-2026.md', 'имя notes-2026.md уже занято'),
    ('KEY:n', 'не заменять notes-2026.md'),
    ('ls ../sorted/logs', ''),
    ('ls notes* todo*', 'оба файла на месте'),
  ], '<code>mv</code> печатает <code>renamed</code> для каждого файла; после ответа <code>n</code> оба файла на месте.'),
  CORE('05-mv', 'mv переименовывает и переносит файлы; с ключом -i спрашивает перед заменой.',
    LENS(1, 12, [(2, 0, 37, 1), (5, 0, 49, 2), (6, 0, 49, 2), (8, 0, 33, 3), (12, 0, 22, 4)],
      ['mv в том же каталоге меняет имя', 'каталог в конце: файлы перенесены туда', '-i спрашивает, прежде чем заменить файл', 'после ответа n оба файла на месте'], cols=64)),
]),
dict(title='Ссылка вместо копии: ln -s', blocks=[
  P('Иногда один и тот же файл нужен в двух местах. Копия подходит для этого, но у копии своё содержимое: если исправить оригинал, копия останется старой. Символическая ссылка, которую мы разбирали на прошлом занятии, хранит только путь к файлу, поэтому через неё всегда читается текущая версия.'),
  P('Пример из практики: в каталоге проекта держат ссылку <code>latest-report.txt</code> на последний отчёт. Когда появляется новый отчёт, ссылку перенаправляют на него, и все, кто открывает <code>latest-report.txt</code>, получают новый файл без изменения своих команд.'),
  P('При копировании ссылки <code>cp</code> переходит по ней и копирует содержимое целевого файла. В результате получается обычный файл. Разница видна в строках <code>ls -l</code>: у ссылки первый символ <code>l</code>, а размер равен длине записанного пути; у копии первый символ <code>-</code>, а размер равен размеру текста.'),
  PROBE('Ссылка и копия', [
    ('cd ~/files-lab', ''),
    ('ln -s inbox/report-final.txt latest-report.txt', 'ссылка на отчёт'),
    ('ls -l latest-report.txt', ''),
    ('cat latest-report.txt', 'чтение через ссылку'),
    ('cp -v latest-report.txt report-copy.txt', 'копия содержимого'),
    ('ls -l latest-report.txt report-copy.txt', 'сравнить ссылку и копию'),
  ], 'ссылка указывает на <code>inbox/report-final.txt</code>; копия — обычный файл размером 13 байт.'),
  CORE('08-ln', 'Символическая ссылка хранит путь; cp по ссылке создаёт обычный файл.',
    LENS(2, 10, [(3, 0, 1, 1), (3, 43, 86, 2), (5, 0, 12, 3), (9, 27, 29, 4), (10, 27, 29, 4), (10, 0, 1, 4)],
      ['тип l — символическая ссылка', 'в ссылке записан путь к файлу', 'cat прочитал отчёт через ссылку', 'ссылка 22 байта — длина пути; копия — обычный файл из 13 байт'], cols=90)),
]),
],
summary=[
  '<code>cp</code> копирует файлы; для каталога нужен ключ <code>-r</code>.',
  '<code>mv</code> переносит и переименовывает; внутри одной файловой системы данные файла не копируются.',
  '<code>cp</code> и <code>mv</code> заменяют существующий файл без вопроса; <code>-i</code> спрашивает, <code>-n</code> пропускает.',
  'Символическая ссылка хранит путь к файлу; <code>cp</code> по ссылке копирует содержимое целевого файла.',
],
report='Снимок ls -R ../backup и снимок с вопросом overwrite.',
bridge='Копии больше не нужны. Дальше — удаление, которое в терминале отменить нельзя.',
)
