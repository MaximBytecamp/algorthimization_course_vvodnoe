# Занятие 4 · Практические замеры времени и памяти

Методичка к HTML-презентации. Формат — лекция с разбором кода на 90 минут:
каждый экран берёт механизм, показывает листинг из файлов занятия, разбирает
его по строкам и заканчивается числом, полученным запуском. Четыре участка
за клавиатурой встроены в эту цепочку. Правила текста — `shared/VOICE.md`,
правило непохожести колод — `shared/DECK_VARIETY.md`.

## 1. Результат занятия

| | |
|---|---|
| **Тема** | Практические замеры времени и памяти |
| **Изучаем** | четыре часа модуля `time`, разрешение прибора, `timeit`, границу замера, прогрев, серию, минимум, медиану, разброс `max / min`, пиковую дополнительную память и `tracemalloc` |
| **Делаем** | сравниваем два решения на шести размерах входа, сохраняем CSV, читаем график и формулируем ограниченный вывод |
| **Сдаём** | `contour.py`, `results_time.csv`, паспорт эксперимента и комментарий к чужой методике |
| **Формат** | 2 академических часа, 35 экранов, 22 блока кода |

После занятия студент должен уметь ответить не только «какое решение быстрее»,
но и «что именно измерялось, на каких данных, каким прибором и насколько
стабилен результат».

## 2. Главная мысль

**Число без методики нельзя проверить.** Поэтому вокруг замера строится контур
из пяти обязательных элементов:

1. **данные** — одинаковый вход для всех решений, созданный до отсчёта;
2. **граница** — внутри участка только исследуемая операция;
3. **прогрев** — первый вызов выполняется до рабочей серии;
4. **серия** — повторы, все сырые значения сохраняются;
5. **агрегат** — итоговое число публикуется вместе с разбросом.

Асимптотическая оценка и эксперимент отвечают на разные вопросы. `T(n)` и
`S(n)` описывают рост, а `t(n)` и `m(n)` показывают стоимость конкретной
реализации в выбранной среде.

## 3. Порядок занятия

Формат подачи взят из колоды «Временная сложность» той же дисциплины: экран =
механизм + листинг + разбор + число. Это намеренный возврат к формату соседнего
занятия, а не совпадение: студент уже умеет читать такие экраны.

| Этап | Минуты | Экраны | Содержание |
|---|---:|---|---|
| Зачем измерять | 0–7 | 1–4 | два решения задачи в коде, множители роста из запуска, определения: замер, `T(n)`, `S(n)`, `t(n)`, `m(n)` |
| Часы | 7–19 | 5–9 | разрешение и монотонность, `get_clock_info`, прошедшее время против процессорного, `timeit`, первый запуск руками |
| Граница | 19–39 | 10–15 | определение границы, цена каждой строки участка, три ошибки листингами, сборка `contour.py`, разбор падения теста |
| Серия | 39–46 | 16–18 | прогрев, функция `series` по строкам, `summarize` и разброс |
| Память | 46–53 | 19–21 | `tracemalloc` и функция `peak_of`, результат замера, ступени хеш-таблицы |
| Свои данные | 53–67 | 22–26 | своя серия, столбцы CSV, полная таблица, график и объяснение множителей, размен время / память |
| Дефекты и приёмка | 67–80 | 27–29 | пять дефектов `broken_bench.py` построчно, честный замер на тех же n, приёмка чужого отчёта |
| Итог | 80–87 | 30–33 | четыре части вывода, чек-лист, вопрос на выход, Git |
| Домашнее задание | 87–90 | 34–35 | |

**Итого: 90 минут.** Четыре участка за клавиатурой занимают 24 минуты, приёмка
чужой методики — ещё 4.

Теория про часы стоит **до** первого запуска намеренно: пока группа поднимает
окружение, преподаватель разбирает разрешение прибора. К экрану 9 окружение
готово у всех.

## 4. Что показывается на экранах

В колоде 22 блока кода на 35 экранов. Все листинги — из файлов
`course-materials/benchmarks/`, а не написаны под слайд:

| Экран | Листинг | Откуда |
|---:|---|---|
| 2 | `match_list` и `match_set` | `lab.py` |
| 5 | цикл по `get_clock_info` | `clock_info.py` |
| 6 | `workload` и два отсчёта вокруг участка | `clocks.py` |
| 8 | две команды `python -m timeit` | терминал |
| 10 | тело серии с выделенной границей | `lab.py` |
| 13 | три способа испортить границу | написано по мотивам `broken_bench.py` |
| 16 | строка прогрева | `lab.py` |
| 17 | функция `series` целиком | `lab.py` |
| 18 | функция `summarize` | `lab.py` |
| 19 | функция `peak_of` | `memory_probe.py` |
| 21 | вызовы `peak_of` на трёх размерах | сессия Python |
| 28 | пять дефектов «как в файле → как в контуре» | `broken_bench.py` и `lab.py` |



Ведущий приём — лабораторный журнал из четырёх полей
`ожидание → сырые значения → агрегат → вывод`. Запись открывается на экране 2
с пустым выводом и закрывается на экране 25, когда методика проверена и
расхождение уже можно объяснять свойствами кода.

Второй, разовый приём — интерактивная граница замера на экране 10: строки
листинга кликаются, а итоговая стоимость складывается из настоящей цены
выбранных строк.

Сквозная деталь, которая держит последовательность, — **карта контура**
`.contour-map`: пять элементов замера списком. На экране 4 она показывает
маршрут занятия целиком, на экране 9 подсвечивает разбираемый элемент.

## 5. Кадры занятия

В колоде тринадцать настоящих кадров терминала (`shots/`). Все сняты на стенде
занятия: окно Terminal фиксированного размера, реальный запуск, снимок области
окна через `screencapture`. Ни один кадр не нарисован — числа на экранах взяты
из этих же запусков.

| Кадр | Что на нём | Экран |
|---|---|---:|
| `13-clock-info.png` | свойства трёх часов из `get_clock_info` | 5 |
| `01-clocks.png` | прошедшее время против процессорного | 7 |
| `12-timeit.png` | `timeit`: 11,1 нс по множеству, 18,7 мкс по списку | 8 |
| `02-boundary-cost.png` | цена каждой части участка при n = 4000 | 11 |
| `03-pytest-red.png` | семь провалившихся тестов на пустом бланке | 14 |
| `04-pytest-green.png` | `7 passed` после сборки контура | 14 |
| `05-generation-inside.png` | падение теста границы с текстом причины | 15 |
| `06-raw-series.png` | пятнадцать сырых значений и четыре агрегата | 18 |
| `09-memory.png` | пиковая память двух решений | 20 |
| `07-series.png` | таблица по шести размерам входа | 22 |
| `08-results-csv.png` | содержимое `results_time.csv` | 23 |
| `10-broken-bench.png` | `broken_bench.py` с выводом «список можно оставить» | 27 |
| `11-honest-bench.png` | тот же вход честным контуром: 58, 102 и 190 раз | 28 |

Кадр `05-generation-inside.png` — учебный отказ, снятый на испорченной копии
контура: в `series` данные создаются заново внутри отсчёта.

## 6. Числа колоды и откуда они взяты

| Число на экране | Откуда |
|---|---|
| разрешение 4,167·10⁻⁸ и 1,000·10⁻⁶ с | `time.get_clock_info` |
| 11,1 нс и 18,7 мкс на одну проверку | `python -m timeit` на реестре 40 000 |
| 0,229 с против 0,024 с | запуск `clocks.py` |
| 1214,5 · 53,0 · 66,4 · 67 201,8 мкс | `lab.py --boundary 4000` |
| 0,04 мкс у `found = 0` и 1,3 мкс у `print` | отдельный прогон в том же окне терминала |
| 34,8 → 21,1 мкс, надбавка 13,7 | десять вызовов подряд при n = 1000 |
| пятнадцать значений серии и разброс 1,02 | `lab.py --raw 2000` |
| таблица шести размеров, наклон 1,96 и 1,11 | `lab.py`, `results_time.csv` |
| 41 264 → 164 144 → 655 664 байта | `memory_probe.py`, `results_memory.csv` |
| 3,9× · 7,3× · 13,7× у испорченного замера | `broken_bench.py` |
| 58× · 102× · 190× на тех же n | `lab.py --sizes 250 500 1000` |

Если пересобираете данные — микросекунды изменятся, и кадры придётся снять
заново вместе с числами на экранах 2, 10–12, 15, 17, 22–25, 27. Байты
в `memory_probe.py` от запуска к запуску не плавают.

## 7. Что подготовить заранее

1. Попросить группу заранее создать `.venv` и поставить `pytest`: это
   единственное место занятия, где риск не в теме, а в окружении.
2. Держать открытым свой терминал с уже собранным `contour.py` — на случай,
   если у кого-то тесты не пойдут и придётся работать в паре.
3. Проверить, что ноутбуки не в режиме энергосбережения: на нём разброс серии
   уходит за 2, и разговор про агрегаты приходится вести на плохих данных.

## 8. Контроль

Студент получает чужой отчёт (`peer_review_report.md`) и отвечает четырьмя
пунктами:

1. какие элементы контура нарушены;
2. какой дефект сильнее всего исказил результат;
3. какой вывод следует из имеющихся данных;
4. каким запуском исходный вывод можно проверить.

Вывод принимается, если в нём названы диапазон входа, метрика, способ замера и
границы применимости. Фраза «решение быстрее» без этих частей не засчитывается.

## 9. Частые вопросы

**Почему минимум, а не среднее?** Помехи умеют только замедлять: ни планировщик,
ни сборщик мусора не выполнят код быстрее, чем он выполняется. Поэтому самый
быстрый повтор ближе всех к чистой стоимости кода. Но публикуется он всегда
вместе с разбросом — именно разброс показывает, спокойная ли была серия.

**Почему один прогрев?** Он отделяет первый вызов от рабочей серии и снимает
один известный источник различий. Остальные помехи он не убирает, поэтому
повторы всё равно обязательны.

**Почему `tracemalloc` показывает меньше диспетчера задач?** Приборы считают
разные величины: `tracemalloc` — трассируемые выделения Python внутри участка,
диспетчер — всю память процесса вместе с интерпретатором.

**Доказывает ли график сложность?** Нет. Шесть измеренных точек согласуются с
оценкой, но асимптотика остаётся результатом разбора алгоритма.

## 10. Файлы занятия

`course-materials/benchmarks/`:

- `clock_info.py` — свойства часов из `get_clock_info`;
- `clocks.py` — прошедшее и процессорное время на одном участке;
- `contour.py` — бланк, который заполняет студент;
- `test_lesson_04.py` — семь проверок поведения контура;
- `lab.py` — рабочая серия времени, режимы `--raw` и `--boundary`;
- `memory_probe.py` — отдельный замер памяти;
- `broken_bench.py` — методика с пятью дефектами;
- `results_time.csv`, `results_memory.csv` — данные, стоящие на экранах;
- `METHOD.md` — паспорт эксперимента;
- `peer_review_report.md` — материал для приёмки.

## 11. Домашнее задание

Проверить замером прикидку из ДЗ № 3 для `two_sum`, заполнить паспорт по
`METHOD.md` и отдельно измерить память двух реализаций. В репозиторий идут
сырые значения, код контура и ограничения вывода. Оформлено по общему правилу
курса: зачин, мем, три задания, отдельный экран с порядком сдачи и печатный
лист.

## 12. Проверка перед сдачей

- [x] в HTML ровно 35 экранов;
- [x] сумма `data-time` равна 90 минутам;
- [x] все тринадцать кадров открываются и имеют осмысленный `alt`;
- [x] кнопки файлов открывают `contour.py`, тест и `broken_bench.py`;
- [x] интерактивная граница и график инициализируются без ошибок;
- [x] числа на экранах совпадают с `results_time.csv` и `results_memory.csv`;
- [x] листинги на экранах совпадают с файлами в `course-materials/benchmarks/`;
- [x] шаблоны файлов в HTML совпадают с файлами на диске;
- [x] в тексте нет запрещённых вводных и рекламных формулировок;
- [x] строка занятия обновлена в `shared/DECK_REGISTRY.md`;
- [x] все 35 экранов проверены в Chromium при 1600 × 900 без переполнения.
