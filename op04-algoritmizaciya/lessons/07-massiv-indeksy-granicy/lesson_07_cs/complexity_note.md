# Заметка о решениях · занятие 7 · C#

Заполняется своими словами. Каждая строка — факт о вашем коде, а не общее правило.

## MaxStreak

- Что хранится между итерациями: …
- Инвариант после шага `i`: …
- Почему серия в конце списка не теряется: …
- Время: O(…), потому что …
- Дополнительная память: O(…), потому что …
- Граничные входы, которые проверены тестами: …

## ReverseSegment

- Контракт границ (включительно или нет, что при неверных `l`, `r`): …
- Инвариант после каждого обмена: …
- Почему условие цикла `i < j`, а не `i != j`: …
- Время: O(…) · Память: O(…)

## RemoveSpam

- Три зоны списка во время прохода и что лежит в каждой: …
- Инвариант после шага `r`: …
- Чем плохи `Remove()` в цикле `foreach` и `RemoveAt(i)` в цикле `for`: …
- Как устроен встроенный `RemoveAll` и почему его нельзя использовать в решении: …
- Время: O(…) · Память: O(…)

## Ошибка, которую я нашёл

- Вход, на котором решение ошибалось: …
- Какое допущение оказалось неверным: …
- Тест, который её воспроизводит (имя в Tests.cs): …
- Что исправлено: …
