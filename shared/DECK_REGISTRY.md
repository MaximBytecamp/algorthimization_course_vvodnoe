# Реестр приёмов и областей

Учёт занятого по правилу `DECK_VARIETY.md`. Строка заполняется в том же коммите,
что и колода. Перед началом нового занятия реестр читается целиком: приём, форма справки
и предметная область берутся из свободных.

## ОП.04 · Основы алгоритмизации и программирования

| № | Тема | Ведущий приём | Точка входа | Формы работы руками | Предметные области | Справка | Итог |
|---|---|---|---|---|---|---|---|
| 1 | Вводная: алгоритмы вокруг нас, формат секции, маршрут курса | Цепочка «вопрос → ответ»: 19 вопросов, на каждый один разворот | Знакомство и вопрос «что начинается сегодня» | нет (лекция без клавиатуры) | утро студента, оплата и фото, медицина и связь, инженерные системы, поиск числа 42 | Словарь на два экрана | Экран «с каким результатом заканчивается курс» |
| 2 | Как проходит алгоритмическое собеседование | *предложено:* параллельная стенограмма двух кандидатов с таймкодами | запись секции, которая закончилась отказом | написать с нуля · прочитать целиком | пункт выдачи, турникет метро, логи доставки | Лист «вопрос интервьюера → что он проверяет» | Чек-лист готовности |
| 3 | Временная сложность и O-нотация | Живой прогон алгоритма: полоса ячеек вспыхивает по ходу выполнения, счётчик считает операции. Каждый класс сложности занимает два экрана — определение с прогоном и три примера с кодом и оценкой; плюс рисующийся график роста | гонка двух решений на одном списке: 64 обращения против 6 | прогнать и найти · прочитать целиком и разметить · обратный ход по ограничению · приёмка | склад, двенадцать областей `fragments.py`, заказы и чеки, ветклиника | Шпаргалка на один экран и шесть заблуждений | Чем эта оценка не является + вопрос на выход |
| 4 | Практические замеры времени и памяти | Запись лабораторного журнала из четырёх полей «ожидание → сырые значения → агрегат → вывод»: открывается на экране 2 с пустым выводом и закрывается на экране 28. Второй приём, работающий один раз, — интерактивная граница замера: строки листинга кликаются, число складывается из реальной стоимости выбранных строк | удвоение n дало ×3,68 и ×2,28 вместо ожидаемых ×4 и ×2, причём промахи разнонаправленные | прогнать и сравнить со своим · написать с нуля под автопроверку · провести серию · найти дефект в чужом коде · приёмка чужой методики | сверка входящих идентификаторов с реестром, лимит ответа API, лимит памяти контейнера | Паспорт эксперимента из одиннадцати полей и чек-лист измерительного контура из десяти пунктов | Форма проверяемого вывода из четырёх частей с явными ограничениями |

Занято и повторно не берётся: цепочка «вопрос → ответ» как каркас всей колоды,
восьмишаговая карточная раскладка «Этап NN из 08», словарь на два экрана подряд,
живой прогон алгоритма по полосе ячеек со счётчиком, рисующийся график кривых роста,
разбор «по одному экрану на каждый вид» с прогоном, параллельная стенограмма с таймкодами,
сквозная запись лабораторного журнала с полем, которое заполняется в конце колоды,
интерактивная граница замера на кликаемом листинге, точки на шкале разброса серии.

Занятие 4 расходится с третьим по всем шести осям. График здесь — результат
лаборатории на логарифмических осях по шести измеренным точкам, а не рисующаяся
справочная диаграмма классов роста: у третьего занятия кривые заданы формулами,
у четвёртого точки получены запуском. Демонстрация в третьем занятии
проигрывается сама (полоса ячеек со счётчиком), в четвёртом ею управляет
преподаватель кликом по строке листинга.

## ОП.03 · Информационные технологии

| № | Тема | Ведущий приём | Точка входа | Формы работы руками | Предметные области | Справка | Итог |
|---|---|---|---|---|---|---|---|
| 1 | Вводная: что входит в курс — маршрут из 10 станций, 5 разделов, инструменты, зачёт | Карта курса: строка из десяти станций `.route` над вертикальным списком пяти разделов `.stage-list` с часами и парами, дальше по одному экрану на раздел | Титул → сразу карта «что входит в курс»: десять станций пути данных и пять разделов на одном экране | нет (вводная без клавиатуры) | цифровая воронка учебного сервиса, пять разделов проекта, инструменты курса | Внутри карты курса: часы и число пар по каждому из пяти разделов | Экран «с чем выходишь с дисциплины»: процесс, который собирается заново |
| 2 (КТП зан. 1) | Цифровая воронка: как данные проходят от ссылки до заявки | Цепочка «вопрос → ответ», 17 пар, разбор с нуля — **назван возвратом к приёму вводной ОП.04**, но здесь это каркас содержательного занятия, а не 19 отдельных вопросов через всю колоду. Каждый ответ — связная проза, книжная цитата (`.q`) или один поясняющий рисунок (воронка `.funnel`, «письмо»-запрос `.letter`, построчный разбор `.parse`, панель Network `.net`, лента эпох `.era`, маршрут `.path`, стык крупным планом `.seam`, ловушки отчёта `.traps`, рамка «в объективе / за кадром` `.frame`). Компоненты — в локальном `<style>` | Вопрос 1 «О чём это занятие?» → одно предложение, к которому возвращаемся в конце | вскрыть живой сайт в DevTools Network · записать журнал из 5–7 наблюдённых событий с параметрами · заполнить карту воронки · отметить точки разрыва | путь заявки на учебном сайте с формой; запросы GA4 `/g/collect`, параметры `en`, `ep.*`, `dl`; история измерения аудитории (AIDA Льюиса, воронка Таунсенда, web bug Смита) | Понятия занятия на бланке (6 терминов) + шпаргалка `/g/collect` + бланки карты, журнала и разрывов в PDF; полный конспект — `LESSON.md` | Возврат к вопросу 1: карта воронки v1 + журнал + 3 точки разрыва, контроль — показать своё событие в Network |

Разбор панели Network идёт как пошаговый визуальный проход (7 шагов): настроить панель →
действие даёт строку → откуда берётся каждый параметр → где в запросе пусто → что означают
столбцы → вкладка Headers (код ответа) → вкладка Payload (все поля). Все кадры — настоящие
скриншоты Chrome DevTools, снятые скриптом на учебном стенде `stend/index.html`
(Playwright + `screencapture`); указатели на кадрах — компонент `.anno`, разбор полей — `.pane`
и `.fields`, разбор «параметр ← откуда» — `.derive`. Отдельный экран разбирает красные строки
(404, блокировщик, нет сети).

Занятие 1 отличается от вводной ОП.03 по всем осям: не карта-обзор, а текстовая лекция
с определениями и историческими фактами (204 No Content, `sendBeacon`, tracking pixel,
уход от лог-файлов); ведущий приём — не диаграмма, а разбор реального сетевого запроса.
Формат экрана — проза и списки (`.prose`, `.def`, `.factlist`, `.steplist`), а не
компонентные плитки.

Занято и повторно не берётся: карта курса из строки-маршрута `.route` и вертикального списка
разделов `.stage-list`, разбивка разделов тройками `example-row` в эйрбрау
«Раздел N · станции · часы · пары».

Колода намеренно короткая: 11 экранов, только «что входит в курс» — без теории, без слайда
знакомства, без экранов «как проходит пара», «работа с ИИ» и домашнего задания, без отдельных мем-экранов.
Расходится с вводной ОП.04 по ведущему приёму: там цепочка «вопрос → ответ» на 19 вопросов
через всю колоду, здесь одна карта маршрута, которую разворачивают по разделам. Точка входа
тоже другая: не «что начинается сегодня», а сразу схема всего курса на одном экране.

## МДК.01.01 · Разработка программных модулей

| № | Тема | Ведущий приём | Точка входа | Формы работы руками | Предметные области | Справка | Итог |
|---|---|---|---|---|---|---|---|
| 1 | Вводная | | | | | | |
