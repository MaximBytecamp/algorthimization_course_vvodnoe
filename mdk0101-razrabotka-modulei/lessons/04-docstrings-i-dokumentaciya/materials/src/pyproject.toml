[tool.ruff.lint]
select = ["E", "F", "D"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"]

[tool.mypy]
# Функция без аннотаций — ошибка: так проверка находит места,
# где типы забыли указать.
disallow_untyped_defs = true
